# Phase 0: Mock 전용. 실제 백엔드는 Phase 7에서 연결한다.
# mock = 브라우저에서 MSW가 API를 가로채 계약대로 응답
# live = 실제 API 서버 사용 (Phase 7 이후)
NEXT_PUBLIC_API_MODE=mock

# 계약상의 API base URL. mock 모드에서는 MSW가 이 주소를 가로챈다.
NEXT_PUBLIC_API_BASE_URL=https://api.sacloud.local
