/**
 * 라운드 복원 집계 규칙 버전 (D-194).
 *
 * **여기 한 곳에만 있다.** 잡(`jobs/roundBuild.ts`)이 이 값으로 저장하고
 * 화면(`apps/web/lib/server/queries/playerTraits.ts`)이 이 값으로 읽는다.
 * 두 곳에 각각 적어 두면 규칙을 올릴 때 한쪽만 바뀌어 조용히 갈라진다.
 *
 * 상수 하나 때문에 잡 모듈을 통째로 끌어오지 않으려고 파일을 따로 뒀다 —
 * `season0Window.ts` 를 화면이 읽는 것과 같은 방식이다 (D-175).
 *
 * 규칙이 바뀌면 이 값을 올린다. 옛 줄은 지우지 않고 남는다.
 */
/**
 * `round-v3` (2026-09-02 · D-260) — **연속킬**(`burstChained` / `burstKills` …)이 붙었다.
 * `round-v1` · `round-v2` 행은 지우지 않는다. 규칙이 다른 집계가 한 칸에 섞이면 안 되므로
 * 화면은 늘 이 값으로만 읽는다.
 *
 * ⚠ **올리면 화면이 잠시 어두워진다.** 화면은 이 값으로만 읽으므로, 새 값으로 집계를
 *   다시 돌리기 전까지는 `round-v2` 행이 **안 잡힌다** — 연속킬뿐 아니라 세이브 ·
 *   소수싸움 · 기회창출까지 여섯 축이 전부 `측정중` 이 된다.
 *   그래서 배포 순서가 정해져 있다:
 *
 *   ```
 *   마이그레이션 → roundBuild 재집계(round-v3 행 생성) → 웹 배포
 *   ```
 *
 *   D-214 에서 `round-v1 → round-v2` 를 올릴 때와 같은 절차다.
 *
 * 옛 판 — `round-v2` (2026-08-31 · D-214): **기회창출**(`openingKills` / `openingRounds`).
 *
 * ── ⚠ 2026-09-02 · **아직 `round-v2` 로 되돌려 둔다 — 위 순서의 ②가 안 끝났다**
 *
 *   `round-v3` 정의와 스키마 칸(`burstChained` …)은 준비됐지만 **집계를 아직 안 돌렸다.**
 *   운영에는 `round-v2` 행만 있다. 이 값을 v3 로 둔 채 배포하면 화면이 v3 행을 찾다가
 *   하나도 못 찾고 **여섯 축이 전부 「측정중」이 된다.** 바로 위 경고가 그 말이다.
 *
 *   같은 사고가 클랜 육각형에서 이미 났다 (D-259) — 그쪽은 카드가 통째로 사라졌고
 *   오류 한 줄 없이 조용했다. 그래서 여기서는 **집계가 끝난 뒤에** 올린다.
 *
 *   올릴 때는 이 블록만 지우고 아래 값을 `round-v3` 으로 바꾼다. 순서는 그대로다:
 *
 *     ① 마이그레이션(칸 추가)  ② roundBuild 재집계(round-v3 행 생성)  ③ 여기 올리고 배포
 */
export const ROUND_BUILDER_VERSION = 'round-v2'
