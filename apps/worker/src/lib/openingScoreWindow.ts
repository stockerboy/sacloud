/**
 * ★★선짤 점수는 언제부터 세나★★ — 한 곳에서만 정한다 (2026-09-20 사장님)
 *
 * > 「이 점수 방식은 ★오늘 경기부터 계산해★ 전꺼 까지 다 하지말고 너무 오래걸리잖아」
 *
 * ── 왜 날짜를 두나
 *
 *   선짤 점수를 옛 경기까지 매기려면 ★배틀로그가 있는 경기 122,829건★ 을 전부
 *   다시 돌려야 한다. 사장님이 정하신 무거운 작업 창(오전 7시~오후 1시)으로도
 *   여러 날이 걸린다. ★그럴 값어치가 없다★ 는 것이 사장님 판단이다.
 *
 *   이 날짜를 두면 ★재계산이 0건★ 이다 — 평소 집계가 새 경기를 처리할 때
 *   자연스럽게 들어간다.
 *
 * ── ⚠ 알고 쓰는 한계
 *
 *   이 날 이전 경기는 ★선짤 점수가 0★ 이다. 그래서 시즌 점수를 보면
 *   ★오래 뛴 사람일수록 선짤 몫이 작아 보인다.★ 시즌이 흐르면 저절로 고르게 된다.
 *
 *   화면에서 「선짤 0점」 과 「선짤을 안 잰 경기」 를 ★가르지 않는다★ —
 *   가르려면 경기마다 깃발이 필요하고, 그 값어치보다 복잡함이 크다.
 *
 * ⚠ ★되돌리려면 이 날짜만 옮기면 된다.★ 계산은 한 곳(`playerHexBuild`)뿐이다.
 */

/**
 * ★2026-09-20 00:00 (KST)★ = 2026-09-19T15:00:00Z
 *
 * 사장님이 「오늘 경기부터」 라고 하신 그 날의 0시다.
 */
export const OPENING_SCORE_FROM = new Date('2026-09-19T15:00:00.000Z')

/** 그 경기에 선짤 점수를 매기나 */
export function scoresOpening(startAt: Date): boolean {
  return startAt.getTime() >= OPENING_SCORE_FROM.getTime()
}
