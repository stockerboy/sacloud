/**
 * ★소개 페이지를 누가 볼 수 있나★ (2026-09-14 사장님).
 *
 * > «따로 만들어 / 앞쪽으로 빼는데 빼기전에 ★일단 관리자로 로그인해서 나부터 볼 수 있게
 * >   해줘 로그인 전에는 못보게★ 내가 보고나서 밖으로 뺄지 결정한다»
 *
 * ── 공개로 바꾸는 법 — ★이 줄 하나★
 *   ```ts
 *   export const ABOUT_PUBLIC = true
 *   ```
 *   그러면 `/about` 도 그 안의 API 도 누구나 볼 수 있다.
 *   환경변수로 두지 않은 이유: 사장님이 «보고 나서 결정» 하신다고 하셨고, 결정이
 *   내려지면 그건 ★코드에 남아야 할 판단★ 이지 배포 설정이 아니다.
 *
 * ⚠ 사이트 전체 잠금(`middleware.ts`)과 ★다른 자물쇠★ 다.
 *   사이트가 열려 있어도 이 값이 `false` 면 소개 페이지는 관리자만 본다.
 */
export const ABOUT_PUBLIC = false
