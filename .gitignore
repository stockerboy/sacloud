node_modules/
.pnpm-store/

# Next.js
.next/
out/
next-env.d.ts

# 빌드 산출물 / TS 증분 빌드 정보
dist/
*.tsbuildinfo

# 환경변수 (예시 파일만 커밋)
.env
.env.local
.env.*.local

# 테스트 / 커버리지
coverage/
test-results/
playwright-report/

# 에디터 / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로그
*.log
npm-debug.log*
pnpm-debug.log*

# 래더 백업 스냅샷 (로컬 전용 — 저장소에 넣지 않는다)
apps/worker/backups/

# 운영 DB 백업/덤프 — 로컬 전용. 절대 커밋하지 않는다.
# **경로를 좁혀 둔다.** `*.sql` 로 넓게 잡으면
# `packages/db/prisma/migrations/*/migration.sql` 까지 가려 버린다.
/backups/
/*.sql
/*.sql.gz

# Vercel CLI 로컬 연결 정보 · CLI 가 받아 오는 토큰 파일 — 커밋하지 않는다.
# (`.env.local` 은 위 환경변수 규칙에서 이미 무시된다. `.env*` 로 넓게 잡으면
#  추적 중인 `.env.example` 까지 가려서 좁게 둔다)
.vercel

# Prisma 엔진 복사본 (빌드 산출물 · D-151)
apps/web/generated/

# 3rd.supply 미러링 수집 체크포인트 (D-153).
# 수십 MB 이고 계속 커진다. 원본 보존은 git 이 아니라 DB(RawImport)가 맡는다.
# 언제든 `nexon supply-mirror` 로 다시 만든다.
packages/db/data/supply-mirror-*.json
packages/db/data/supply-season7.json

# 성능 측정용 임시 빌드 산출물
apps/web/.next-perf/

# 성능 검증용 임시 빌드 산출물
apps/web/.next-verify/

# 미러 수집 파일 — 수십~수백 MB. 원본 보존은 DB(RawImport)가 맡는다
packages/db/data/supply-mirror-*.jsonl
packages/db/data/supply-mirror-*.legacy
packages/db/data/supply-mirror-*.corrupt

# 지난시즌 수집 체크포인트 (B10). 미러 파일과 같은 원칙이다 —
# 계속 커지고(수집 시작 20분 만에 5MB, supply 한 리그만 요청 2만건),
# 원본 보존은 git 이 아니라 DB 가 맡는다. `nexon supply-seasons` 로 다시 만든다.
packages/db/data/supply-seasons-*.json
packages/db/data/supply-seasons-*.jsonl

# 3rd.supply 선수 프로필 수집 (position · note · renewed_at · D-161).
# 위 수집 파일들과 같은 이유로 뺀다 — 21,107줄 5MB 이고
# `nexon supply-player-profiles` 로 언제든 다시 만든다.
packages/db/data/supply-player-profiles.json
packages/db/data/supply-player-profiles.jsonl

# 워커 리포트 산출물 (로컬 전용 — 백업과 같은 성격이다)
apps/worker/reports/

# 배틀로그 전수조사 작업목록 — 생성물이다. 다시 만들 수 있다 (D-218)
data/barracks/battlelog-worklist/

# 폰 알림 훅 — 토픽과 상태·로그는 올리지 않는다 (2026-08-31)
.claude/hooks/notify.config.json
.claude/hooks/.notify-state.json
.claude/hooks/notify.log

# 일회성 진단 스크립트 — `__` 접두사는 "커밋하지 않는다" 는 표시다 (관례)
apps/worker/src/dev/__*
apps/web/scripts/__*

# 진영 판정 내보내기 산출물 — 다시 만들 수 있다 (matchFirstSideExport)
first-side-export.json

# 이 PC 전용 설정 (절대경로·권한모드가 박혀 있다). 훅 자체는 올린다
.claude/settings.local.json

# IPL 투영 내보내기 산출물 — 다시 만들 수 있다 (iplProjectExport)
ipl-project-export.json
.tmp-scripts/

# 클랜 육각형 V2 내보내기 산출물 — 다시 만들 수 있다 (clanHexV2Export)
clan-hex-v2-export.json

# 육각형 요약 내보내기 산출물 — 다시 만들 수 있다 (clanHexV2SummaryExport)
clan-hex-v2-summary-export.json

# 성능 측정 임시 폴더
.tmp-perf/
design/__tmp-*

# 수집 감시 상태 (지시 #18) — 러너 캐시로만 넘긴다. 저장소에 넣지 않는다
.watchdog/
