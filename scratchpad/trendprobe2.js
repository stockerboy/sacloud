(() => { const svg=[...document.querySelectorAll('svg')].find(s=>[...s.querySelectorAll('text')].some(t=>/10\/1/.test(t.textContent))); if(!svg) return 'no svg'; const sr=svg.getBoundingClientRect(); const out=[`svg top=${Math.round(sr.top)} h=${Math.round(sr.height)} vb=${svg.getAttribute('viewBox')}`]; for (const t of svg.querySelectorAll('text')) { const s=(t.textContent||'').trim(); if(/오늘|누적|10\/1|%$/.test(s)) { const r=t.getBoundingClientRect(); out.push(`${s} y=${Math.round(r.top-sr.top)}..${Math.round(r.bottom-sr.top)} x=${Math.round(r.left)} attrY=${t.getAttribute('y')}`) } } return out.join(' | ') })()
