(() => { const out=[]; for (const svg of document.querySelectorAll('svg')) { const vb = svg.getAttribute('viewBox') || ''; if (!vb.startsWith('0 0 ')) continue; const r = svg.getBoundingClientRect(); if (r.width < 200) continue; const p = svg.parentElement; const pr = p.getBoundingClientRect(); out.push(`svg vb=${vb} rect=${Math.round(r.width)}x${Math.round(r.height)} style=${svg.getAttribute('style')||''} | parent ${Math.round(pr.width)} cls=${(p.className||'').toString().slice(0,30)} | gp ${Math.round(p.parentElement.getBoundingClientRect().width)}`) } return out.join('\n') })()
