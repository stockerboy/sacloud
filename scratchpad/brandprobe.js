(() => { const out = []; for (const el of document.querySelectorAll('.v2-brand')) { const r = el.getBoundingClientRect(); const cs = getComputedStyle(el); const img = el.querySelector('img,svg'); const ir = img ? img.getBoundingClientRect() : null; out.push({ cls: el.className, x: Math.round(r.x), y: Math.round(r.y), w: Math.round(r.width), h: Math.round(r.height), display: cs.display, vis: cs.visibility, op: cs.opacity, pos: cs.position, img: img ? { tag: img.tagName, src: (img.getAttribute('src')||'').slice(0,40), w: Math.round(ir.width), h: Math.round(ir.height) } : null }) } const inner = document.querySelector('.v2-topbar__inner'); const ic = inner ? getComputedStyle(inner) : null; return JSON.stringify({ brands: out, inner: inner ? { cls: inner.className, pos: ic.position, ov: ic.overflow, h: Math.round(inner.getBoundingClientRect().height) } : null }) })()
