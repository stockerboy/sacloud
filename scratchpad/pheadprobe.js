(() => { const root = document.querySelector('.v3-phead-id') || document.querySelector('[class*="phead"]'); if (!root) return 'no phead'; const out = []; const walk = (el, d) => { if (d > 3) return; for (const c of el.children) { const r = c.getBoundingClientRect(); const t = (c.textContent || '').trim().replace(/\s+/g, ' ').slice(0, 30); out.push(`${'  '.repeat(d)}${c.tagName.toLowerCase()} [${Math.round(r.left)},${Math.round(r.top)} ${Math.round(r.width)}x${Math.round(r.height)}] ov=${getComputedStyle(c).overflow} «${t}»`); walk(c, d + 1) } }; const rr = root.getBoundingClientRect(); out.unshift(`ROOT [${Math.round(rr.left)},${Math.round(rr.top)} ${Math.round(rr.width)}x${Math.round(rr.height)}] display=${getComputedStyle(root).display} cols=${getComputedStyle(root).gridTemplateColumns}`); walk(root, 0); return out.join('\n') })()
