(() => { const sec=[...document.querySelectorAll('section')].find(s=>/HOT/.test(s.textContent||'')); if(!sec) return 'no'; const out=[]; const sr=sec.getBoundingClientRect(); out.push(`section ${Math.round(sr.left)}..${Math.round(sr.right)} display=${getComputedStyle(sec).display} align=${getComputedStyle(sec).alignItems}`); for (const c of sec.children){ const r=c.getBoundingClientRect(); out.push(`${c.tagName} ${Math.round(r.left)}..${Math.round(r.right)} w=${Math.round(r.width)} cssW=${getComputedStyle(c).width} margin=${getComputedStyle(c).marginLeft}/${getComputedStyle(c).marginRight}`)} return out.join(' | ') })()
