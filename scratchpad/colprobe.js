(() => { const row=[...document.querySelectorAll('div')].find(d=>/육덕미시애호가|거친수달19/.test(d.textContent||'') && d.children.length>=4 && d.getBoundingClientRect().height<80); if(!row) return 'no row'; return [...row.children].map(c=>{const r=c.getBoundingClientRect(); return `${Math.round(r.width)}[${(c.className||'').toString().split(' ').filter(x=>/w-|flex-1|min-w|grow/.test(x)).join(' ')}]`}).join(' | ') })()
