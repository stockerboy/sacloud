# 로컬 개발 DB 접속 정보
#
# 이 파일은 예시다. 실제 값은 `.env`에 두며 Git에 커밋하지 않는다.
# 운영 비밀키(넥슨 API 키, SMTP 비밀번호 등)는 절대 이 저장소에 넣지 않는다.
#
# 포트를 5433으로 잡은 이유: 나중에 시스템에 PostgreSQL을 따로 설치해도
# 기본 포트(5432)와 충돌하지 않게 하기 위해서다.
DATABASE_URL="postgresql://sacloud:sacloud@127.0.0.1:5433/sacloud?schema=public"
