/**
 * **게시판 준비중** 안내 문구 — 순수 로직 (2026-09-01 사용자 지시).
 *
 * ```
 * "게시판 준비중으로 냅두고 마이페이지는 해야돼"
 * ```
 *
 * 리그 쪽 `preparingText.ts`(D-178)와 같은 방식이다 — 문구를 컴포넌트 안에 두면
 * 테스트가 JSX 를 끌고 들어와야 한다. 문자열만 따로 둔다.
 *
 * ── **날짜를 적지 않는다**
 *   언제 열리는지 정해진 것이 없다. `9월 중 오픈` 같은 것을 지어내면 그날 거짓말이 된다
 *   (`CLAUDE.md` 3장 7번 — 확인되지 않은 것을 그럴듯하게 메우지 않는다).
 */

export const BOARD_PREPARING_HEADLINE = '게시판 준비중'
export const BOARD_PREPARING_MESSAGE = '게시판은 아직 열지 않았습니다. 준비가 끝나면 곧 열립니다.'
