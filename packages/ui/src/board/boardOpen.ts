/**
 * **게시판이 사람에게 열려 있는가** — 한 곳에서 정한다 (O-004 · 2026-09-02).
 *
 * ══ 왜 이 스위치가 필요한가 ══
 *
 * 사장님이 쓰신 사이트 소개에 「게시판」 문단이 있다. 그런데 게시판이 닫혀 있을 때
 * 「게시판은 로그인해야 씁니다」를 적어 두면 **없는 것을 있다고 쓰는 것**이 된다
 * (`CLAUDE.md` 2-1). 그렇다고 문단을 지우면 열 때 다시 써야 한다.
 * 그래서 문단은 남기고 **이 값 하나로** 보이고 안 보이게 한다.
 *
 * ══ 지금 값이 `false` 인 이유 — 상태가 애매해서다 (2026-09-02 실측) ══
 *
 * ```
 * 주소는 살아 있다      https://3rdcloud.my/board/free  → 200 · 글쓰기 버튼도 있다
 *                      (2026-09-02 에 다시 열었다 · D-260)
 * 그런데 갈 길이 없다   홈에서 받은 HTML 에 /board 링크가 **한 개도 없다**
 *                      GNB 를 「IPL · SPL · 10」 셋으로 줄이면서 게시판 메뉴를 감췄다
 * ```
 *
 * 즉 **주소를 직접 치는 사람만** 들어간다. 처음 온 사람에게 「게시판이 있습니다」라고
 * 적어 두면 찾다가 못 찾는다. 그래서 소개에서는 아직 없는 쪽으로 센다.
 *
 * ══ 열 때 하는 일 ══
 *
 * 게시판으로 가는 길(GNB·푸터 링크)을 화면에 되살리고 **이 값을 `true` 로** 바꾼다.
 * 그러면 소개의 「게시판」 문단이 저절로 따라 나온다. 문단을 다시 쓸 일이 없다.
 *
 * ⚠ 이 값은 **게시판 라우트를 켜고 끄지 않는다.** 라우트는 `app/board/layout.tsx` 가
 * 쥐고 있다. 여기는 **「소개 글이 게시판을 있다고 말해도 되는가」** 만 정한다.
 */
export const BOARD_OPEN: boolean = false
