/**
 * 게시판 개폐 스위치 — **값은 계약(`@sacloud/contract`)에 있다** (O-011 · 2026-09-02).
 *
 * O-004 때는 이 파일이 값을 들고 있었다. 그런데 **화면만 이 값을 보고 있어서
 * 문이 반쪽만 닫혀 있었다** — 쓰기 API 에는 개폐 검사가 아예 없었고, 주소를 아는
 * 사람은 로그인도 없이 계속 글을 쓸 수 있었다.
 *
 * 서버 라우트가 이 패키지를 가져오면 React 가 딸려 오므로, 값을 React 를 모르는
 * 계약 쪽으로 옮기고 여기서는 **다시 내보내기만** 한다. 화면 코드는 안 바뀐다.
 *
 * 자세한 경위와 「읽기는 왜 안 막았나」는 `packages/contract/src/boardOpen.ts` 에 있다.
 */
export { BOARD_OPEN } from '@sacloud/contract'
