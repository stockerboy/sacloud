/**
 * 공식/비공식 경기 문구 (Phase 10 final cleanup · 정책 1 · 4 · 5 · 7).
 *
 * 사용자에게 보이는 상태는 **두 가지뿐**이다 — `공식 경기` / `비공식 경기`.
 * (`참고 기록`이라는 표현은 폐기했다. DB의 `official` 불리언은 그대로 쓴다.)
 *
 * 왜 화면 파일에서 떼어 냈는가
 *   비공식 경기에 "개인 래더 100% 반영" 같은 문구가 뜨면 반영되지 않은 경기가
 *   반영된 것처럼 읽힌다. 실제로 그렇게 나갔던 적이 있다. 그래서 분기 자체를
 *   테스트할 수 있는 순수 함수로 만든다.
 */

/** 비공식 경기 배지 — 목록에서 한눈에 구분되게 한다 */
export const UNOFFICIAL_BADGE = '비공식 경기 · 래더 미반영'

/** 배지에 붙는 설명 */
export const UNOFFICIAL_BADGE_TITLE =
  '양쪽 모두 동일 클랜원 3명 미만으로 구성되어 공식 통계에 반영되지 않습니다'

/** 경기 상세에 붙는 래더 반영 안내 */
export function ladderNotice(official: boolean): string {
  return official
    ? '실제 출전이 확인된 선수는 용병 포함 개인 래더 100% 반영'
    : '비공식 경기이므로 개인/클랜 래더 및 시즌 누적 통계에 반영되지 않습니다'
}

/**
 * 클랜 래더 반영률(100/70/40/0%)을 화면에 띄울 것인가.
 *
 * 공식 경기에서만 띄운다. 비공식 경기는 애초에 계산 대상이 아니라서
 * `70%`를 보여 주면 그만큼 반영됐다는 뜻으로 읽힌다. 구성(클랜원/용병 수)은 그대로 보여 준다.
 */
export function showsClanWeight(official: boolean): boolean {
  return official
}
