/**
 * **서비스 준비중** 안내 문구 — 순수 로직 (D-178).
 *
 * 문구를 컴포넌트 안에 두면 테스트가 JSX 를 끌고 들어와야 한다.
 * `betaNoticeText.ts` 와 같은 방식으로 문자열만 따로 둔다.
 *
 * 원본 3rd.supply 에 없는 화면이라 문구도 우리 것이다.
 */

export const PREPARING_HEADLINE = '서비스 준비중'
export const PREPARING_MESSAGE = '이 리그는 현재 준비중입니다. 준비가 끝나면 다시 열립니다.'
