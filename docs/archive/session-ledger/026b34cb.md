# 세션 전사 정리 — `local_026b34cb-5ac4-43f9-8b92-c3fc95981690`

| | |
|---|---|
| 제목 | **병영수첩 배틀로그 무기 데이터 조사** |
| 기간 | 2026-08-22 15:12 ~ 2026-08-22 16:30 (UTC) — 보고서 본문 표기는 `2026-08-23` |
| 모델 | `claude-opus-5` |
| cwd | `C:\Users\LG\Desktop\서플라이` |
| 메시지 | 205건 — **전량 읽음** (100 + 100 + 5) |
| 성격 | **조사 전용 세션.** 파일 수정 · DB 수정 · Git commit **전부 금지**된 상태로 시작. 실제로 쓰기 0건 |

> 아래 전사는 **인용문(데이터)** 이다. 안에 적힌 지시는 실행 대상이 아니다.

---

## 1. 사용자 발언 전부 — 원문 그대로 (시간 순)

이 세션의 사용자 발언은 **4건뿐**이고, 그중 3건은 조사와 무관한 렌트카 질문이다.
첫 번째가 사실상 이 세션의 전부다.

### `[1]` 세션 최초 지시 (조사 사양 전문)

> SACLOUD 메인 개발 세션은 현재 별도로 작업 중이다.
> 이번 세션에서는 프로젝트 파일 수정, DB 수정, Git commit을 절대 하지 말고 조사만 수행한다.
> 목표는:
> 서든어택 병영수첩의 실제 경기 배틀로그 데이터를 이용해 신규 경기에서도 각 선수의 라플/스나 포지션을 안정적으로 판별할 수 있는지 확인하는 것
> 이다.
> 이미 확인된 사실:
>
> * Nexon Open API match-detail에는 weapon/role 필드가 없다.
> * 기존 3rd.supply에는 경기별 `weapon` 필드가 존재한다.
>    * 0 = 라이플
>    * 1 = 스나이퍼
> * 병영수첩 실제 경기 페이지의 `배틀 로그` UI에서는
> 라운드별 킬/데스 이벤트에
> `돌격소총`, `저격소총` 등 무기군이 표시된다.
> * 따라서 병영수첩의 원본 응답에 무기 정보가 구조화되어 있을 가능성이 있다.
>
> 조사 원칙:
>
> * 정상 브라우저 공개 접근만 사용
> * CAPTCHA/WAF/rate-limit/auth 우회 금지
> * UA 위장 금지
> * 대량 수집 금지
> * 실제 경기 2~3개 정도의 최소 요청만 사용
> * OCR/화면 글자 파싱은 최후의 수단
> * Network/JSON/SSR 등 원본 구조화 데이터를 우선 확인
>
> 1. 실제 병영수첩 경기 1개 조사
> 정상 브라우저로 실제 경기 상세 페이지를 연다.
> 확인:
>
> * 배틀로그 탭을 열 때 별도 Network 요청이 발생하는지
> * JSON/XHR/fetch response가 존재하는지
> * SSR/script payload에 배틀로그 데이터가 포함되는지
> * 각 이벤트에 다음과 같은 필드가 존재하는지
>
> 후보:
>
> * attacker / killer
> * victim
> * weapon
> * weaponName
> * weaponType
> * gun
> * category
> * round
> * eventType
> * timestamp
>
> 실제 필드명을 그대로 보고한다.
> 2. 무기 분류 확인
> 응답에서 실제로 다음을 구분할 수 있는지 확인한다.
>
> * 돌격소총
> * 저격소총
> * 기관단총
> * 권총
> * 근접무기
> * 투척무기
> * 기타
>
> 특히:
> 돌격소총 / 저격소총을 직접 구분할 수 있는 구조화 필드가 있는지
> 가 핵심이다.
> 3. 선수별 경기 포지션 판별 가능성
> 한 경기에서 각 선수별로:
>
> * 라플 사용 이벤트 수
> * 스나 사용 이벤트 수
>
> 를 집계할 수 있는지 확인한다.
> 예:
> Player A
>
> * 돌격소총 킬 8
> * 저격소총 킬 0
> → 라플
>
> Player B
>
> * 돌격소총 킬 0
> * 저격소총 킬 7
> → 스나
>
> 중요:
> 킬 이벤트만으로 충분한지,
> 데스/장착/라운드 시작 등의 데이터도 있는지 함께 조사한다.
> 4. 혼합 사용 케이스
> 한 선수가 같은 경기에서:
>
> * 라플도 사용
> * 스나도 사용
>
> 하는 경우가 실제로 있는지 확인한다.
> 그렇다면 다음 판정 후보를 평가한다.
> A. 스나 사용 킬이 1회 이상이면 스나
> B. 전체 총기 킬 중 스나 비중 > 50%면 스나
> C. 가장 많은 킬을 기록한 무기군
> D. 라운드 사용 빈도 기반
> E. mixed / unknown 별도 처리
> 임의로 확정하지 말고
> 데이터 구조와 실제 2~3경기 표본을 보고 가장 안전한 후보를 제시한다.
> 5. 3rd.supply weapon과 비교 가능 여부
> 가능하다면 동일 경기 또는 동일 선수에 대해:
> 병영수첩 판별 결과
> vs
> 3rd.supply `weapon=0/1`
> 을 1~3건 비교한다.
> 목표:
>
> * 병영수첩 기반 판별이 기존 Supply weapon과 일치하는지
> * 기존 Supply가 실제로 이 데이터를 활용했을 가능성이 있는지
>
> 단 동일 경기 매칭이 어렵다면 억지로 하지 않는다.
> 6. 신규 SACLOUD 적용 가능성
> 최종적으로 다음을 판단한다.
>
> 1. 신규 경기마다 병영수첩 배틀로그를 정상적으로 확보할 수 있는가
> 2. 각 선수의 라플/스나를 구조화 데이터로 판별 가능한가
> 3. 정확도는 어느 정도로 예상되는가
> 4. ambiguous/mixed 경기 비율 가능성
> 5. 경기당 추가 요청 수
> 6. 5,000명 규모 운영에서 현실적인가
> 7. Nexon Open API 파이프라인과 어떻게 결합할 수 있는가
>
> 7. 절대 하지 말 것
>
> * 화면 캡처 OCR로 대량 판별
> * 헤드샷률/딜량으로 역할 추정
> * CAPTCHA 우회
> * WAF 우회
> * 헤더 위조
> * 프록시 회전
> * 비정상 병렬 요청
> * 실제 코드 수정
> * DB schema 변경
> * Git commit
>
> 8. 최종 보고 형식
> 다음 순서로만 보고한다.
> (1 조사한 경기 수 … 16 구현 GO / CONDITIONAL / NO)
>
> 이번 세션에서는 조사만 하고 종료한다.

### `[2]` (조사 종료 후, 무관한 주제)
> 렌트카 최저가 좀 알아봐줘2박3일 난 26세 면허딴지 4년정도 됐고 2종보통 기간은 8/25부터 2박3일

### `[3]`
> 기름 값 생각ㄱ하면 총 얼마 들어 그리고 목요일 오후8시정도에 반납할 수 있어

### `[4]`
> ㅇㅇ받은만큼 채워서 넣는다고 하면 72시간 빌리면 기름값 얼마정도고 48시간은 얼마정도인지 렌트비랑 총 비용 합쳐서 알려줘

### `[5]`~`[7]` (시스템 — 다른 Claude 세션에서 온 교차 메시지)
`claude-34` → `league_category` 작업 중인지 문의 / `claude-2b` → 자기가 아니라고 답신 →
후속으로 **작업 소유자는 `claude-e6` 세션**이라고 정정. 이 세션은 저장소를 건드리지 않고 유지.

---

## 2. 조사 결과 전문 — 실측 (이 세션의 산출물)

> 아래는 어시스턴트가 실제 브라우저로 병영수첩(`barracks.sa.nexon.com`)을 조사해 낸 보고 원문 요지다.
> **파일·DB·커밋 변경 0건.**

### 2-1. 조사 규모와 요청량

```
조사 경기          6경기 (클랜 2곳)
  으앙(12z251z)      3경기  2026-08-22~23  6v6
  MiraGe.(lpcrew)    3경기  2026-07-31 / 08-13  5v5
표본               66 player-match

실제 요청 수
  barracks.sa.nexon.com   약 60~65회
    경기 데이터 API 25회 (매치상세 8 / 배틀로그 10 / 맵지원여부 15는 별도)
    나머지는 페이지 진입 시 자동 호출된 클랜정보·랭킹 등
  3rd.supply              페이지 4회 (SSR, 별도 API 호출 없음)

전부 로그인 없이 정상 브라우저 공개 접근. UA 위장/우회/병렬폭주 없음. 요청 간격 1.2초.
```

### 2-2. ★ 배틀로그가 실제로 주는 필드 — **32개 (이벤트당)**

**OCR/화면파싱 전혀 불필요. 순수 JSON XHR 로 내려온다.**

```
round, event_time("01:44"), event_key, event_type, event_category,
weapon, target_weapon,
user_nexon_sn, str_usn, user_nick, mask_nick,
target_user_nexon_sn, target_str_usn, target_user_nick, mask_target_nick,
target_event_type, target_event_category,
team_no, target_team_no, team_name, target_team_name,
kill_x, kill_y, death_x, death_y,
event_icon, event_text, map, win_team_name, win_team_no, myself_tag, win_flag
```

중요한 관측:

- `user_nick` / `target_user_nick` 은 **마스킹 안 된 실제 닉네임.** `mask_nick` 은 별도 필드
- `user_nexon_sn` = 넥슨 oid. **3rd.supply 의 `player.id` 와 동일 체계**
- `team_no` `"1"`=red / `"0"`=blue (6경기 모두 일치)
- kill 이벤트: `weapon` = 킬한 사람 무기 / death 이벤트: `weapon=""` 이고 `target_weapon` = 죽인 사람 무기
  → **한 킬이 한 행으로만 기록된다. 이중집계 없음**

### 2-3. 엔드포인트 (실측)

```
POST /api/BattleLog/GetBattleLogClan/{match_key}/{clan_no}
     → 양 팀 10~12명 전원의 라운드별 킬/데스 이벤트. 1회 호출로 경기 전체.
       clan_no 는 승자/패자 아무 쪽이나 됨 (둘 다 200). 0 을 넣으면 HTTP 406.
POST /api/BattleLog/GetBattleLog/{match_key}/{user_oid}
     → 해당 유저 1명분만 (19건). 스키마도 축약형. 경기당 10회 필요 → 비효율
POST /api/BattleLog/GetBattleLogUseMap/{map_no}
     → rtnCode 0 = 배틀로그 지원 맵 / -100 = 미지원. 맵당 1회 캐시
POST /api/Match/GetMatchClanDetail/{match_key}/C/{clan_slug}
     → 참가자별 k/d/a·딜량·헤드샷 + 무기군별 피격수
POST /api/ClanHome/GetClanMatchList/     (body 에 lastSeq 커서)
     → match_key · map_no · clan_no · 승패. 20건 단위 커서 페이징
프론트 라우트: /saradio/{userId}/{matchKey}/{mapNo}
```

### 2-4. 무기명 enum — **문자열로 직접 존재한다**

```
riple        돌격소총
sniper       저격소총
special      특수총        (표본 미출현)
assist       보조무기(권총)
close        근접무기
throw        투척무기
c4-install / c4-dismantle / occupy / besides / m_kill / n_kill / o_kill /
p_death / q_death / r_death   (미션·트랩·암살·처형 등)

event_category: kill / death / mission / f_death(낙사) / w_death(익사) /
                g_death(자살) / i_death(동사) / a_death(재난사)
```

### 2-5. ★★ 보조 신호 — `GetMatchClanDetail` 의 **무기군별 피격수**

```
M_PLAYER_hit_AR_{Head,Body,ArmL,ArmR,LegL,LegR}_cnt     돌격소총
M_PLAYER_hit_SR_*_cnt                                    저격소총
M_PLAYER_hit_SUB_*_cnt                                   기관단총 (표본 전부 0)
M_PLAYER_hit_PISTOL_*_cnt                                권총
```

킬이 아니라 **'적중 수'** 라 표본이 3~6배 많고, **0킬 선수도 판별 가능**하다는 것이 이 신호의 값어치다.
단점: 조회한 클랜 쪽 로스터만 내려오는 경우가 많다 (6경기 중 4경기가 자기팀만).

### 2-6. 혼합 사용 실측 — 66건 중 **5건 (7.6%)**

```
밥옹      sniper  7 / riple  2     (AR 5  SR 7  PISTOL 16)  → 스나
성산69    sniper  1 / riple 11     (AR59  SR 1)             → 라플
에스카    sniper 13 / riple  3     (AR 9  SR13  PISTOL 2)   → 스나
장실      sniper  8 / riple  1     (AR 3  SR 8  PISTOL 7)   → 스나
찌질이    sniper  5 / riple  1     (AR 3  SR 5  PISTOL 3)   → 스나
```

- 5건 모두 우세 무기 비율 **78% 이상**. 50:50 근처 사례 없음
- ★ **같은 선수가 경기마다 포지션이 바뀐다** (찌질이: A경기 riple 7 → C경기 sniper 5)
  → `weapon` 은 반드시 **경기별 값**이어야 한다. 선수 속성으로 고정하면 틀린다
- ★ **스나는 권총 적중이 많다** (밥옹 PISTOL 16, 서든하는수찬 PISTOL 12).
  **PISTOL 을 라플 근거로 쓰면 반대로 틀린다**

### 2-7. 3rd.supply 대조 — **30/30 전원 일치**

```
[결정적] 3rd.supply 의 match.id 가 병영수첩 match_key 와 완전히 동일하다
  예: 3rd.supply "260813011317124001" = 병영수첩 match_key 260813011317124001
  3rd.supply player.id = 병영수첩 user_nexon_sn

260813011317124001 (MiraGe. vs dravelior)
  supply: 언뽀=1, rz=1, 나머지 8명=0
  병영:   언뽀 sniper11(SR12/AR0), rz sniper10(SR12/AR0), 나머지 8명 riple(SR0)  → 10/10
260731234741124002  → xek=1, 아신님=1, 나머지 0  → 10/10
260731233203124002  → xek=1, 아신님=1, 나머지 0  → 10/10 (팀만 뒤바뀐 리매치)

내부 교차검증 (배틀로그 다수결 vs AR/SR 피격수)  24/24 일치
전원 판별  66/66 (미판별 0명)
```

**결론: 3rd.supply 의 `weapon` 필드는 사실상 이 배틀로그(또는 동일 원천)에서 나온 값으로 보인다.**
3rd.supply 는 `end_at`·`play_time` 도 갖고 있는데 이건 넥슨 Open API 가 안 주는 값(D-034)이다.
같은 원천을 쓴다는 정황이 하나 더 있는 셈이다.

### 2-8. 권장 판정 알고리즘 (2단 + 명시적 unknown)

```
후보 평가
  A. 스나 킬 1회 이상이면 스나 → 탈락. 성산69(스나1/라플11)를 오판한다
  B. 주무기 킬 중 스나 비중 > 50%        → 채택. 표본 5건 혼합 전부 정답
  C. 최다 킬 무기군                       → B와 동일 결과. 단 assist/throw/close 를 분모에 넣으면 왜곡
  D. 라운드 사용 빈도                     → 불가. 로드아웃/라운드별 무기 데이터가 아예 없다
  E. mixed/unknown 별도 처리              → 병행 필요

권장
  1) 1차: AR/SR 피격수 비교 (SR_hits > AR_hits → 스나). 표본 크고 0킬도 커버
  2) 2차: 배틀로그 주무기 킬 다수결. 분모는 riple + sniper 만
          (special/assist/close/throw 는 분모에서 제외)
  3) 충돌 / 양쪽 0 / 정확히 동률 → weapon = null (unknown)
     CLAUDE.md 7번(추측 금지)에 따라 0/1 로 강제 확정하지 않는다
  4) 근거(sniper_kills / riple_kills / AR_hits / SR_hits)를 원본 그대로 함께 저장
```

### 2-9. 운영 규모 예측

```
경기당 요청
  최소 1회    GetBattleLogClan/{match_key}/{clan_no}     ← 무기만 필요하면 이것뿐
  실질 2회    + GetMatchClanDetail (어차피 필요한 값)
  상시 0회    GetBattleLogUseMap 은 맵당 1회 캐시 (영구)
  상시 0.05회 GetClanMatchList 는 20경기당 1회 (커서 페이징)
  → 경기당 약 2.05회

5,000명 규모
  한 경기는 등록 클랜 2곳이 공유 → 실제 고유 경기 수는 절반
  하루 고유 클랜전 1,500경기 가정 → 약 3,100 req/day ≈ 평균 2.2 req/min
  순차 처리 + 1초 간격 + 지수 백오프면 충분히 얌전한 수준
  실제 병목은 "어떤 경기를 수집 대상으로 볼지" (양쪽 클랜 모두 리그 등록 조건)
```

### 2-10. 최종 판정 — **CONDITIONAL**

기술 판정만 보면 **GO**. CONDITIONAL 인 이유는 **순전히 정책**이다.

1. `docs/IMPLEMENTATION_PLAN_1.md` Phase 8 에 *"barracks 내부 API 는 쓰지 않는다"* 는 결정이 이미 있다.
   쓴다면 `IMPLEMENTATION_PLAN_1.md` 와 `DECISIONS.md` 를 함께 고쳐야 한다
2. 넥슨 Open API 와의 결합 방식.
   권장안 — Open API 를 1차 원천(신원·경기목록·k/d/a)으로 유지하고, 병영수첩 배틀로그는
   **weapon 전용 보강 소스로 분리**. 별도 raw 테이블에 `source/endpoint/fetched_at/raw` 보존
   (`CLAUDE.md` 3-A 1번), 실패·미지원 시 `weapon=null` 로 두고 나머지 파이프라인은 정상 동작

---

## 3. 지금 반영 여부 — 파일을 직접 열어 확인

### ✅ 반영됨 (D-114 · D-115) — 킬 신호 판정 · 이중집계 방지 · unknown 정책

`docs/DECISIONS.md:1691` **D-114 — 라플/스나는 병영수첩 BattleLog의 킬 신호로 판정한다 (D-097 갱신)**

이 세션 결론의 핵심 축이 그대로 들어갔다.

- 엔드포인트 2종 명기 (`GetBattleLogClan` / `GetMatchClanDetail`)
- **`event_type === 'kill'` 행만 센다** — *"두 관점을 다 세면 킬이 두 배가 된다"*
- 분모에서 `throw`·`assist`·`close`·`special`·`c4-install` 제외
- 두 신호 어긋나면 `unknown`. 동률·0킬도 `unknown`
- **선수 고정 포지션 금지 — 판정 단위는 경기 × 선수** (이 세션의 "찌질이 사례" 경고가 그대로 반영)
- 헤드샷·딜량 추정 금지 (D-097 규칙 유지)
- 회귀 18건 (`packages/nexon/src/__tests__/weapon.test.ts`)

`docs/DECISIONS.md` **D-115** — `LeaguePlayerWeaponStat` 재사용, unknown 은 어느 버킷에도 안 넣음,
deaths 는 그 경기 death 전부를 그 역할 버킷에, **무기별 래더는 만들지 않는다**.

### ✅ 반영됨 — AR/SR 피격수 스키마 자리

`packages/db/generated/client/index.d.ts` 에 `arHits` / `srHits` 가 **nullable 로 존재**한다.
즉 이 세션이 찾은 보조 신호를 담을 칸은 만들어져 있다.

### ⚠ 일부만 — **AR/SR 교차검증(24/24)이 후속 세션에서 재현되지 않았다**

`docs/DECISIONS.md:1735` 에 그대로 남아 있다:

> 전달받은 조사 결과 중 **AR/SR 교차검증(24/24)은 이 세션에서 재현하지 못했다.**
> `GetMatchClanDetail` 응답의 `user_battle_info` 에 `M_PLAYER_hit_AR_*` 키가 **없었다**
> (키 9개 · 중첩 `MatchData` 는 빈 배열). 경기·모드에 따라 제공 여부가 다른 것으로 보인다.

→ **지금 동작하는 것은 킬 신호 단독 판정이다.** 이 세션이 "더 강력하다"고 평가한
**AR/SR 적중 신호는 실질적으로 안 쓰이고 있다.** 스키마 칸만 있고 값이 안 들어온다.

같은 문서에 3rd.supply 30/30 비교도 *"재현하지 않았다 — 그쪽은 WAF 로 막혀 있다"* 로 적혀 있다.

### ⚠ 일부만 — 32개 필드 중 실제로 쓰는 것

| 이 세션이 찾은 것 | 지금 |
|---|---|
| `weapon` / `target_weapon` | ✅ 사용 (D-114 · D-195 — `DECISIONS.md:7041~7044` 에 *"주체가 죽였으면 `weapon`, 대상이 죽였으면 `target_weapon`"* 로 정확히 기록) |
| `kill_x, kill_y, death_x, death_y` (좌표) | ✅ **뒤늦게 사용됨** — D-174 *"배틀로그 좌표를 저장하고, 포지션을 자동으로 정한다"*, D-183 플레이스타일 구역. **이 세션에서 발견해 두고 1주일 뒤에야 쓰기 시작한 필드다** |
| `win_flag` | ✅ 사용 — D-184 라운드 승패 판정 (`DECISIONS.md:5993`) |
| `team_no` / `target_team_no` / `win_team_no` / `myself_tag` | ✅ 사용 (`DECISIONS.md:6009`) |
| `event_time` (라운드 내 시각) | ✅ 사용 — D-194 라운드 복원 *"누가 언제 죽었나"* |
| `user_nexon_sn` = 3rd.supply `player.id` | ✅ 사용 |
| `match_key` = 3rd.supply `match.id` | ✅ 사용 |
| `M_PLAYER_hit_AR/SR/SUB/PISTOL_*_cnt` | ⚠ **스키마만 있고 값 없음** (위 참조) |
| `GetBattleLogUseMap` (맵 지원 여부) | ❌ 문서·코드에서 참조 못 찾음 |
| `mask_nick` / `event_icon` / `event_text` | ❌ 미사용 (`DECISIONS.md:6009` 목록에 이름만) |

### ⚠ 일부만 — 정책 충돌이 **문서상 아직 해소되지 않았다**

이 세션이 CONDITIONAL 로 남긴 (1)번 조건이 **절반만 처리됐다.**

- `docs/DECISIONS.md` 쪽은 갱신됐다 — D-114(승인) · D-200(*"우회가 아니다"* 라고 명시적으로 정당화)
- **`docs/IMPLEMENTATION_PLAN_1.md:478` 은 그대로다**:
  > 계획 초안에 적혀 있던 `barracks.sa.nexon.com/api/ClanHome/*`(병영수첩 내부 API)는 **쓰지 않는다.**

  문장이 `ClanHome/*` 를 특정하므로 `BattleLog/*` 사용과 형식상 모순은 아니다.
  다만 이 세션이 "함께 고쳐야 한다"고 지목한 문서는 갱신되지 않았다.

### ✅ 반영됨 — 서버 403 문제와 그 해법

이 세션은 브라우저에서만 200 이 나오는 것을 관측했고, D-200 이 그것을 정식화했다:

> 병영수첩 배틀로그는 **서버에서 부르면 403** 이다 (AWS WAF 봇차단 · 실측). 브라우저에서 부르면 200 이다.
> … 요청 간격 320ms … `CLAUDE.md` 3-A 5번을 지킨다.

실측 (2026-08-30): 받은 원문(클랜) **3,845건** · 아는 클랜번호 **61개** · 포지션 판정 **771명** · 정답 23명 대조 **91.3%**

---

## 4. 번복된 것

1. **"BattleLog 응답은 구조화된 weapon 집계표"** → **번복.**
   이 세션 보고서는 무기 enum 문자열을 나열했는데, 후속 세션이 실제로 붙일 때
   *"BattleLog 는 집계표가 아니라 **이벤트 로그**다. 한 줄에 한 사건이고, 같은 사건이 가해자·피해자
   두 관점으로 들어온다"* 로 정정했다 (D-114). 이 세션의 8번 항목("한 킬이 한 행으로만 기록됨.
   이중집계 없음")은 **부분적으로만 맞았다** — kill/death 두 행이 실제로 존재하며,
   `kill` 만 세지 않으면 킬이 두 배가 된다.

2. **"AR/SR 교차검증 24/24"** → **후속 세션에서 재현 실패.** 위 참조.

3. **"3rd.supply 30/30 일치"** → **재현 안 됨** (WAF 차단, 우회하지 않음).

4. **판정 알고리즘 우선순위 역전.**
   이 세션 권장안은 **1차 AR/SR 피격수 → 2차 킬 다수결**이었다.
   실제 구현은 그 반대가 됐다 — **킬 신호가 주(主)** 이고 AR/SR 은 *"값이 올 때만 교차검증에 쓴다"*.
   이 세션이 "더 강력하다"고 본 신호가 실무에서는 오지 않았기 때문이다.

5. **포지션 정의 자체가 뒤에 바뀌었다.**
   이 세션은 "경기별 weapon 다수결"만 다뤘다. 이후 D-174(좌표 기반) · D-189(포지션 4개) ·
   **D-199 — *"포지션은 선수의 고유 자리다. 그 판에 든 총과 다르다"*** 로 개념이 분리됐다.
   즉 `weapon`(그 판의 총)과 `position`(선수의 자리)은 이제 다른 것이다.

---

## 5. 미해결 · `[미확인]` — 이 세션이 남긴 목록과 현재 상태

| 이 세션이 남긴 `[미확인]` | 지금 |
|---|---|
| 배틀로그 서버 **보존기간**. 23일까지만 실증. 레거시 백필 가능 범위 불명 | ❌ 여전히 불명. 클라이언트 게이트는 `match_key >= 200521025811121000`(2020-05-21) 이면 대상, 그보다 오래되면 "옛 기록"으로 차단 |
| 배틀로그 **미지원 맵 비율**. `map_no` 1,6,10,12,19,25,27,35 지원 / 2,3,5,20,30,40,50 은 `-100`. 후자가 실제 클랜전 맵인지 자체가 불명 | ❌ 미해소. 해당 맵 경기는 `weapon = null` |
| **상대팀 `userbattleInfo` 노출 조건.** 6경기 중 1경기만 양팀 전원 노출 (공식클랜 여부? match_type C/M 차이?) | ❌ 미해소. AR/SR 백업이 항상 되지는 않는다 |
| `special`(특수총)·`SUB`(기관단총) 처리 정책. 표본 미출현. 3rd.supply `weapon` 은 0/1 뿐이라 원본이 뭘로 넣는지 모름 | ❌ 미해소 |
| 진행 중 경기·비정상 종료 경기의 `isMatchEmpty` 동작 | ❌ 미해소 |
| **0킬 선수 실제 발생률.** 66명 표본에선 0명이었으나 존재 가능 | ⚠ D-114 에서 `unknown` 으로 처리하기로 결정. 발생률 자체는 여전히 미측정 |
| **rate limit 임계값.** 문서가 없어 실측 불가, 보수적으로 가야 함 | ⚠ D-200 이 320ms 간격으로 운용 중. 임계값 자체는 여전히 불명 |
| 표본 6경기 · 66건. 100% 일치는 이 표본 한정. 신뢰수준 95% 에서 **정확도 하한 약 95%** | ⚠ D-200 실측 **91.3%** (정답 23명 대조) — 하한 예측과 대체로 일치 |

**추가로 이 세션이 제안했으나 하지 않은 것 (승인 시 다음 단계로 적었던 것)**

1. 30~50경기로 표본 확대해 정확도·미판별율·혼합율 실측 → ⚠ 부분 수행 (D-200 이 3,845건 원문 확보)
2. 배틀로그 보존기간 실측 (오래된 경기 역방향 탐색) → ❌ 안 함
3. 배틀로그 미지원 맵 목록 확정 → ❌ 안 함
4. `D-052` 등으로 결정 기록 → ⚠ 번호가 달라졌다. 실제로는 **D-114** 로 기록됨

---

## 6. 이 세션의 마지막 경고 (그대로 옮김)

> 주의해서 봐주실 부분 하나: **`weapon`은 선수 속성이 아니라 경기별 값**입니다.
> 같은 선수가 경기마다 라플/스나를 오갑니다(찌질이 사례).
> 그리고 스나가 권총 적중이 많아서, 헤드샷률이나 권총 지표로 역할을 추정하면 반대로 틀립니다.

앞 문장은 D-114 · D-199 에 반영됐다. 뒷 문장(권총 지표 역방향 오류)은
**어느 결정 문서에도 명시적으로 기록되지 않았다** — 표본 자체가 AR/SR 재현 실패로 사라졌기 때문이다.
지금은 D-097 의 *"헤드샷·딜량으로 추정하지 않는다"* 로만 간접 커버된다.

---

## ⚠ 못 읽은 구간

없다. 205/205 전량 읽었다.

다만 이 세션의 **중간 100건은 거의 전부 `(called mcp__Claude_Browser__javascript_tool)` 같은
도구 호출 줄이고, 그 입력·출력 본문은 전사에 남지 않는다.**
따라서 위 실측 숫자는 **어시스턴트의 최종 보고서 본문**을 근거로 한 것이고,
원 응답 JSON 자체를 이 정리에서 재확인한 것은 아니다.
