# LADDER_TUNING_REPORT.md — 래더 상수 결정 근거

> 확정된 정책은 `docs/DECISIONS.md` D-057 ~ D-068.
> 이 문서는 **그 정책 안에서 상수를 어떻게 골랐는가**의 기록이다.
>
> - 작성 2026-08-22
> - 엔진: `packages/rating/` (순수 함수, 운영·시뮬레이션 **같은 코드**)
> - 시뮬레이터: `apps/worker/src/lab/tuneSim.ts` · `tuneSweep.ts` (sandbox, DB 미사용)
> - 재현: `pnpm --filter @sacloud/worker exec tsx src/lab/tune.ts`

---

## 1. 확정 상수

```ts
expectedScoreDivisor: 800      // D — 원본 3400에서 바꿨다 (D-060)
personalKBase: 36.6            // K(R) = 36.6 - R/200, 하한 8
personalKSlope: 200
personalKFloor: 8
personalWinMultiplier: 1       // 1.15 → 1.0 (인플레이션 제거)
clanK: 16
rewardCapStart: 300            // 이 차이까지는 안 깎는다
rewardCapFull: 900             // 이 차이부터 승리 보상 0
minWinReward: 1
repeatDecay: 0.6               // 같은 결과 반복 시 0.6ⁿ
repeatDecayFloor: 0.15
repeatWindowDays: 14
seasonBaseline: 1500
seasonCarryRate: 0.5
lineupBlend: 0.5               // 상대 클랜 래더와 상대 라인업 평균을 반반
lineupMinConfirmed: 4          // 확인 4명 미만이면 라인업 미반영
minConfirmedPerSide: 3         // 경기 인정 기준 (D-057)
placementMatches: 10
initialRating: 1500
ratingFloor: 0
```

---

## 2. 통과 기준 (사용자 지시 20번)

후보 12세트를 11개 기준으로 채점했다. **전부 11/11 통과** — 즉 기준만으로는 후보가 갈리지
않는다. 그래서 3장의 파라미터별 변별 지표로 골랐다.

| 기준 | 결과 |
|---|---|
| 양학만으로 최상위권 진입이 어렵다 | 양학 300경기 **1496** vs 정상 65% **2211** |
| 강한 상대를 이기는 것이 가치 있다 | 업셋 **+30** vs 근소우위 **+7** |
| 동일 상대 반복 farming 효율이 낮다 | 같은 상대 20연승 **+64** vs 새 상대 **+238** (73% 억제) |
| 50% 유저가 무한 inflation하지 않는다 | 동급 1경기 증감 합 **0** · 리그 100명 5,000경기 평균 **+1.4** |
| 음수·NaN·무한대가 없다 | 20,000경기 연속 검사 통과 |
| 신규 유저가 합리적 경기 수에 안정화 | 강자 **60경기** · 약자 **47경기** |
| division이 점수를 왜곡하지 않는다 | 공식 입력에 division이 **없다** |
| incomplete participant를 추측하지 않는다 | 3v3 경기 → 개인 결과 **6건**, 라인업 미반영 |
| replay가 결정적이다 | 같은 입력 → 같은 결과 |
| soft reset 후 순위 정보가 남는다 | 순위 상관 **0.9999** · 폭 **12.5%**(3시즌 연속) |
| 낮은 승률로 강자와 붙어도 손해만 보지 않는다 | 45% 승률로 강자 300경기 → **2082** |

---

## 3. 파라미터별 근거

### 3-1. `D` (기대승률 분모) — **800** ⭐ 가장 중요한 결정

| D | 클랜 래더 폭 | 전력 상관 | 양학 마진 | 신규 안착 | 리그 평균 이동 |
|---|---|---|---|---|---|
| 400 | 1097 | 0.978 | **796** | 101경기 | +1.4 |
| **800** | **1690** | **0.990** | 715 | **60경기** | +1.4 |
| 1200 | 2080 | 0.989 | 688 | 63경기 | +1.0 |
| 1600 | 2344 | 0.992 | 682 | 63경기 | +0.3 |
| 3400 (원본) | 2390 | 0.988 | 678 | 67경기 | -1.2 |

800을 고른 이유: **전력 상관이 가장 높으면서 신규 안착이 가장 빠르다.**
400은 양학 마진이 가장 크고 관측 클랜 폭(약 850)에 가장 가깝지만, 신규 유저가 자리를
찾는 데 101경기가 걸린다 — 부계정을 만드는 편이 빨라지는 구간이다.

> **원본(3400)을 버린 것은 의도적이다.** 정책 10번이 "재현보다 래더 품질 우선"을 명시했다.

### 3-2. `clanK` — **16**

| clanK | 폭 | 최저~최고 | 전력 상관 |
|---|---|---|---|
| 12 | 1375 | 790~2165 | 0.991 |
| **16** | **1507** | 697~2204 | 0.990 |
| 20 | 1611 | 622~2233 | 0.991 |
| 24 | 1690 | 563~2253 | 0.990 |
| 30 | 1770 | 485~2255 | 0.987 |

관측된 클랜 래더는 개인보다 훨씬 좁다(987~1,840). K를 낮출수록 폭이 좁아지고 상관은 그대로다.
12가 가장 좁지만 시즌 안에 수렴하기엔 느리다. **16이 절충점**이다.

### 3-3. `rewardCap` — **300 / 900**

| cap | 양학 마진 |
|---|---|
| 200 / 700 | 783 |
| **300 / 900** | **715** |
| 400 / 1200 | 635 |
| 600 / 1800 | 551 |

더 조이면 마진은 커지지만, `D=800`에서 300점 차는 이미 기대승률 0.70이다.
그보다 이르게(200) 깎기 시작하면 **정상적인 우세 경기까지 보상이 죽는다.**

### 3-4. `repeatDecay` — **0.6**

| decay | 억제율 |
|---|---|
| 0.3 | 76.9% |
| 0.4 | 76.1% |
| **0.6** | **73.1%** |
| 0.8 | 65.5% |
| 1 (끔) | 0% |

0.3~0.6은 사실상 차이가 없다(하한 0.15가 먼저 걸린다). 0.6은 **2·3차전까지는 의미가 남는다**.
정책이 "반복 경기를 금지하지 않는다"고 했으므로 가장 완만한 쪽을 골랐다.

### 3-5. `carryRate` — **0.5**

폭 유지율이 carryRate와 정확히 같고, 순위 상관은 어느 값이든 1.0000이다.
즉 **순위 정보는 어떤 값에서도 사라지지 않는다.** 0.5는 "절반은 남기고 절반은 되돌린다"는
가장 설명하기 쉬운 값이라 골랐다. 정책적으로 언제든 바꿀 수 있다.

### 3-6. `lineupBlend` — **0.5**, `lineupMinConfirmed` — **4**

| blend | 클랜 전력 상관 | 반영된 경기 |
|---|---|---|
| 0 (미반영) | 0.976 | 0% |
| 0.3 | 0.982 | 72% |
| **0.5** | **0.982** | 72% |
| 0.7 | 0.976 | 72% |
| 1 (라인업만) | 0.976 | 72% |

**정책 5번이 물었던 질문에 대한 답이다.** 확인된 선수만으로 계산하되 절반만 섞는 쪽이
반영하지 않는 것보다 정확하고, 전면 반영보다도 정확하다.
확인 인원이 4명 미만인 경기(전체의 28%)에서는 **반영하지 않는다.**

---

## 4. 남은 한계 (숨기지 않는다)

1. **실제 경기 데이터로 검증하지 못했다.** 재구성된 넥슨 경기가 아직 0건이라 전부 합성 데이터다.
   실경기가 쌓이면 같은 시뮬레이터에 실분포를 넣어 다시 확인해야 한다
2. **클랜 래더 폭이 관측치(약 850)보다 넓다** (1507). 관측은 원본 사이트의 값이고 우리는
   공식을 바꿨으므로 같을 이유는 없지만, 실운영에서 폭이 과하면 `clanK`를 더 낮춘다
3. **`placementMatches = 10`은 우리가 정한 값**이다. 원본 기준은 `[미확인]`이다
4. P-A / P-B 구분은 **여전히 미해결**이다. division 보정을 쓰지 않게 되어 지금은 차이가
   나타나지 않지만, 과거 표본을 확보하면 재검증 대상이다
