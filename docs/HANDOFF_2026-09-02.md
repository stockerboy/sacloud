# HANDOFF — 2026-09-02 아침 세션

> **새 세션은 이 문서를 먼저 읽는다.** 그 다음 `docs/HANDOFF_2026-09-01_NIGHT.md` 를 읽되,
> 아래 1장의 **정정**을 먼저 보고 읽어라. 밤 문서에 틀린 것이 세 군데 있다.
>
> 작성 시각 2026-09-02 06:20 KST. 확인한 것은 전부 **운영 DB 를 직접 세었다.**
> 로그를 보고 적은 것은 하나도 없다.

---

## 0. 결론부터

| 밤 문서가 「가장 먼저 하라」고 한 것 | 결과 |
|---|---|
| 래더 전수 재계산이 운영에 반영됐는지 확인 | **반영됐다.** 계획 파일과 최저 점수가 자릿수까지 일치 |

그 뒤로 세 가지를 더 끝냈다 — 화면의 「배치고사」 표기 제거 · 밀려 있던 운영
마이그레이션 3개 적용 · IPL 선수 전원 소속 클랜 채우기.

---

## 1. ⚠ 밤 문서의 정정 — **읽기 전에 이것부터**

### 1-1. 「IPL 개인랭킹 (운영) — 로컬 15,620행을 운영에 미는 경로가 **없다**」 → 틀렸다

밤 문서 4장 표의 첫 줄이다. **이미 운영에 올라가 있다.**

```
nolink  LeaguePlayer 1,456명 · 전원 래더 매겨짐 · 참가행 15,620 · 최고 4280 최저 1863
```

`cc0be67` 이 `SEASON0_ORIGINS` 에 `nexon_barracks` 를 더하면서 `season0Apply` 가
IPL 을 집계하기 시작했다 (D-258 에 이미 적혀 있다). 밤 문서 4장이 그 사실을 못 따라갔다.
`supplyPush.ts` 를 본떠 IPL push 를 만들 필요는 **없다.**

### 1-2. 「IPL 래더가 774경기 밀려 있다」 → 이건 **내가 오늘 아침 잘못 읽은 것**이다

내가 처음에 「밀린 경기 774」라고 썼다. 다시 세어 보니 그 774경기는 **참가자 명단 자체가 없다.**

```
nolink 경기 24,662 · 참가행이 있는 경기 1,562 (6.3%)
명단 있는 경기 기간  07-11 04:18 ~ 08-29 18:00 (KST)
그 뒤로 명단 있는 경기 0건
```

**명단이 있는 1,562경기는 전부 래더에 반영돼 있다.** 밀린 것이 아니라 계산할 재료가 없다.
IPL 의 진짜 구멍은 래더가 아니라 **나머지 23,100경기(93.7%)의 라인업**이다.
그건 배틀로그에서만 오고, 배틀로그 자동화는 아직 막혀 있다 (밤 문서 0-7).

### 1-3. 「`prisma migrate status` 는 5분 타임아웃이 났다. 시간을 넉넉히 줘라」 → 시간 문제가 아니다

**포트 문제였다.** 6543(트랜잭션 풀러)으로는 마이그레이션 엔진이 영원히 멈춘다.
5432(세션 풀러)로 바꾸면 몇 초 만에 끝난다. 자세한 것은 **D-261**.

---

## 2. 오늘 한 것 — 전부 검증된 것만

### 2-1. 래더 전수 재계산 반영 확인 (밤 문서 3장 1번)

운영 DB 실측 2026-09-02 05:2x KST.

| 리그 | 선수 | 래더≠3000 | 래더 매겨짐 | 배치 표시 | 최고 | 최저 | 계획 최저 |
|---|---|---|---|---|---|---|---|
| supply (SPL) | 10,438 | 1,247 | **1,431** | 9,007 | 4445 | **1992** | 1992 ✓ |
| sanply (10mtn) | 15,335 | 2,013 | **2,356** | 12,979 | 5142 | **1424** | 1424 ✓ |
| nolink (IPL) | 1,456 | 1,423 | **1,456** | 0 | 4280 | 1863 | — |
| daerule (닫음) | 4,163 | 174 | 237 | 3,926 | 3651 | 2299 | — |

- 밤 문서 dry-run 계획은 **랭킹 노출 supply 1,426 · sanply 2,345** 였다.
  실제 1,431 / 2,356 — 그 뒤에 들어온 경기만큼 늘었다. **일치로 판정한다.**
- **최저 점수가 계획과 자릿수까지 같다.** 이것이 반영됐다는 가장 강한 증거다.
- 밀린 경기 **0건**. SPL 은 마지막 경기(09-02 04:08 KST)까지, 10mountain 은
  (03:54 KST)까지 래더가 따라잡았다. `season0-apply.yml` 이 매시간 돌면서 유지한다.

### 2-2. 화면에 남아 있던 「배치고사」 표기 제거 — 커밋 `530298a`

배치고사는 2026-09-01 에 폐지됐는데(`2d0086f`) **랭킹 표 안내문만 고쳤고
점수 자리의 표기는 6개 컴포넌트에 그대로 남아 있었다.**

- 운영에서 그 표기가 뜨던 선수 **25,912명** (supply 9,007 · sanply 12,979 · daerule 3,926)
- 그중 **승/패가 하나라도 있는 사람은 0명**이다 → 이 플래그의 뜻은 이제
  「배치고사 진행중」이 아니라 **「이 시즌 창(7/1~)에 0판」**이다
- `배치고사` → **`기록 없음`**. 프로필 두 곳은 진홍(`text-accent`)이었는데 `text-meta` 로 내렸다

**경기 상세(`MatchCard` · `matchDetailView`)는 손대지 않았다.** 그쪽은 경기 당시 스냅샷
(`MatchPlayerStat.isPlacement`)이고, 미러 참가행 **180,139건**이 원본값을 그대로 들고 있다
(supply 71,897 · sanply 78,100 · daerule 30,142 · nolink 0).
그때는 실제로 배치고사였으므로 바꾸면 거짓이 된다 (`CLAUDE.md` 3-A 2번).

### 2-3. 밀려 있던 운영 마이그레이션 3개 적용 — **D-261**

```
20260901180000_signup_username     User.username 추가 · User.email NOT NULL 해제
20260901180000_title_challenge     TitleChallenge + 칭호 관측 2칸
20260901200000_clan_master_claim   ClanMasterClaim + ClanMasterClaimImage
```

- 밤 문서 4-1 이 「typecheck 21건 오류부터 고치고 적용하라」고 했는데,
  **오늘 `pnpm typecheck` 는 전 패키지 통과(오류 0건)다.** 차단 조건이 이미 풀려 있었다
- 6543 으로 두 번(10분·9분) 멈췄고 **두 번 다 0건 적용**이었다. 5432 로 바꾸니 몇 초
- 확인: 표 3개 생성 · `User.email` 44행 그대로(NULL 0행) · 손으로 넣은 **부분 유니크
  인덱스 4개 전부 생성됨** · 운영 API 전부 200

→ **클랜 마스터 인증이 이제 DB 상으로는 살아 있다.** 화면·API 가 실제로 도는지는
  아직 안 봤다 (아래 4장).

### 2-4. IPL 선수 1,456명 전원 소속 클랜 채우기 — 커밋 `4d04b3c`

IPL 개인랭킹에서 **1,456명 전원 클랜 칸이 비어 있었다.** 원인은 버그가 아니라 경로 부재다 —
미러 리그는 `supplyRollup` 이 D-161 규칙으로 채우는데 IPL 엔 그게 없고,
`season0Apply` 는 이 칸을 **읽어서 되쓸 뿐** 만들지 않는다.

새 잡 `player-current-clan` 을 만들었다. 가장 늦은 경기의 `matchTimeLeagueClanId` 를
옮기기만 한다 (IPL 참가행 15,620건 전부 그 칸이 차 있다). 판정을 새로 만들지 않았다.

```
미리보기  선수 1456 · 이미있음 0 · 찾음 1456 · 바꿀대상 1456 · 모름 0
--confirm 반영 뒤 재실행 → 바꿀대상 0 (멱등 확인)
운영 API 1위 [h].AixIeft 의 clan: null → hingˇ (마크 2장 포함)
```

```bash
pnpm --filter @sacloud/worker nexon player-current-clan [--league <slug>] [--confirm]
```

---

## 3. ★사용자에게 물어야 할 것 — 임의로 뒤집지 않았다★

**IPL 랭킹의 K/D 칸이 비는 것은 버그가 아니다.** D-107 이 `category='independent'` 리그의
**누적** 킬·데스·킬뎃을 응답에서 빼도록 정해 놨다 (`queries/visibility.ts`).

그런데 그 결정은 이 리그가 **「무소속리그」**이던 2026-08-23 것이다. 지금 이 리그의 이름은
**IPL** 이고(D-246), `CLAUDE.md` 9장은 **「리그별로 칸을 감추는 분기를 만들지 않는다.
모든 리그가 같은 화면을 쓴다」**고 못박고 있다. 두 문서가 정면으로 부딪힌다.

- 없앨 거면 `League.category` 를 `official` 로 바꾸거나 `hidesCumulativeKd` 를 끈다
- **문서화된 결정을 세션이 임의로 뒤집는 것은 하지 않았다.** 사용자 판단이 필요하다

---

## 4. 아직 안 된 것 — 「됐다」고 말하지 마라

| 안 된 것 | 상태 | 다음 수 |
|---|---|---|
| 클랜 마스터 인증 **화면·API 동작** | DB 는 올라갔다. **실제로 눌러 보지 않았다** | 운영에서 신청→심사→승인 한 바퀴 돌려 본다 |
| IPL 라인업 23,100경기 (93.7%) | 배틀로그에서만 온다 | 밤 문서 0-7 의 「실제 브라우저 프로필」 가설 검증 |
| IPL 새 경기 수집 | 08-31 05:18 KST 이후 0건 | 사용자 브라우저 필요 |
| 병영수첩 자동 수집 | 헤드리스도 403 | 밤 문서 0-7 |
| 넥슨 API 로 클랜전 | 엔드포인트 자체가 없다 | 영영 안 된다 |
| 3rd.supply 수집기 제거 | 대체 경로가 아직 없다 | 대체가 실제로 돌면 |
| `ratingBackup.ts` sanply 백업 | 202만 행에서 Prisma 가 죽는다 | chunk 읽기로 고친다 (밤 문서 2-3) |
| **D-260(연속킬) 작업** | 커밋 안 된 채 작업트리에 있다 | 아래 5장 |

---

## 5. 작업트리에 커밋 안 된 채 남아 있는 것

이 세션이 시작할 때 이미 있던 것이다. **내 작업이 아니라서 손대지 않았다.**

```
M packages/db/prisma/schema.prisma          PlayerRoundProfile 에 연속킬 5칸 (D-260)
M packages/nexon/src/index.ts               연속킬 재료
M apps/worker/src/lib/roundBuilderVersion.ts round-v3
?? packages/nexon/src/playerBurst.ts + 테스트
?? .tmp-marks/ · .tmp-twotone/ · shots-final/ · packages/db/_backup/ · _q.ts
?? apps/worker/src/dev/_tmp*  (lint error 37건이 전부 여기서 난다)
```

- **`D-260` 번호는 이 연속킬 작업이 이미 쓰고 있다.** 그런데 `docs/DECISIONS.md` 에는
  아직 안 적혀 있다. 그래서 오늘 만든 결정은 **D-261** 로 달았다. 번호를 겹치지 마라
- 이 스키마 변경에는 **마이그레이션 파일이 없다.** 운영에 올리려면 먼저 만들어야 한다

---

## 6. 다음 세션이 먼저 할 일

1. **클랜 마스터 인증을 실제로 눌러 본다.** DB 만 올라간 상태다. 화면이 도는지 모른다
2. 3장(IPL 의 K/D 숨김)을 사용자에게 물어 결정을 받는다
3. `ratingBackup.ts` 를 chunk 읽기로 고친다 — sanply 백업이 안 되면 되돌릴 길이 없다

**돌리면 안 되는 것** (밤 문서 3장 2번 그대로 유효):
`supply-rollup` · `supply-mirror` 계열을 손으로 돌리지 마라. `season0-finish` 도 돌리지 마라.
