# WORKLOG.md — 자율 진행 작업 로그

사용자가 자리를 비운 동안 진행한 내용을 시간순으로 남긴다.
아침에 이 파일만 읽어도 현재 상태를 파악할 수 있어야 한다.

---

## 2026-08-20 밤 ~ 2026-08-21 아침 (자율 진행)

### 진행 방식
`구현 → localhost 실행 → Chrome 실렌더 검수 → 원본 비교 → 수정 →
lint / typecheck / test / build → Git 체크포인트 커밋 → 다음 작업` 반복.

### 완료한 Phase

| Phase | 내용 | 커밋 |
|---|---|---|
| Phase 0 | 모노레포 · API 계약(Zod) · 결정적 Mock 픽스처 · MSW | `6ff61ba` |
| Phase 1 | 공통 레이아웃 · 홈 · 약관 2종 · 404 | `35a8a09` |
| (문서) | 마이그레이션 요구사항 반영 + 접근 가능성 조사 | `55ef281` |
| Phase 3 | 리그 목록 · 리그홈 · 클랜랭킹 · 개인랭킹 | `62cf6e6` |
| Phase 2 | 플레이어 프로필 · 클랜 프로필 · 클랜원 | `19cbbf9` |
| Phase 4 | 개인/클랜 기록실 · 매치 상세 · 지난시즌 | `5d8a7aa` |
| Phase 5 | 커뮤니티 게시판 (목록/상세/작성/댓글/검색) | `e3817ae` |
| (문서) | 래더 계산 구현 사양 (LADDER_IMPLEMENTATION_SPEC.md) | `bbe44ff` |
| Phase 6 | 인증 · 마이페이지 · 리그/클랜/플레이어 관리 | `0ed681a` |

## ✅ Checkpoint 1 도달 — Mock 기반 UI/흐름 복원 완료 (2026-08-21)

M1 마일스톤(계획서 4장) 달성. 여기서 사용자 보고 후 대기한다.

### 이후 남은 작업 (Checkpoint 2~)
- **Phase 7** DB 스키마 · 실제 API → **Checkpoint 2** (스키마 설계 완료 시)
- **Phase 8** 넥슨 Open API 수집 파이프라인 → **Checkpoint 4** (소규모 실데이터 검증)
- **Phase 8-M** historical 마이그레이션 → **Checkpoint 3** (dry-run + 원본 대조)
- **Phase 9** 래더 · 배치고사 · 시즌 → **Checkpoint 5** (개발환경 시뮬레이션)
- **Phase 10** SSR/SEO/성능/운영 → **Checkpoint 6** (배포 직전)

### 밤새 발견하고 고친 것
- 킬뎃은 `킬/데스` 비율이 아니라 **`킬/(킬+데스)×100` 백분율** (원본 실측으로 확정, Phase 0 회귀 수정)
- 매치 카드 승/패 색이 Phase 0 추정값이었으나 실측값으로 교체
- 최근매치 요약이 상대 클랜 수가 많으면 아래 카드와 겹치던 레이아웃 버그
- 매치 상세 요청에 잘못된 `league_clan_id`를 넘기던 버그
- 클랜 헤더에 없어야 할 갱신 버튼이 있던 것 (원본 실측 후 제거)
- 인증 화면에 전역 GNB/푸터가 나오던 것 (원본은 단독 카드 레이아웃)
- `@sacloud/mock` 루트 진입점을 클라이언트에서 import해 하이드레이션이 막히던 문제 (D-020)
- `.next` 오염으로 CSS/라우트가 깨지는 문제 재발 → `pnpm clean` 후 재기동으로 해결 (D-014)

### 검수 시 주의 (D-019)
Chrome 탭이 **백그라운드면 React 상태가 flush되지 않는다.** 화면은 보이는데 클릭·입력이
안 먹는 것처럼 보인다. 앱 버그로 오인하기 쉬우니 `document.visibilityState`를 먼저 확인할 것.


---

## Phase 7 — DB + 실제 API (2026-08-21)

### 한 일

**기반**
- `packages/db` 신설. Prisma 스키마를 계약(Zod)을 채울 수 있는 형태로 설계하고,
  `CLAUDE.md` 3-A(마이그레이션 절대 규칙)를 **스키마 수준에서 강제**했다.
  `RawImport`(원본 응답 보존) · `SourceMapping`(원본 ID 보존) · `ImportJob`/`ImportFailure`
  (중단 후 재개, 실패 기록) · `MigrationCheck`(숫자 대조) · 경기 시점 division 스냅샷 ·
  래더 재현 정보 · 무기별 분리 · 시즌 스냅샷 · `RatingConfig`.
- 로컬 PostgreSQL을 `embedded-postgres`로 띄웠다 (Docker·PostgreSQL 미설치 환경).
  한국어 Windows에서 `initdb`가 두 번 막혔고 둘 다 원인을 찾아 우회했다 (D-022).
- Mock과 **같은 결정적 픽스처**를 DB에 적재하는 시드 + 자가 점검(`pnpm db:check`).

**API**
- 계약 엔드포인트 전량을 Prisma 기반 Route Handler로 구현 (57개 라우트).
- 쓰기가 **실제로 저장된다**: 글·댓글·추천·설정 변경. 서버 측 새니타이즈 · rate limit ·
  권한 검사 · 비로그인 글 비밀번호 해시.
- 인증은 `jose`(JWT) + `bcryptjs`로 직접 구현. httpOnly 쿠키 + 리프레시 토큰 rotation.
  계획 문서의 Auth.js와 다른 선택이라 사유를 남겼다 (D-025) — **사용자 확인 필요**.

### 검증

- **`pnpm compare` 신설** — Mock과 실제 API의 응답을 **값까지** 재귀 비교. **25/25 일치.**
- 계약 준수 테스트 9건 (실제 응답을 계약 Zod로 파싱).
- typecheck / lint / test(157건) / build 전부 통과.
- mock 모드로 되돌려 주요 화면 회귀 확인.

### 발견하고 고친 것

- **랭킹·기록실이 화면에서 404** — 계약은 `:leagueId`인데 화면은 슬러그를 넘긴다 (D-028).
  curl과 계약 테스트로는 못 잡고 **실제 화면을 열어야** 드러난 종류였다.
- **Phase 0 픽스처 3건의 시각이 id 순서와 어긋나 있었다** (D-029).
  게시글·댓글·리그 참여 시각. 목록이 최신순인데 시각 열이 뒤죽박죽으로 보이던 상태였고,
  DB(시각 정렬)와 순서도 달라졌다. 회귀 테스트 4건을 추가했다.
- Next 서버 번들에서 `Prisma.sql`이 동작하지 않는 문제 (D-027).

### 남은 것 / 확인 필요

- **Chrome 창이 최소화돼 있어 확장이 응답하지 않는다.** 스크린샷과 나머지 화면 검수를 못 했다.
  창을 복구한 뒤 다시 확인해야 한다.
- **서든어택 계정 연동이 소유권을 증명하지 않는다.** 운영 노출 전 반드시 해결.
- 캡차 · 이메일 발송 · 이미지 업로드(오브젝트 스토리지) · 리치텍스트 에디터는 아직 없다.
- Auth.js 미사용 결정(D-025)에 대한 사용자 확인.

### Phase 7 화면 검수 (2026-08-21)

프로덕션 빌드(`pnpm build` + `start`)로 검수했다. 개발 서버는 라우트마다 첫 요청에서
컴파일이 일어나 화면 하나에 수십 초가 걸리고 렌더러가 멈춰, 앱 문제와 구분이 안 됐다 (D-032).

**확인한 화면** — 홈 / 로그인 / 회원가입 / 마이페이지(내 정보·정보 수정·비밀번호 변경·계정 연동) /
게시판 목록 / 글 상세 / 글쓰기 / 댓글 / 추천 / 클랜랭킹(1·2부) / 개인랭킹 / 리그홈 /
클랜 기록실 + 매치 상세 펼침 / 개인 기록실 / 지난시즌 / 클랜 프로필 / 클랜원 / 플레이어 프로필

**실제로 조작해서 확인한 것** — 로그인 → 복귀, 로그아웃, 회원가입(실제 계정 생성),
닉네임 변경 저장, 글쓰기(실제 저장), 댓글 작성, 추천, 매치 상세 아코디언.

**발견하고 고친 결함 3건**
1. 로그인 성공했는데 화면이 로그인 페이지에 남음 (D-031)
2. 1번을 고치다 만든 회귀 — 보호막이 화면을 다시 마운트해 "저장했습니다"가 사라짐 (D-031)
3. **로그인 1시간 뒤 말없이 로그아웃** — 리프레시 토큰을 쓰지 않고 있었다 (D-030)

**끝내지 못한 것** — 리그 목록 표 / 리그 관리(소유자 화면) / 리그 만들기 / 통합검색 UI /
404 화면 / 작은 화면 레이아웃의 **스크린샷 확인**. Chrome 창이 포그라운드가 아니어서
탭이 `hidden`으로 throttle됐다(D-019). 권한과 API는 서버 수준에서 확인했다.

### Phase 7 화면 검수 2차 (2026-08-21)

Chrome 창을 올린 뒤 남은 화면을 이어서 확인했다.

**추가로 확인한 화면** — 리그 목록(개설일 오름차순·공식 배지) / 리그 만들기(제약 안내·부리그·맵·대전인원) /
리그 관리(클랜 초대·참여 클랜 관리) / 추방 확인 모달.

**함께 재확인한 것**
- 로그인 → `returnUrl` 복귀가 프로덕션 빌드에서도 동작한다 (앞서 고친 것)
- 관리자 화면에서 "관리자만 접근할 수 있습니다"가 스치지 않는다 (앞서 고친 것)

**끝내 확인하지 못한 3건** — 통합검색 UI 조작 / 404 화면 / 작은 화면 레이아웃.
Chrome 창이 반복해서 백그라운드로 내려가 탭이 `hidden`이 되고 렌더링이 throttle됐다(D-019).
셋 다 **Phase 1에서 스크린샷으로 확인했고 Phase 7에서 코드가 바뀌지 않았다.**
404는 HTTP 404 응답을, 작은 화면은 `pc-container` 고정폭(재배치 없음)을 코드로 확인했다.

**추방 확인 문구는 테스트로 옮겼다.** 백그라운드 탭에서는 입력이 React 상태에 반영되지 않아
UI로 확인하는 것이 불안정하다. 되돌릴 수 없는 작업이라 브라우저와 무관하게 검증돼야 한다.

---

## Phase 7 완료 (2026-08-21)

사용자가 검수 계정으로 직접 확인한 뒤 완료 처리했다.

### 최종 검증

| 항목 | 결과 |
|---|---|
| typecheck / lint | 통과 |
| test | **162건 통과** (계약 준수 9 + 추방 문구 4 포함) |
| build | 통과 (37 페이지) |
| `pnpm db:check` | 15항목 전부 통과 |
| `pnpm compare` (Mock↔실제 값 대조) | **25/25 일치** |

### Phase 7에서 고친 결함

1. 랭킹·기록실이 화면에서 404 — 계약은 `:leagueId`인데 화면은 슬러그를 넘긴다 (D-028)
2. Phase 0 픽스처 3건의 시각이 id 순서와 어긋나 있었다 (D-029)
3. 로그인 1시간 뒤 말없이 로그아웃 — 리프레시 토큰을 쓰지 않고 있었다 (D-030)
4. 로그인 직후 되돌아감 / 보호막이 화면을 재마운트해 상태 소실 (D-031)

### 다음 Phase 착수 전 확인할 것

- **넥슨 Open API 키** — Phase 8 실수집에 필요하다. 키 없이도 파이프라인 구조·검증 로직은 만들 수 있다.
- **Auth.js 미사용 결정(D-025)** — 계획 문서와 다른 선택이라 사용자 확인이 아직 남아 있다.
- **서든어택 계정 연동이 소유권을 증명하지 않는다** — 운영 노출 전 반드시 해결.

---

## Phase 8 — 넥슨 수집 파이프라인 (2026-08-21, 키 없는 구간 완료)

### 먼저 한 일: 수집원 실측

넥슨 개발자 포털 문서는 클라이언트 렌더링이라 정적으로 읽히지 않았다.
문서 페이지가 참조하는 **공식 OpenAPI 3.0.3 스펙 파일**(`/static/api/suddenattack/*.yaml`)을
직접 받아 엔드포인트·파라미터·응답 필드를 확정했다.

키 없이 경로 존재 여부는 확인할 수 없다 — 게이트웨이가 **없는 경로에도 같은 400**을 준다
(`/suddenattack/v1/no-such-endpoint` 로 확인).

### 이 Phase의 가장 큰 발견

**넥슨은 무기·MVP·탈주·플레이시간·종료시각·선공진영을 주지 않는다.**
`weapon`을 0(라이플)으로 채우면 3rd.supply 재현이 아니라 **거짓말**이 된다.
그래서 스키마·계약·UI를 전부 nullable로 넓혔고, `false`도 쓰지 않기로 했다 (D-034).
`false`는 "아니다"라는 실제 정보이기 때문이다.

### 만든 것

- `packages/nexon` — 클라이언트(주입 가능한 fetch/시계) · Zod 스키마 · 오류 분류 ·
  토큰버킷(429 자동 감속) · 지수 백오프 · 정규화 · 내용 해시 · 키 리댁션
- `apps/worker` — identities / collect / project / refresh / check / status CLI,
  `ImportJob` 체크포인트, `ImportFailure`, `MigrationCheck`
- 마이그레이션 `20260821194025_nexon_ingest` — nullable 확장 · `RawImport` append-only ·
  스테이징 5개 테이블
- `apps/worker/src/dev/offlineSmoke.ts` — 네트워크 없이 실제 DB에 전 구간을 돌리고 정리까지 한다

### 검증

| 항목 | 결과 |
|---|---|
| typecheck / lint / build | 통과 |
| test | 257 passed / 1 skipped (신규 65건) |
| `pnpm db:check` | 18항목 통과 |
| `pnpm compare` | 25/25 일치 (nullable 확장 후에도 Mock↔실제 동일) |
| 오프라인 스모크 | 27항목 통과 — 멱등성·원본 보존·투영·정리 |
| Chrome 육안 검수 | 기록실 카드/상세 정상 (플레이시간·[S]·MVP·선블루 표기 유지) |

### 하지 않은 것

- 실제 넥슨 호출 **0건** (키가 없다)
- 래더·랭킹 계산 (Phase 9)
- 관리자 수집 대시보드 (CLI 리포트로 대체)
- Redis / BullMQ

---

## Phase 8 — 실응답 소량 검증 (2026-08-21)

닉네임 1명, **총 15회 호출**. 429·403 없음. 테스트 키(`test_`)로 서든어택 API가 정상 동작한다.

### 스펙이 틀렸던 곳

1. 참가자 클랜명은 `clan_name`이 아니라 **`guild_name`** (D-043). 스키마·정규화를 실제 기준으로 고쳤다.
2. **한 경기 응답에 양 팀이 함께 오지 않는다** (D-044). 5경기 전부 그랬다.
   승리 팀 전원 + (졌으면) 본인. 상대 팀 라인업이 없으니 클랜 vs 클랜 경기를 재구성할 수 없다.
   투영 규칙이 틀린 게 아니라 **입력이 부족한 것**이다. Phase 9의 선결 과제로 올렸다.

### 확인된 형식

`ouid` 32자리 hex · `match_id` **18자리 숫자**(3rd.supply와 같은 형식) · `date_match` 밀리초 포함 UTC ·
`damage` 정수 · `team_id`는 개인전에서 `null` · 참가자 `ouid` 없음 · 무기/MVP/탈주/플레이시간 필드 자체가 없음.

### 사고와 복구 (D-045)

빈 DB 기준으로 만든 `offlineSmoke.ts`가 실데이터가 있는 DB에서 돌면서
**상세 미수집 스테이징 200건을 가짜 응답으로 덮어썼다.**

`RawImport`에 목록 원본이 남아 있어 **200건 전부 되돌렸다.**
"원본을 버리지 않는다"는 규칙이 실제로 사고를 막았다.
이후 `runCollect(detailSourceMatchIds)` / `runProject(sourceMatchIds)`로 범위를 인자로 강제했다.

### 검증

- typecheck / lint 통과, test **275 passed / 1 skipped** (실응답 회귀 11건 추가)
- `pnpm nexon:check` 7항목 · `pnpm db:check` 18항목 · 오프라인 스모크 27항목 통과
- 최종 DB: RawImport 11 · NexonMatch 698(상세 6) · 참가자 37 · 운영 `Match(origin=nexon)` **0**
- 실응답 픽스처 3종을 가명화해 저장 (`packages/nexon/src/fixtures/real/`)

---

## Phase 8.1 — D-044 후속 검증 + 적응형 폴링 (2026-08-21)

### D-044를 추측 대신 실측으로 좁혔다 (실호출 7회)

1. **같은 `match_id` 재호출 3건 → 응답이 완전히 동일**했다(contentHash 일치, 새 행 없이 fetchCount만 증가).
   `/match-detail`은 파라미터가 `match_id` 하나뿐이므로 **다른 계정으로 다시 불러도 같다.**
   → "multi-OUID로 상세를 합친다"는 방법은 성립하지 않는다 (CASE 1).
2. 같은 경기가 **다른 참가자의 매치 목록에는 있었다.** 그 목록이 준 kill/death/assist는
   상세의 같은 사람 기록과 일치했다 → 목록 관측값으로 **아는 사람만** 채울 수 있다 (조건부 CASE 3).
3. 상대 팀 전원은 여전히 불가하다. 상세가 상대 닉네임을 주지 않아 **모르는 사람은 조회조차 못 한다.**

부수 발견: 상세에 있던 닉네임으로 `/id`를 불러 얻은 계정의 목록에 그 경기가 **없었다.**
닉네임이 같다고 같은 사람이 아니라는 증거다 — D-036이 실데이터로 확인됐다.

### 적응형 폴링

5,000명 전수 조회를 폐기했다. 한 명당 모드 4개 = 호출 4회이므로 고정 주기는 비용이 선형으로 커진다.

- `NexonPollState` / `NexonPollRun` / `NexonMatchObservation` 3개 모델 추가
- 정책은 순수 함수(`pollingPolicy.ts`)에 두고 숫자는 전부 `NEXON_POLL_*` 설정값
- 새 경기 → hot(짧게) / 빈 조회 누적 → warm·cold·dormant(길게) / 실패는 티어를 내리지 않음
- 수동 갱신 최우선, 오래 밀린 대상은 우선순위를 올려 굶지 않게
- 이미 상세를 가진 경기는 다시 부르지 않는다. 목록 관측값은 호출 없이 쌓인다

### 또 한 번의 스모크 사고 (범위 문제, 즉시 수정)

D-045를 만들고도 `runPoll`에는 범위 인자가 없어서, 스모크가 **실제 신원 3명의 폴링 상태**를 건드렸다.
데이터 손상은 없었고(가짜 응답이 만든 매치는 정리됨) 폴링 상태만 초기화하면 되는 문제였다.
`runPoll(ouids)` / `ensurePollStates(ouids)`에 범위를 추가하고 스모크가 자기 계정만 넘기게 고쳤다.

교훈은 D-045와 같다. **개발 도구는 자기 데이터 밖을 건드릴 수 없어야 한다.**
새 실행 경로를 추가할 때마다 범위 인자를 함께 넣는다.

### 검증

- typecheck / lint / build 통과, test **290 passed / 1 skipped** (폴링 정책 15건 추가)
- 오프라인 스모크 41항목 · `nexon:check` 7항목 · `db:check` 20항목 통과
- 실호출 누계 22회(Phase 8 15 + 8.1 7). 429·403 없음
