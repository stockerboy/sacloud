# 밤샘 보고 — 2026-09-01 (02:30 ~ 07:00)

> 사용자가 02:30 에 자러 갔다. 07:40 에 깬다.
> 지시: *"너희가 할 수 있는 건 전부 너희가 해라. 나를 절대 최대한 부르지마.
> 나를 불러야 하는 순간 이 프로젝트는 망가져."*
>
> **그래서 이 문서의 목표는 「아침에 할 일을 0에 가깝게 만드는 것」이다.**
> 못 한 것은 못 했다고, 모르는 것은 모른다고 적는다. 지어내지 않는다.

---

## 0. 한 줄

**사이트는 살아 있고 훨씬 빨라졌다. 9.4시간 멈춰 있던 데이터 수집이 다시 돈다.
그리고 아무도 몰랐던 **운영 장애 두 건**을 찾아 고쳤다 — 1위 클랜의 클랜 상세와,
상위 선수들의 선수 상세가 **둘 다 500 이 나고 있었다.**

---

## 1. 오늘 밤 고친 것 — 큰 것부터

### ① 새 경기가 안 들어오던 것 — **원인 확정 · 고침**

```
증상   운영 최신 경기가 몇 시간씩 밀린다. CI 는 계속 빨갛다
```

**원인이 셋이었고, 처음 짐작한 것은 틀렸다.**

**(a) CI 검증이 태어날 때부터 빨갰다.** 성공한 run 이 **한 번도 없었다**(19건 중 0건).
`formulaVersion 은 있는데 rating 이 없음 → 361만건` 이라는 단언이 매번 깨졌는데,
**단언이 틀렸다.** 미러 적재는 「우리 공식으로 계산하지 않았다」는 표시로 그 칸을
채우므로 `ratingUpdate` 가 비어 있는 것이 정상이다. 한 커밋(`bf83348`)이 **규칙을 깨는
쓰기와 그것을 검사하는 CI 를 동시에** 넣었다. → 고쳤다 (D-224)

**(b) 검사가 검사를 가렸다.** 그 스텝이 죽으면 뒤가 전부 skip 되는데, 바로 뒤가
**열산 오염 감시(D-210)** 였다. 「조용히 넘기지 마라」고 못 박아 둔 알람이 **3일 동안
꺼져 있었다.** → `always()` 로 분리했다 (D-224)

**(c) 진짜 원인은 GitHub 이었다.** 예약 슬롯 153칸 중 **17칸만 발사됐다(89% 드롭)**.
놓친 것의 80%는 그때 러너가 놀고 있었다. concurrency 취소는 실측 0건.
→ **잡이 끝나기 직전 자기 자신을 부르게** 했다(self-dispatch). 예약은 후퇴값으로 남겼다.
폭주 방지 세 겹(체인 깊이 40 · 1시간 5건 · 최소 생존 10분). (D-229)

**결과**: `2026-08-31T19:21Z` 에 **9.4시간 만에 첫 run 이 돌았고, 데이터가 실제로 들어온다.**

```
05:50 실측 (운영)
  DPL     최신 경기 04:30 (1.0h 전)   마지막 적재 05:01 (0.5h 전)
  열산     최신 경기 04:48 (0.7h 전)   마지막 적재 05:19 (0.2h 전)
  대룰     최신 경기 00:38 (4.8h 전)   마지막 적재 05:17 (0.2h 전)
```

**어젯밤 8시간 밀려 있던 것이 12분 전 적재로 회복됐다.**
대룰도 0.2시간 전에 적재됐다 — 폴링 하한(사이클당 1곳 → 6곳)이 실제로 먹었다는 뜻이다.

### 그리고 **체인이 실제로 돈다** (06:30 확정)

```
#17  ev=push               failure   ← C팀이 넣은 시각 가드가 스스로 체인을 끊었다
                                       (첫 실전 30분 만에 찾아서 고쳤다)
#18  ev=push               SUCCESS   ← **이 저장소 최초의 완전 초록 run**
#19  ev=workflow_dispatch  running   ← #18 이 만들었다. 아무도 안 눌렀고 예약도 아니다
```

**`GITHUB_TOKEN` 으로 `workflows/{id}/dispatches` 가 통한다.** 오늘 밤 최대 `[미확인]` 이
닫혔고 **PAT 숙제가 사라졌다.**

run #18 은 스텝이 전부 초록이다 — 무결성 검증이 **3일 만에** 초록이고, D-210 도, 신선도도.
**직전 19건 중 성공은 0건이었다.** 워크플로가 태어난 날부터 한 번도 초록인 적이 없었다.

> #17 진단이 **추론에서 확인으로** 바뀐 것도 값지다.
> `self-dispatch = success` 인데 run 이 안 생겼다 → 403 이 아니라 가드였다.
> **재려고 미리 넣어 둔 장치**(dispatch 실패를 빨갛게 · `29f8e5e`)가 없었으면
> 「403 인가 가드인가」를 못 갈랐다.

> ⚠ **정정**: D-224 는 「전수 집계가 concurrency 로 밀려 취소됐다」고 적었다. **틀렸다.**
> 스텝별 시각을 열어 보니 셋 다 실제로 돌았고 **90분 타임아웃에 죽었다**(step 10 이 84~88분).
> 로컬 5분짜리가 운영에서 88분이다 — **운영 DB 가 17배 느리다.**
>
> ⚠ **정정**: 「daerule 이 49시간째 밀렸다」도 **판정을 철회한다.** 8일 간격을 재 보니
> daerule 은 경기가 12건뿐이고 최대 공백이 116시간(4.8일)이다. 49h 는 정상 분포 안이다.

### ② 사이트 속도 — **26배**

```
X-Vercel-Id: icn1::iad1        접속은 서울, **함수는 미국 워싱턴DC**
```
DB 는 서울(Supabase)인데 함수가 미국에 있어서 **질의 한 번마다 태평양을 왕복**(~200ms)했다.
메인 화면은 질의를 수십 번 던지므로 그대로 10초가 됐다. `vercel.json` 에 `regions:["icn1"]`.

```
                        전         후(엣지 HIT)
메인 /                 —      →   0.07 초
리그 목록              —      →   0.06 초
개인랭킹               —      →   0.11 초
클랜 상세          1.2~1.7초  →   0.09 초
/api/home/top      10.4 초   →   0.06 초
```

같이 고친 것 둘 — 개인랭킹이 **쓰지도 않을 집계를 항상 물던 것**(운영 1.3초),
그리고 랭킹·클랜상세·리그목록을 **엣지가 대신 답하게** 한 것(함수를 아예 안 깨운다).
로그인 상태가 섞이는 곳(마이페이지·관리자·알 목록)에는 **붙이지 않았다.** (D-223)

### ③ 관리자 대시보드 25초 → **운영 0.89초**

정체는 **질의 한 줄**이었다. 나머지 22개를 다 더해야 2.9초다.
`origin='nexon'` 경기가 38만 중 **136건**뿐인데 `orderBy id desc` 가 플래너를
**PK 역방향 스캔**으로 끌고 가 361만 행을 거꾸로 훑었다.

```
운영 실측  114.7초 → 0.935초  (123배)
```
`_count` 하나가 8.7초였던 것도 고쳤다(50건 보려고 361만 행을 GROUP BY 했다). (D-230)

### ④ `<a>` 에 준 색이 **한 개도 안 먹고 있었다**

빌드된 CSS 번들을 바이트 오프셋으로 갈라 잡았다. `styles.css` 의 `a { color: inherit }` 이
`@layer` **밖**에 있었고, **언레이어드 선언은 모든 `@layer` 를 이긴다.**
→ 서브내비 · GNB · 관리자 탭의 **활성/비활성 글자색 구분이 전부 죽어 있었다** (23곳 · 17파일).

**한 줄만 지워서 고쳤다.** preflight 가 이미 같은 선언을 `@layer base` 에서 하기 때문이다.
`a:hover` 는 언레이어드로 남겼다 — 다른 세 안은 **전부 전역 hover 를 죽인다.** (D-231)

> **운영 CSS 번들에서 직접 확인했다** (브라우저 없이):
> ```
> 남아 있는 a{color:inherit}  preflight 것 하나뿐 (@layer base 안 · h1~h6 옆)
> 우리 규칙                    a{text-decoration:none; transition:color .12s}
> a:hover{color:var(--color-accent)}  살아 있다
> ```
> 즉 **의도한 대로 정확히 반영됐다.** 남은 것은 눈으로 보는 확인 사살뿐이다.

### ⑤ 최대 클랜의 클랜 상세가 **운영에서 500 이었다** — 아무도 몰랐다

**캐시가 진짜 비용을 가리고 있었다.** 밤 초반에 「클랜 상세 1.2초」로 재고 넘어갔는데,
그건 **엣지 캐시 HIT** 였다(D-223 이 붙인 30초). 캐시를 우회해 다시 재니:

```
운영  /clans/lpcrew/show      12.0초 → **500** (함수 제한시간 초과)
      /clans/lpcrew/players    8.0초 → 200 (경계선)
```

`lpcrew`(MiraGe.)는 8,755경기짜리 **최대 클랜**이다. 다른 클랜은 멀쩡했다.
**가장 큰 클랜 하나만 죽고 있었고, 그 클랜이 곧 1위 클랜이다.**

범인은 `resolvePositionsOf` 의 weapon `groupBy` — 인덱스에 `weapon` 이 없어서
1.9GB 표에서 힙 페치 3만 블록을 했다. `INCLUDE ("weapon")` 하나로 끝났다.

```
                     전         후
운영 /show        500(12초)  →  0.32초
운영 /players      8.0초     →  0.17초
로컬 /show        21.4초     →  2.3초
```

`CONCURRENTLY` 라 쓰기를 안 잠갔고(64초 · 229MB), **옛 인덱스는 안 지웠다** —
되돌릴 자리를 남겼다.

> **규칙**: **캐시를 붙인 경로는 캐시 뒤의 값을 따로 재라.**
> 안 그러면 캐시가 장애를 숨긴다. 이번엔 실제로 숨겼다.

**고친 뒤 세 리그의 큰 클랜 18곳을 캐시 우회로 훑었다 — 전부 200.**
`lpcrew` 보다 큰 클랜들도 산다.

```
supply   Onepoint 10,634경기 2.5초 · footmania2 10,258 4.6초 · AimEnvy 10,136 2.2초
         lpcrew 8,755 2.0초 · resun 8,478 1.7초 · togs4033 6,713 1.6초
sanply   suddenalexia 14,032 3.2초 · kym2101 8,019 1.6초 · …
nolink   pigforever 2,225 4.1초 · 나머지 0.3초대
```

인덱스가 없었으면 **1만 경기 넘는 클랜들도 같이 죽고 있었을 것**이다.
콜드 2~4.6초는 아직 느리지만 죽지는 않고, 반복 조회는 엣지가 덮는다.

⚠ **옛 인덱스(297MB)를 아직 안 지웠다.** 새 인덱스가 선두 칼럼이 같아 완전히 대체하므로
지우면 297MB 를 되찾고 적재 쓰기도 빨라진다. **지금은 안 지운다** —
지금 수집 체인을 시험하는 중이라 변수를 하나 더 넣지 않는다.
아침에 `node scripts/prod-run.mjs perf-covering-index --drop-old --confirm` 한 줄이다.

### ⑥ 선수 상세도 **운영에서 500 이었다** — 이쪽이 더 나빴다

클랜 상세를 고치고 **같은 방법으로**(캐시 우회 + 가장 무거운 대상) 훑다가 찾았다.
선수 상세는 엣지 캐시가 아예 없어서 **사용자가 그대로 맞는다.**

```
운영 DPL 상위 6명    4명이 500 (11~22초)
운영 열산 상위 4명    **4명 전부 500** (11~17초)
```

가지별로 재니 범인이 하나였다 — **`playerTraits`(육각형) 6.6초.**
나머지 넷(누적·폼·오늘·티어별)을 다 더해야 0.7초다.

육각형 백분위는 **그 리그 같은 무기 선수 전원의 분포**가 있어야 나온다. 그래서 리그
전체 참가기록을 통째로 집계하는데, 열산은 200만 행이다. 프로세스 안 캐시가 있지만
**서버리스는 매번 차갑다 — 캐시는 콜드 스타트를 못 덮는다.**

```
                     전              후
열산 상위 4명    전부 500      →   대부분 200 (1.4~9.3초)
DPL 상위 8명     4명 500       →   전부 200 (1.1~6.6초)
로컬 playerTraits  6.6초       →   1.05초
```

인덱스 하나(`matchId` 에 `playerId·weapon·kill·damage` 를 얹어 index-only)와,
**선수 상세에도 엣지 캐시**를 붙였다 — 인덱스로도 콜드 첫 요청은 무겁고, 그 한 번을
엣지가 덮는다.

> **두 장애를 같은 방법으로 찾았다** — 캐시를 우회해서, 가장 무거운 대상으로.
> 앞서 캐시 HIT 값을 보고 「1.2초」로 넘어갔던 것이 **둘 다 숨기고 있었다.**

**고친 뒤 표본으로 확인했다 (05:35 · 전부 캐시 우회):**
```
클랜 상세  세 리그 30곳   30/30 = 200
선수 상세  두 리그 30명   30/30 = 200
그 밖      기록실 · 매치 상세 · 지난시즌 · 참여리그 · 통합래더 · 무소속래더 · 폼랭킹
           전부 200 (0.17 ~ 5.6초)
선수 상세 엣지 캐시  MISS 5.7초 → HIT 0.25초
```

> **곁다리 — B팀이 한 시간 전에 문서화한 함정을 내가 그대로 밟았다.**
> 이 표본 검사를 처음 돌렸을 때 **30곳이 전부 `000`** 이 나왔다. 원격을 의심할 뻔했는데,
> B팀의 규칙(**「우리가 보낸 바이트를 먼저 본다」**)대로 `cat -A` 로 URL 을 찍으니
> `.../clans/lpcrew^M/show` — Windows 파이썬이 `` 을 붙이고 있었다.
> `tr -d ''` 하나로 30/30 이 됐다. **같은 함정에 두 사람이 두 시간 안에 걸렸다.**

### ⑦ 그 밖

- **알 시스템** — 깨짐을 서버가 기억하고(`EggBreak`), 공개 조회 API 와 관리자 화면
  (`/admin/eggs`)이 붙었다. 관리자가 알을 하나씩 깨고 되돌릴 수 있다 (D-222)
- **클랜원 카드** — 포지션을 걷어내고 목록으로. 오른쪽에 접속중/미접속 불 (D-226)
- **클랜마크** — `static.3rd.supply` 주소는 **넥슨 주소를 base64 로 감싼 것**이었다.
  운영은 이미 넥슨을 문다 (D-227)
- **배틀로그** — 빠진 파일 6개를 넣어 8,015행 · 고유 경기 7,167.
  포지션 프로필 1,275 → **4,381명** · 정확도 91.3%
- **`position-build` 가 죽던 것** — 전량을 한 번에 메모리에 올리다 힙 2GB OOM,
  데이터가 늘자 **힙을 올려도 Prisma 가 먼저 죽었다.** 배치로 흘려 읽게 고쳤다 (D-225)
- **배포가 3.2GB 를 올리다 죽던 것** — Vercel CLI 는 `.gitignore` 를 안 본다.
  `.vercelignore` 를 만들었다

---

## 1-A. 아침에 열어 볼 때를 위해 — **운영 전 경로 스모크 (04:45 실측)**

**32개 경로 전부 `200` · 전부 1.2초 이하.**

```
/                            0.22   /auth/login                 0.37
/leagues                     0.59   /auth/signup                0.62
/league/supply/home/info     1.20   /auth/password/forget       0.39
/league/supply/rank/player   0.30   /clause/service · policy    0.37 · 0.89
/league/supply/rank/clan/1   0.27   /me · /me/link · /me/setting  0.39 · 0.42 · 0.54
/league/nolink/rank/player   0.14   /leagues/create             0.45
/league/sanply/rank/player   0.25   /league/supply/clan/lpcrew  0.40
/board/hot · /board/free     0.32 · 0.15   …/clan/lpcrew/player 0.19
/board/free/write            0.35   /clan/lpcrew · /player      0.37 · 0.18
/admin · /admin/eggs         0.46 · 0.42
/admin/clans · matches · seasons · legacy   0.70 · 0.51 · 0.93 · 0.49
/notfound                    0.46
```

**깨진 화면 없음.** 관리자 화면 넷도 전부 1초 아래다 — 어제까지 25초였던 자리다.

### 알 기능 — **운영에서 왕복으로 확인했다** (04:45)

기능이 붙었다고만 하고 끝내지 않았다. 실제로 깨 보고, 보이는지 보고, 되돌렸다.

```
1  관리자 로그인                                        OK
2  대상 고르기 (아직 안 깨진 클랜)                        togs4033
3  POST /api/admin/eggs/clan/togs4033                  broken=true · reason='admin'
4  GET  /api/eggs/broken  (공개)                        clans: ["togs4033"]   ← 즉시 보인다
5  GET  /api/admin/eggs   (관리자)                       reason=admin · brokenAt 기록됨
6  DELETE /api/admin/eggs/clan/togs4033                broken=false
7  GET  /api/eggs/broken                               clans: []             ← 되돌아갔다
8  비로그인 GET /api/admin/eggs                          403                   ← 여전히 막힌다
```

**원상복구했다.** 운영 DB 의 `EggBreak` 는 다시 0건이다 — 시험 흔적을 남기지 않았다.
`reason='admin'` 이 제대로 박히는 것도 확인했다. 나중에 칭호 인증이 들어오면
이 사유로 남은 것만 골라 지우면 된다 (D-222).

---

## 2. 아직 안 되는 것 (정직하게)

| | 무엇 | 왜 |
|---|---|---|
| 1 | **알이 화면에서만 가려진다** | API 를 직접 부르면 승률·육각형이 그대로 나온다. 설계는 나왔고 **칭호 인증이 들어온 뒤에** 켜야 한다 (지금 켜면 연동만 한 사용자가 자기 기록을 못 본다) |
| 2 | **클랜원 접속 표시가 전원 「알수없음」** | 운영에 병영 명단이 0건이고 선수 연결도 0건이다. 화면은 데이터가 들어오면 그때 한 줄도 안 고쳐도 된다 |
| 3 | **클랜 육각형 V2 가 화면에 없다** | 사양은 완성. 재료도 이미 있다(배틀로그 6,844경기 · 91클랜). 순수 함수부터 만드는 중 |
| 4 | **개인 육각형이 3~5/6 이다** | 스나수 3/6 · 라플수 4/6. 막던 재료(상대 무기)는 **이미 있다는 것이 밝혀졌다** — `target_weapon` 커버리지 97.8% |
| 5 | **전수 집계가 한 번도 완주 못 했다** | 90분 타임아웃. 리그별로 쪼갰지만 100분이 맞는 값인지 **아직 안 쟀다** |
| 6 | 이메일 발송 · 캡차 · 이미지 업로드 · 리치텍스트 에디터 · 계정 소유권 증명 | 손대지 않았다 |

---

## 3. 아침에 사람 손이 필요한 것

**전부 30초~2분짜리다. 급한 것은 없다.**

```
① clan-mark-restore --confirm
   node scripts/prod-run.mjs clan-mark-restore            (미리보기 402곳)
   node scripts/prod-run.mjs clan-mark-restore --confirm  (실행)
   그 직후 CSP 에서 static.3rd.supply 제거 (next.config.ts + clanMarkAudit.ts 둘 다)
   → 이게 D-227 완료 판정이다
   ※ 기술적 위험 없음 확인됨. 미리보기·검증·백업 경로 다 잡혀 있다

② 배틀로그 수집 시작
   절차서 docs/BATTLELOG_COLLECT_RUNBOOK.md · 목록 5,153짝 준비됨
   전제: 병영수첩 로그인된 크롬
   ※ 밤에 무인으로 돌리지 않았다 — 사용자 신원으로 제3자를 몇 시간 두드리는 일이고,
     막히면 5시간 반 동안 아무도 못 멈춘다

③ 넥슨 이용약관 확인  [미확인]
   클랜마크를 넥슨 CDN 에서 직접 물기로 했다. 아무도 약관을 안 봤다.
   물리면 되돌린다 — 백업 JSON + 코드 되돌리기

④ 칭호 하나를 인게임에서 바꿔 주기 (1회)
   칭호 인증의 반영 지연을 재려면 실제로 칭호를 바꿀 사람이 필요하다.
   바꾸고 몇 분 뒤 알려 주면 우리가 잰다

⑤ (아마) GitHub 화면에서 「Run workflow」 한 번
   전수 집계 소요 시간을 dry_run 으로 재려면 필요하다. 토큰이 없어 우리가 못 쏜다
```

---

## 4. 물어야 할 것 (답이 없으면 못 넘어가는 것)

```
Q1  개인 육각형 5번축 「원어택 성공률」의 정의 확정          → 재료는 이제 있다
Q2  클랜 육각형 ①「A쪽」이 어디인가 · ⑥「녹뒤」「머리」가 어디인가
    구역 파일에 그 두 지명이 **없다.** 지어내지 않았다.
    상대 스나가 실제로 죽는 자리 좌표를 뽑아 뒀으니 칠해 주기만 하면 된다
Q3  클랜 육각형에서 두 클랜을 **어떻게 갈라 그릴 것인가**
    사용자 원문은 「색을 다르게」인데 적진 팔레트는 진홍 하나다.
    추천: 레드팀=진홍 실선+옅은 채움 / 블루팀=새 색 점선+채움 없음
Q4  게시판 이름 「3부게시판」을 「열산게시판」으로 바꿀지
Q5  「래더 미반영」 배지를 남길지 · MVP 표기 ★ 가 맞는지
```

---

## 5. 측정 못 한 것 · 뒤집은 판단

**측정 못 한 것**
- **전체 게이트는 재는 데 성공했다** (05:28~05:40 · 세션이 줄어든 뒤):
  ```
  118 파일 · 1,915 통과 · 2 실패 · 6 skip
  ```
  **로직 실패는 0건이다.** 실패 2건은 `adminApi.test.ts` 의 **정확히 30,000ms 타임아웃**이고,
  둘 다 「로그인 안 하면 막힌다」류의 권한 검사다 — 값이 틀린 게 아니라 안 끝난 것이다.
  ⚠ **이 숫자는 저장소 건강보다 DB 경합을 더 많이 잰다.** 같은 파일이 04시엔 훅
  타임아웃으로 빨갛고 05시엔 완주했다. 낮에 다시 재면 또 다르다.
  참고로 밤 초반(경합이 심할 때) 측정은 **1,335 통과 · 3 실패**였다
- **CSS 수정(④)을 눈으로는 확인 못 했다.** 다만 **운영 CSS 번들에서 규칙 자체는 확인했다** —
  남은 `a{color:inherit}` 은 preflight 것 하나뿐이고 `a:hover` 는 살아 있다.
  즉 「반영됐는가」는 닫혔고, 「보기에 어떤가」만 남았다
- **커버링 인덱스를 안 만들었다.** 순간 700MB 여유가 필요한데 Supabase 실제 할당량이
  SQL 로 안 읽힌다

**뒤집은 판단 (그대로 적는다)**
1. **「넥슨이 연결을 막았다」 → 우리 버그였다.** Windows 파이썬이 URL 목록에 CRLF 를
   붙였고 curl 이 `\r` 을 물고 나갔다. `od -c` 로 확증하고 같은 주소를 다시 쏴서 8/8 로
   반증했다. **없는 위험을 보고하면 다음 사람이 그것 때문에 잘못된 결정을 한다**
2. **「전수 집계가 밀려서 취소됐다」 → 90분 타임아웃이었다**
3. **「daerule 49시간은 밀린 것」 → 정상 분포 안이었다**
4. **「러너가 미국이라 지연이 지배적」 → 반증됐다.** 52초 대 72~108초, 2배지 10배가 아니다.
   원인은 지연이 아니라 **데이터 양**이다
5. **「배틀로그 적재가 1,174행」 → 이미 7,815행이었다.** 기록이 낡아 있었다
6. **「정답 라벨 20명이 없다」 → 파일이 있었다** (23명)
7. **「UI 차이 45건 중 35건 미착수」 → 재감사 결과 유효 1 · 무효 6 · 해소 37**
8. **C팀이 자기 추정을 실측으로 내려잡았다** — 「하루 260사이클」 → 실측 196,
   step 9 예상 20분 → 실측 26.9분
9. **내가 「푸시했습니다」라고 말했는데 안 했다.** B팀이 원격을 직접 확인해 잡았다.
   ⚠ **「했다고 말했는데 안 한 것」은 틀린 판단보다 나쁘다** — 다음 사람이 확인 없이 믿는다.
   원인은 단순하다: `git push` 를 다른 명령과 묶어 돌리고 **출력을 안 읽었다.**
   앞으로 푸시는 원격 해시를 **다시 읽어** 확인한다

---

## 6. 밤새 어떻게 일했나

사용자가 팀 넷을 켜 주었고, 각 팀이 서브에이전트를 붙였다.

```
나(claude-ce)  성능 · 알 시스템 · 통합 · 배포 · 결정 문서       + 서브에이전트 8
A팀 lg-8f      칭호 인증 · UI 45건 재감사
B팀 lg-68      클랜마크 · CSS 레이어 분석 · 수집 준비
C팀 lg-94      예약 드롭 · 폴링 하한 · 신선도 알람
D팀 lg-c8      렌더 검수 (헤드리스 크롬 CDP 하네스를 직접 만들었다)
```

**밤새 서로를 여러 번 고쳤다. 그게 이 밤의 방법이었다.**
- B팀이 내 「수집 상한 5,000건」 승인을 **거부했다** — *「규모를 줄인다고 성격이 바뀌지
  않는다」*. 옳았다. 내 승인이 사용자 승인이 아니라는 지적도 정확했다
- C팀이 내 D-224 원인 판정을 뒤집었다 (concurrency 가 아니라 90분 타임아웃이었다)
- C팀이 **자기 코드가 만든 버그**를 첫 실전 30분 만에 찾았다 —
  체인을 시각으로 끊는 가드가 「고치려던 병을 하루 한 번 다시 앓게」 하고 있었다
- B팀이 자기 오탐(CRLF)을 스스로 잡아 보고서에서 빼게 했다
- B팀이 **자기 공으로 적힌 것을 정정했다** — 「캐시가 비용을 가린다」는 관점은 자기가
  낸 게 아니라고. 실제로 그쪽이 한 건 **관측(`X-Cache: HIT`)을 해석 없이 날것으로
  남겨 둔 것**이고, 거기서 원인을 본 건 내 쪽이다.
  **해석을 붙이지 않고 숫자를 그대로 남기는 습관이 값을 했다**

> 그리고 **틀린 판단을 고친 것보다, 고쳤다는 사실을 문서에 남긴 것이 더 값지다.**
> D-224 는 3일을 잃었는데 원인이 「빨간 CI 를 아무도 안 본 것」이었다.
> 오늘 밤 뒤집은 일곱 개를 안 적었으면 다음 사람이 같은 자리에서 또 넘어진다.

**A팀과 D팀은 서브에이전트를 못 썼다.** 그 세션들의 설정이 「사용자가 직접 요청하지
않으면 에이전트를 쓰지 않는다」이고, **동료의 메시지는 사용자 승인이 아니다.**
아침에 그 두 세션에 직접 한 줄 쳐 주면 병렬로 전환된다.
