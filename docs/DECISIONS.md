# DECISIONS.md — 프로젝트 결정 기록

`CLAUDE.md` 3장 11번(중요한 프로젝트 지식과 결정은 문서화)에 따라, **원본 관측으로 정해지지 않아 우리가 결정한 사항**을 남긴다.
여기 적힌 것은 전부 "원본과 동일함이 검증되지 않은" 자체 결정이며, 실측 결과가 나오면 갱신한다.

---

## Phase 0 (2026-08-20)

### D-001. 저장소 구조 — pnpm workspace 모노레포

```
apps/web/            Next.js 15 (App Router)
packages/contract/   Zod 스키마 + 엔드포인트 레지스트리   ← 단일 진실 원천
packages/mock/       결정적 픽스처 생성기 + MSW 핸들러
```

- 계획서의 `apps/worker`, `packages/db`, `packages/ui`는 **아직 만들지 않았다.**
  각각 Phase 8, Phase 7, Phase 1에서 실제로 필요해질 때 만든다. (지금 만들면 빈 껍데기만 남는다.)
- 내부 패키지는 빌드 단계 없이 `src/*.ts`를 직접 참조한다(`exports` → `./src/index.ts`).
  Next.js는 `transpilePackages`로 컴파일한다. 빌드 산출물 관리 비용을 없애기 위한 선택이다.

### D-002. 계약 표기 규칙

| 항목 | 결정 | 근거 |
|---|---|---|
| 필드명 | 원본 응답 그대로 **snake_case** (`win_rate`, `rating_update`) | 관측된 응답 형태를 유지해야 비교 검수가 쉽다 |
| 식별자 타입 | 전부 **문자열** | 매치 ID가 18자리(`YYMMDDHHmmss`+6)라 `Number.MAX_SAFE_INTEGER`를 넘는다 |
| 날짜/시각 | ISO 8601 + `+09:00` (예: `2026-06-05T00:06:24+09:00`) | 원본 포맷은 `[미확인]`. 계약으로 고정 |
| 파생값 | `win_rate`, `kd_rate`, `kill_per_match`, `damage_percent`를 **응답에 포함** | 원본이 서버 계산인지 `[미확인]`. 계산 규칙을 `packages/mock/src/derive.ts` 한 곳에 두고 Phase 7 서버도 동일 규칙을 구현한다 |
| 커서 | `base64url("next__<id>" \| "prev__<id>")`, 랭킹 20 / 게시판 15 | 관측값. 테스트로 고정 |

### D-003. 엔드포인트 출처 표시 (`origin`)

모든 엔드포인트에 `origin: 'observed' | 'designed'`를 붙였다.

- `observed` — `docs/3rd-supply-structure.md`에 기록된 실제 관측 엔드포인트
- `designed` — 화면 동작은 관측됐지만 경로·본문이 확인되지 않아 **우리가 설계**한 것

`designed`로 표시된 것은 전부 `[미확인]`이며, 실측되면 경로를 바꾼다.
현재 `designed`인 것: 비밀번호 재설정/이메일 인증, 갱신 요청(`/renew`), 플레이어·클랜 설정,
클랜 지난시즌, 리그 참여 클랜원 목록, 맵 목록, 리그 생성·관리 전반, 추천/비추천.

### D-004. 상대팀 결측(`알수없음`) 재현 방식

원본은 상대 클랜 플레이어의 딜량·헤드샷을 `알수없음`으로 표시한다.

- Mock 데이터에는 **양쪽 값을 모두 저장**하고, 응답 단계에서 **보는 쪽이 아닌 팀의 값을 null로 지운다.**
- 매치 상세(`GET /leagues/:leagueId/matches/:matchId`)에는 "어느 기록실에서 펼쳤는지"가 필요해서
  선택 쿼리 `league_clan_id`를 추가했다 — **자체 설계** `[미확인]`.
  원본은 아코디언이라 매치 개별 URL이 없어 이 정보를 어떻게 넘기는지 알 수 없다.

### D-005. 픽스처 정책

- 시드 고정(`FIXTURE_SEED = 20260820`) + 기준 시각 고정(`FIXTURE_NOW = 2026-08-20T12:00:00+09:00`).
  실제 시계를 쓰지 않으므로 스냅샷·테스트가 흔들리지 않는다.
- 규모: 리그 4(단일 1 + N부 3, 공식 2) / 클랜 60 / 플레이어 920 / 매치 3,000 / 게시글 400 / 댓글 1,200.
- 클랜명·닉네임·리그명·게시글 문구는 **전부 새로 만든 가상의 이름**이다. 원본 자산·문구를 복사하지 않았다.
- 클랜마크·업로드 이미지 URL은 존재하지 않는 자리표시자 호스트(`static.sacloud.local`)를 쓴다.
- 래더 증감은 관측 범위(승 +7~+12 / 패 −10~−19)의 **난수**다. **레이팅 엔진이 아니다** — Phase 9에서 다룬다.
- 승/패·킬뎃·래더는 생성된 매치를 시간순으로 누적해서 만들었다. 랭킹과 기록실이 서로 어긋나지 않게 하기 위함이다.

### D-006. 임시로 정한 값 (원본 미확인)

| 값 | 임시 결정 | 실제 확인 필요 시점 |
|---|---|---|
| 배치고사 판정 경기 수 | 10경기 | Phase 9 |
| `hot`(인기) 선정 가중치 | `추천×3 + 댓글×2 + 조회수/100` | Phase 7 |
| 리그맵 목록 | `맵 01`~`맵 08` 자리표시자 | Phase 3 이전 |
| 익명 별칭 형식 | `무명-123` 형태 | Phase 5 |
| 공식 리그 배지 필드명 | `official: boolean` | 실측 시 |
| 에러 응답 포맷 | `{ message, data?, errors? }` | Phase 7 |

### D-007. 툴체인

- **TypeScript는 5.9로 고정.** 최신 7.0.2에서 `next build`가 `next.config.ts`를 읽지 못한다
  (`Cannot read properties of undefined (reading 'fileExists')`). Next 15.5가 TS 7을 지원하면 올린다.
- pnpm 11은 설치 스크립트를 기본 차단한다. `pnpm-workspace.yaml`의 `allowBuilds`로 `msw`(워커 설치)와
  `sharp`(Next 이미지 최적화)만 허용했다.
- ESLint는 flat config 하나(루트)를 쓰고, `apps/web/eslint.config.mjs`는 루트 설정을 재수출한다
  (`next build`가 앱 디렉터리에서 설정을 찾기 때문).
- **Redis / BullMQ / Prisma / PostgreSQL / Auth.js / Tiptap은 설치하지 않았다.**
  각각 Phase 7~9에서 실제로 필요할 때 넣는다.

### D-008. Phase 0 범위에서 하지 않은 것

- 실제 데이터 연결 없음. 넥슨 병영수첩·3rd.supply의 어떤 API도 호출하지 않는다.
- 로그인 상태는 항상 비로그인(`/infos`의 `user: null`). 세션 전환 스위치는 Phase 6.
- 쓰기 계열(POST/PUT/DELETE) 핸들러는 **계약 형태만** 돌려준다. 저장·rate limit·캡차 시뮬레이션은 Phase 5~6.
- 랭킹은 요청 시 정렬한다. 1시간 주기 배치 재현은 Phase 9.
- **광고 관련 요소는 일절 만들지 않았다** (`CLAUDE.md` 4장).

---

## Phase 1 (2026-08-20)

### D-009. 디자인 토큰은 원본 실측값으로 확정

Phase 0의 토큰은 전부 추정값이었다. Phase 1에서 Chrome 개발자 도구(`getComputedStyle` / `cssRules`)로
3rd.supply 홈·약관·404 화면을 직접 측정해 아래 값으로 교체했다. 정의 위치는 `packages/ui/src/styles.css`.

**원본은 루트 폰트 크기가 14px 이다.** (`1rem = 14px`) 이 값을 맞춰야 Tailwind의 rem 기반 유틸리티가
원본과 같은 픽셀로 떨어진다. 원본은 Tailwind v2 계열을 쓰고 우리는 v4라 색 팔레트 기본값이 다르므로,
아래 색은 v4 기본값을 쓰지 않고 실측값을 토큰으로 고정했다.

| 토큰 | 실측값 | 원본에서의 쓰임 |
|---|---|---|
| `--color-ink` | `#000000` | 헤더 · 히어로 · 푸터 배경 |
| `--color-page` | `#f2f2f2` | 본문 배경 (`bg-gray-light`) |
| `--color-nav-fg` | `#e5e7eb` | GNB 글자 |
| `--color-nav-active` | `#292929` | 현재 보고 있는 대표 리그 항목 배경 |
| `--color-divider` | `#e5e7eb` | 목록 구분선 |
| `--color-meta` | `#374151` | 목록의 상대시간 |
| `--color-accent` | `#1e3a8a` | 카드 머리글 글자 · 4px 밑줄 |
| `--color-comment` | `#ef4444` | 게시글 제목 옆 댓글 수 |
| `--color-selector` / `-fg` | `#334155` / `#f3f4f6` | 통합검색 좌측 셀렉터 |
| `--color-input-fg` / placeholder | `#4a4a4a` / `#9ca3af` | 통합검색 입력 |
| `--shadow-card` | `0 1px 3px rgb(0 0 0/.1), 0 1px 2px rgb(0 0 0/.06)` | 카드 그림자 |
| `--spacing-nav` | `4.5rem` (63px) | 고정 헤더 높이 |
| `--spacing-container` | `80rem` (1120px) | 데스크톱 고정폭 |
| `--spacing-logo` | `44rem` (616px) | 홈 로고 박스 |
| `--spacing-search` / `-selector` | `28rem` / `11rem` | 검색 입력 / 셀렉터 |
| `--spacing-board` / `-title` / `-time` | `48rem` / `42rem` / `6rem` | 인기게시글 카드 / 제목 칸 / 시간 칸 |
| `--spacing-notfound` | `24rem` (336px) | 404 일러스트 |

**실측하지 못해 역산한 값** — 셀렉터 드롭다운 hover 색(`#4338ca` / `#818cf8`).
원본 클래스명(`hover:bg-indigo-700` / `hover:border-indigo-400`)에서 Tailwind v2 팔레트로 되짚었다 `[미확인]`.

승/패·래더·MVP 색(`--color-win/-lose/-rating/-mvp`)은 Phase 0의 임시값 그대로다.
해당 화면(Phase 2~4)에서 실측해 교체한다 `[미확인]`.

### D-010. `.pc-container` 는 고정폭 1120px 가운데 정렬

원본 CSS 규칙에는 `min-width: 80rem; margin: 0 auto` 만 있지만, **관측된 동작**은 다음과 같다.

- 창이 넓으면 1120px 고정폭으로 가운데 정렬 (뷰포트 1517px에서 좌측 여백 198.5px)
- 창이 1120px보다 좁아지면 줄어들지 않고 넘쳐서 가로 스크롤 (뷰포트 1014px에서 `margin-right: -105.6px`)

`min-width` 만으로는 넓은 창에서 가운데 정렬이 나오지 않으므로 `width: 80rem` + 좌우 `auto` 마진으로 구현했다.
CSS 선언은 원본과 다르지만 **두 경우의 렌더 결과가 모두 원본과 일치**하는 것을 실측으로 확인했다.

### D-011. 상대시간 포맷 규칙

원본 홈에서 관측된 표기는 `6시간 전` `15시간 전` `1일 전` `2일 전` `3일 전` 뿐이다.
`분` `달` `년` 단위와 1분 미만 표기는 해당하는 글이 없어 확인하지 못했다 `[미확인]`.

우리 규칙(`packages/ui/src/common/relative-time.ts`, 테스트로 고정):
`60초 미만 = 방금 전` / `분` / `시간` / `일` / `30일 = 1달` / `365일 = 1년`, 미래 시각은 `방금 전`.
**원본과 동일함이 검증되지 않았다.**

### D-012. 원본 자산·문구를 쓰지 않은 지점 (의도된 차이)

`CLAUDE.md` 3장 4번(원본 자산·문구 복사 금지)에 따라 아래는 **일부러 원본과 다르게** 두었다.
박스 크기·배치·정보 구조는 원본과 맞추고 내용만 우리 것으로 채웠다.

| 항목 | 원본 | 우리 |
|---|---|---|
| 홈 로고 | `main_logo.png` 616×143.5 | 직접 그린 SVG, 같은 박스 |
| GNB 로고 | `nav_logo.png` 152×24 | 직접 그린 SVG, 같은 박스 |
| 404 일러스트 | 이미지 336×189 | 직접 그린 SVG, 같은 박스 |
| 검색·화살표 아이콘 | Font Awesome | 직접 그린 SVG, 같은 크기 |
| 푸터 저작권·문의 | 원본 상호/메일 | `© 2026 SACLOUD` / `sacloud@local.invalid` |
| 문서 제목 | 원본 서비스명 | `SACLOUD - 서든어택 클랜전 전적검색` |
| 약관·개인정보 본문 | 원본 약관 문구 | **새로 작성한 초안** — 법적 검토를 받지 않았다. 오픈 전 교체 필요 |
| GNB 대표 리그 3개 | `공식리그`/`3부리그`/`대룰리그` → `/league/supply` 등 | Phase 0 픽스처의 가상 리그 `공식전`/`세컨드`/`친목전` (D-005 연장). 설정은 `packages/ui/src/site-config.ts` 한 곳 |

### D-013. Phase 1에서 추가한 것 / 하지 않은 것

- `packages/ui` 를 계획대로 이번 Phase에 만들었다. `next/link`·`next/navigation` 에 의존한다(peer).
- **TanStack Query 도입.** Phase 1에서 실제로 서버 상태(`/boards?category=hot`)를 다루기 시작했기 때문이다.
  커서 무한스크롤(`useInfiniteQuery`)은 Phase 3에서 쓴다.
- Mock(MSW)은 브라우저 Service Worker라 **첫 방문에는 아직 페이지를 제어하지 않는다.**
  그 사이 나간 요청은 가로채이지 못하고 응답 없이 매달린다. `controllerchange` 를 기다렸다가
  준비 완료로 표시하도록 `apps/web/app/providers.tsx` 에서 처리했다. 원본에 대응하는 동작이 아니라
  Mock 단계의 구현상 장치이며, Phase 10(SSR 전환)에서 대부분 사라진다.
- `Skeleton` / `EmptyState` / `ErrorState` 도 같은 성격의 구현상 장치다.
  원본은 SSR이라 로딩·빈 목록·오류 표시를 관측할 수 없었다 `[미확인]`.
- **Playwright E2E와 반응형 스냅샷(1280/1024/390)은 하지 않았다.** 계획 6장에는 있으나,
  원본이 1120px 고정폭 데스크톱 전용이고 모바일 화면이 `[미확인]` 이라 기준이 없다.
  이번 Phase의 검수는 Chrome 실브라우저 직접 비교로 대신했다.
- 광고 관련 요소는 만들지 않았다. 원본 홈의 광고 슬롯(`h-wide-ad` 280px + 상하 여백)을 통째로 빼고
  히어로 다음에 인기게시글 카드가 바로 이어지게 했다 (`CLAUDE.md` 4장).


### D-014. `.next` 오염으로 CSS가 통째로 빠지는 문제 (2026-08-20 발생 · 해결)

**증상** — `localhost:3000`이 브라우저 기본 스타일로만 렌더됐다. HTML과
`<link rel="stylesheet" href="/_next/static/css/app/layout.css">`는 정상인데
**그 CSS 파일이 404**였고 `.next/static` 안에 CSS 파일 자체가 없었다.

**원인** — Tailwind/PostCSS 설정 문제가 아니었다. `app/globals.css`를
`@tailwindcss/postcss`로 직접 돌려보면 18KB가 정상 생성되고
`@import '../../../packages/ui/src/styles.css'` 인라인과
`@source "../../../packages/ui/src"` 스캔도 모두 정상이었다.
문제는 **`.next` 디렉터리가 깨진 상태**였다는 것이다.
`next build`(프로덕션)와 `next dev`를 같은 `.next`에 번갈아 돌렸고,
그 중 일부가 컴파일 도중에 강제 종료되면서 매니페스트는 CSS를 가리키는데
정작 CSS 청크는 없는 상태로 남았다. 포트를 붙잡고 있던 좀비 dev 서버가 그 깨진 `.next`를 계속 서빙했다.

**해결 / 재발 방지**
- `pnpm clean` 스크립트 추가 (`apps/web/.next` 삭제)
- `pnpm build` 를 `pnpm clean && next build` 로 변경 — 프로덕션 빌드가 dev 산출물 위에 겹쳐 쓰이지 않는다
- 증상이 보이면 **`pnpm clean` 후 dev 서버 재시작**. 3000번 포트를 잡고 있는 좀비 프로세스가 있는지도 함께 확인한다

**교훈** — 이 Phase의 검수에서 `HTTP 200`과 `getComputedStyle` 수치만 보고 완료로 판단했다.
둘 다 통과하는데도 실제 화면은 무스타일일 수 있었다(계측 시점의 서버와 사용자가 본 서버가 달랐다).
**앞으로는 화면 단위 작업의 완료 판정에 반드시 실제 렌더 스크린샷을 포함한다.**


---

## Phase 진행 방식 변경 (2026-08-20)

### D-015. Phase별 승인 대기 폐지 · 자율 진행

이후 Phase는 사용자 승인을 기다리지 않고 V1 완료까지 자율 진행한다.
Phase마다 아래 절차를 반복한다.

```
계획/문서 확인 → 필요시 원본 조사 → 구현 → localhost 실행 → Chrome 실렌더·기능 확인
→ 원본 비교 → QA 서브에이전트 독립 검수 → FAIL이면 수정 → 재검증
→ lint → typecheck → test → build → 회귀 테스트 → Git 체크포인트 커밋 → 다음 Phase
```

이전 Phase의 문제가 나중에 발견되면 되돌아가 수정하고 회귀 테스트한 뒤 계속 진행한다.
`HTTP 200` 이나 테스트 통과만으로 UI 완료를 판단하지 않는다 (D-014 교훈).

**사용자에게 묻고 멈추는 경우 — 이 목록으로 한정한다**

- 넥슨 Open API Key가 실제로 필요한 시점
- 사용자가 직접 로그인·본인인증해야 하는 경우
- 유료 서비스 가입·결제
- production DB 파괴·초기화 가능성이 있는 작업
- production 배포
- `55sa.cloud` DNS 변경
- 되돌리기 어려운 외부 서비스 변경
- 조사만으로 해결할 수 없는 중요한 제품 의사결정
- 기존 3rd.supply 핵심 데이터를 현실적으로 마이그레이션할 방법이 없는 경우

그 외(CSS·Tailwind·TypeScript·테스트·빌드·개발서버·의존성·API·Mock·렌더 차이·DB 개발 오류)는
묻지 않고 스스로 원인을 찾아 고친다.

**비밀키는 코드·Git에 넣지 않는다.** `.env` 환경변수로만 관리한다.

### D-016. 마이그레이션 접근 경로 확정 (조사 결과)

`docs/MIGRATION_GAPS.md` 참조. 요약하면,

- `api-v2.3rd.supply` → 외부 요청 전면 403 (AWS ELB)
- `3rd.supply` 웹 → AWS WAF **CAPTCHA** 챌린지 (`x-amzn-waf-action: captcha`)
- → **3rd.supply 직접 대량 수집은 하지 않는다.** 접근 통제 우회 금지 원칙에 해당한다.
- 경기 원천 기록의 1차 출처는 **넥슨 Open API** (서든어택 정식 지원, 게임 id 41).
  `x-nxopen-api-key` 헤더 필수 → **키 발급이 선행 조건**이다.
- 넥슨 이용 조건: 가져간 데이터는 **최소 30일마다 갱신** 필요 → 수집 주기 설계에 반영한다.
- 3rd.supply 고유 산출물(래더·`rating_update`·리그/시즌/부리그·랭킹·배치고사)은
  운영자 협조 없이 확보 불가. `MIGRATION_GAPS.md` 4장에 결정 요청으로 정리했다.

### D-019. Chrome 검수 시 탭이 백그라운드면 React 상태가 갱신되지 않는다 (2026-08-21)

**증상** — 로그인 폼에 실제로 타이핑해도 버튼이 계속 비활성이었다.
DOM `value`는 바뀌는데 React 상태가 안 바뀌고, 컨트롤드 입력인데도 React가 값을 되돌리지 않았다.
`__reactProps`에 `onChange`는 정상적으로 붙어 있었고 콘솔 오류도 없었다.

**원인** — 앱 버그가 아니다. 검수용 탭이 `document.visibilityState === 'hidden'` 상태였다.
Chrome은 백그라운드 탭에서 타이머·MessageChannel을 throttle하는데,
React 스케줄러가 이것을 쓰기 때문에 **상태 업데이트가 flush되지 않는다.**
CDP 평가가 45초 타임아웃 나던 것("renderer frozen")도 같은 원인이다.

**대응**
- 상호작용을 검수할 때는 **탭이 visible인지 먼저 확인한다** (`document.visibilityState`).
  스크린샷을 찍으면 대체로 활성화된다.
- 데이터 렌더(React Query 결과)는 백그라운드에서도 보이므로 "화면이 나온다 = 상호작용도 된다"가 아니다.
- 폼 검증 같은 순수 로직은 브라우저 대신 **단위 테스트로 고정**한다
  (`signup-rules.test.ts`, `league-create.test.ts`).

### D-032. 검수는 개발 서버가 아니라 프로덕션 빌드로 한다 (2026-08-21)

`next dev`는 라우트를 **처음 요청받을 때 컴파일한다.** 라우트가 57개까지 늘어나면서
화면 하나 여는 데 10~60초가 걸렸고, 그 사이 렌더러가 멈춰 스크린샷과 DOM 조회가 계속 실패했다.
앱 문제로 오인하기 쉽다 — 실제로 몇 번 그렇게 헷갈렸다.

→ 화면 검수는 `pnpm build` + `pnpm --filter @sacloud/web start`로 한다. 응답이 0.2~0.3초로 떨어진다.
**사용자가 실제로 보는 것과 같은 빌드**라는 점에서도 이쪽이 맞다.

### D-031. 로그인 직후 화면 전환의 두 가지 함정 (2026-08-21 수정)

**증상 1** — 로그인은 200인데 로그인 화면에 그대로 남았다.
`invalidateQueries`를 기다리지 않고 이동해서, 도착한 화면의 `AuthGuard`가 아직 갱신되지 않은
`/infos`(비로그인)를 보고 되돌려보냈다. → 갱신을 `await` 한 뒤 이동한다.

**증상 2** — 1번을 고치려고 `AuthGuard`가 "갱신 중에는 children을 내리도록" 만들었더니,
`/infos`를 다시 받을 때마다 **하위 화면이 통째로 다시 마운트**됐다.
마이페이지에서 저장은 되는데 "저장했습니다"가 나타나지 않는 것으로 드러났다.
→ **판정만 미루고 화면은 계속 그린다.**

교훈: 보호막이 로딩 상태를 이유로 children을 내리면, 그 아래의 입력값·성공 메시지 같은
지역 상태가 조용히 사라진다. 로딩 처리는 "무엇을 감출지"가 아니라 "무엇을 판정하지 않을지"로 짠다.

### D-030. 리프레시 토큰을 실제로 쓰게 만들었다 (2026-08-21 수정)

발급해서 DB에 저장까지 해놓고 **아무도 쓰지 않았다.** 계약이 응답 본문으로만 주는데
브라우저 클라이언트는 그걸 보관할 곳이 없어서, 액세스 토큰(1시간)이 만료되는 순간
**말없이 로그아웃**됐다. 검수 중 정확히 1시간 뒤에 드러났다.

- 리프레시 토큰도 **httpOnly 쿠키**로 내려보낸다
- 액세스 토큰이 만료됐으면 리프레시 쿠키로 **조용히 재발급**한다
- 이때 **리프레시 토큰은 돌리지 않는다.** 한 페이지가 동시에 여러 요청을 보내는데
  그중 하나가 회전시키면 나머지가 무효 토큰으로 실패한다. 회전은 `POST /auth/token`에서만.
- 로그아웃은 두 쿠키를 모두 지운다

### D-029. Phase 0 픽스처의 시각이 id 순서와 어긋나 있었다 (2026-08-21 수정)

Mock↔실제 API 응답을 값까지 대조하다가 발견했다. 픽스처가 아래 세 곳의 시각을 **무작위로**
뽑고 있어서, id(=표시 순서)와 시각이 맞지 않았다.

| 대상 | 증상 |
|---|---|
| 게시글 `createdAt` | 목록은 최신순인데 `작성시간` 열이 내림차순으로 보이지 않았다 |
| 댓글 `createdAt` | 한 글 안에서 댓글 시각이 뒤죽박죽이었다. 대댓글이 부모보다 앞서기도 했다 |
| 리그 참여 `joinedAt` | 리그 목록의 대표 클랜 3개가 Mock과 실제 DB에서 서로 달랐다 |

Mock은 배열 순서(=id)로 정렬하고 실제 DB는 시각으로 정렬하므로, 이대로면 두 모드의
화면이 달라진다. **화면 자체도 틀린 상태였다** — 원본은 최신순 정렬이다.

→ `rng`를 뽑는 **순서는 그대로 두고**(다른 필드가 흔들리지 않게) 값의 배치만 정렬했다.
회귀 테스트 4건을 추가해 다시 어긋나면 바로 잡히게 했다.
댓글 시각이 기준 시각을 넘어 **미래로 찍히던 것**도 함께 막았다.

### D-028. 계약이 `:leagueId`인 자리에 슬러그도 받는다 (2026-08-21 사고)

**증상** — 실제 화면에서 클랜랭킹·개인랭킹·기록실이 전부 비어 보였다.

**원인** — 계약은 랭킹·매치 경로를 `:leagueId`로 적어 두었지만,
화면은 이 자리에 **URL 슬러그를 그대로 넣어 호출한다**
(`app/league/[leagueSlug]/rank/...`). Mock 핸들러가 `resolveLeagueId`로 둘 다 받아 왔기 때문에
Phase 1~6 내내 드러나지 않았다.

→ 실제 API도 `resolveLeagueId`로 슬러그·ID를 모두 받는다. 회귀 테스트로 고정했다.

**교훈** — 이 버그는 **curl로도, 계약 준수 테스트로도 잡히지 않았다.**
둘 다 "우리가 옳다고 생각한 경로"로 호출했기 때문이다. 실제 화면을 열어봐야 드러났다.
`CLAUDE.md` 3장 10번(렌더 확인 필수)이 왜 있는지 보여주는 사례다.

### D-027. Next 서버 번들에서 `Prisma.sql` 태그드 템플릿을 쓰지 않는다 (2026-08-21)

`hot`(인기) 게시판은 저장 컬럼이 아니라 계산식으로 정렬해야 해서 raw SQL이 필요하다.
그런데 Next 서버 번들 안에서는 `Prisma.Sql` 인스턴스 검사가 통과하지 못해,
SQL 조각으로 넣은 값이 **바인드 파라미터(jsonb)로 직렬화**된다.

```
ERROR: argument of WHERE must be type boolean, not type jsonb
```

같은 코드가 `tsx` 단독 실행에서는 정상이라 재현이 헷갈린다.

→ `$queryRawUnsafe` + 번호 플레이스홀더(`$1`)로 직접 조립한다.
**사용자 입력은 전부 `bind()`를 거쳐 `$n`으로만 넣고**, 테이블·컬럼명과 정렬식은
파일 안의 리터럴만 쓴다. `LIKE` 검색어는 `%`·`_`·`\`를 이스케이프한다.

### D-026. 라우트 세그먼트 이름을 계약과 1:1로 맞추지 않는다 (2026-08-21)

계약은 같은 자리에 서로 다른 이름을 쓴다.

```
/leagues/:leagueSlug          /leagues/:leagueId/ranks/clans
/leagues/:leagueSlug/clans/:clanSlug/show
/leagues/:leagueSlug/clans/:leagueClanId/division
```

Next는 **한 세그먼트에 서로 다른 이름의 동적 라우트를 둘 수 없다.**
그래서 파일 경로에서는 이름을 하나로 통일하고(`[league]`, `[clan]`),
**핸들러마다 계약대로 슬러그인지 ID인지 해석**한다. 각 파일 주석에 어느 쪽인지 적어 둔다.

외부에서 보이는 URL은 계약 그대로다. 바뀐 것은 우리 저장소의 폴더 이름뿐이다.

### D-025. Auth.js(NextAuth)를 쓰지 않고 직접 구현한다 (2026-08-21)

**계획 문서(`docs/IMPLEMENTATION_PLAN_1.md` 1장)와 어긋나는 결정이다.** 사용자 확인이 필요하다.

계획은 인증에 Auth.js(NextAuth v5)를 적었다. 그런데 Phase 0에서 확정한 계약이
자체 토큰 흐름을 정의하고 있다.

```
POST /auth/login  → { access_token, refresh_token, expires_at, user }
POST /auth/token  → 토큰 갱신
```

NextAuth의 세션 모델(자체 쿠키 + `/api/auth/*` 예약 경로)과 정면으로 부딪힌다.
NextAuth를 얹으면 계약을 맞추려고 감싸는 코드가 더 늘어난다.

→ `jose`(JWT) + `bcryptjs`로 직접 구현했다.
- 액세스 토큰: JWT 1시간. **httpOnly 쿠키로도 내려보낸다** (스크립트로 못 읽는다)
- 리프레시 토큰: 불투명 문자열 30일. **평문을 저장하지 않고 해시만** DB에 둔다. 갱신 시 폐기(rotation)
- 계약이 본문에도 토큰을 요구하므로 본문에도 넣는다

로그아웃은 계약에 없었다(원본에 엔드포인트가 있는지 `[미확인]`). 세션이 httpOnly 쿠키라
서버가 지워야 해서 `authLogout`을 **계약에 `designed`로 추가**했다.

### D-024. 닉네임 유일 제약을 걸지 않는다 (2026-08-21)

Phase 7 스키마 초안에서 `User.nickname`에 `@unique`를 걸었다가 **되돌렸다.**

원본이 닉네임 중복을 막는지는 **관측되지 않았다**. 회원가입 폼에서 확인한 것은 길이 제약(2~16자)뿐이다.
확인되지 않은 제약을 임의로 만드는 것은 `CLAUDE.md` 3장 7번 위반이다.
게다가 Mock 픽스처(사용자 40명)에 이미 닉네임 충돌이 있어, 유일 제약을 걸면
**mock 모드와 live 모드의 데이터가 달라진다.** 화면 비교의 기준이 무너진다.

→ `@unique` 대신 조회용 `@@index([nickname])`만 둔다.
원본이 중복을 막는다는 것이 확인되면 그때 제약과 마이그레이션을 추가한다.

### D-023. 시드 데이터는 출처를 남긴다 (2026-08-21)

개발 시드로 들어가는 3,000경기는 **가짜다.** 실제 3rd.supply 기록이 아니고,
래더 값도 픽스처 난수라 SACLOUD 공식으로 계산된 것이 아니다.

나중에 실제 기록과 섞이면 구분할 방법이 없어지므로 두 곳에 표시를 남긴다.

- `Match.origin = "mock"` — 실제 수집분(`nexon` / `3rd.supply`)과 구분
- `MatchPlayerStat.formulaVersion = "mock-fixture"` — 공식 계산 결과로 오인하지 않게

같은 이유로 `CLAUDE.md` 3-A 2번(기존 `rating_update`를 추정 공식으로 덮어쓰지 않는다)을
스키마 수준에서 지킬 수 있다. 이전된 과거 기록은 `formulaVersion`이 다르므로 재계산 대상에서 제외된다.

### D-022. 로컬 개발 DB는 `embedded-postgres`로 띄운다 (2026-08-21)

**문제** — 이 개발 PC에는 Docker도 PostgreSQL도 설치돼 있지 않다(2026-08-21 확인).
관리자 권한 설치를 요구하지 않고 개발을 진행할 방법이 필요했다.

**선택** — `embedded-postgres`(PostgreSQL 17 공식 바이너리를 `node_modules`에 내려받아
일반 사용자 권한으로 기동). `pnpm db:start` / `db:stop` / `db:reset`으로 다룬다.
**개발 전용이며 운영에는 쓰지 않는다.** 운영은 관리형 PostgreSQL을 쓴다.

**한국어 Windows에서 막혔던 지점 두 가지** (둘 다 실제로 실패를 확인하고 우회했다)

1. 데이터 디렉터리를 저장소 안(`C:\Users\LG\Desktop\서플라이\.pgdata`)에 두면
   `initdb`가 경로를 CP949로 다뤄 깨진다. → 저장소 밖 **ASCII 경로**
   (`%LOCALAPPDATA%\sacloud\pgdata`)에 둔다. `SACLOUD_PGDATA`로 바꿀 수 있다.
2. 기본 로케일이 `Korean_Korea.949`라
   - `initdb`가 "could not find suitable text search configuration"으로 실패 → `--locale=C`
   - post-bootstrap의 `pg_import_system_collations()`가 Windows collation 이름을 CP949로 넣어
     `invalid byte sequence for encoding "UTF8": 0xbc`로 실패 → 템플릿을 `--encoding=SQL_ASCII`로 만든다.

   **템플릿만 SQL_ASCII이고, 실제 사용 DB(`sacloud`)는 `TEMPLATE template0`으로
   UTF8 + `LC_COLLATE=C`로 따로 만든다.** 한글 저장은 UTF8이며,
   `pnpm db:check`가 한글 왕복과 DB 인코딩을 매번 검증한다.

**포트는 5433.** 나중에 시스템에 PostgreSQL을 설치해도 기본 포트(5432)와 겹치지 않는다.

### D-021. 개발 서버는 반드시 하나만 띄운다 (2026-08-21 사고)

**증상** — `/auth/login`이 브라우저에서 **404**로 보였다. 그런데 코드·빌드·라우트는 전부 정상이었다.

**원인** — `pnpm dev`가 여러 번 실행돼 있었다. Next는 3000이 점유돼 있으면
`Port 3000 is in use ... using available port 3003 instead` 로 **다른 포트에 뜬다.**
그래서 3000번에는 **인증 페이지가 생기기 전에 띄운 옛날 서버**가 남아 있었고,
사용자가 보는 3000번만 404였다. 실제로 3000/3001/3002/3003 네 개가 동시에 떠 있었다.

**대응**
- `pnpm dev:clean` 추가 (`scripts/dev-restart.mjs`) — 3000~3010의 프로세스를 정리하고
  `.next`를 지운 뒤 서버를 **한 번만** 띄운다.
- 404나 "옛날 화면"이 보이면 먼저 **몇 번 포트에 무엇이 떠 있는지** 확인한다.
  Next 시작 로그의 `Local: http://localhost:PORT` 줄을 반드시 본다.

### D-020. 클라이언트 번들에 `@sacloud/mock` 진입점을 넣지 않는다 (2026-08-21)

개발용 세션 스위치를 만들면서 클라이언트 컴포넌트가 `@sacloud/mock`(패키지 루트)을 import했다.
루트 진입점은 `./handlers`(msw)와 `./dataset`(매치 3,000건 픽스처 생성)을 함께 끌어온다.
그 결과 픽스처 생성 코드가 클라이언트 번들에 통째로 들어갔다.

→ `@sacloud/mock/session` 서브경로를 만들어 세션 모듈만 가져오도록 바꿨다.
루트 진입점에도 "클라이언트에서 import 금지" 주석을 달았다.

### D-018. 래더 사양 문서 신설 (2026-08-21)

사용자가 제공한 래더 역추적 결과를 `docs/LADDER_IMPLEMENTATION_SPEC.md`에 정리했다.
D-017에서 "없다"고 기록한 `LADDER_REVERSE_ENGINEERING.md`를 대신하는 문서다.

핵심: 공식 1개(D=3400, Kw(R)=36.6-R/200) + division 조합별 K/multiplier,
교차 division 보정은 **비대칭**(div1 측만 0.6 감쇠).
`CLAUDE.md` 3-B장에 어기면 안 되는 원칙만 요약해 두었다.

미확정으로 남긴 항목: 0.6의 적용 위치 · 2v1 경기 division 오염 · 배치 종료 후 초기 래더 ·
시즌 전환 시 래더 처리 · rounding 순서.

### D-017. `LADDER_REVERSE_ENGINEERING.md` 는 아직 존재하지 않는다

사용자 지시에서 참조된 문서지만 저장소에 없다(2026-08-20 확인).
Phase 9(레이팅) 착수 시 새로 작성하며, 모든 항목을 **확정 / 유력 / 추정 / 미확인** 으로 구분해 표기한다.
확정되지 않은 값(기대승률 함수 · K값 · K 구간 경계 · division multiplier · rounding ·
배치고사 종료 후 초기 래더 산식 · dropout 패널티)을 "원본과 동일한 공식"이라고 단정하지 않는다.
래더 설정값은 코드에 상수로 박지 않고 **변경 가능한 설정/전략 구조**로 분리한다.


---

## 검수 기록

| 일자 | 대상 | 방법 | 결과 |
|---|---|---|---|
| 2026-08-20 | Phase 0 | `pnpm typecheck` / `pnpm lint` / `pnpm test` / `next build` / dev 서버 HTTP 확인 | 전부 통과 |
| 2026-08-20 | Phase 0 브라우저 확인 | Chrome 자동화 | **미실시** — 이 세션에서 브라우저 도구가 꺼져 있었다. Phase 1 착수 시 함께 확인한다 |
| 2026-08-20 | Phase 1 | `pnpm typecheck` / `pnpm lint` / `pnpm test`(98건) / `next build` | 전부 통과 |
| 2026-08-20 | Phase 1 원본 비교 | Chrome 실브라우저 — 원본과 localhost 동시 계측(getComputedStyle/getBoundingClientRect) + 스크린샷 | 헤더·히어로·통합검색·인기게시글·푸터·404·약관 치수/색/서체 일치. 남은 차이는 D-012의 의도된 차이뿐 |
| 2026-08-20 | Phase 1 재검수 (CSS 미적용 신고) | 원인 추적: 서빙되는 CSS 에셋 직접 요청 / Tailwind PostCSS 단독 실행 / `.next` 내용 확인 / 콘솔 로그 / 좀비 프로세스 확인 | `.next` 오염이 원인(D-014). `pnpm clean` 후 재시작하여 해결, 실제 렌더 스크린샷으로 확인 |
| 2026-08-21 | Phase 7 시드 | `pnpm db:check` — 건수 대조 · 한글 UTF8 왕복 · 래더 정합성(base+무기별 delta = 통합) · division 스냅샷 누락 · 대댓글 1단계 | 15항목 전부 통과 |
| 2026-08-21 | Phase 7 Mock↔실제 API 동등성 | `pnpm compare` — 25개 엔드포인트의 응답을 **값까지** 재귀 비교 (커서 메타 포함) | **25/25 일치.** 의도적 차이(`view_count`)만 제외 |
| 2026-08-21 | Phase 7 계약 준수 | 실제 API 응답을 `packages/contract` Zod로 파싱 (`apps/web/tests/apiContract.test.ts`) | 9건 통과. 커서 중복·순위 연속성·404 형태 포함 |
| 2026-08-21 | Phase 7 정적 검사 | `pnpm typecheck` / `pnpm lint` / `pnpm test`(157건) / `pnpm build` | 전부 통과. API 라우트 57개 빌드됨 |
| 2026-08-21 | Phase 7 mock 모드 회귀 | 모드를 `mock`으로 되돌려 주요 7개 화면 + MSW 워커 응답 확인 | 전부 200 (`/rank/clan`의 307은 의도된 리다이렉트) |
| 2026-08-21 | Phase 7 Chrome 렌더 검수 (1차) | 실브라우저 DOM 확인 (홈 · 게시판 · 랭킹) | **부분 실시.** 홈·게시판은 live 데이터 렌더 확인, 랭킹에서 404를 발견해 수정(D-028). 이후 Chrome 창이 최소화돼 중단 |
| 2026-08-21 | Phase 7 Chrome 렌더 검수 (2차, 프로덕션 빌드) | 스크린샷 + 콘솔/네트워크 + 실제 조작(로그인·가입·글쓰기·댓글·추천·아코디언) | 홈 / 로그인 / 회원가입 / 마이페이지 4탭 / 게시판 목록·상세·글쓰기·댓글·추천 / 클랜랭킹 1·2부 / 개인랭킹 / 리그홈 / 클랜 기록실 + 매치 상세 펼침 / 개인 기록실 / 지난시즌 확인. **콘솔 오류 0, 실패 API 0.** 결함 3건 발견·수정 (D-030 · D-031) |
| 2026-08-21 | Phase 7 권한 검증 (서버) | curl로 비로그인/비소유자/소유자 3방향 | 리그소개 수정 401/403/200 · 추방 확인문구 400 · 비소유자 추방 403 · 미연동 계정 리그생성 403 — 전부 의도대로 |
| 2026-08-21 | Phase 7 Chrome 렌더 검수 (3차) | 리그 목록 · 리그 만들기 · 리그 관리(소유자) · 추방 확인 모달 | 스크린샷 확인 완료. **로그인 → returnUrl 복귀**와 **관리자 판정 깜빡임 없음**을 프로덕션 빌드에서 재확인 |
| 2026-08-21 | Phase 7 화면 검수 미완 (3건) | 통합검색 UI 조작 · 404 화면 · 작은 화면 레이아웃 | **스크린샷 미실시.** Chrome 창이 반복해서 백그라운드로 내려가 탭이 `hidden`이 되고 렌더링이 throttle됐다(D-019). 셋 다 Phase 1에서 스크린샷으로 확인했고 Phase 7에서 코드가 바뀌지 않았다. 404는 HTTP 404 응답 확인, 작은 화면은 `pc-container` 고정폭이라 재배치가 없음을 코드로 확인 |

### D-033. 검수용 계정을 시드에 포함한다 (2026-08-21)

픽스처 사용자만으로는 검수 조합이 하나 빈다 —
**"리그에 참여 중인 클랜 소속인데 리그 소유자는 아닌"** 계정이 없다.
픽스처는 리그 소유자에게만 플레이어를 연동하기 때문이다.

→ 시드가 두 계정을 함께 만든다 (`seedTestAccounts`). **로컬 개발 전용이다.**

| 계정 | 역할 | 용도 |
|---|---|---|
| `admin-test@naver.com` | 운영자(`role=2`), 리그 소유 없음 | 모든 리그의 관리 화면 검수 |
| `user-test@naver.com` | 일반 회원, 리그 참여 클랜 소속 플레이어와 연동 | 연동된 일반 회원 흐름 검수 |

**리그를 소유하게 하지 않는다.** 소유자를 바꾸면 리그 목록·상세의 `user`가 달라져
mock↔live 대조(`pnpm compare`)가 깨진다. `admin-test`는 `role=2`로 모든 리그 관리에 접근한다.

비밀번호는 시드 공통값(`DEV_PASSWORD`)이며 **개발 전용**이다. 운영에는 이 시드를 쓰지 않는다.

---

## Phase 8 — 넥슨 Open API 수집 (2026-08-21)

사양 문서: `docs/NEXON_INGEST_SPEC.md` (엔드포인트 실측 · 흐름 · 규칙)

### D-034. 원본이 주지 않는 값은 전부 `null`이다. `false`를 기본값으로 쓰지 않는다

넥슨 Open API는 **무기 · 플레이시간 · 종료시각 · 선공진영 · MVP · 탈주**를 제공하지 않는다
(공식 OpenAPI 스펙 실측, 2026-08-21).

`mvp = false` / `dropout = false` / `blueFirst = false` 는 "아니다"라는 **실제 정보**다.
우리는 그 사실을 모르므로 `null`(= 알 수 없음)이어야 한다.

바꾼 것

| 위치 | 변경 |
|---|---|
| `Match.endAt` `Match.playTime` `Match.blueFirst` | NOT NULL → nullable (기본값 제거) |
| `MatchPlayerStat.weapon` `dropout` `mvp` | NOT NULL → nullable (기본값 제거) |
| 계약 `MatchPlayerStat.weapon/dropout/mvp` | `.nullable()` |
| 계약 `MatchListItem.end_at/play_time/blue_team` | `.nullable()` |
| 계약 `MatchLineupEntry.weapon/dropout` | `.nullable()` |
| UI `MatchCard` | 플레이시간·무기 `알수없음`, `[S]`·MVP·탈주 미표기, 선레드/선블루 **행 자체를 감춤** |

**기존 mock 시드는 실제 값이 있으므로 그대로 둔다.** 값을 지우지 않는다.

### D-035. 내부 ID와 외부 source ID를 분리한다

```
Match.id            = SACLOUD 내부 식별자 (18자리 숫자 규칙, 계약 MatchId)
Match.origin        = "nexon" | "3rd.supply" | "sacloud" | "mock"
Match.sourceMatchId = 넥슨 원본 match_id (문자열 원형)
중복 방지            = @@unique([origin, sourceMatchId])
```

넥슨 `match_id`를 내부 `Match.id`로 쓰지 않는다. 외부 공급자의 ID 형식이 내부 도메인 규칙에
침투하면 공급자가 늘 때마다 계약이 흔들린다. 내부 ID는 `YYMMDDHHmmss`(KST) + 6자리 일련번호로
우리가 만든다. **원본 대조는 언제나 `sourceMatchId`로 한다.**

### D-036. ouid로도, 닉네임으로도 동일인을 자동 확정하지 않는다

- 넥슨은 "게임 콘텐츠 변경으로 ouid가 변경될 수 있다"고 명시했다 → **ouid는 영구 식별자가 아니다.**
- 닉네임도 영구 식별자가 아니다 → 닉네임 일치만으로 병합하지 않는다.
- `Player.nexonOuid`는 **비권위 캐시**다. 판단 근거로 쓰지 않는다.

`NexonIdentity.status` = `unresolved`(기본) | `active` | `superseded` | `conflicted`.
근거가 부족한 연결은 `NexonIdentityCandidate`(open/accepted/rejected)로만 남기고
**자동 승인 경로를 만들지 않았다.** 참가자 해석은 `active` + `playerId`가 있는 신원만 쓰며,
둘 이상 걸리면 `ambiguous`로 두고 투영을 보류한다.

### D-037. 진영(red/blue) 배정은 내부 규칙이다

넥슨은 진영 색을 주지 않고 `team_id`의 의미도 `[미확인]`이다.
그래서 **`team_id` 오름차순으로 red/blue를 배정**한다. 재실행해도 같은 결과가 나오게 하려는
내부 규칙일 뿐, "원본이 이랬다"는 주장이 아니다. 승패는 참가자 `match_result`로 판정하며
갈리지 않으면 투영하지 않는다.

### D-038. `damage` 정밀도 — 스테이징은 원본, 도메인은 정수

넥슨 `damage`는 소수(double)다. 계약의 딜량은 정수(`Count`)다.
`NexonMatchParticipant.damage`에 **소수 원본을 그대로** 두고, 운영 `MatchPlayerStat.damage`에만
반올림해 넣는다. 정확한 값이 필요하면 스테이징·원본에서 다시 읽을 수 있다.

### D-039. `RawImport`는 append-only다

`contentHash`(sha256)를 유니크 키에 넣었다.

- 같은 내용을 다시 받으면 **새 행을 만들지 않고** `fetchCount` / `lastFetchedAt`만 올린다.
- 내용이 달라지면 **새 행**을 추가한다. 이전 원본을 덮어쓰지 않는다.

30일 갱신 재수집이 과거 원본을 지워 버리면 `CLAUDE.md` 3-A 1번(원본 보존)이 깨진다.
**요청 헤더는 저장하지 않는다.** API 키가 저장소에 들어갈 경로 자체를 없앴다.

### D-040. Phase 8은 큐 인프라를 도입하지 않는다

Redis / BullMQ를 쓰지 않는다. 체크포인트는 `ImportJob`(+`ImportFailure`)에 남기고
실행은 워커 CLI가 한다. 넥슨 `/match`에 커서가 없어 체크포인트 단위는 매치가 아니라
**대상(ouid × match_mode)** 이다. 규모가 실제로 문제가 되면 별도 Phase에서 큐를 얹는다.

`정보갱신` / `전적갱신` 버튼은 넥슨 API를 인라인 호출하지 않고 `ImportJob`에 `pending`으로
등록만 한다. 사용자 클릭이 곧바로 외부 호출이 되면 호출 한도를 순식간에 소진한다.

### D-041. 신선도 주기는 설정값이다 (`NEXON_REFRESH_INTERVAL_DAYS`, 기본 30)

넥슨 이용 조건에 "가져간 데이터는 최소 30일마다 갱신"이 명시돼 있다.
다만 **그 의무가 어느 데이터 범위까지 어떻게 적용되는지는 검증되지 않았다 `[미확인]`.**
그래서 특정 일수를 비즈니스 규칙처럼 코드에 고정하지 않고 환경변수로 받는다.
기한이 지난 스테이징은 `refreshDueAt`으로 뽑아 재수집하고, 갱신하지 못하면 `staleAt`을 남긴다.

### D-042. 호출 한도를 추측하지 않는다

넥슨 스펙·문서 어디에도 호출 한도 수치가 없다. 테스트 키의 한도는 더더욱 모른다.
그래서 `NEXON_RATE_LIMIT_PER_SEC`(보수적 기본값)에서 시작해 **429를 받으면 스스로 감속**한다
(`Retry-After` 존중 + 지수 백오프 + 지터). 403·키 오류는 재시도하지 않고 **전체를 멈춘다.**
"우리 추정 한도"를 코드에 적어 두지 않는다.

---

## Phase 8 — 실응답 검증 (2026-08-21)

닉네임 1명으로 실제 넥슨 API를 소량 호출해 확인한 결과다. **총 15회 호출.**
가명화한 실응답은 `packages/nexon/src/fixtures/real/*.json`에 있고,
`packages/nexon/src/__tests__/realResponse.test.ts`가 그것으로 스키마를 고정한다.

### D-043. 실제 응답의 클랜명 필드는 `clan_name`이 아니라 `guild_name`이다

공식 OpenAPI 스펙에는 `clan_name`이라고 적혀 있으나, 실제 `/match-detail` 응답의
참가자 필드는 **`guild_name`** 이다. 실측한 참가자 키 전체는 다음과 같다.

```
kill · death · assist · damage · team_id · headshot · user_name · guild_name ·
match_result · season_grade
```

- 스키마에 두 이름을 모두 두고, 정규화에서 `clan_name ?? guild_name`으로 합친다.
- **원본은 RawImport에 그대로 남는다.** 나중에 넥슨이 스펙대로 바꿔도 재변환할 수 있다.
- 문서(스펙)와 실제가 다르면 **실제를 따른다.** 문서를 근거로 코드를 되돌리지 않는다.

### D-044. 한 경기 응답에 양 팀이 함께 오지 않는다 — **Phase 9 blocker**

실측 5경기(개인전 1 · 퀵매치 클랜전 4)에서 **양 팀 라인업이 온전히 온 경우가 한 번도 없었다.**

| 조회 대상의 결과 | 응답 구성 |
|---|---|
| 승리 (3건) | 승리 팀 6명만. 상대 팀 0명 |
| 패배 (1건) | 승리 팀 6명 + **본인 1명** (총 7명) |
| 무승부 (1건) | 한 팀 5명만 (전원 k/d 0) |
| 개인전 (1건) | 7명, `team_id`가 전부 `null` |

관측된 규칙은 "승리 팀 전원 + (졌다면) 조회 대상 본인"에 가깝다. **원인은 `[미확인]`**
(공개 동의 여부·API 정책 등). 넥슨 스펙에는 이런 제한이 적혀 있지 않다.

영향

- 클랜 vs 클랜 경기를 **한 사람의 조회만으로 재구성할 수 없다.**
- 우리 투영 규칙(양 팀·양 클랜·인원 일치)은 실데이터에서 항상 보류된다. 규칙이 틀린 게 아니라
  **입력이 부족한 것**이다.
- 해결 후보: ① 같은 경기를 여러 참가자 ouid로 조회해 합치기(양쪽 클랜원을 모두 알아야 한다)
  ② 넥슨에 문의 ③ 클랜 단위 조회 수단 확보. **어느 것도 아직 검증되지 않았다.**

Phase 9(래더 계산)는 경기 단위 승패와 양 팀 구성이 필요하므로, 이 문제가 풀리기 전에는
**넥슨 데이터만으로 래더를 산정할 수 없다.**

### D-045. 개발 도구는 자기 데이터 밖을 건드리면 안 된다 (사고 기록)

빈 DB에서 만든 `offlineSmoke.ts`가 실수집 데이터가 있는 DB에서 돌자,
상세 미수집 상태였던 **실제 스테이징 200건에 가짜 응답을 덮어썼고** 180건을 임시 리그로 투영했다.

- 원인: 스모크가 "상세가 없는 매치"를 전역에서 골랐다. 자기 데이터만 고르지 않았다.
- 복구: **원본을 버리지 않았기 때문에**(`RawImport`) 목록 원본에서 값을 되돌려 200건 전부 복구했다.
  `CLAUDE.md` 3-A 1번이 실제로 사고를 막아 준 사례다.
- 조치: `runCollect`에 `detailSourceMatchIds`, `runProject`에 `sourceMatchIds`를 추가해
  **범위를 인자로 강제**했다. 스모크는 자기 경기 ID만 넘긴다.
- 교훈: "빈 DB에서 통과"는 안전의 근거가 아니다. 개발 도구도 대상 범위를 명시해야 한다.

### D-046. 실응답 픽스처는 가명화해서만 저장소에 넣는다

실응답에는 제3자의 닉네임·클랜명·계정 식별자가 들어 있다.

- `user_name` / `guild_name` → `유저01` / `클랜1` (같은 값은 같은 가명)
- `ouid` / `match_id` → **글자 종류를 유지한 채** 치환 (32자리 hex, 18자리 숫자라는 형식은 남는다)
- 시각·수치·모드·맵은 **원본 그대로** — 검증 대상이기 때문이다
- 저장 직전 API 키 포함 여부를 다시 검사하고, 걸리면 파일을 쓰지 않는다

도구는 `apps/worker/src/dev/exportFixtures.ts`.

### D-047. 넥슨 `match_id`도 18자리 숫자였다 (그래도 내부 ID는 따로 쓴다)

실측 698건 전부 `^\d{18}$`이며, 3rd.supply의 매치 ID 규칙(`YYMMDDHHmmss`+6)과 형식이 같다.
3rd.supply가 넥슨 값을 그대로 썼다는 정황이다.

그래도 **D-035는 유지한다.** 형식이 우연히 같다는 것과 외부 ID를 내부 PK로 쓰는 것은 다른 문제다.
`Match.id`는 우리가 만들고, 넥슨 값은 `sourceMatchId`에 보존한다.

---

## Phase 8.1 — D-044 후속 검증 · 적응형 폴링 (2026-08-21)

### D-044 결론 — **CASE 1 + 조건부 CASE 3** (실측 근거)

**A-1. 같은 `match_id`를 다시 조회하면 완전히 같은 응답이 온다.**

클랜전 3건을 재호출한 결과 `contentHash`가 **전부 동일**했고, `RawImport`에 새 행이 생기지 않고
`fetchCount`만 2로 올랐다. `/match-detail`의 파라미터는 `match_id` 하나뿐이므로
**"다른 사람의 계정으로 같은 경기를 다시 조회해 합친다"는 방법은 성립하지 않는다** → **CASE 1**.

(단, 같은 API 키로만 확인했다. 다른 앱/키에서도 같은지는 `[미확인]`.)

**A-2. 같은 경기가 다른 참가자의 매치 목록에는 존재한다.**

7명짜리 클랜전(상대 팀 6명 + 본인)에서 상대 팀 참가자 2명을 골라 확인했다.

| 대상 | 그 경기가 목록에 있는가 | 목록이 준 값 |
|---|---|---|
| `MMA수련중` (상대 팀) | **있음** | `result=1` `kill=9` `death=8` `assist=3` |
| `미키` (본인) | 있음 | `result=2` `kill=13` `death=11` `assist=3` |
| `혀반샷` (상대 팀) | **없음** | — |

목록이 준 값은 상세 응답의 같은 사람 기록과 **일치**했다(kill 9 / death 8 / assist 3).
→ 여러 사람의 목록을 모으면 **그 사람들의 개인 기록**은 채울 수 있다 → **조건부 CASE 3**.

**한계 (그래서 CASE 3만으로 완성되지 않는다)**

1. 목록에는 `team_id`도 클랜명도 없다. 승패(`match_result`)로만 편을 가를 수 있다.
2. **모르는 참가자는 발견할 방법이 없다.** 상세가 상대 팀 닉네임을 주지 않으므로
   그 사람의 `/id`조차 부를 수 없다. 우리가 이미 아는 사람만 채워진다.
3. `혀반샷`처럼 **닉네임으로 조회한 계정이 그 경기의 그 사람이 아닐 수 있다.**
   닉네임은 영구 식별자가 아니다 — D-036이 실제 데이터로 확인된 사례다.

**결론**: 넥슨 데이터만으로 **양 팀 전원을 보장하는 재구성은 불가능**하다.
다만 SACLOUD 리그처럼 **참가자 명단을 우리가 이미 아는 경우**에는, 리그 소속 선수들을 폴링해
같은 `match_id`를 교차 발견하고 각자의 기록을 합치는 경로가 있다. 그것도 미등록 참가자가 있으면
인원이 맞지 않아 투영되지 않는다. **BLOCKER는 유지된다.**

### D-048. 상세 참가자와 목록 관측값을 섞지 않는다

목록에서 얻은 개인 기록은 `NexonMatchObservation`에 **출처(`player_match_list`)와 함께** 쌓고,
상세에서 온 참가자(`NexonMatchParticipant.source = "match_detail"`)와 같은 표에 넣지 않는다.

이유: 둘은 신뢰도와 내용이 다르다. 상세는 팀·클랜명이 있고, 목록은 본인 기록뿐이다.
섞으면 "이 참가자 정보가 어디서 왔는지"를 나중에 구분할 수 없다.
**나오지 않은 참가자를 만들지 않는다** — 관측된 사람만 저장한다.

### D-049. 활동량 기반 적응형 폴링 (고정 전수 조회 폐기)

5,000명을 같은 주기로 계속 조회하지 않는다. 한 명을 한 번 보는 데 모드 4개 = **호출 4회**다.

| 티어 | 뜻 | 기본 주기 |
|---|---|---|
| `hot` | 방금 새 경기를 발견 | 30분 |
| `warm` | 연속 빈 조회 2회 이상 | 3시간 |
| `cold` | 연속 빈 조회 5회 이상 | 1일 |
| `dormant` | 연속 10회 이상 또는 30일간 새 경기 없음 | 7일 |

- 전환 규칙: 새 경기 발견 → 즉시 `hot`·연속 빈 조회 0으로 초기화 / 빈 조회 → 누적 후 강등 /
  **실패는 활동량이 아니다** → 티어를 내리지 않고 다음 조회만 미룬다(백오프)
- 우선순위: 수동 갱신(0) > hot(1) > warm(2) > cold(3) > dormant(4), 같으면 오래 기다린 순
- **starvation 방지**: 예정보다 크게 밀린 대상은 티어와 무관하게 우선순위를 올린다
- **숫자는 전부 설정값**(`NEXON_POLL_*`). 실제 호출 한도를 모르는 상태에서
  "몇 분마다 반드시"를 영구 규칙으로 코드에 박지 않는다

`정보갱신`/`전적갱신` 버튼은 `manualRefreshRequestedAt`을 찍어 **최우선**으로 올린다.
여전히 화면에서 넥슨 API를 직접 부르지 않는다(E 결정).

### D-050. 이미 저장한 경기의 상세는 다시 부르지 않는다

A-1에서 확인했듯 같은 `match_id`는 언제 불러도 같은 응답이다.
그래서 다른 사람의 목록에서 같은 경기가 나와도 **상세를 다시 부르지 않는다.**
예외는 신선도 정책(기본 30일) 재검증뿐이다(`decideDetailFetch`).

목록 관측값 병합(D-048)과 상세 재조회는 **다른 일**이다. 전자는 호출 없이 쌓이고,
후자는 호출이 든다. 코드에서도 분리했다.

### D-051. 호출량을 기록한다 (`NexonPollRun`)

5,000명 운영 비용을 추정이 아니라 **숫자로** 계산하기 위해 실행마다 남긴다:
대상 수 · 목록 호출 · 신규/중복 match_id · 상세 호출 · dedupe로 건너뛴 수 · 빈 조회 ·
429 횟수 · 실패 · 활동/비활동 대상별 호출 수. `pnpm nexon:report`로 본다.
**관리자 UI는 만들지 않는다** (F 결정 유지).

---

## Phase 8.2 — 로스터 기반 재구성 (2026-08-22)

> 전제: **D-044는 해결되지 않았다.** 넥슨 `/match-detail`은 여전히 참가자 일부만 준다.
> Phase 8.2는 그 한계를 없애는 것이 아니라, **우리가 아는 정보(리그 로스터)로 완전한 경기를
> 만들 수 있는 조건**을 정한 것이다.

### D-052. 경기 시점 소속은 **등록 기간**으로 판정한다 (`LeagueRosterMembership`)

넥슨은 참가자의 클랜을 문자열(`guild_name`)로만 주고, 그것도 상세에 나온 사람만 준다.
문자열이 같다고 같은 클랜이라고 볼 수 없고(동명 클랜·개명), 상세에 없는 사람은 아예 알 수 없다.

대신 SACLOUD는 리그 운영자로서 **누가 언제 어느 클랜으로 등록됐는지**를 안다.
그 사실을 `joinedAt ~ leftAt` 기간으로 보존하고, 경기 시각이 그 기간 안인지로만 판정한다.

- 판정 규칙: `joinedAt <= 경기시각 < leftAt` (leftAt이 null이면 현재까지)
- 같은 시각에 소속이 둘 이상이면 **판정하지 않는다.** 추측하지 않는다
- `verified=false`(운영자 미확인) 소속은 기본적으로 완전성 판정에 쓰지 않는다.
  `--allow-unverified-roster`로 운영자가 명시할 때만 쓴다
- 닉네임으로 소속을 정하지 않는다 (D-036 유지)

### D-053. 공식리그 등록 선수를 **같은 티어 안에서** 먼저 조회한다

우리가 완성해야 하는 것은 리그 경기이고, 리그 경기의 완전성은 등록 선수 **전원의 관측**에 달려 있다.
그래서 `NexonPollState.priorityClass = "league"`인 대상의 우선순위를 `leaguePriorityBoost`만큼 올린다.

- **호출량을 늘리지 않는다.** 주기(티어)는 그대로고 **순서만** 바뀐다
- 티어를 뒤집지 않는다. hot 일반 유저가 warm 리그 선수보다 여전히 먼저다
- 수동 갱신 요청은 여전히 최우선이다
- 값은 `NEXON_POLL_LEAGUE_BOOST` 설정값이다. `0`이면 기능이 꺼진다

### D-054. 상세(`match_detail`)는 **보조 증거**다

재구성의 1차 근거는 각 선수의 매치 목록에서 온 개인 관측값(D-048)이다.
상세는 (1) 교차검증과 (2) 상세에만 있는 값(헤드샷·데미지·team_id) 보완에만 쓴다.

- 상세와 관측이 어긋나면 **자동 투영하지 않는다.** 사유를 남기고 사람이 본다
- 상세가 주지 않은 값(`null`)은 불일치로 보지 않는다. 모른다는 뜻이다
- 상세에 나왔는데 목록 관측이 없는 **로스터 선수**가 있으면 `missing_observation`으로 보류한다.
  그 사람을 폴링하면 채워질 자리이지, 우리가 채울 자리가 아니다

### D-055. 새 클랜전을 발견하면 같은 경기를 봤을 사람의 조회를 **앞당긴다** (전파)

각자의 티어대로만 돌면 한 경기가 다 모이는 데 며칠이 걸린다.

- **호출을 새로 만들지 않는다.** 어차피 조회할 사람의 `nextPollAt`을 당길 뿐이다
- 대상은 (1) 발견자와 같은 클랜의 등록 선수, (2) **증거로 확인된** 상대 클랜의 등록 선수뿐이다.
  상대 클랜이 관측·상세로 확인되지 않았으면 **추측해서 전파하지 않는다**
- 한 번에 앞당기는 수에는 상한이 있다 (`NEXON_POLL_PROPAGATION_FANOUT`, 기본 20)
- 티어·주기·수동 갱신 표시는 건드리지 않는다. 사유는 `propagationReason`에 남는다

### D-056. 재구성은 **완전할 때만** 투영하고, 판정 근거를 남긴다

부분 저장을 하지 않는다. 다음이 모두 참일 때만 운영 `Match`를 만든다.

1. 경기 시각·맵·매치 유형이 리그 기록 대상이다
2. 참가자 전원이 **목록 관측**으로 확인됐다 (관측되지 않은 사람을 만들지 않는다)
3. 전원이 경기 시점 로스터로 뒷받침된다 (D-052)
4. 클랜이 정확히 둘이고, 양 팀 인원이 같고, 리그 대전 인원과 일치한다
5. 같은 클랜 안에서 승패가 일치하고 승자가 하나다
6. 상세와 어긋나는 값이 없다 (D-054)

판정 결과는 성공·실패 모두 `NexonMatch.reconstruction`(JSON) + `reconstructedAt`에 남긴다.
**계산 결과가 아니라 증거다.** 무엇이 모자라서 미완인지 숫자로 남아야 다음 판단을 할 수 있다.

래더 값은 여전히 넣지 않는다. 재구성된 경기도 `origin="nexon"`이고 래더 컬럼은 전부 `null`이다 (Phase 9).

---

## Phase 9 — 래더 정책 (2026-08-22 사용자 확정)

> 이 장의 결정은 **사용자가 직접 확정한 제품 정책**이다. 우리가 추정한 것이 아니다.
> 일부는 3rd.supply 재현보다 **새 SACLOUD의 래더 품질**을 우선한 선택이며, 그 사실을 명시한다.
> 상수의 근거는 `docs/LADDER_TUNING_REPORT.md`의 시뮬레이션이다.

### D-057. 경기 인정 기준 — **양측 각각 3명 이상**

같은 `sourceMatchId`에서 **양 클랜이 각각 3명 이상** 확인되면 경기로 인정한다.

```
A 4명 · B 3명 → 인정
A 5명 · B 2명 → 인정하지 않음
```

기존 Phase 8.2 기준(양 팀 전원 복원 + 인원 일치)은 **폐기한다.** 넥슨이 참가자 전원을 주지
않는 이상(D-044) 전원 복원을 기다리면 대부분의 경기가 영원히 인정되지 않는다.

**확인(confirmed)의 정의** — 그 선수의 *자기 기록*(승패·k/d/a) 근거가 그 경기에 있어야 한다.

- 목록 관측값(`NexonMatchObservation`) **또는** 매치 상세의 본인 행(`NexonMatchParticipant`)
- 그리고 신원이 확정돼 있고, 경기 시각에 그 클랜 로스터에 등록돼 있어야 한다

모르는 참가자를 추측해서 채우지 않는다.

### D-058. 개인 래더는 P-A 계보를 **잠정** 채택한다

`0.6`의 적용 위치 해석 중 P-A(반올림 전)를 채택한다. 다만 D-059로 division 보정 자체를
쓰지 않게 되어 **P-A와 P-B의 계산 차이는 나타나지 않는다.**

그래도 `formulaVersion`을 `sacloud-p1-PA`로 분리해 둔다. 과거 `rating_update` 표본을 확보하면
P-A/P-B를 다시 비교할 수 있어야 하기 때문이다.

### D-059. division을 공식에 넣지 않는다

`1부니까 +x%` 같은 보정을 두지 않는다. **division은 시즌 상태일 뿐이다.**

- 2부 클랜의 rating이 1부 최하위보다 높아질 수 있다. 그래도 시즌 중 자동 승격하지 않는다
- 승강은 시즌 종료 후 별도 규칙으로 처리한다 (기본안: 1부 최하위 ↔ 2부 최상위, 플레이오프 없음)

원본은 div1에 `K=24 · 배수 1.15`, div2에 `K=30 · 배수 1.00`, 교차 시 div1만 `×0.6`이라는
**비대칭 보정**을 썼다. 그것을 버린다.

### D-060. 인플레이션을 없앤다 — `D=800`, 승리 배수 1.0, 대칭 K

원본 구조를 그대로 쓰면 두 가지 문제가 생긴다(시뮬레이션으로 확인).

| 문제 | 원본 값에서의 현상 |
|---|---|
| div1 인플레이션 | 동급 경기마다 `+17 / -12` = **경기당 +5가 주입**된다. 승률 50%로 200경기만 해도 +158 |
| 양학 무한 상승 | `D=3400`은 너무 평평해서 800점 차에서도 기대승률이 0.63이다. 약체만 잡아도 계속 오른다 |

바꾼 것 세 가지.

1. **승리 배수 1.0** — 1.15는 점수를 주입한다
2. **패배 K도 `K(R)`을 쓴다** — 승/패에 같은 K를 써야 동급 경기가 제로섬이 된다
3. **`D = 3400 → 800`** — 실력차가 점수에 제대로 반영된다

결과: 동급 경기 증감 합 **정확히 0**, 리그 100명 5,000경기 평균 이동 **+1.4점**.

**과거 3rd.supply 기록은 원본값 그대로 보존한다. 새 시즌만 이 공식으로 계산한다** (스펙 §9).

### D-061. 클랜 래더 = Team Elo

클랜 자체가 rating을 가진다. 개인 증감의 평균이 아니다(라인업 조작에 노출된다).
division을 입력으로 쓰지 않고, 개인과 같은 구조에 `K`만 고정값(16)을 쓴다.

### D-062. 양학 억제 — 점수차 보상 감쇠

이긴 상대와의 점수 차가 크면 **승리 보상을 깎는다.**

```
차이 ≤ 300  → 그대로
300 ~ 900   → 선형 감소
차이 ≥ 900  → 0 (아무리 이겨도 오르지 않는다)
```

**패배에는 적용하지 않는다.** 약한 상대에게 지면 제값을 잃어야 한다(D-066).

### D-063. 반복 대전 감쇠

같은 상대에게 **같은 결과가 반복되면** 보상을 깎는다 (`0.6ⁿ`, 하한 0.15, 기간 14일).
결과가 뒤집히면 새 정보이므로 깎지 않는다. **반복 경기를 금지하지 않는다** — 효율만 낮춘다.

### D-064. 시즌 종료 soft reset

```
newRating = baseline + (oldRating - baseline) × carryRate     baseline 1500 · carryRate 0.5
```

완전 초기화가 아니다. 순위 순서는 **그대로 보존**되고 폭만 절반으로 줄어든다.

### D-065. 라인업 전력은 **확인된 선수만**으로 계산한다

클랜 공식 rating과 별개로, 그 경기에 **실제로 확인된** 선수들의 개인 래더 평균을 낸다.
양측 확인 인원이 **4명 미만이면 반영하지 않는다.** 3명 평균을 5명 팀의 전력으로 쓰는 것은
추정이기 때문이다. 확인되지 않은 선수의 래더를 채워 넣지 않는다.

시뮬레이션에서 이 방식(blend 0.5)이 반영하지 않는 쪽보다 클랜 전력 추적이 더 정확했다
(상관 0.982 vs 0.976).

### D-066. 업셋 보상은 유지한다

양학 억제 때문에 업셋 보상까지 죽이면 안 된다. 다음이 항상 성립해야 한다.

```
약팀이 강팀을 이김  →  큰 보상   (+30)
강팀이 약팀을 이김  →  적은 보상 (+7, 격차가 크면 0)
비슷한 팀끼리       →  정상 변화
```

### D-067. 개인 점수는 **확인된 선수에게만** 만든다

경기가 3v3으로 인정됐더라도, 확인되지 않은 4·5번째 선수의 개인 점수는 만들지 않는다.
없는 참가자를 만들지 않는다.

### D-068. 확인 수준을 보존한다 · 로스터는 "후보 목록"이다

경기마다 다음을 저장해 화면까지 그대로 들고 간다.

```
clanAConfirmedCount · clanBConfirmedCount
observationParticipantCount · detailParticipantCount
participantCompleteness ("5v4") · reconstructionConfidence ("high"|"medium"|"low")
```

그리고 로스터의 역할을 명확히 한다.

> 로스터는 "이 선수가 경기에 참가했을 것이라고 **추측**"하는 근거가 아니라,
> "이 선수의 넥슨 매치 목록을 **확인해야 한다**"는 후보를 제공하는 것이다.

로스터 등록만으로는 참가가 확인되지 않는다. 로스터는 `LeaguePlayer` 기반 자동 파생 +
클랜장/운영자 verify를 기본 경로로 쓰고, CSV는 **대량 등록 보조 수단**으로만 둔다.

---

## Phase 9 audit — 정책 정정 (2026-08-22)

> 아래는 Phase 9 최종 audit에서 **사용자가 확정한 정정 사항**이다.
> 앞선 D-057 ~ D-068 중 충돌하는 부분을 대체한다.

### D-069. 보상 감쇠는 **부호**를 본다. 승리 증감은 음수가 되지 않는다

점수차 보상 감쇠(D-062)는 `본인 래더 − 상대 래더`라는 **부호 있는 값**으로 걸린다.

```
2600이 1200을 이김  → 차이 +1400 → 감쇠 → +0
1200이 2600을 이김  → 차이 -1400 → 감쇠 없음 → +30
```

절대값으로 걸면 업셋 보상까지 죽는다. 그래서 **강자의 승리만** 깎고 약자의 승리는 그대로 둔다.

그리고 **승리 증감은 어떤 조건에서도 0 미만이 되지 않는다.** 코드로 못박았고 전 구간을 테스트로 고정했다.

**감쇠 구간을 뒤로 물렸다** (`300~900` → `600~1200`).
이유: Elo는 기대 승률대로 이기면 장기 기댓값이 0이 되게 설계돼 있는데, 승리만 깎고 패배는
그대로 두면 "기대대로 이겼는데도 점수가 줄어드는" 구간이 생긴다. 그 구간이 실제 리그에서
자주 나오는 격차(≤600)에 걸리지 않게 했다.

측정 결과 — 격차별 1경기 기댓값: `0점차 0.00 · 200점차 -0.08 · 400점차 +0.27 · 600점차 +0.07`

> 참고로 audit에서 지적된 "300승 후 1496"은 **300연승이 아니라 90% 승률 300경기**(270승 30패)였다.
> 실제 300연승은 `1500 → 1938`이고 최소 승리 증감은 `+1`이다. 보고서의 표기가 부정확했다.

### D-070. 반복 대전 감쇠는 **기본값 꺼짐**이다 (멸망전 보호)

SACLOUD에는 같은 두 클랜이 연달아 붙는 **멸망전** 문화가 있다. 정상적인 실력 검증이다.

- 반복 경기를 **전부 기록하고 전부 승패에 반영한다.** 제외·무효·0점 처리를 하지 않는다
- 감쇠 기능은 남기되 **기본값 `repeatDecay = 1`(꺼짐)** 이다
- 켜더라도 **점수 차가 `repeatDecayMinGap`(600) 이하면 걸리지 않는다**

왜 껐는가 — 측정해 보니 점수차 보상 감쇠만으로 farming이 이미 죽어서, 반복 감쇠를 켜도
결과가 **한 점도 달라지지 않았다**.

```
900 낮은 상대에게 20연승   켬 1820 · 끔 1820   (차이 없음)
비슷한 상대 멸망전 20연승   켬 1817 · 끔 1817   (차이 없음, 새 상대 20연승과도 동일)
```

승패 교대(A승 B승 A승 …) 패턴을 조작으로 판정하지 않는다. 정상적인 접전에서도 나온다.
collusion 탐지는 Phase 9 래더 공식의 요소가 아니며, 필요해지면 별도 이상탐지로 다룬다.

### D-071. 공식전 인정은 **본클랜원** 3+3이다 (용병은 못 채운다)

D-057의 "확인 3명"을 정밀화한다. 양 팀 각각 **그 클랜 소속 본클랜원**이 3명 이상 확인돼야 한다.

```
베리타스 본클랜원 3 + 용병 2  vs  메쏘드 본클랜원 4 + 용병 1  → 공식전 인정
베리타스 본클랜원 2 + 용병 3  vs  메쏘드 본클랜원 5          → 불인정
```

### D-072. "원소속 클랜"과 "이 경기에서 뛴 팀"을 분리한다

두 값은 다르다. 팀 배정은 다음 근거로 한다 — **추측하지 않는다.**

1. 본클랜원 3명 이상이 모인 클랜 **두 곳**이 그 경기의 두 팀이다
2. 각 팀의 승패는 그 팀 본클랜원들의 **다수 결과**로 정해진다
3. 나머지 확인된 선수는 **자기 승패와 같은 팀**으로 배정한다 (승자가 하나뿐이므로 승패가 곧 팀이다)
4. 배정된 팀이 자기 등록 클랜과 다르면 그 경기의 역할은 **용병**이다

본클랜원 승패가 정확히 반반이면 판정하지 않는다. 그때는 근거가 없다.

### D-073. 용병도 개인 기록과 개인 래더를 받는다

3+3은 **경기를 공식전으로 인정하기 위한 기준**일 뿐이다.
경기가 인정된 뒤에는 **출전이 확인된 전원**이 `MatchPlayerStat`·개인 승패·K/D·개인 래더를 받는다.

판단 기준은 **소속이 아니라 실제 출전 확인 여부**다.
본클랜원이 아니라는 이유로 개인 기록을 만들지 않는 일은 없다.
반대로 로스터에 있다는 이유만으로 출전을 추정하지도 않는다 (D-068 유지).

역할과 원소속은 경기별로 저장한다 — `MatchPlayerStat.participantRole` · `rosterLeagueClanId`.

### D-074. 확인 수준(completeness)은 **출전자 전원** 기준이다

`5v5` 같은 표기와 UI 배지는 **실제로 확인된 전체 참가자 수**를 쓴다.
본클랜원만 세서 `3v4`로 표시하지 않는다.

상세 근거는 따로 보존한다 — 이긴 팀·진 팀의 `본클랜원 수`와 `용병 수`를 각각 남긴다.

### D-075. 용병의 원소속 클랜 래더는 변하지 않는다

클랜 래더는 그 경기를 치른 **두 클랜**만 변한다.
리릭 소속 선수가 베리타스 용병으로 이겨도 리릭의 승패·래더는 그대로다.

### D-076. 라인업 전력에 용병을 포함한다

실제로 출전이 확인된 용병도 그 경기의 전력 일부다. 라인업 평균에 넣는다.
단 확인된 선수만 쓴다. 확인 인원이 모자라면 라인업 전력을 아예 반영하지 않는다 (D-065 유지).

### D-077. 시즌 전환은 **운영자 액션**으로만 한다

현재 활성 시즌은 **Season 7**이다. 운영자가 Season 8 시작을 실행하기 전까지 모든 새 공식경기는
Season 7에 기록한다. 날짜·배포·월 변경으로 자동 전환하지 않는다.

### D-078. 시즌 귀속은 **실제 경기 시각**으로 정한다

늦게 수집된 경기는 수집 시각이 아니라 `startAt`으로 시즌을 정한다.

```
Season 8 시작 20:00
19:55 경기 → 20:10에 수집돼도 Season 7
20:05 경기 → Season 8
```

### D-064(개정). 시즌 soft reset 폐기 — 새 시즌은 **모두 같은 출발점**

```
(폐기) newRating = 1500 + (old - 1500) × 0.5
(현행) newRating = seasonBaseline (1500)   ← 개인·클랜 전부 동일
```

전 시즌 1위라고 높은 점수에서 시작하지 않고, 하위권이라고 낮게 시작하지도 않는다.
`seasonCarryRate`는 설정에서 **제거했다.**

**지난 시즌 기록은 그대로 보존한다.** 최종 개인·클랜 랭킹, 최종 rating, 승패, 경기 기록,
시즌 통계(`LeaguePlayerSeason` · `LeagueClanSeason` · `RankSnapshot` · `Match`)를 건드리지 않는다.
`nexon:season-start`가 바꾸는 것은 "지금 시즌의 현재 점수"뿐이다.

---

## Phase 9 정책 동기화 — 최신 (2026-08-22, authoritative)

> 이 장이 **가장 최신 정책**이다. 앞선 D-057 ~ D-078 중 충돌하는 부분을 대체한다.

### D-079. 공식 경기 인정은 **OR** 조건이다 (D-071 대체)

**양 팀 중 한쪽이라도** 같은 클랜 본클랜원이 3명 이상 확인되면 **공식 경기**다.

```
클3+용2 vs 클3+용2  → 공식        클2+용3 vs 클2+용3  → 비공식 경기
클3+용2 vs 클2+용3  → 공식        클2+용3 vs 클1+용4  → 비공식 경기
클3+용2 vs 클1+용4  → 공식        클1+용4 vs 클1+용4  → 비공식 경기
클3+용2 vs 클0+용5  → 공식
```

```
homeClanMembers >= 3  OR  awayClanMembers >= 3
```

**AND가 아니다.** 기존 D-071의 "양쪽 모두 3명" 기준은 폐기한다.

### D-080. 공식 통계 미반영 경기도 **지우지 않는다**

양쪽 다 3명 미만이어도 경기 자체는 기록실에 남긴다.
참가자·K/D/A·맵·경기시간·결과·양 팀 구성을 그대로 볼 수 있다.

다만 다음에 **전혀 반영하지 않는다**:
시즌 승패 · 시즌 킬/데스 · 킬뎃 · 평균킬 · MVP 누적 · 개인 래더 · 클랜 래더 · 랭킹

`Match.official = false`로 저장하고, 화면에 **`비공식 경기 · 래더 미반영`** 배지를 보여 준다.

### D-081. 클랜 래더 반영률은 **팀마다 독립**이다

같은 공식 경기라도 각 팀의 본클랜원 수에 따라 클랜 래더 반영률이 다르다.

| 본클랜원 | 반영률 |
|---|---|
| 3명 이상 | 100% |
| 2명 | 70% |
| 1명 | 40% |
| 0명 | 0% |

승리·패배 양쪽에 같은 배율을 쓴다.

```
라텐시 클1+용4  vs  메쏘드 클3+용2
원래 증감 +20 / -20  →  실제 라텐시 +8 · 메쏘드 -20
```

**한 경기의 클랜 증감 합이 0이 아닐 수 있다.** 의도된 것이며 장기 영향은 D-083에서 확인했다.

### D-082. 개인 래더에는 차등이 없다

정상 경기라면 **출전이 확인된 모든 선수**가 개인 래더를 100% 받는다.
용병이라는 이유로 승리 점수를 줄이거나 패배 점수를 덜 깎지 않는다.

> 개인 래더는 "그 사람이 실제 경기에서 누구를 상대로 어떤 결과를 냈는가"를 평가한다.
> 클랜 래더는 "그 클랜이 얼마나 자기 전력으로 참가했는가"까지 평가한다.

### D-083. 반영률 차등의 장기 영향 — 측정 결과 문제없다

40클랜 4,000경기 시즌 시뮬레이션(`apps/worker/src/lab/clanWeightSim.ts`).

| 시나리오 | 평균 이동 | 표준편차 | 하한 집중 | 실력 상관 |
|---|---|---|---|---|
| 전원 본클랜원 5명 | -1.7 | 339 | 0 | 0.986 |
| 가끔 용병 (본클 3~5) | -2.1 | 340 | 0 | 0.992 |
| 용병 흔함 (본클 1~5) | -1.4 | 298 | 0 | 0.980 |
| 용병 남용 (본클 0~3) | -4.0 | 199 | 0 | 0.979 |

인플레이션·디플레이션·분포 붕괴·하한 집중 모두 나타나지 않았다.
용병을 많이 쓸수록 클랜 점수가 덜 움직여 분포가 좁아지는 것은 **의도된 효과**다.
숨은 보정을 추가하지 않았다.

### D-084. 경기마다 순차 계산한다

여러 경기가 한꺼번에 발견돼도 `startAt` 오름차순으로 **한 경기씩** 계산한다.
멸망전 6판을 시작 시점 래더로 일괄 계산하지 않는다.
각 경기마다 개인 래더·클랜 래더·라인업 전력을 **직전 경기 결과가 반영된 상태**로 다시 잡는다.

`nexon:rate`는 시즌 전체를 처음부터 다시 계산하므로 이 순서가 항상 보장된다.

### D-085. 시즌 종료 시 최종 랭킹을 스냅샷으로 굳힌다

새 시즌이 시작되면 현재 점수가 초기화되므로, 닫기 전에 그 시즌의 최종 순위를
`RankSnapshot`(클랜은 부리그별, 개인은 전체)으로 저장한다. 경기·시즌 통계는 건드리지 않는다.

### D-086. 시즌 승강은 시즌 시작 시점에만 (기본안 유지)

**1부 최하위 ↔ 2부 최상위** 1팀 교환. 플레이오프는 넣지 않는다.
시즌 중에는 절대 승강하지 않는다. `season --start`를 부를 때만 반영된다.

### D-087. 열산·열빡은 추측으로 분류하지 않는다

넥슨 API로 확실히 구분할 수 없다. 그래서 **추측으로 강제 분류하지 않는다.**
현재는 본클랜원 구성 조건(D-079)이 사실상 그 역할을 한다 — 클랜원이 모이지 않은 경기는
자동으로 비공식 경기이 되어 공식 통계에서 빠진다.
확실하지 않은 것은 `reconstruction` 근거와 confidence로 남긴다.

### D-088. 클랜 자동 병합 금지 (운영 규칙)

`Veritas` · `VERITAS` · `베리타스`를 이름이 비슷하다는 이유로 같은 클랜으로 묶지 않는다.
병합은 운영자가 **정확한 slug 두 개를 지정**할 때만 실행된다(`nexon:clan --merge --from --into`).
병합해도 원본 클랜 행을 **삭제하지 않고** 이름에 표시만 남긴다 — 되돌릴 수 있어야 한다.

---

## Phase 10 — 베타 운영 준비 (2026-08-22)

### D-089. 운영 기능은 **CLI와 웹이 같은 코드**를 쓴다

시즌·클랜·로스터 운영 로직을 `packages/db/ops/`에 두고 `apps/worker`(CLI)와
`apps/web`(관리자 화면)이 함께 부른다. 두 경로가 갈라지면 "CLI로는 되는데 화면으로는
다르게 되는" 사고가 난다.

### D-090. 관리자 권한은 **서버에서** 판정한다

`requireAdmin(request)`가 모든 관리자 API의 첫 줄이다. 화면에서 버튼을 감추는 것은 보안이 아니다.
비로그인·일반 사용자가 요청을 직접 만들어 보내도 403이다 (조회·변경 모두 테스트로 고정).

### D-091. 파괴적 작업은 **미리보기 → 확인** 두 단계다

시즌 종료·시작은 `confirm: false`(기본)면 미리보기만 한다.
무엇이 바뀌는지(스냅샷 건수·승격/강등 클랜·초기화 대상 수)를 보여 준 뒤에만 실행된다.
실행은 **트랜잭션**이라 "시즌 7은 닫혔는데 8이 없는" 중간 상태가 생기지 않는다.

### D-092. 삭제 대신 **비활성·종료 표시**를 쓴다

- 클랜은 `active = false`로 감춘다. 행을 지우지 않는다
- 로스터에서 뺄 때는 `leftAt`을 찍는다. 지우면 **과거 경기의 소속 판정이 바뀐다**
- 클랜 병합도 원본 행을 남기고 이름에 표시만 더한다

### D-093. 클랜 별칭(`ClanAlias`)은 운영자가 등록할 때만 생긴다

넥슨 `guild_name`이 우리 클랜명과 글자가 다를 수 있다. 그 연결은 팀 식별의 1차 근거지만
(D-072), **비슷하다고 자동으로 만들지 않는다** (D-088). 관리자 화면에서 등록한 것만 쓴다.

### D-094. 베타오픈 시각은 설정값이다 (`AppSetting.betaOpenedAt`)

코드에 날짜를 박지 않는다. 운영자가 관리자 API로 정한다. 정해지기 전에는 `미설정`이다.

### D-095. 공식 ↔ 비공식 전환은 비대칭이다

운영자가 잘못 인식된 경기를 바로잡을 수 있어야 한다 (정책 18).

- **공식 → 비공식**: 바로 된다. 통계에서 빼는 방향이라 안전하다
- **비공식 → 공식**: `reason`을 적어야만 된다. 근거 없이 통계에 넣지 않는다

어느 쪽이든 `AdminAuditLog`에 남고, 래더에 반영하려면 `nexon:rate`를 다시 돌려야 한다.

### D-096. 검증 도구는 **실운영 데이터가 들어와도 흔들리지 않아야 한다**

Phase 10 E2E에서 실제로 깨졌던 것들을 고쳤다.

| 도구 | 문제 | 수정 |
|---|---|---|
| `db:check` | 전체 개수를 세서 실운영 클랜·리그가 생기자 실패 | **픽스처가 만든 행만** 센다 |
| `nexon:check` | "참가자 전원이 로스터로 뒷받침" — 용병은 원래 아니다 | **본클랜원만** 검사 |
| `nexon:check` | "양 팀 인원 균형" — 확인 수준은 원래 다를 수 있다 | 검사 삭제 |
| `nexon:check` | 인정 기준이 AND로 남아 있었다 | **공식 경기만** OR 조건 검사 + 비공식 경기 래더 미반영 검사 추가 |
| `compare` | 실제 리그가 생기자 목록 길이가 달라 실패 | **픽스처 항목만** 골라 대조 |
| 오프라인 스모크 | 전역 `origin=nexon` 개수를 세고 있었다 | 스모크 리그로 범위 제한 (D-045와 같은 실수) |

> 검사가 정상적인 운영 데이터에 반응해 실패하면, 진짜 문제와 구분할 수 없게 된다.

### D-097. 라플/스나 포지션 래더는 **데이터가 없어서 구현하지 않는다** (D-034 재확인)

경기별 라플/스나를 나누려면 "이 선수가 **이 경기에서** 어떤 역할이었나"를 알아야 한다.
실제 저장된 원본을 다시 뒤져서 확인했다 (`pnpm --filter @sacloud/worker exec tsx src/dev/probeWeaponRole.ts`).

**넥슨 `match-detail` 참가자 필드는 이게 전부다.**

```
user_name  team_id  match_result  guild_name  season_grade
kill  death  assist  headshot  damage
```

무기·역할·병과·포지션에 해당하는 키는 **하나도 없다.** `match` 목록에도 없다.
정규식(`weapon|gun|rifle|sniper|role|class|position`)으로 전체 키를 훑어도 0건이다.
즉 **normalize에서 버리고 있는 값이 아니라, 원본에 처음부터 없다.**

| 출처 | 무기 정보 | 왜 못 쓰는가 |
|---|---|---|
| `/match-detail` | 없음 | 필드 자체가 없다 |
| `/match` 목록 | 없음 | 필드 자체가 없다 |
| `/user/recent-info` | **총기별 킬데스는 있다** | **계정 단위 최근 누적**이다. 경기 하나를 라플/스나로 판정할 수 없다 |
| mock 픽스처 `weapon` 0/1 | 있어 보이지만 | `rng.chance(0.22)`로 **생성한 난수**다. 정보가 아니다 |
| 실수집 `MatchPlayerStat.weapon` | 전부 `null` | 7건 모두 `null` (D-034대로) |
| 3rd.supply 원본 사이트 | 있었다 | 그쪽 자체 데이터다. 우리에게 임포트된 적이 없다 (`origin='3rd.supply'` 0건) |

**딜량·헤드샷으로 추정하지 않는다.** 스나이퍼가 헤드샷이 많고 딜량이 낮은 경향은 있지만,
경향은 판정이 아니다. 틀린 분류는 그 선수의 포지션 래더·포지션 킬뎃·포지션 랭킹을 전부 오염시키고,
사용자는 그게 추정값이라는 것을 알 수 없다.

**그래서 다음 중 하나가 생기기 전까지 포지션 래더를 만들지 않는다.**

1. 넥슨이 `match-detail` 참가자에 무기/역할 필드를 추가한다
2. 경기별 역할을 **사람이 입력**하는 경로를 만든다 (선수 자기 신고 또는 운영자 입력).
   이 경우 `roleSource` / `roleConfidence`를 반드시 같이 남겨서 추정과 구분한다
3. 역할이 기록된 외부 데이터를 확보한다

지금 스키마·래더·랭킹·UI를 먼저 만들어 두는 것도 하지 않는다.
분류를 한 건도 받을 수 없는 통계 화면은 "라플 0판 / 스나 0판"만 영원히 띄운다.

#### 보강 (2026-08-22) — 3rd.supply 쪽에는 **경기별 weapon이 실제로 있다**

정상 브라우저 실측으로 확인된 내용을 기록만 해 둔다. **결론은 바뀌지 않는다.**

```
GET https://api-v2.3rd.supply/leagues/1/players/{playerId}/matches
SSR payload: data.matches.data[]
경기 summary 안: matches[].summary.red[] / matches[].summary.blue[]
  { "player": { "id": 1963120755, "name": "천세", "clan": null }, "weapon": 1 }
```

- `weapon 0 = 라이플` · `weapon 1 = 스나이퍼` — 렌더된 화면과 10명 전원 일치
- 같은 선수의 값이 **경기마다 달라진다** → 계정 고정값이 아니라 **경기별 값**이다

그래도 지금 우리가 쓸 수 없는 이유는 그대로다.

1. **우리 수집 경로(넥슨 Open API)에는 이 필드가 없다.** 위는 3rd.supply 자체 데이터다
2. 그 값의 **원 출처가 무엇인지 검증되지 않았다** (넥슨이 준 값인지, 그쪽에서 입력·추정한 값인지)
3. 3rd.supply 접근은 **WAF로 막혀 있다**(D장). 우회하지 않는다

적용 범위도 시즌마다 다르다.

| 시즌 | weapon 취급 |
|---|---|
| Season 1~7 (legacy) | 원본에 있으면 **그대로 보존**만 한다. 지금 legacy 이관은 시즌 카드 단위라 경기별 weapon이 들어올 자리가 아직 없다 |
| Beta | 신규 경기 weapon을 **정상·안정·허용된 경로로** 확보할 수 있을 때만 시험한다. 없으면 `weapon = null`, 역할 랭킹 미출시 |
| Season 8 | 베타에서 그 경로가 검증된 경우에만 라플/스나 랭킹 정식 적용. 아니면 계속 보류 |

헤드샷률·딜량·킬수로 추정하지 않는다는 규칙은 어느 시즌에도 예외가 없다.

### D-098. 베타 시즌은 **번호 0**이고, 사용자에게는 `Beta Season`이다

사용자에게 보이는 시즌 흐름은 하나로 이어져야 한다.

```
Season 1 … Season 7 → Beta Season → Season 8
```

`@@unique([leagueId, number])` 때문에 베타에도 번호가 필요한데, 정식 번호를 쓰면
Season 7 다음이 8이 아니게 된다. 그래서 **0**을 쓴다.

- 다음 정식 번호는 `max(number)+1`로 뽑으므로 7 다음은 그대로 8이다
- **화면에 "Season 0"이라고 쓰지 않는다.** 표기는 `seasonLabel()` 한 곳에서만 정한다
- 화면 정렬은 번호가 아니라 **`startedAt`** 기준이다. 그래야 베타가 7과 8 사이에 온다

`Season.seasonType`은 `legacy` | `beta` | `official` 셋이다.
`legacy`는 3rd.supply에서 이전된 기록이고 **재계산 대상이 아니다**.

### D-099. 과거 시즌 카드의 빈 값은 `null`이다. 역산하지 않는다

3rd.supply의 지난시즌 응답에는 `rank rank_count win lose win_rate kill death kd_rate`만 있다.
`rating` `assist` `headshot` `kill_per_match` `mvp_count` `clan` `division`은 없다.

승률·킬뎃은 승패·킬데스로 계산할 수 있지만 **계산하지 않는다.**
원본이 준 값과 반올림이 다를 수 있고, 그러면 그 시즌의 화면과 우리 화면이 어긋난다.
원본에 있으면 그대로 저장하고, 없으면 `null`로 둔다.

없는 값을 채우려면 시즌 **마감 직전**의 현재 성적을 따로 떠서 병합해야 한다.
`mergeRows()`가 그 일을 한다 — 뒤에 오는 `null`이 앞의 값을 덮지 않는다.

### D-100. 과거 기록은 **닉네임으로 사람을 합치지 않는다**

닉변·동명이인·OUID 변경이 전부 실재한다. 닉네임이 같다는 이유로 과거 카드를 붙이면
엉뚱한 사람의 프로필에 남의 시즌 성적이 들어간다.

그래서 과거 선수는 3rd.supply `player.id`로만 만든다 — `Player.id = "SUPPLY-{playerId}"`.
`legacyPlayerId` / `legacyLeaguePlayerId` / `nicknameAtSeason`을 그대로 보존한다.
현재 신원(`NexonIdentity`)과의 연결은 **근거가 있을 때 운영자가** 한다.

### D-101. 시즌을 닫을 때 **카드**를 남기고, 시작할 때 **누적까지** 0으로 되돌린다

Phase 11에서 찾은 실제 버그 두 개다. 둘은 같이 고쳐야 한다.

| 문제 | 결과 |
|---|---|
| `startSeason`이 rating만 되돌렸다 | 베타의 승패·킬데스·MVP가 다음 시즌에 그대로 남는다 |
| `closeSeason`이 시즌 카드를 안 만들었다 | 위를 고치는 순간 그 시즌 성적이 통째로 사라진다 |

또 시즌 시작 로직이 `ops`와 `apps/worker`에 **두 벌** 있었다. 한쪽만 고치면
CLI로 시작할 때 격리가 안 된다. `runSeasonStart`는 이제 `startSeason`에 위임한다.

무기별 누적(`LeaguePlayerWeaponStat`)도 같이 비운다 —
`통합 = baseRating + 무기별 delta 합` 불변식이 깨지기 때문이다.

### D-102. `[폐기됨 — D-107로 대체]` 무소속은 계산 조건이 아니라 공개 범위 조건이다

> **2026-08-23 폐기.** "무소속 선수의 장기 누적 개인 기록을 숨긴다"는 결론은 더 이상 쓰지 않는다.
> 무소속리그는 별도 리그이고 개인 기록·개인 랭킹이 정상으로 존재한다. 감추는 것은
> **누적 kill/death/킬뎃 세 가지뿐**이다 — **D-107**을 본다.
>
> 아래 본문에서 아직 유효한 부분은 하나다: **무소속을 래더 계산에서 빼지 않는다.**
> 그 이유(라인업 전력 계산에 실제 실력값이 필요하다)는 D-107에서도 그대로 유지된다.

먼저 잘못 구현했던 것을 적는다. 처음에는 `Clan.category === 'independent'`인 팀의
참가자를 **래더 계산에서 뺐다**. 그건 틀렸다.

왜 틀렸나
  클랜 경기의 전력차를 계산하려면 **실제 참가 선수의 실력값**이 필요하다.
  무소속 선수를 계산에서 빼거나 1500 고정으로 두면,
  `무소속 A(라인업 평균 1700) vs 무소속 B(1450)` 같은 경기의 예상 승률이 통째로 틀어진다.
  그러면 무소속 클랜의 래더도 같이 틀린다.

확정된 정책

| 단계 | 무소속을 어떻게 다루나 |
|---|---|
| 경기 저장·공식 판정 | 1부/2부와 **완전히 동일** |
| 클랜 래더 (Team Elo) | 동일. 무소속 전용 K값·전용 공식 없음 |
| 개인 래더·개인 누적 | **계산하고 저장한다**. 생략하지 않는다 |
| 사용자 화면 | 그 선수의 **장기 누적 개인 기록**만 숨긴다 |

즉 "수집 안 함 / 계산 안 함 / 저장 안 함"이 아니라 **"사용자에게 숨김"** 하나다.

숨기는 곳은 조회 계층 한 곳이다 — `apps/web/lib/server/queries/visibility.ts`.
개인 랭킹 목록과 **순위 계산 모집단**에서 같이 빼야 한다.
한쪽만 빼면 "N명 중 M위"의 명수와 등수 기준이 어긋난다.

숨기지 않는 것: 경기 상세(참가자·K/D/A·맵·시간·승패), 클랜 래더, 클랜 승패, 상대별 전적.
개별 경기 결과는 숨기지 않는다. 숨기는 것은 장기 누적 커리어뿐이다.

### D-103. 공식 판정과 클랜 가중치는 **순서가 있는 별개 단계**다

섞으면 둘 다 틀린다.

```
1단계  공식인가?        homeMembers >= 3  OR  awayMembers >= 3     ← 경기 하나에 한 번
2단계  얼마나 반영하나?  팀마다 자기 본클랜원 수로 100/70/40/0%     ← 팀마다 따로
```

`클1+용4 vs 클5`는 **공식**이다(오른쪽이 3명 이상). 그리고 클랜 반영은 40% / 100%다.
1단계에서 떨어뜨리면 안 되고, 2단계의 40%를 인정 조건으로 쓰면 안 된다.

가중치는 승패 **양방향 모두** 같게 적용한다. 이길 때만 깎지 않는다.
양쪽 가중치가 다르므로 한 경기의 증감 합이 0이 아닐 수 있다 — **의도된 것이다**.
zero-sum으로 맞추는 normalization을 넣지 않는다.

개인 래더에는 가중치가 없다. 용병도 100%다.

### D-104. 클랜 순위는 **네 가지**이고 서로 섞지 않는다

| 순위 | 모집단 | 정렬 | 쓰임 |
|---|---|---|---|
| 1부 standings | 1부만 | rating | **승강 판단** |
| 2부 standings | 2부만 | rating | **승강 판단** |
| 무소속 Tier 내 순위 | 같은 Tier만 | rating | Tier 화면 |
| 무소속 전체 래더 | 무소속 전부 | rating | Tier **무시** |
| 전체 통합 래더 | 1부+2부+무소속 | rating | 부리그·Tier **무시** |

두 가지가 특히 중요하다.

**① Tier는 rating으로 자동으로 움직이지 않는다.**
Tier 1의 W가 1500이고 Tier 2의 A가 1800이어도 W는 Tier 1, A는 Tier 2다.
`Clan.tier`는 운영자가 정한 값이고 순위 계산은 그 값을 **읽기만** 한다.
점수 구간 → Tier 매핑은 코드 어디에도 없다. 무소속 Tier 1의 1부 진입은
운영자 승인 방식이며 자동 승격 로직을 만들지 않는다.

**② 전체 래더는 Tier도 부리그도 보지 않는다.**
Tier 5 클랜이 전체 1위일 수 있고, 2부 클랜이 1부 클랜보다 위일 수 있다.
정렬 기준은 `rating` 하나뿐이고, 동점은 id로 고정한다.

한 무소속 클랜은 **두 순위를 동시에** 가진다 — `tier_rank`와 `overall_rank`.
둘 다 파생값이라 저장하지 않고 질의로 계산한다.

무소속 클랜의 승·패·승률·rating·최근 경기·상대 전적은 **전부 공개한다.**
숨기는 것은 무소속 선수의 **장기 누적 개인 기록**뿐이다 (D-102).

### D-105. 베타 안내는 **배지 + 리그홈 안내 한 번**이다

베타는 공개 시즌이다(D-098). 그래서 두 가지를 동시에 지켜야 한다 —
**숨기지 않는다**, 그리고 **과도한 경고 UI를 만들지 않는다**.

| 어디 | 무엇 |
|---|---|
| 리그 서브내비 (모든 리그 화면) | `Beta Season` 배지. tooltip에 승계 안내 한 문장 |
| 리그홈 헤더 아래 (한 번) | 제목 + 두 문장 안내 |

```
Beta Season
현재 SACLOUD 래더 시스템을 검증하는 공개 테스트 시즌입니다.
베타 시즌의 랭킹과 점수는 정식 Season 8에 승계되지 않습니다.
```

- 랭킹·기록실·프로필 화면에 같은 안내를 반복하지 않는다. 배지가 이미 상시로 알린다
- 경고색(빨강)·모달·상시 배너를 쓰지 않는다. 베타는 사고가 아니라 공개 운영 상태다
- 문구는 `packages/ui/src/league/betaNoticeText.ts` **한 곳**에서만 정한다.
  화면마다 다시 쓰면 문장이 갈라진다
- 시즌 종류를 **모를 때는 아무것도 띄우지 않는다.** 로딩 중에 경고가 번쩍이면 안 된다

### D-106. 과거 기록에 **없는 값은 `null`이다.** 0으로 채우지 않는다

Phase 11 감사에서 찾은 실제 버그다. 파서는 규칙을 지켰는데 **importer가 어겼다.**

```
parser        win: null        ← 원본에 없으니 null (D-099 준수)
importer      win: row.win ?? 0   ← 여기서 0이 됐다
화면          0승 0패 · 승률 56.9%   ← 거짓 기록
```

3rd.supply 시즌 카드는 **시즌마다 제공 필드가 다르다.** 시즌 6은 승패·킬데스를 주지만
시즌 4는 순위·승률·킬뎃만 준다. 그 시즌에 0을 넣으면 "0번 이겼다"가 되는데, 사실은 "모른다"다.
**0과 모름은 다른 값이고, 한 번 섞으면 되돌릴 수 없다.**

확정 사항

| 곳 | 규칙 |
|---|---|
| `LeaguePlayerSeason.rating/win/lose/kill/death` | **nullable**. 마이그레이션 `20260822235000_legacy_season_nullable_stats` |
| importer | `legacySeasonCardData()` 한 곳에서 매핑. `?? 0` 금지 |
| 조회 계층 | `winRateOrNull` / `kdRateOrNull` — 입력이 모르면 결과도 `null` (0%로 만들지 않는다) |
| 화면 | `알수없음` (프로젝트 표기 규칙과 동일) |

SACLOUD가 계산한 카드(`closeSeason`)는 값이 **항상 있다.** nullable은 과거 기록을 위한 것이지
우리 계산 결과를 비워도 된다는 뜻이 아니다.

같이 고친 것: 지난시즌 표가 `season.season`을 그대로 찍어 **베타 카드가 `시즌 0`** 으로 보였다.
서버가 주는 `season_label`을 쓰도록 바꿨고, 클랜 시즌 응답에도 `season_label`/`season_type`을 넣었다 (D-098).

### D-107. 무소속리그는 **리그다.** 개인 기록도 랭킹도 존재한다 (2026-08-23 확정)

**이전 결정을 폐기한다.** D-102의 "무소속 개인 커리어를 숨긴다"도,
그보다 앞선 "무소속 개인 커리어를 만들지 않는다"도 둘 다 아니다.

#### 확정 정책

무소속리그는 공식리그와 **완전히 별개인 리그**이고, 그 안에 개인 기록이 정상으로 존재한다.

| | 무소속리그 | 공식리그 |
|---|---|---|
| 개인 rating · ranking | **있다** | 있다 |
| 승 · 패 · 승률 · 평균킬 · MVP | **있다** | 있다 |
| 시즌 카드 · 최근 경기 · 경기 상세 | **있다** | 있다 |
| 경기 한 판의 K/D/A | **보인다** | 보인다 |
| **누적** kill · death · 킬뎃 | **화면에 안 보인다** | 보인다 |

숨기는 것은 그 하나뿐이다. `23 / 7 / 3` 같은 그 경기 성적은 숨기지 않고,
`시즌 누적 13,123킬 / 12,837데스 / 50.6%` 만 내보내지 않는다.

**저장은 그대로 한다.** DB에는 누적 킬·데스가 계속 쌓이고 계산에도 쓴다.
정하는 것은 "응답에 넣는가"뿐이다.

#### 기록은 리그 단위로 완전히 분리된다

같은 선수라도 리그가 다르면 **다른 그릇**이다.

```
길수 + 무소속리그   100전 89승 11패  rating 1742
길수 + 공식리그     238전 149승 89패 rating 1625
```

한쪽 경기 결과가 다른 쪽 rating·승패·랭킹·최근 경기·시즌 카드에 닿는 경로가 없어야 한다.
**기록이 쌓이는 곳을 정하는 것은 선수의 현재 소속이 아니라 "어느 리그 경기였는가"다.**
무소속 클랜 선수가 공식리그에 용병으로 뛰면 그 경기는 공식리그 기록이고,
공식리그 카드에는 누적 킬뎃이 정상으로 나온다.

#### 구현 — 스키마를 다시 짜지 않았다

개인 기록은 이미 리그 단위였다.

```
LeaguePlayer        @@unique([leagueId, playerId])   rating·승패·킬·데스·MVP
LeaguePlayerSeason  @@unique([leaguePlayerId, seasonId])
Match.leagueId
```

그래서 무소속리그는 **별도 League 행**으로 둔다. 추가한 것은 컬럼 하나뿐이다.

- `League.category` = `official` | `independent` (기본 `official`)
- 마이그레이션 `20260823001500_league_category` — 추가만, 파괴적 변경 없음
- 공개 범위 판단은 `apps/web/lib/server/queries/visibility.ts` 한 곳
- 응답 계약에 `LeagueSummary.hides_cumulative_kd` 를 실어 화면이 이유를 알 수 있게 했다
- 감출 때는 `null`이다. **0으로 만들지 않는다** — 0킬은 사실이 아니다
- 화면은 `null`이면 그 항목·칸을 **아예 그리지 않는다** (`알수없음`으로도 쓰지 않는다.
  그건 "원본이 값을 안 줬다"는 뜻이라 D-106과 섞인다)

#### 폐기된 동작

`PUBLIC_CAREER_WHERE`로 개인 랭킹 목록과 순위 모집단에서 무소속 선수를 빼던 것,
`isCareerPublic`으로 순위를 `null`로 만들던 것 — 둘 다 제거했다.
무소속리그 선수는 자기 리그 안에서 정상으로 순위를 받는다.

회귀 테스트는 `apps/web/tests/independentLeague.test.ts` 24건이다
(실제 DB에 임시 리그 두 개를 만들어 조회 함수를 그대로 돌린다).


### D-108. 클랜은 **등록된 순간부터** 기록이 시작된다. 소급하지 않는다

무소속 클랜은 운영자가 등록하기 전까지 SACLOUD에 존재하지 않는다.
등록 전에 치른 경기를 나중에 끌어와 공식 기록으로 만들면, 그 클랜만 과거가 생긴다.

```
8/25 18:00 A클랜 무소속 등록
  8/25 17:59 경기 → A클랜 기록 아님
  8/25 18:01 경기 → A클랜 기록 대상
```

**새 컬럼을 만들지 않았다.** `LeagueClan.joinedAt`이 이미 "이 클랜이 이 리그에 참여한 시각"이고,
등록이 곧 리그 참여다. 재구성이 그 값을 읽어 경기 시각과 비교한다
(`ReconstructionLeague.clanJoinedAt`, 미충족 시 `before_registration`).

1부·2부에도 같은 규칙이 적용된다. Beta로 승계된 클랜은 `joinedAt`이 Season 7 때 값이라
Beta 구간(8/20~) 경기가 정상으로 통과한다.

### D-109. 신원 연결은 **닉네임 + 실제 경기 근거**가 함께 있을 때만 한다

D-036("닉네임으로 자동 병합하지 않는다")은 그대로다. 다만 "사람이 하나씩 손으로"만
가능하면 5,000명 규모에서 아무것도 못 한다. 그래서 **근거를 요구하는 자동화**를 만들었다.

연결 조건 세 가지를 **전부** 만족해야 한다.

1. 운영자가 등록한 로스터의 선수 이름과 **정확히** 같다 (유사도 매칭 없음)
2. 그 계정의 **실제 경기 이력에 찍힌 `guild_name`** 이 로스터의 클랜과 같다
3. 일치가 `minEvidence`건(기본 3) 이상이고, 다른 클랜으로 찍힌 비율이 20% 이하다

즉 "이름이 같다"가 아니라 **"이 계정이 실제로 그 클랜으로 뛰었다"** 를 본다.
D-051에서 틀렸던 사례(닉네임으로 찾은 계정의 목록에 그 경기가 없었다)는 2번에서 걸린다.

연결 근거는 `linkReason`에 문장으로 남는다. 사람이 보고 끊을 수 있어야 하기 때문이다.
`pnpm --filter @sacloud/worker nexon identity-link --league <slug>` 는 기본이 미리보기이고,
`--confirm` 을 붙여야 실제로 연결한다.

### D-112. 공개 Beta는 **1경기부터** 래더를 계산한다 (Beta 전용 예외)

배치고사 10경기 정책(3-B 7번)은 **정식 시즌의 규칙이고 그대로 남는다.**
바꾸는 것은 공개 Beta 한 시즌뿐이다.

**왜:** Beta의 목적이 "래더가 실제로 어떻게 움직이는지 보는 것"인데,
10경기를 채우기 전에는 전원 1500이고 랭킹도 비어 있어서 아무것도 검증되지 않는다.

| | Beta Season | Season 8+ |
|---|---|---|
| 래더 계산 시작 | **1경기부터** | 배치고사 10경기 이후 |
| 개인 랭킹 노출 | **1경기부터** | 배치고사 종료 후 |
| 공식 경기만 반영 | 그대로 | 그대로 |

**하드코딩하지 않았다.** `constantsForSeason(base, season, flags)` 한 곳에서 정한다.

```
seasonType === 'beta'  &&  flags.betaImmediateRating
  → { ...base, placementMatches: 0 }
그 외 → base 그대로
```

- `official` · `legacy` · 시즌 미상 → **손대지 않는다**
- 배치고사 말고 다른 상수는 바꾸지 않는다
- 스위치를 끄면(`betaImmediateRating: false`) Beta도 기존 정책으로 돌아간다
- Beta가 끝나도 정식 시즌 정책이 따라 바뀌지 않는다 — 조건이 `seasonType`이기 때문이다

회귀 `packages/rating/src/__tests__/seasonPolicy.test.ts` 9건.
그 중 절반이 **"정식 시즌이 안 깨졌는가"** 를 본다. 예외를 넣을 때 가장 위험한 것이
예외가 본 규칙까지 바꿔 버리는 것이라서다.

### D-113. 래더 재계산은 **시즌 누적 전적도 같이** 쓴다

`nexon:rate`가 rating만 되돌려 쓰고 승패·킬·데스·어시스트를 안 쌓고 있었다.
그래서 경기가 6건 들어왔는데 화면에는 `0승 0패`로 보였다.

replay 도중 확정된 참가자에서 승패와 K/D/A를 함께 누적해
`LeaguePlayer` / `LeagueClan`에 같이 기록한다. 래더와 전적이 **같은 replay**에서 나오므로
서로 어긋날 수 없다.

### D-114. 라플/스나는 **병영수첩 BattleLog의 킬 신호**로 판정한다 (D-097 갱신)

D-097의 결론("넥슨 Open API에 무기가 없어 역할 래더 불가")은 **Open API에 대해서는 그대로**다.
바뀐 것은 **보조 출처가 생겼다**는 것이다.

```
POST /api/BattleLog/GetBattleLogClan/{match_key}/{clan_no}     ← 무기별 킬
POST /api/Match/GetMatchClanDetail/{match_key}/C/{clan_slug}   ← AR/SR 적중 (조건부)
```

#### 실측한 응답 구조 (2026-08-23)

**BattleLog는 집계표가 아니라 이벤트 로그다.** 한 줄에 한 사건이고,
같은 사건이 **가해자·피해자 두 관점으로** 들어온다.

```
{ event_type: 'kill',  user_nexon_sn, user_nick, weapon: 'riple' }
{ event_type: 'death', user_nexon_sn, target_user_nexon_sn, target_weapon: 'sniper' }
```

그래서 `event_type === 'kill'` 행만 센다. 두 관점을 다 세면 **킬이 두 배가 된다.**
실제 확인한 한 경기(90 이벤트 · kill 49 · death 38)에서 선수별로 깨끗하게 갈렸다.

```
째근호 riple 10 · 채운2 sniper 4 · SC3..나실인 riple 13 · 제니 sniper 6 · 천사순수 riple 6
```

#### 판정 규칙

| 신호 | 근거 |
|---|---|
| 킬 신호 | `riple` vs `sniper` 킬 수. **`throw`·`assist`·`close`·`special`·`c4-install` 제외** |
| 적중 신호 | AR vs SR 적중 수 |

- 두 신호가 같으면 그 답
- **어긋나면 `unknown`** — 한쪽을 이기게 하지 않는다
- 하나만 있으면 그것으로 판정, 둘 다 없으면 `unknown`
- **동률이면 `unknown`.** 0킬도 `unknown` (0킬은 무기 근거가 아니다)
- 선수 고정 포지션 금지 — 판정 단위는 **경기 × 선수**
- 헤드샷·딜량으로 추정하지 않는다 (D-097의 금지 규칙 유지)

#### 재현하지 못한 것

전달받은 조사 결과 중 **AR/SR 교차검증(24/24)은 이 세션에서 재현하지 못했다.**
`GetMatchClanDetail` 응답의 `user_battle_info`에 `M_PLAYER_hit_AR_*` 키가 **없었다**
(키 9개 · 중첩 `MatchData`는 빈 배열). 경기·모드에 따라 제공 여부가 다른 것으로 보인다.

그래서 지금 동작하는 것은 **킬 신호 단독 판정**이고, 적중 신호는 값이 올 때만 교차검증에 쓴다.
3rd.supply 30/30 비교도 재현하지 않았다 — 그쪽은 WAF로 막혀 있다.

**구현 상태**: 판정기와 파서는 완성됐고 회귀 18건으로 고정했다
(`packages/nexon/src/__tests__/weapon.test.ts`).
DB 저장(`weapon` 컬럼 채우기)과 라플/스나 버킷 집계는 **아직 붙이지 않았다.**

### D-115. 라플/스나 누적은 **판정된 공식 경기만** 쌓는다

`LeaguePlayerWeaponStat`(이미 있던 모델)을 그대로 쓴다. 새 모델을 만들지 않았다.

| | 규칙 |
|---|---|
| 대상 | **공식 경기만**. 비공식은 통합 기록에도 안 들어간다 (D-080) |
| `unknown` | 어느 버킷에도 넣지 않는다. 통합 기록에는 이미 들어가 있다 |
| deaths | 그 경기 death **전부**를 그 역할 버킷에 넣는다. "무엇에 죽었나"는 역할별 KD와 무관하다 |
| 재작성 | 처음부터 다시 만든다. 두 번 돌려도 값이 두 배가 되지 않는다 |
| 래더 | 무기별 래더는 **만들지 않는다.** 통합 래더가 진실이다 (`CLAUDE.md` 3-B 1·2번) |

화면은 판정된 경기가 하나도 없으면 **패널 자체를 그리지 않는다.**
`0경기 0킬 0데스`는 정보가 아니라 소음이다.

수집은 정상 브라우저가 하고 워커는 그 결과 파일을 읽는다 — Node에서 병영수첩을 부르면
403이고, UA를 위조해 뚫지 않는다.

```bash
pnpm nexon:weapon-import  --file <수집.json> [--confirm]   # 원문 + 근거 (멱등)
pnpm nexon:weapon-apply   [--confirm]                      # 근거 → MatchPlayerStat.weapon
pnpm nexon:weapon-rebuild --league <slug> [--confirm]      # 공식 경기 → 라플/스나 누적
```

판정기를 고쳐도 **외부에 다시 요청하지 않는다.** 저장된 근거에서 다시 계산한다.

### D-116. 공개 화면에는 **시드가 나가지 않는다** — `origin` 한 컬럼으로 거른다

Phase 11 감사에서 확인한 것: **공개 경로 전체가 개발용 시드를 필터 없이 노출하고 있었다.**
조회 계층 12개 파일 어디에도 걸러내는 코드가 한 줄도 없었다.

실측된 노출 범위 (전부 재현 확인)

| 화면 | 무엇이 보였나 |
|---|---|
| 전역 GNB | 시드 리그 3개가 **하드코딩**. 실제 운영 리그로 가는 링크는 사이트에 0개였다 |
| 리그 목록 · 리그 상세 | 시드 리그 4개 |
| 1부/2부/통합/개인 랭킹 | 전부 시드. **베타 배지도 안 붙었다**(시드 시즌이 `official`이라) |
| 통합검색 | 시드 선수 920 · 클랜 60 |
| 게시판 · 홈 인기글 | 시드 글 400건. **공지 게시판까지** |
| 기록실 · 경기 상세 | 시드 경기 3,000건 |
| `/api/infos` | 존재하지 않는 리그의 게시판 탭 3개 |

즉 방문자는 **가짜 리그 3개와 가짜 커뮤니티**를 먼저 보고, 진짜 Beta 리그는 URL을 직접 쳐야 도달했다.

#### 판별자

`origin` 컬럼 하나다. `Match.origin`이 이미 하던 일을 **League · Clan · Player · Board**로 넓혔다.
시드는 `mock`, 그 외는 `sacloud` / `nexon` / `3rd.supply`.

경기 수를 세거나 slug를 하드코딩해 추측하지 않는다. 백필 판별자는 **id가 순수 숫자인가**였다 —
시드는 원본 사이트의 숫자 ID를 흉내낸 결정적 픽스처이기 때문이다. 실측 대조로 확인했다:
League 4/5 · Clan 60/68 · Player 920/943 · Board 400/401 이 숫자 ID였고 **시드 건수와 정확히 일치**했다.

#### 거르는 곳은 한 곳이다

`apps/web/lib/server/queries/publicScope.ts`. 목록은 `publicOriginWhere()`,
단건은 `isPublicRow()`로 판정해 **없는 것처럼 404**를 낸다 — "있지만 숨김"이 아니다.
시드가 존재한다는 사실 자체를 흘리지 않는다.

리그 스코프 경로는 `resolveLeagueId()` / `getLeagueIdBySlug()` **한 곳만 막으면**
랭킹·기록실·매치 상세가 함께 닫힌다.

#### 스위치가 있는 이유

`pnpm compare`(Mock ↔ 실제 API 값 대조)는 **실제 API가 시드를 그대로 돌려주는 것**이 전제다.
그 도구를 죽이지 않으려고 `SACLOUD_PUBLIC_SCOPE=all`로 열 수 있게 뒀다.
**기본값은 감춘다.** 설정을 빠뜨렸을 때 안전한 쪽으로 실패해야 한다.

#### 함께 뒤집은 것 — `API_MODE` 기본값

`resolveApiMode()`의 기본값이 `mock`이었다(fail-open). 배포에서 `NEXT_PUBLIC_API_MODE`를
빠뜨리면 **공개 사이트 전체가 MSW 픽스처로 동작**하고 개발용 역할 전환 위젯까지 떴다.
`NEXT_PUBLIC_*`은 빌드 시점에 인라인되므로 되돌리려면 재빌드가 필요하다.
기본값을 `live`로 뒤집고 `mock`을 명시적 opt-in으로 바꿨다.

#### 지우지 않는다

DB의 시드 행을 삭제하지 않는다. 저장·계산은 그대로 두고 **응답에 넣는가**만 정한다.
(누적 킬뎃 비공개는 다른 축이다 — `visibility.ts` D-107)

### D-117. 리그 선수는 **클랜에 연결돼 있어야** 조회 계층이 성립한다

Phase 11 감사에서 찾은 실제 버그다. 공개 Beta의 **선수 축이 통째로 죽어 있었다.**

`LeaguePlayer.clanId`가 실데이터 리그 11명 **전원 null**이었다.
`rate.ts`가 `LeaguePlayer`를 만들 때 rating·승패·K/D/A만 넣고 `clanId`를 빼먹었기 때문이다.

조회 계층이 "리그 선수는 클랜에 속한다"를 전제로 `clanId → LeagueClan`을 찾으므로 결과는 이랬다.

| 화면 | 증상 |
|---|---|
| 개인 상세 | **11/11 HTTP 404** |
| 개인 기록실 | 빈 배열 (실제로는 7~10경기) |
| 클랜 상세 소속 선수 | **0명** (5승 0패인 클랜에 인원 0) |
| 개인 랭킹 클랜 칸 | 전원 공란 |
| 무기별 기록 패널 | **한 번도 렌더되지 않음** |

정답은 이미 DB에 있었다 — `LeagueRosterMembership` 23행이 전원의 소속을 들고 있었다.
**데이터 문제가 아니라 한 컬럼 전달 누락**이었다.

고친 것

1. `rate.ts` — 현재 유효한 로스터 등록(`leftAt = null`)에서 `LeaguePlayer.clanId`를 채운다.
   로스터에 없으면 **건드리지 않는다**(있던 소속을 null로 지우지 않기 위해서)
2. `rosterSync.ts` — 병영수첩 클랜 멤버 목록은 그 클랜의 공식 명단이므로
   `Player.clanId`(전역 표시용 현재 소속)도 함께 채운다
3. 화면 — 개인 기록실이 **로딩과 "없음"을 구분**하지 않아, 404가 나면 영원히 스켈레톤이 돌았다.
   `isPending`과 `!data`를 나눴다

경기 시점 소속은 `Match`/`MatchPlayerStat`의 division 스냅샷이 따로 들고 있다.
여기서 채우는 `clanId`는 **표시용 현재 소속**이며 래더 계산에 쓰이지 않는다.

### D-118. 평균킬의 분모는 **공식 경기만** 센다

`LeaguePlayer.kill`(분자)은 공식 경기만 누적하는데 분모(`matchCountByPlayer`)는
공식+비공식 전체를 셌다. D-080("비공식은 평균킬에 반영하지 않는다")을 정면으로 어긴다.

실측 오차 — 지금 Beta 랭킹 화면에 나가던 값이다.

| 선수 | 올바른 값 | 표시되던 값 |
|---|---|---|
| 씨야 | **16.0** | 11.43 |
| MMA수련중 | **11.33** | 6.8 |
| 울상진리 | **8.2** | 5.13 |

mock 픽스처는 전부 `official: true`라 `pnpm compare`로는 **절대 잡히지 않는** 종류의 버그였다.

### D-119. 공용 개발 비밀번호를 **폐기한다** — 새로 정하지 않고 무효화한다

시드가 만든 계정 **42개 전원이 같은 비밀번호 하나**를 썼고 그중 둘이 운영자(role 2)였다.
그 값은 저장소에 평문으로 있었고 시드 실행 때마다 콘솔에도 찍혔다. 로그인에는 시도 제한이 없다.
사이트가 잠깐이라도 외부에 열리면 **누구나 관리자 권한**(시즌 종료·시작 · 경기 공식 전환 ·
클랜 조작)에 도달할 수 있었고, 감사로그에는 정당한 운영자로 기록된다.

#### 새 비밀번호를 "정하지" 않는다

무작위 32바이트를 해시해서 덮고 **원문을 버린다.** 우리도 모르는 값이라 유출될 것이 없다.
"새 공용 비밀번호"를 만들면 같은 문제가 이름만 바꿔 돌아온다.

#### 판별은 소속이 아니라 **실제 값으로** 한다

`origin`이나 id 규칙으로 고르지 않는다. `bcrypt.compare`로 **그 비밀번호가 실제로 맞는
계정만** 무효화한다. 시드 밖에서 같은 값이 쓰였어도 함께 잡히고, 정상 계정은 절대 건드리지 않는다.
멱등하므로 두 번 돌려도 두 번째는 0건이다.

#### 재시드로 되살아나지 않게 한다

`seed.ts`의 기본값을 **무작위(로그인 불가)** 로 바꿨다. 로컬에서 로그인이 필요하면
`SACLOUD_SEED_PASSWORD`를 직접 넣어 실행한다. 콘솔에는 상태만 찍고 원문은 찍지 않는다.

#### 검수 계정은 운영 관리자와 분리한다

`pnpm nexon:accounts --provision-test --email <주소>` — **기본 최소 권한(role 0)** 이고,
비밀번호는 `SACLOUD_TEST_ACCOUNT_PASSWORD` **환경변수로만** 받는다.
인자로 받지 않는다(셸 히스토리에 남는다). 관리자 화면 검수가 필요할 때만 `--admin`을 붙인다.

테스트도 같은 규칙을 따른다 — `adminApi.test.ts`는 자격증명을 env로 받고,
없으면 **조용히 통과시키지 않고 skip**한다.

#### 상시 점검

`pnpm nexon:accounts --audit` 가 공용 비밀번호로 로그인 가능한 계정을 센다.
0이 아니면 **exit 1**이라 자동화에서 바로 걸린다.

> 남은 것: 로그인·가입에 rate limit이 없다. `PUT /api/me/link`는 소유권을 증명하지 않는다.
> 둘 다 외부 공개 전에 처리해야 한다.

### D-120. 인증 경로에 시도 제한을 건다 — **IP 헤더를 함부로 믿지 않는다**

로그인·가입·비밀번호찾기에 서버측 제한이 하나도 없었다. 게시판 글쓰기에만 있었다.
공용 비밀번호(D-119)와 겹쳐 있었을 때는 그 자체로 사고였다.

#### 정상 사용자를 막지 않는 설계

로그인은 **실패만** 센다. 비밀번호를 제대로 넣는 사람은 몇 번이든 로그인할 수 있고,
**성공하면 그 계정의 실패 기록을 즉시 지운다.** 누적 때문에 잠기는 일이 없다.

| 축 | 한도 | 막는 공격 |
|---|---|---|
| 계정(이메일)별 | 15분 5회 | 한 계정을 여러 IP에서 두드리기 |
| IP별 | 15분 20회 | 한 IP에서 계정 갈아 가며 훑기 |
| 가입 IP별 | 1시간 3회 | 대량 계정 생성 |

한도를 넘으면 **비밀번호를 검사하기 전에** 끊는다. 검사까지 가면 bcrypt 비용이
그대로 공격자의 무기가 된다. 응답은 `429` + `Retry-After`(초)다.

#### `X-Forwarded-For`를 믿지 않는 이유

그 헤더는 **클라이언트가 마음대로 넣을 수 있다.** 프록시가 덮어쓴다는 보장이 없으면
공격자가 요청마다 다른 값을 넣어 IP 제한을 통째로 무력화한다.
지금 구조(`next start` + 터널)에서는 소켓 주소가 항상 `127.0.0.1`이라 그것도 못 쓴다.

그래서 **명시적으로 설정할 때만** 헤더를 믿는다.

```
SACLOUD_CLIENT_IP_HEADER=cf-connecting-ip   # 프록시가 덮어쓰는 헤더
SACLOUD_TRUST_FORWARDED_FOR=true            # 차선책. 위 헤더가 없을 때만
```

아무것도 설정하지 않으면 **IP를 모르는 것으로 취급**하고, 훨씬 느슨한 전체 한도
(로그인 15분 300회)를 회로차단기로만 쓴다. 정상 사용자를 막지 않기 위해서다.
**제대로 된 IP 제한을 원하면 헤더를 설정해야 한다.**

#### 저장소

DB의 `RateLimit` 테이블을 쓴다. 메모리 맵이 아니라서 **인스턴스를 늘려도 그대로 동작한다.**
부하가 커지면 Redis 같은 전용 저장소가 낫지만, 공개 Beta 규모에서 행 하나 갱신은 병목이 아니다.

#### 흘리지 않는 것

키에 이메일·IP를 **원문으로 넣지 않는다**(해시만). 429 응답은 남은 시간만 알려 주고
"몇 번 틀렸는지"나 "그 이메일이 있는지"는 말하지 않는다. 로그인 실패 메시지도 예전처럼
아이디 없음과 비밀번호 틀림을 구분하지 않는다.

### D-121. 계정 연동은 **운영자가 승인한다** — 없는 증명을 흉내 내지 않는다

`PUT /api/me/link`가 닉네임만 받으면 그 선수를 곧바로 계정에 붙였다.
닉네임은 공개 정보이므로 **먼저 입력한 사람이 임자**였고, 랭킹 상위 선수의 신원을
아무나 선점할 수 있었다. 코드 주석이 스스로 "운영에 노출하면 안 된다"고 적어 둔 채였다.

#### 자동 검증이 불가능하다는 결론

넥슨 Open API 엔드포인트는 `id` · `user/basic` · `user/rank` · `user/tier` ·
`user/recent-info` · `match` · `match-detail` 이 전부이고, **모두 닉네임으로 조회하는
공개 정보**다. OAuth도 사용자 동의 흐름도 없다.

"게임 안에서 무언가를 바꾸고 우리가 읽어 확인한다"는 방식도 성립하지 않는다.
닉네임 변경은 유료·파괴적이고 그 닉네임이 곧 우리가 매칭하는 키다. 클랜 이동은 남에게 영향을 준다.
"지금부터 한 판 해라" 같은 도전은 **기다렸다가 남의 활동을 자기 것이라 우기는 것**을 막지 못한다.

그래서 **확인할 수 없는 것을 확인한 척하지 않기로 했다.** 그럴듯한 절차를 만들어
"인증됨"이라고 표시하면 없는 보증을 있는 것처럼 파는 셈이다.

#### 지금 구조

```
사용자  PUT /api/me/link          → PlayerLinkClaim(pending) 생성. 연결되지 않는다
운영자  GET  /api/admin/link-claims
        PATCH /api/admin/link-claims/{id}  {action: approve|reject}  ← 연결은 여기서만
```

안전장치

1. 이미 다른 계정에 연결된 선수는 **신청조차 받지 않는다**
2. 한 사람에게 열린 신청은 하나뿐이다
3. 닉네임은 **정확히 일치**할 때만 찾는다. 동명이인이면 임의로 고르지 않고 사람에게 넘긴다
4. 시드 선수는 연동 후보가 아니다 (D-116)
5. 승인은 트랜잭션 안에서 하고, 최종 보증은 `UserPlayerLink.playerId` **유니크 제약**이다 —
   동시에 두 신청이 승인돼도 DB가 한쪽만 통과시키고, 그 실패를 삼키지 않고 사유로 돌려준다
6. 승인·거부는 전부 `AdminAuditLog`에 남는다

> 나중에 넥슨이 사용자 인증 수단을 제공하면 그때 자동 검증을 붙인다.
> 그전까지 이 경로는 **사람이 판단하는 큐**다.

### D-122. SACLOUD 서플라이 리그는 **제3보급창고 5vs5만** 인정한다

원본 3rd.supply 리그홈 실측값 (2026-08-23, 정상 브라우저):

```
48개의 클랜 참여중
리그맵     제3보급창고
대전인원   5 vs 5   6 vs 6
```

즉 원본에는 6vs6이 **정식으로 존재한다.** 그럼에도 SACLOUD는 5vs5만 기록한다
(사용자 확정 정책. 이 결정이 이전 해석보다 우선한다).

#### 6vs6은 "비공식"이 아니라 **대상 외**다

이 구분이 핵심이다.

| 상태 | 뜻 | 기록실 | 래더 |
|---|---|---|---|
| `eligible` → official | 리그 경기 | 10명 전원 | 반영 |
| `eligible` → unofficial | 리그 경기지만 본클랜원 부족 | 10명 전원 | 미반영 |
| **`out_of_scope`** | **리그 대상이 아님** (다른 맵 · 6vs6) | **넣지 않음** | 없음 |
| `incomplete` | 대상이지만 참가자를 덜 복원함 | 보류 | 없음 |

6vs6을 `unofficial`로 두면 기록실에 6인전이 남고 "왜 점수가 없나"라는 질문이 생긴다.
아예 리그 밖이다.

#### 맵 판정은 **정확 일치**다

`contains('보급')` 같은 느슨한 매칭을 쓰지 않는다. 실제 스테이징에
`3보급-개인전`(일반전)이 있어서 그런 필터는 전혀 다른 모드를 통과시킨다.

**넥슨이 이 맵을 어떤 문자열로 주는지는 아직 관측되지 않았다.**
수집된 클랜전 15,879건 중 제3보급창고는 **0건**이다
(관측된 클랜전 맵: 올드타운 · 데저트2 · 드래곤로드 · 크로스포트 · 시티캣 · 프로방스).
그래서 별칭을 **추측해서 넣지 않았다.** 실제 표기가 관측되면 근거와 함께 추가한다.

> 지금 Beta에 들어 있는 10경기는 **전부 대상 외 맵**이다.
> 사용자 지시에 따라 아직 제거하지 않았다 — 실제 제3보급창고 5vs5를 확보한 뒤 전환한다.

### D-123. 참가자는 **버리지 않는다** — 신원을 모른다고 기록을 지우지 않는다

Phase 12 감사에서 찾은 가장 큰 데이터 결함이다.

`match-detail`은 참가자 10명 전원을 K/D/A까지 정상적으로 준다. 그런데
`resolveParticipant`가 **이미 아는 닉네임**만 `resolvedPlayerId`로 붙였고,
`projectionRule`이 미해석 참가자가 하나라도 있으면 **경기 전체를 버렸다.**

실측: `NexonMatchParticipant` 668건 중 resolved **7건(1.0%)**.
그래서 원본에 10명이 있는 경기가 화면에 4명으로 나왔다.

#### 참가자 **존재**와 신원 **확정**은 다른 문제다

실제로 뛴 사람이면 기록은 남긴다. 다만 그 사람이 **누구인지**는 근거가 있을 때만 말한다.

우선순위
1. 확정된 신원 (`NexonIdentity` → `playerId`)
2. 병영수첩 계정 번호 (`user_nexon_sn`) — 닉변에도 안 흔들리는 강한 키
3. **관측 전용 Player** (`OBS-<닉네임 해시>`)

3번은 "같은 사람"이라는 주장이 **아니다.** "같은 닉네임으로 관측됐다"는 주장이다.
기존 Player와 **합치지 않고**, `NexonIdentity`·`UserPlayerLink`도 만들지 않는다
(D-036 · D-100). 동명이인·닉변은 이 구조로 해결되지 않으며, 해결됐다고 표시하지도 않는다.
강한 근거가 생기면 그때 운영자가 병합한다.

#### 결과

```
before  668건 중 resolved 7건 (1.0%)   · 미해석 661건
after   668건 전원 신원 부여           · 미해석 0건
        resolved 140 · observed 528
```

대상 경기 `260821194726124001` 검증: 참가자 10명 전원 복원, K/D/A 보존,
아는 사람 3명은 `resolved`, 모르는 7명은 `observed`.

#### `nexon:reresolve`

`--reproject`는 `pending`/`skipped`만 보기 때문에 **이미 투영된 경기의 참가자는
영원히 재해석되지 않았다.** 그 구멍을 메우는 전용 명령을 추가했다.
투영 상태와 운영 `Match`를 건드리지 않아, 공개된 경기를 지우지 않고 신원만 채운다.

### D-124. 공식 참가 클랜은 **확인된 수만** 쓴다 — 48이라고 표시하지 않는다

원본 리그홈은 `48개의 클랜 참여중`이라고 표시한다.
공개 경로에서 실제로 확인한 것은 **44개**다.

| 경로 | 얻은 것 |
|---|---|
| `/league/supply/rank/clan/1` | 1부 상위 20 |
| `/league/supply/rank/clan/2` | 2부 상위 20 |
| `/league/supply/home/info` | 참여 클랜 20 (랭킹 밖 4개 추가 확보) |

합집합 **44개**. 나머지 4개는 커서 페이지네이션 뒤에 있고 그 API는 직접 접근이 **403**이다.
페이지에 더보기·무한스크롤도 없다. **차단을 우회하지 않았고, 4개를 만들어 채우지도 않았다.**

화면에는 `44개의 클랜 참여중`이라고 쓴다. 모르는 것을 아는 척하지 않는다.

#### 경기에서 본 클랜은 참가 클랜이 아니다

클랜 상세의 상대전적에서 클랜 22개를 더 찾았다. 넣지 않았다.
`guild_name`에 등장 · 상대전적에 존재 · 이름 유사 — 어느 것도 **공식 참가 근거가 아니다**.

#### 원본 rating을 가져오지 않는다

스냅샷에는 원본의 rating·승패가 들어 있지만 옮기지 않았다.
SACLOUD Beta는 전원 같은 출발점에서 시작한다(D-101). 원본 점수를 옮기면
베타가 원본의 연장이 되어 버린다. 가져오는 것은 **정체성**(id·이름·slug·부리그·마크)뿐이다.

#### slug 정규화와 링크 보존

dev 발판이 만든 `real-` 접두사를 실제 slug로 바꿨다
(`real-ulsan-cian` → `ulsanulsan`). 옛 slug는 `ClanAlias`로 남겨 기존 링크가 죽지 않는다.

#### 지울 수 없는 것은 **비활성**으로 뺀다

공식 목록 근거가 없는 dev 잔존 클랜 4개(전설 · Xenics-Storm · 해적 · 악마)는
경기가 걸려 있어 삭제하면 그 경기까지 cascade로 지워진다.
그래서 `Clan.active = false`로 두었다 — 스키마 주석이 말하는 "삭제 대신 쓰는 값"이다.

그런데 **공개 조회 계층이 `active`를 보지 않고 있었다.** 반영하지 않으면
확인된 44개에 잔존 4개가 섞여 화면에 48처럼 보인다. 목록·랭킹·집계에 조건을 넣었다.

### D-125. 제3보급창고 경기는 **넥슨 수집 경로에서 아직 한 건도 나오지 않았다**

로스터/관측 seed를 실제 서플라이 공식 클랜 선수로 확대한 뒤에도 결과는 같았다.

#### 확대한 것

| 단계 | 결과 |
|---|---|
| 3rd.supply 클랜 선수 목록 (8개 클랜) | 닉네임 160명 확보 |
| 파일럿 신원 해석 (20명) | **13명 ouid 확보** · 7명 실패(닉변·특수문자 추정) |
| 목록 수집 | 스테이징 15,879 → **24,772** (+8,893) |
| 상세 수집 (경계 안에서) | 78 → **86건** |

#### 그런데도 0건이다

```
클랜전 계열 상세 86건의 맵
  올드타운 19 · 데저트2 16 · 드래곤로드 14 · 크로스포트 13 · 시티캣 12 · 프로방스 11
  제3보급창고 0
```

그리고 더 중요한 사실 — **순수 `클랜전` 유형이 24,772건 중 0건이다.**
관측된 클랜 계열은 `퀵매치 클랜전`(7,304)과 `클랜 랭크전`(243)뿐이다.

#### 가장 그럴듯한 설명 (아직 확정 아님)

`퀵매치 클랜전`은 **6종 맵 로테이션의 픽업 매치**로 보인다 —
실제로 한 경기에 7개 클랜이 섞여 있는 것을 확인했다(D-123).
반면 서플라이 공식리그는 **약속 대전(`클랜전`)을 제3보급창고에서** 하는 것으로 보이며,
그 모드가 넥슨 Open API 매치 목록에 아직 한 번도 등장하지 않았다.

**확정하지 않는다.** 상세를 받은 것은 클랜 계열 10,086건 중 86건(0.85%)뿐이다.
가능성은 셋이고 아직 어느 것도 배제되지 않았다.
1. `클랜전` 모드가 Open API 매치 목록에 노출되지 않는다
2. 노출되지만 우리가 seed로 잡은 선수들이 그 기간에 하지 않았다
3. 맵 이름이 우리가 아는 표기와 다르다 (그래서 별칭을 추측해 넣지 않았다 — D-122)

#### 그래서 하지 않은 것

**기존 Beta 10경기를 지우지 않았고 replay도 하지 않았다.**
전환 조건("실제 제3보급창고 5vs5 최소 1건")이 충족되지 않았기 때문이다.
조건이 충족되지 않았는데 전환하면 공개 Beta가 빈 화면이 된다.

#### 병영수첩 로스터는 막혀 있다

서버측 호출은 **403**, 브라우저에서도 클랜 멤버 목록(`GetClanUserList`)은
`rtnCode -999`로 막힌다(로그인 게이트로 보인다). 인증·우회를 하지 않았다.
대신 **3rd.supply 클랜 선수 목록**이라는 정상 공개 경로로 우회 없이 닉네임을 얻었다.

### D-126. 공식리그 경기는 Open API에 **정상적으로 존재한다** — 놓친 원인은 seed 범위였다

D-125에서 "제3보급창고 0건"을 확인하고 표본을 늘리는 대신, **정답 샘플 1건**을 먼저 확보했다.
결과적으로 그게 옳았다 — 표본을 늘렸어도 원인을 못 찾았을 것이다.

#### 기준 경기 확보 (정상 공개 브라우저)

3rd.supply 클랜 상세(`/league/supply/clan/ulsanulsan`)의 최근매치에서 실제 경기를 찾았다.
화면에 `제3보급창고 · 5 vs 5 · 선레드 · 래더 -8점 · UlsaN_CIaN vs Iatency-`가 그대로 있고,
렌더된 HTML에 **Nexon 형식 18자리 match_id**가 들어 있다.

#### Open API 직접 대조 — 5건 전부 존재

```
260818140312124001  type="퀵매치 클랜전"  mode="폭파미션"  map="제3보급창고"  참가자 8명
260724214851125001  type="퀵매치 클랜전"  mode="폭파미션"  map="제3보급창고"  참가자 10명 ← 5v5 완전
260724213834125001  type="퀵매치 클랜전"  mode="폭파미션"  map="제3보급창고"  참가자 5명
260603164839124001  type="퀵매치 클랜전"  mode="폭파미션"  map="제3보급창고"  참가자 7명
260508214956124001  type="퀵매치 클랜전"  mode="폭파미션"  map="제3보급창고"  참가자 6명
```

확정된 것

| 항목 | 실측값 |
|---|---|
| `match_type` | **`퀵매치 클랜전`** — 우리가 **이미 수집하는 타입**이다 |
| `match_mode` | `폭파미션` — 이미 수집한다 |
| `match_map` | **`제3보급창고`** — D-122의 canonical과 **정확히 일치**. 별칭이 필요 없다 |
| 참가자 | 8 · 10 · 5 · 7 · 6 — **5건 중 1건만 10명** (D-044 재확인) |

#### 그래서 원인은 무엇이었나 — **Case 1: seed 범위**

필터가 아니었다. 타입·모드·맵 전부 우리가 이미 다루는 값이다.

증명: 그 경기 참가자 4명(`hutgaesoo` `원포인트레몬` `MacBookAir` `래띠`)의 신원을 붙이고
목록을 수집하자 **즉시 3건이 잡혔다**(나머지 2건은 그 계정들의 목록 창 밖이다).

우리가 못 본 이유는 단순하다 — **그 경기를 뛴 사람이 우리 seed에 없었다.**
기존 26개 신원은 공식리그 선수와 거의 겹치지 않았다.

#### 또 하나 — 목록에는 맵이 없다

`/match` 목록 응답에는 `match_map`이 **없다.** 맵은 `/match-detail`에만 있다.
그래서 스테이징 클랜전 10,086건 중 7,470건의 `matchMap`이 `null`이었고,
"맵 분포에 제3보급창고가 없다"는 관측은 **상세를 받은 86건만의 이야기**였다.
맵으로 거르려면 상세를 받아야 한다 — 이건 수집 설계에 그대로 반영해야 한다.

#### 다음 수집 계획 (근거가 생겼으므로 확정 가능)

1. seed를 **44개 공식클랜의 실제 선수**로 채운다 (3rd.supply 클랜 선수 목록 = 공개 SSR)
2. `퀵매치 클랜전` 목록을 모은다 (타입 변경 불필요)
3. 그 목록의 **상세를 받아야** 맵을 알 수 있다 — 맵 선필터는 불가능하다
4. `map === '제3보급창고' && 5vs5 && 참가자 10명`만 리그에 투영한다 (D-122)
5. 참가자 10명은 여러 관측을 합쳐 채운다 (D-044 · D-123)

### D-127. 공식리그 경기 발견은 **선수 목록 무차별 수집으로 안 된다**

파일럿에서 확정한 것이다. 파이프라인은 정상 동작하지만 **발견 경로가 틀렸다.**

#### 호출량은 1/4로 줄일 수 있다 (적용함)

기준 경기의 `match_type`이 `퀵매치 클랜전`, `match_mode`가 `폭파미션`으로 확정됐고,
Open API `/match`는 `match_type` 필터를 지원한다. 스테이징 실측도 같은 방향이다 —
`퀵매치 클랜전` 12,152건 중 12,134건이 `폭파미션`이다.

그래서 공식리그 discovery는 **선수당 1회**(`폭파미션` + `퀵매치 클랜전`)면 된다.
4모드 전수 조회는 필요 없다. 실행으로 확인했다 — 신원 36명에 `listCalls=36`.

#### 그런데 상세를 116건 받아도 제3보급창고는 0건이었다

```
올드타운 25 · 데저트2 20 · 드래곤로드 20 · 크로스포트 19 · 프로방스 16 · 시티캣 15
제3보급창고 0
```

이유는 분명하다. 선수의 매치 목록은 **최신순 수천 건**이고 그 대부분이
6종 로테이션 맵의 픽업 매치다. 제3보급창고 공식 경기는 그 안에 드물게 섞여 있다.
최신 N건만 상세를 받으면 **영원히 안 걸린다.**
10,086건을 전부 받으면 언젠가 걸리지만 그건 호출 낭비다.

#### 올바른 발견 경로 — 3rd.supply가 match_id를 준다

3rd.supply 클랜 상세의 최근매치에는 **공식 경기의 18자리 match_id가 그대로 있다**(D-126).
그 id로 `/match-detail`을 직접 부르면 정확히 그 경기가 온다.

```
3rd.supply 클랜 최근매치  →  공식 경기 match_id
                          →  Nexon /match-detail  (1건당 1회)
                          →  맵·참가자·K/D/A
```

실행으로 확인했다. 목록 무차별 수집 대신 **id 3건을 지정해 상세 3회**를 부르자
제3보급창고 3건이 곧바로 스테이징에 들어왔다.

즉 **discovery는 3rd.supply, enrichment는 Nexon**이다.
넥슨 Open API만으로 공식리그를 자동 발견하려던 설계는 비용이 맞지 않는다.

#### 판정 결과 — 파이프라인은 정상이다

```
260724214851125001  5v5  신원 10/10  → eligible      ← qualifying
260818140312124001  5v3  신원  8/8   → incomplete
260724213834125001  5    신원  5/5   → incomplete
```

`eligible` 1건이 실제로 나왔다. 맵 판정·인원 판정·신원 부여(D-123)가 전부 동작한다.

#### 그런데 셋 다 Beta 기간 밖이다

Beta 시작은 2026-08-19이고 확보된 경기는 8/18 · 7/24 · 7/24다.
**Beta 전환에 쓸 수 있는 경기가 아직 0건이다.**

#### 부수 확인 — 퀵매치 클랜전은 역시 픽업이다

`260724214851125001`의 10명은 8개 클랜에 흩어져 있다
(CeIebrity · aIIure · mikael · 멘토르 · resun`z · PokerFace. · UlsaN_CIaN · LulIaby).
그런데 3rd.supply는 이 경기를 특정 두 클랜의 대전으로 표시한다.
즉 **본클랜원 + 용병** 구조이며, 우리 D-079/D-081 모델과 일치한다.
`guild_name`은 그 선수의 **현재 클랜**이지 그 경기에서 뛴 팀이 아니다.

### D-128. 공식리그 경기 **발견은 3rd.supply, 사실값은 넥슨** — 파이프라인으로 확정

D-127의 판정을 명령으로 만들고 실제로 돌렸다. `pnpm nexon:supply-matches`.

#### 발견 — 3rd.supply 클랜 상세 SSR payload

정상 브라우저로 공식 클랜 44곳의 `/league/supply/clan/{slug}` 를 열고
`#supplyPc-state` 를 읽었다. Node fetch 는 여전히 **405**다 — 우회하지 않았고
헤더를 위조하지도 않았다.

```
클랜 페이지 44곳 × 최근매치 20건  →  고유 경기 750건
맵    제3보급창고 750 / 750       (다른 맵 0건)
인원  10명 739 · 12명 11
기간  2025-09-30 ~ 2026-08-24
```

선수 매치 목록을 무차별로 훑어 상세 116건을 받았을 때 제3보급창고는 **0건**이었다(D-127).
같은 대상을 클랜 페이지로 접근하면 **750건 전부**가 공식 경기다. 발견 경로의 문제였다는
판정이 실측으로 확인됐다.

스냅샷은 `packages/db/data/supply-official-matches.json` 에 그대로 보존한다.

#### 스냅샷의 값은 **필터 힌트로만** 쓴다

스냅샷에는 `map` `player_count` `red/blue` 라인업 `rating_update` 가 들어 있다.
그럼에도 스테이징에 적재되는 **사실값은 전부 넥슨 `/match-detail` 응답**이다.
원본 두 곳이 어긋나면 넥슨이 기준이다 — 우리가 재현하는 것은 넥슨 전적이기 때문이다.

#### `match_id` 앞 12자리가 시작 시각(KST)이다

`260818140312124001` → 2026-08-18 14:03:12 (KST). 750건 전부에서 스냅샷 `start_at`
표기와 일치했다. 회귀로 고정했다(`packages/db/ops/__tests__/supplyMatches.test.ts`).
덕분에 넥슨을 부르기 **전에** 기간으로 대상을 좁힐 수 있다.

#### 실수집 결과 — 스냅샷 750건 **전량**

```
상세 조회 747건 · 실패 0 · invalid 0     (3건은 파일럿에서 이미 받아 둔 것)
맵     제3보급창고 750 / 750             ← 하나도 빠지지 않았다
유형   퀵매치 클랜전 / 폭파미션
검증   valid
```

발견한 id 가 **100% 공식리그 경기**였다. 선수 목록 무차별 수집이 116건에 0건이던 것과
대비된다 — 발견 경로가 문제였다는 판정의 가장 강한 증거다.

Beta 구간(2026-08-19 ~ 08-24) 214건만 재구성했다. 나머지 536건은 **스테이징까지만** 두고
재구성하지 않았다 (과거 시즌 이관 대상인지 아직 정하지 않았다).

`nexon league-maps --confirm` 로 제3보급창고를 리그 맵에 등록한 뒤 재구성했다.

```
considered 214  →  projected 72  ·  incomplete 142
  single_clan        94
  unidentified_side  48
```

운영 `Match` **10 → 82**건. 다만 새로 들어온 72건은 **전부 `official = false`** 다.

#### 그래서 무엇이 남았나 — **여전히 로스터다**

공식 인정은 한쪽 팀 본클랜원 3명 이상이고(D-079), 본클랜원 판정은
`LeagueRosterMembership` 이다. 현재 23명뿐이라 어떤 팀도 3명을 못 채운다.
경기 데이터는 이제 충분하고, 부족한 것은 **명단**이다.

비공식 경기는 D-080대로 기록만 남고 래더·시즌 통계·랭킹에 **전혀 반영되지 않는다.**
`nexon:rate` 결과가 그대로 6경기인 것이 그 증거다 — 공개 래더는 이번 수집으로
바뀌지 않았다.

### D-129. 3rd.supply 스냅샷에 **양 팀 10명 라인업이 통째로 있다** — D-044 의 우회로 후보

발견 스냅샷을 뜯어보다 확인한 것이다. **아직 쓰지 않았고, 쓰려면 결정이 필요하다.**

넥슨은 한 경기 응답에 양 팀을 함께 주지 않는다(D-044). 이번 214건에서도
10명이 다 온 것은 **42건뿐**이다 (5명 89 · 6명 28 · 7명 27 · 8명 21 · 9명 23).

그런데 3rd.supply 클랜 상세의 같은 경기에는 `summary.red[5]` · `summary.blue[5]` 가
있고, 각 줄에 **선수 id · 닉네임 · 소속 클랜 id · weapon(0 라이플 / 1 스나이퍼)** 이 붙어 있다.
스냅샷 750건 중 **739건이 10명 전원**을 담고 있다.
경기당 `perspectives` 로 양 클랜 시점의 **원본 `rating_update`** 도 함께 있다.

즉 넥슨이 주지 않는다고 D-034에 적어 둔 값 중 **무기 · 플레이시간 · 종료시각 · 선공진영 ·
MVP** 가 이 payload 에는 있다.

#### 그런데 자동으로 쓰지 않는다

- 사실값의 출처를 넥슨으로 고정한 D-128과 **정면으로 충돌**한다
- 라인업을 3rd.supply 에서 가져오면 "우리가 재구성했다"가 아니라 "원본을 옮겼다"가 된다
- 원본 `rating_update` 를 쓸지 말지는 CLAUDE.md 3-A 규칙 2(과거 기록은 원본값 보존)와
  D-101(Beta 는 원본 점수를 승계하지 않는다)이 **서로 다른 답**을 가리킨다

그래서 **스냅샷에 보존만 하고 파이프라인에는 연결하지 않았다.**
쓸지 말지는 사람이 정한다. 선택지는 세 가지다.

| 안 | 내용 | 대가 |
|---|---|---|
| A | 지금처럼 넥슨만 사실값 | 214건 중 42건만 10명. 공식 판정 대부분 불가 |
| B | 라인업만 3rd.supply 보강, K/D/A 는 넥슨 | 출처가 섞인다. 어느 값이 어디서 왔는지 표시 필요 |
| C | 3rd.supply 를 과거 기록 이관 경로로 따로 쓴다 | Beta 와 과거를 분리. 구현량이 가장 크다 |

### D-130. 현재 소속의 출처 — **3rd.supply 라인업 clan** (클랜원 목록이 아니다)

로스터를 사람이 CSV로 넣는 구조를 없애기 위해 공개 출처를 하나씩 실측했다.
**세 번째만 쓸 수 있었고, 그 과정에서 전제가 뒤집혔다.**

| 후보 | 실측 | 판정 |
|---|---|---|
| 병영수첩 클랜 멤버 목록 | 서버 403 · 비로그인 브라우저에도 멤버 목록 없음 | **로그인 게이트.** 우회하지 않는다 |
| 3rd.supply 클랜원 목록 페이지 | 선수 1,235명 중 **181명이 두 곳 이상**에 동시 등장 | 이력이지 현재가 아니다 |
| 3rd.supply 라인업 `player.clan` | 750경기 11개월에 선수 1,091명 중 **0명**이 다른 클랜으로 나옴 | **현재 소속** ← 쓴다 |

클랜원 목록 페이지는 원본이 화면에 직접 적어 뒀다 —
"리그에서 최소 1경기 이상 참여한 플레이어만 표시되며". 참여 이력이다.

라인업 clan 이 현재 소속이라는 근거는 **넥슨과의 대비**다. 같은 기간 넥슨
`guild_name` 은 닉네임 1,341명 중 69명이 다른 클랜으로 나오고 그중 65명은
기간이 겹치지 않는다(이적의 모양). 11개월 동안 아무도 이적하지 않았을 리 없으므로
3rd.supply 쪽이 렌더 시점 값을 붙인 것이다.

#### 시각을 지어내지 않는다

이 출처는 "지금 누가 어느 클랜인가"만 준다. 가입 시각을 주지 않는다.

```
처음 보는 소속     joinedAt = LeagueClan.joinedAt   (그 클랜이 리그에 들어온 시각)
이적을 관측한 경우 joinedAt = observedAt            (그 이전이라는 것만 알 뿐이다)
```

어느 쪽이든 `observedAt` · `confidence` · `sourceRef` 를 함께 남긴다.
이적 시 **이전 소속을 지우지 않고** `leftAt` 으로 닫는다.

#### 탈퇴 감지의 안전장치

목록에서 사라진 사람은 탈퇴로 보되 **이 명령이 만든 소속(`source`)만** 닫는다.
운영자가 손으로 넣은 로스터는 건드리지 않는다.

#### 결과

```
현재 소속 도출  선수 621명 · 근거 갈림 0명
반영           소속 452건 · 클랜 43곳
로스터         23명 → 475명 (열린 소속) · 3명 이상인 클랜 40곳
재실행         선수 생성 0 · 소속 신규 0 · 유지 452  (idempotent)
```

**캡처는 브라우저가 필요하다.** 3rd.supply 는 서버 fetch 가 405/403 이다.
스냅샷은 저장소에 커밋돼 있고, 갱신 명령 자체는 그 스냅샷만 읽는다 —
운영자가 이름을 타이핑할 일은 없다.

### D-131. 현재 소속과 **경기 당시 소속**을 분리한다

같은 "소속"이라는 말이 화면마다 다른 것을 가리켜야 한다.

| 화면 | 무엇을 보여 주는가 |
|---|---|
| 기록실 목록 라인업 · 경기 상세 | **그 경기를 했을 당시** 소속 |
| 선수 프로필 · 개인 랭킹 · 클랜원 목록 | **현재** 소속 |

내부 판정(본클랜원·용병·official·클랜 승패·클랜 래더·가중치)은 **전부 경기 당시** 기준이다.
현재 소속으로 과거 경기를 다시 계산하지 않는다.

#### 저장 위치 — 값을 박아 둔다

현재 소속을 join 해서 과거 화면을 그리면 **선수가 이적하는 순간 과거 기록이 통째로 바뀐다.**
그래서 `MatchPlayerStat` 에 스냅샷을 고정한다.

```
matchTimeClanName / matchTimeLeagueClanId / matchTimeClanSlug
matchTimeClanMarkBgUrl / matchTimeClanMarkFrontUrl
matchTimeClanSource / matchTimeClanObservedAt / matchTimeClanConfidence
```

리그 밖 클랜일 수 있으므로 **이름은 항상** 남기고 우리 클랜 연결은 있을 때만 남긴다.
기존 `rosterLeagueClanId`(원소속 클랜)는 그대로 내부 판정용이고, 위 값들은 표시 + 근거다.

#### 근거 우선순위 — 사용자가 적어 준 순서를 실측으로 바꿨다

```
1. 넥슨 /match-detail 의 guild_name   ← 경기 기록 안에 들어 있다. 경기 당시 소속
2. 그 시각에 열려 있던 로스터 소속     ← 추론이라 confidence = medium
```

**3rd.supply 라인업 clan 은 여기 쓰지 않는다.** D-130에서 현재 소속으로 판명됐다.
(선수 목록 자체는 경기 당시 사실이라 참가자 복원 근거로는 유효하다 — D-129)

근거가 없으면 전부 `null` 이다. 현재 소속으로 메우지 않는다.

#### 결과

```
복원 626 / 639 (98%) · 리그 클랜 연결 495 · 근거 없어 null 13
현재와 실제로 다른 참가 기록 4건 (규때 · 산툐리)
```

실화면 확인 — 산툐리, 8/20 경기
```
경기 상세 · 라인업   VaIiant    (경기 당시)
선수 프로필          dravelior  (현재)
```

#### 원본과의 **의도된 차이**

3rd.supply 원본은 라인업에도 **현재 클랜**을 붙인다(D-130의 근거가 곧 그 증거다).
우리는 사용자 지시에 따라 **경기 당시 소속**을 보여 준다.
CLAUDE.md 3장 원본 재현 원칙에 대한 명시적 예외이며, 이 항목이 그 근거다.

#### 같이 드러난 버그

개인 기록실이 **현재** 소속 리그클랜으로 경기를 걸러내고 있었다.
이적한 선수의 과거 경기가 자기 기록실에서 통째로 빠졌다.
보는 기준을 "그 경기에서 뛴 팀"으로 바꿨다.

### D-132. 두 출처의 사람을 잇는 근거 — **같은 경기 · 닉네임 정확 일치**

넥슨은 참가자에게 계정 id 를 주지 않고 닉네임만 준다. 3rd.supply 는 `player.id` 를 준다.
그래서 경기에서 만들어진 `Player(origin=nexon)` 와 로스터에서 만들어진
`Player(origin=3rd.supply)` 가 **같은 사람인데 다른 행**이 됐다.
실측 — 경기 참가 229명 중 로스터가 붙은 사람 **17명**.

전역 닉네임 매칭은 하지 않는다. 두 독립 출처가 **같은 경기 하나**를 각각 기술한 것을 맞춘다.

```
같은 match_id 의 3rd.supply 라인업에 (선수 id P, 닉네임 N) 이 있고
같은 경기의 넥슨 참가자에 닉네임 N 이 있으면  →  그 사람은 P 다
```

안전장치
- 한 경기 안에서 닉네임이 겹치면 **그 경기를 통째로 건너뛴다** (실측 0건)
- 유사 매칭·부분 일치·대소문자 무시 없음. 정확히 같은 문자열만
- `sourcePlayerId` 는 unique 다. 로스터가 먼저 만든 **빈 행**(경기 기록 0 · 계정 연결 없음)만
  자리를 비켜 준다. 기록이 있는 행은 건드리지 않고 충돌로 남긴다

결과
```
대조 경기 72 · 참가 기록 591 · 연결 212 · 충돌 0 · 근거 없음 0
경기 참가자의 로스터 보유  17명 → 135명
official 경기              6건 → 17건
본클랜원 판정              전원 용병 → 팀당 3~5명
```

### D-133. D-129 확정 — 3rd.supply 라인업은 **팀 식별 보조 증거**로만 쓴다

D-129에 적어 둔 세 선택지 중 **B안의 축소판**으로 확정했다.
"라인업으로 참가자를 보강한다"가 아니라 **"라인업으로 팀 이름표를 붙인다"** 이다.

#### 허용 / 금지

| | |
|---|---|
| 허용 | 그 경기에 어떤 진영(red/blue)이 있었고 각 진영이 **어느 클랜**인가 |
| 금지 | 라인업 `clan` 을 **경기 당시 소속**으로 쓰기 — 그건 현재 소속이다 (D-130) |
| 금지 | 라인업만 보고 **참가자를 만들기** — k/d/a 가 없다. 지어내지 않는다 |
| 금지 | `rating_update` 를 SACLOUD 래더에 쓰기 |
| 금지 | `weapon` 을 최종 사실값으로 쓰기 (기존 무기 체계 유지) |

#### 진영과 승패를 어떻게 아는가

스냅샷 `perspectives[]` 는 한 클랜 시점의 기록이다.

```
blue_team === true  → 그 클랜은 blue 진영
win        === true → 그 진영이 이겼다
             → (승리 진영 클랜, 패배 진영 클랜)
```

#### 넥슨 우선 — 조용히 덮지 않는다

1. 넥슨 상세로 양 팀 클랜이 **서로 다르게** 정해지면 보조 증거를 **보지 않는다**
2. 쓰기 전에 **넥슨 참가자 승패와 라인업 진영이 일치하는지 검증**한다
3. 한 명이라도 어긋나면 그 경기는 보조 증거 **없이** 판정하고 충돌로 센다
4. 겹치는 참가자가 없어 검증 자체가 안 되면 역시 쓰지 않는다

실측 — Beta 구간 142경기에서 **충돌 0 · 검증 불가 0**.
사전 조사에서도 승패가 142/142 완전히 일치했다. 두 출처가 같은 경기를 같게 기술한다.

#### 보조 증거는 참가자를 늘리지 않는다

한쪽 팀만 있는 경기는 보조 증거가 있어도 **기록하지 않는다**(`single_clan` 유지).
팀 이름표를 붙일 수는 있어도 그 팀에 아무도 없으면 경기가 아니다.

#### 1차 결과

```
projected    72 → 126   (+54)
incomplete  142 →  88   (전부 single_clan)
운영 Match   82 → 136
official     17 → 17    (새로 들어온 54건은 본클랜원 3명 미만)
보조증거 사용 54 · 충돌 0 · 검증 불가 0
```

#### 남은 88건은 **참가자를 지어내지 않고** 푼다

넥슨이 한쪽 팀만 준 경기다(D-044). 상대 팀 선수의 k/d/a 가 없다.
라인업이 알려 준 것은 **누가 빠졌는지**이고, 그 사람들의 기록은 넥슨에 있다 —
자기 매치 목록에는 자기 기록이 들어 있기 때문이다(D-048).

```
라인업에서 빠진 선수 추출  →  신원 확보(nickname → ouid)
                          →  그 선수들의 매치 목록 수집
                          →  관측값(실제 k/d/a)  →  재구성
```

라인업은 **대상을 좁히는 데만** 쓰이고, 저장되는 값은 전부 넥슨 응답이다.

#### 2차 시도 — 남은 88건은 **세 가지가 막고 있다** (미해결)

라인업이 알려 준 "빠진 선수" 216명을 넥슨에서 채우려 했다. 결과는 이렇다.

```
신원 확보   204명 시도 → 161명 확보 · 43명 실패(bad_request)
매치 목록   99건 성공 → 그 뒤 전부 실패
            HTTP 400 OPENAPI00003  28건
            HTTP 429 OPENAPI00007  17건 (이후 계속)
관측값      16,358 → 41,242 (백필은 성공)
재구성      88건 그대로 — 하나도 안 줄었다
```

**막고 있는 것 세 가지. 전부 사람이 정할 항목이다.**

1. **넥슨 호출 한도** — 429가 처음으로 관측됐다(그동안 `[미확인]`이었다).
   `done` 이 99에서 멈추고 실패만 쌓이길래 **중단했다.** 우회하지 않는다.
   남은 수집은 더 느린 속도로 나눠 돌려야 한다
2. **`Player` 행이 없다** — 라인업에만 있는 선수는 우리 DB에 없다.
   `deriveCurrentMembership` 은 **클랜이 확정된 선수만** 만들기 때문이다(D-130).
   무소속·미등록 클랜 선수까지 `Player` 를 만들지는 **정하지 않았다**
3. **`identity-link` 가 로스터를 요구한다** — D-109 는 "로스터에 같은 이름"을 조건으로 둔다.
   위 선수들은 로스터가 없어 전원 `no_roster` 로 보류됐다(178명 검토 · 연결 0).
   조건을 푸는 것은 **D-109 정책 변경**이라 임의로 하지 않았다

즉 88건은 "라인업을 안 써서" 남은 것이 아니라 **넥슨 쪽 실제 기록이 아직 없어서** 남았다.
참가자를 지어내면 지금 당장 풀리지만, 그건 하지 않는다.

#### replay 일관성 (같이 고친 것)

`rate` 가 official 경기를 다시 판정할 때 팀을 **처음부터 다시 추론**하고 있었다.
그래서 재구성에서 인정한 경기가 replay 에서 `unidentified_side` 로 빠졌다(17건 중 1건).
`Match` 행에 이미 저장된 진영 클랜을 보조 증거로 넘겨 같은 판정을 재현하게 했다.
**공식 자체는 손대지 않았다.**

```
래더 반영 경기  16 / 17  →  17 / 17
선수            56 → 58
```

### D-134. 신원의 조건에서 **로스터를 뺀다** — D-109 완화 (사용자 결정 · 2026-08-24)

#### 무엇이 문제였나

D-109는 "로스터에 같은 이름이 있어야" 신원을 연결했다. 그 결과

- **무소속·용병·미등록 클랜 선수는 영원히 `unresolved`** 로 남았다
- 그 사람이 나중에 공식 클랜에 가입해도 **다른 사람**으로 잡혔다
- 실측 — 178명을 검토해서 **연결 0건**, 전원 `no_roster` 보류

#### 새 규칙

```
신원 생성 조건   강한 넥슨 식별자   (필수)
로스터의 역할    본클랜원 / 용병 판정에만   (D-079 · D-073)
```

**로스터는 더 이상 신원의 조건이 아니다.**

#### 강한 넥슨 식별자란 무엇인가

`ouid` 는 넥슨이 준 계정 식별자지만 **닉네임 → ouid 만으로는 부족하다.**
`/id` 로 얻은 계정의 매치 목록에 그 경기가 없던 사례가 있다(D-051).

그래서 **그 계정이 실제로 뛴 경기**(`NexonMatchObservation`)를 요구한다.
그 관측은 계정 **본인의 매치 목록**에서 나오므로 "이 계정이 이 경기를 뛰었다"는
넥슨 자신의 진술이다. 경기 기록이 없으면 연결하지 않는다.

#### 기존 선수와 잇는 근거 — 같은 경기 · 같은 닉네임 (D-132와 동일)

```
넥슨        계정 O 가 경기 M 을 뛰었다 (본인 매치 목록) · O 의 닉네임은 N
3rd.supply  선수 P(닉네임 N) 가 경기 M 을 뛰었다 (라인업)
⇒ O 와 P 는 같은 사람
```

전역 닉네임 매칭이 아니다. 두 독립 출처가 **같은 경기 하나**를 각각 기술한 것을 맞춘다.

#### 이을 곳이 없으면 **새 선수를 만든다** ← 완화의 핵심

무소속·용병도 정상적으로 `Player` 가 된다.
`ouid` 가 계정을 붙잡고 있으므로 그 사람이 나중에 공식 클랜에 가입해도
**같은 `Player` 행이 그대로 이어진다.**

#### 그래도 하지 않는 것 (D-036 유지)

- 닉네임이 비슷하다는 이유로 잇지 않는다. 근거가 없으면 **새로 만든다**
- 근거가 두 사람을 같은 무게로 가리키면 **고르지 않는다** (conflict)
- 이미 다른 계정이 붙은 선수를 **빼앗지 않는다**
- `--confirm` 없이는 쓰지 않고, 연결 사유를 `linkReason` 에 남긴다

#### 결과

```
NexonIdentity  active 26 → 49   (기존 선수 연결 10 · 새 선수 13) · 충돌 0
로스터 없는 active 신원  0 → **15명**
재실행         연결 0 · 생성 0 (idempotent)
```

#### 그런데 88건은 아직 안 풀렸다 — **넥슨 호출 한도**

나머지 155개 계정은 `no_activity` 로 보류됐다. 매치 목록을 못 받았기 때문이다.

```
속도 2/s   → HTTP 429
속도 0.5/s → **여전히 HTTP 429**
```

초당 속도 문제가 아니라 **키의 기간 할당량이 소진된 것**이다.
(이 세션에서 상세 747 + 신원 204 + 목록 ~150회를 썼다)
우회하지 않고 중단했다. 할당량이 회복되면 `--resume` 으로 이어진다.

즉 88건은 **정책이 아니라 호출 한도**가 막고 있다. 정책은 이제 풀려 있다.

### D-135. 무소속 선수 프로필이 404였다 — D-134 이후의 구멍

개인 랭킹에는 보이는데 프로필을 열면 404였다.
`getLeaguePlayerDetail` 이 소속 리그클랜을 못 찾으면 `null` 을 돌려주고 있었다.

D-134로 무소속·용병도 정상적인 선수가 되면서 그 가정이 깨졌다 —
실측 리그 선수 58명 중 **14명이 clanId 없음**, 전원 프로필 404.

계약은 이미 `clan: ClanSummary.nullable()` 이었다. **서버 쪽 가드만 과했다.**

같이 고친 것 — `buildMatchSummary` 가 고정된 현재 소속으로 진영을 판정하고 있었다.
무소속이면 요약이 통째로 비고, 이적한 선수는 과거 경기가 빠졌다.
**그 경기에서 본인이 뛴 진영**을 쓰도록 바꿨다 (D-131과 같은 원칙).

```
선수 상세 API  44/58 → 57/58
남은 1건은 E2E 자리표시자 (400) — 공개 전 정리 대상
```

### D-136. 보안 헤더를 넣는다

외부 공개 전까지 **하나도 없었다.**

```
Content-Security-Policy · X-Frame-Options: DENY · X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin · Permissions-Policy
Cross-Origin-Opener-Policy: same-origin · Strict-Transport-Security
poweredByHeader: false
```

CSP 는 **실제로 필요한 것만** 열었다.

- `img-src` 에 `https://static.3rd.supply` — 클랜마크가 거기서 온다.
  원본 자산을 복사해 오지 않고 링크한다 (CLAUDE.md 3장 4번)
- `connect-src 'self'` — `NEXT_PUBLIC_API_BASE_URL=/api` 라 같은 오리진이다
- `frame-ancestors 'none'` — 클릭재킹 차단

**남은 타협** — `script-src 'unsafe-inline'`.
Next 하이드레이션 부트스트랩 때문이다. nonce 로 바꾸려면 미들웨어가 필요해서 이번 범위 밖이다.
의도된 타협이고 숙제로 남긴다.

HSTS 에 `preload` 는 넣지 않았다 — 도메인이 확정되고 HTTPS 가 안정된 뒤 사람이 정한다.

### D-137. `/api/health` — 수집이 멈춘 것을 사람보다 먼저 안다

원본 서플라이는 넥슨 패치 뒤 며칠씩 전적 갱신이 밀리곤 했다. 그것을 막기 위한 것이다.

```
checks.db         쿼리가 도는가
checks.collector  마지막 성공 시각 · 24시간 신규 · 상세 실패율 · 429 발생
checks.data       공개 화면이 비어 보이지 않을 최소 조건
```

`degraded` 조건 — 마지막 성공 48시간 초과 · 최근 24시간 429 · 상세 실패율 50% 초과 ·
공개 클랜/선수 0. `down` 이면 HTTP 503, 그 외 200.

**인증 없이 열어 둔다.** 대신 민감한 값을 하나도 담지 않는다 — 숫자·시각·판정뿐이다.
회귀 6건이 그것을 강제한다(금지 문자열 전수 검사 포함).

### D-138. `match_time_clan` 이 기록실을 통째로 비우고 있었다

Phase 14에서 만든 버그다. **읽기 화면 전체가 조용히 비는 종류**라 따로 남긴다.

경기 당시 소속을 `ClanSummary` 로 실었는데, 우리 리그 **밖** 클랜은 id·slug 를 모른다.
그래서 빈 문자열로 채웠다 — 그런데 `Id`·`Slug` 는 `z.string().min(1)` 이다.

```
외부 클랜이 한 명이라도 낀 경기가 목록에 있으면
→ 계약 검증 실패 → 그 목록 **전체가 빈 배열**
→ 화면에는 "기록된 경기가 없습니다"
```

API 는 200에 15건을 돌려주는데 화면만 비어 있어서 눈에 안 띄었다.

전용 스키마를 만들어 고쳤다 — **아는 것만 싣는다.**

```ts
MatchTimeClan { league_clan_id: Id | null, slug: Slug | null, name, mark }
```

실화면 확인 (Haunted 클랜 기록실)

```
전:  "기록된 경기가 없습니다"
후:  경기 15건 · 비공식 배지 14 · 클랜마크 234개 전부 로드
```

**교훈** — 없는 식별자를 빈 문자열로 있는 척하지 않는다. `null` 로 둔다.
`apiContract.test.ts` 가 이것을 잡는 테스트였고 지금은 통과한다.

---

## D-142 — 레이팅 최종 확정 (2026-08-24)

**결정.** 후보 2안 위에서 남은 4가지를 확정하고, 자유대전 환경 검증에서 발견한 구멍 하나를 메웠다.
최종 사양은 `docs/RATING_FINAL_SPEC.md`, 판정 근거는 `docs/RATING_DESIGN_VERDICT.md`.

| 결정 | 값 |
|---|---|
| 표시 함수 | `3000 + (내부-3000) x 신뢰도 x 3.5` 선형 · percentile 앵커 없음 |
| 미참여 감점 | 표시 점수 구간별 · **내부 Elo 는 보존** · 경기당 -8 회복 |
| 구성 보정 상한 | **+50** · 최근 20경기 |
| 개인 퍼포먼스 | **0%** (KD/MVP 를 점수에서 완전히 제외) |
| 약팀 사냥 차단선 | 승리 && 기대승률 >= 0.90 이면 delta=0 ★신규 |

**왜 차단선을 새로 넣었나.** 순수 Elo 는 "3100 상대 95% 승" 과 "3500 상대 60% 승" 을 같게
본다. 매치메이킹이 있으면 맞는 말이지만 제3보급에는 없다. 검증 결과 약팀만 골라 잡아
**4,876** 까지 올라갈 수 있었다 — 강한 상대를 한 번도 이기지 않고서.
차단선을 두면 양학 상한이 "사냥하는 팀 중 가장 센 팀 + 382" 로 고정된다.
**정직한 일정의 점수는 한 점도 바뀌지 않는다** (회귀 테스트로 고정).

**왜 퍼포먼스를 0으로 했나.** 시드 3개에서 ±2% 는 실력 재현도를 +0.001 올리는 대신
1순위 지표(일정 감안 승리의 질)를 -0.011 내렸다. 1순위를 더 많이 깎으면서 사는 거래다.

**official 게이트 폐기.** `home>=3 OR away>=3` 와 클랜원 가중치(100/70/40/0)를 없앤다.
정상 5v5 는 전부 레이팅 대상이다. 클1용4 도 점수를 받는다.
구성은 게이트가 아니라 **상한 있는 보정(+50)** 으로만 반영한다.

**운영 반영은 아직 하지 않았다.** `packages/rating` 은 1500 기준 그대로다.
이식·replay 계획은 FINAL_SPEC 8~9장에 있다.

---

## D-143 — 랭커 승률 자격선 · 일방적 경기 억제 (2026-08-24)

**D-142 를 부분 대체한다(supersedes).** D-142 의 구조(K=50 · 기준 3000 · 퍼포먼스 0% ·
신뢰도 표 · 표시 배율 3.5 · 활동 페널티 · 구성 보정 +50 · official 게이트 폐기)는 **그대로 유지**하고,
아래 두 가지만 바꾼다.

| 항목 | D-142 | **D-143** |
|---|---|---|
| 약팀 사냥 대응 | 차단선 — 승리이고 E >= 0.90 이면 delta=0 | **일방적 경기 억제** — h=(승이면 E, 패면 1-E), ramp(0.80 -> 0.88) 를 delta 에 곱한다 |
| 랭커 자격 | 없음 | **밴드별 최소 승률** 4000/48% · 4300/50% · 4500/52% · 4700/55% · 4800/58% · 4900/60% (soft) |
| 클랜 자격 | 없음 | **3150/45% · 3300/50%** (개인과 다른 표) |

**왜 고쳤나.** D-142 에서 "경계선" 으로 남겼던 항목이 실제로는 FAIL 이었다 —
최상위 상대 **승률 30%** 선수가 **4,115** 로 랭커가 됐다.
상대 강도는 승리의 값어치를 정하는 것이지 **패배의 면죄부가 아니다.**

**왜 계단 대신 경사인가.** 차단선은 (a) 기대 89.9% 와 90.0% 가 급변하고
(b) **이긴 쪽만** 0 이 되어 점수가 사라졌다(제로섬 위반). 사냥당한 약팀만 일방적으로 손해였다.
경사는 양쪽에 함께 걸린다. **이변은 언제나 만점**이라 "강한 상대를 실제로 이긴 것" 이 가장 크게 남는다.

**대가.** 9시즌 평균 실력 재현도 0.9062 -> 0.9023 (-0.004), 일정 승리의 질 0.987 -> 0.960.
그 대신 승률 30% 랭커가 사라지고, 약팀 600판 전승이 4,945 -> 4,774 로 역사적 영역에서 빠졌다.

**적용 순서.** 표시 점수 -> 승률 자격선 -> 활동 페널티.
순서를 바꾸면 잠수로 4000 아래로 내려간 사람이 자격선을 우회한다.

**운영 반영은 아직 하지 않았다.** `packages/rating` 은 1500 기준 그대로다.

---

## D-145 — 레이팅 FINAL LOCK · 승인 (2026-08-25)

**상태: APPROVED / FINAL.** 사용자가 승인했다. `docs/RATING_FINAL_SPEC.md` 가 정식 사양이다.

**D-143 을 supersede 하는 범위는 아래 두 가지뿐이다.** 그 외 D-143 항목은 전부 그대로 유효하다.

| 항목 | D-143 | **D-145** |
|---|---|---|
| 일방적 경기 억제 끝점 | `m = 1 - (h-0.80)/0.08`, h >= 0.88 -> 0 | **`m = 1 - (h-0.80)/0.06`, h >= 0.86 -> 0** |
| 신뢰도 | 계단식 40/55/70/85/95/100% | **`min(1, sqrt(games/150))`** |

**유지되는 것** — 기준점 3000 · K=50 · 제로섬 · floor 1000 · 표시 배율 3.5 ·
퍼포먼스(KD/MVP) 0% · 승률 자격선(4000/48% ~ 4900/60%) · 미참여 감점 ·
구성 보정 상한 +50 / 최근 20경기 · official 게이트 폐기 · 모든 complete 5v5 rating 대상.

**억제 0.86 인 이유.** 0.88 에서는 약팀 600판 전승(4,774)이 강한 일정 55%(4,705)를 이겼다.
0.86 에서 4,684 < 4,705 로 뒤집힌다. 정직한 일정의 점수 변화는 0 이다.

**신뢰도 연속화 이유.** 계단식은 한 경기로 신뢰도가 15%p 뛰어
내부 편차 450 인 선수의 표시 점수가 한 판에 +236 움직였다. sqrt 곡선은 최대 +12 다.
150판에서 정확히 1 이 되고 이후 평평해 **판수 보너스가 되지 않는다.**

**기준점 2500 은 검토했고 채택하지 않았다** (`docs/RATING_D145_PROPOSAL.md` 1장).
선형 변환으로는 랭커 인원과 고점 희귀성을 동시에 못 맞춘다 —
배율 5.0+ 면 4900+ 가 시즌당 0.6~1.0명, 배율 4.5 면 랭커가 42% 감소한다.

**formula version: `sacloud-d145`.** 기존 `sacloud-p1-PA` 결과는 백업 후 보존한다.

**운영 이식 범위**는 FINAL SPEC 8장 (REMOVE/CHANGE/KEEP + 추가 스키마) 참조.


---

## D-148 — 참가자 10명 복원 · `official` 라벨 잔재 제거 (2026-08-25)

### 문제

경기 상세에 5vs5 10명이 다 나오지 않았고, 래더도 대부분 반영되지 않았다.

- 실경기 136건 중 **10명인 경기는 39건**뿐이었다 (6~9명이 88건)
- 래더가 붙은 경기는 **17건**뿐이었다
- 화면에는 `0전 0승 0패`, `판당 0.0킬`, 스나/라플 `집계 없음` 이 남았다

### 원인 (두 개다)

**(1) 넥슨이 10명을 주지 않는다.**
`match-detail` 은 한 경기에 6~9명만 준다. 파라미터가 `match_id` 뿐이라 나머지를 따로
요청할 방법이 없고, 같은 경기가 여러 `match_id` 로 쪼개져 있지도 않다 (D-044).
실측: 상세 수집이 끝난 866경기 중 **865경기에서 상세 참가자 > 우리가 관측한 수** 였다.
즉 우리가 버리고 있던 게 아니라 애초에 그만큼만 온다.

**그런데 `packages/db/data/supply-official-matches.json` 에는 10명이 다 있었다**
(750경기 중 739경기가 정확히 10명). 명단을 이미 손에 들고 있으면서 안 쓰고 있었다.

**(2) `official` 라벨이 D-145 이후에도 집계 필터로 남아 있었다.**
D-145 에서 "비공식이라 레이팅 0" 은 폐기됐다 — 기준은 **정상 5v5 인가**뿐이다.
그런데 승패 집계 · 평균킬 분모 · 무기별 누적은 여전히 `Match.official = true` 로 걸러졌다.
실경기 중 `official=true` 는 17건, 래더가 붙은 경기는 98건이다. 그래서
**래더는 오르는데 전적은 `0전 0승 0패`** 인 상태가 됐다.

### 결정

**1. 3rd.supply 라인업으로 참가자 명단을 완성한다.**

| 값 | 출처 |
|---|---|
| 10명 명단 · 닉네임 · 경기 당시 클랜 · **무기** | 3rd.supply 스냅샷 |
| 킬/데스/어시 · 딜량 · 헤드샷 | 넥슨 상세 |

무기는 넥슨이 주지 않는 값이다 (D-034). 3rd.supply 는 준다.

**2. 모르는 KDA 는 `null` 이다. 0으로 채우지 않는다.**
넥슨 상세에 없던 사람은 킬/데스/어시를 **모른다.** 0으로 채우면 "0킬을 했다"는
거짓 정보가 된다 (`CLAUDE.md` 3-A 8번). `MatchPlayerStat.kill/death/assist` 의
`NOT NULL` 을 풀고 (migration `20260825120000_d148_nullable_kda`, additive)
화면은 `알수없음` 을 그린다. 계약도 `Count.nullable()` 로 바꿨다.

**3. 신원은 채우기 전에 확정한다 (2-pass).**

1차 pass 는 아무것도 쓰지 않고 근거만 모은다 — 같은 경기 안에서 넥슨 참가자의
닉네임과 라인업 닉네임이 **정확히** 일치하고 그 이름이 **양쪽 모두 그 경기에서 유일**하면
같은 사람이다. 전체를 모은 뒤 `supplyId ↔ playerId` 가 **1:1 일 때만** 확정한다.

2차 pass 의 해석 순서 — **이 순서가 핵심이다**:

1. 같은 경기에 이미 그 `sourcePlayerId` 로 앉아 있는 사람
2. 같은 경기에 이미 그 닉네임으로 앉아 있는 사람
3. 1차 pass 가 확정한 전역 신원
4. 저장된 `Player.sourcePlayerId` (1차 근거와 충돌하지 않을 때만)
5. 없으면 새 `Player` 생성 (`SUP-<supplyId>` · `origin='3rd.supply'`)

**저장된 `sourcePlayerId` 를 먼저 믿으면 안 된다.** 1차 시도에서 그렇게 했다가,
다른 경기에서 만들어진 `SUP-` 그림자 선수가 같은 경기에 앉아 있는 진짜 넥슨 선수를 가려
**한 사람이 두 줄**이 됐다 (경기당 11~16명, 55경기). 전부 되돌리고 순서를 고쳤다.

안전장치: 라인업 인원을 넘기면 **쓰지 않고 보류**한다. 지어내는 것보다 비는 게 낫다.
닉네임 fuzzy 매칭은 하지 않는다. 같은 경기에 동명이인이 둘이면 그 이름은 근거에서 버린다.

**4. 집계 기준을 `official` 에서 "래더에 반영된 경기"로 바꾼다.**
판정은 `Match.redRatingUpdate != null` 이다. replay 가 래더 대상이 아닌 경기의 증감을
지우므로 이 값은 항상 최신이다. 바꾼 곳 — 승패·연승·상대전적 집계, 평균킬 분모,
무기별 누적. mock 픽스처는 3000건 전부 증감을 들고 있어 mock↔live 대조는 그대로다.

### 결과

| | 전 | 후 |
|---|---|---|
| 10명인 경기 | 39 / 136 | **126 / 136** |
| 래더가 붙은 경기 | 17 | **98** |
| 10명 초과(중복 신원) | — | 0 |
| `nexon:check` | 3 FAIL | **17항목 전 통과** |

나머지 10경기는 3rd.supply 스냅샷에 아예 없는 경기다. 없는 것을 지어내지 않는다.

### 검사 정의도 함께 고쳤다

셋은 지금 정책과 어긋나 있었다. 느슨하게 만든 게 아니라 **지켜야 할 것을 다시 적었다.**

- `stats_within_detail_participants` — 라인업으로 채운 행(`matchTimeClanSource='supply-lineup'`)은
  상세 참가자보다 많은 게 정상이다. **넥슨 유래 행**만 센다
- `reconstructed_members_roster_backed` — 이 검사는 *우리가 추론한* 본클랜원 판정을 본다.
  라인업 행은 추론이 아니라 원본이 그 경기 소속을 직접 적어 준 것이라 제외한다
- `reference_not_rated` → **`rated_matches_are_5v5`** 로 교체. D-145 에서 `official` 은
  래더와 무관해졌다. 지켜야 할 것은 "5v5 가 아닌 경기에 래더가 붙지 않는 것"이다
- 추가: `supply_lineup_kda_is_null` — 명단만 복원한 행에 KDA 가 채워져 있으면 FAIL

### 남은 것

- 3rd.supply 스냅샷에 있으나 우리 DB 에 없는 경기 **624건**. import 는 별도 작업이다
  (`--include-new` 자리는 열어 뒀다). 사용자 승인 전에는 하지 않는다
- 3rd.supply 는 KDA·딜량·헤드샷을 주지 않는다. 그 224명은 계속 `알수없음` 이다
- 팀 판정이 Match 의 진영 클랜과 어긋나는 경기 2건 (`side_clan_mismatch`). 추측해 맞추지 않고
  래더에서 뺐다
- 전역 근거가 갈려 확정하지 못한 신원 26건. 틀린 연결보다 빈 채로 두는 쪽을 택했다

### 명령

```bash
pnpm --filter @sacloud/worker nexon lineup-complete            # 미리보기
pnpm --filter @sacloud/worker nexon lineup-complete --confirm  # 반영 (idempotent)
```

---

## D-149 — 무기별 전적 실제 분리 · `official` 라벨 UI 제거 (2026-08-26)

### 1. 무기별 전적이 `집계 없음` 이던 진짜 이유

D-148 에서 무기(3rd.supply)와 KDA(넥슨)를 다 가져왔는데도 화면은 계속 `집계 없음` 이었다.
원인은 데이터가 없어서가 아니라 **둘이 만난 적이 없어서**였다.

D-148 의 `lineup-complete` 는 **새로 만드는 참가자에게만** 무기를 썼다.
이미 앉아 있던 넥슨 참가자(= KDA 를 가진 사람)는 `present` 라 건너뛰었다.
그 결과 —

```
weapon 있음        170     ← 전부 KDA 가 null 인 라인업 복원 행
weapon + KDA known   0     ← 결합 0건
```

무기를 아는 사람은 KDA 가 없고, KDA 를 아는 사람은 무기가 없었다. 집계될 수가 없었다.

**고친 뒤**

```
weapon 있음        980     ← 래더 경기 참가자 전원
weapon + KDA known 810
```

`lineup-complete` 가 이미 있는 참가 기록에도 라인업의 무기를 채운다.
이미 다른 무기 값이 있으면 덮어쓰지 않고 충돌로 센다(현재 0건).
인원이 이미 10명인 경기도 건너뛰지 않는다 — 그러면 완성된 경기일수록 무기가 비었다.

### 2. 부분 집계 — `games` 와 `knownStatGames`

무기는 3rd.supply 가, K/D 는 넥슨이 준다. **한쪽만 있는 참가자가 있다.**

| | 뜻 |
|---|---|
| `games` | 그 무기로 뛴 경기 전부 |
| `knownStatGames` | 그중 K/D/A 를 아는 경기 — **킬·데스·킬뎃의 분모** |

- 모르는 경기를 0킬로 세지 않는다. 평균이 거짓이 된다
- 모르는 경기 하나 때문에 **아는 경기까지 버리지 않는다**
- 승패와 래더 증감은 K/D 를 몰라도 안다 → `games` 에는 들어간다
- 아는 경기가 0이면 킬뎃은 `null` 이다 (0% 가 아니다)
- 랭킹 모집단은 `knownStatGames > 0` 인 선수다 — 비교할 실적이 없으면 등수를 주지 않는다

### 3. 무기 랭킹 기준 — 새 공식을 만들지 않았다

기존 설계의 `ratingDelta` 를 **그대로 쓴다.** 다만 값이 채워진 적이 없었다 —
옛 `rebuildWeaponBuckets` 가 항상 `ratingDelta: 0` 을 넣어 **전원 동점, 전원 1위**였다.
(실제로 `라이플 89명 중 1위` 가 그렇게 나왔다.)

이제 그 무기로 뛴 경기의 `ratingUpdate` 합을 넣는다.
**무기별 공식은 없다** (`CLAUDE.md` 3-B 1번) — D-145 통합 공식이 계산해 둔 값을
무기에 따라 나눠 담기만 한다. `LeaguePlayer.rating` 은 무기별로 쪼개지 않는다.

### 4. K/D 정의는 하나다

전체·스나이퍼·라이플 모두 `킬 / (킬 + 데스) × 100` (`kdRate`).
전체와 무기별이 다른 정의를 쓰면 나란히 놓았을 때 비교가 거짓이 된다.

### 5. `official` 은 더 이상 사용자에게 보이지 않는다

D-145 에서 `official` 은 래더 자격도 가중치도 아니게 됐다. 그런데 배지로 남아 있어
화면이 계속 "비공식이라 점수를 덜 준다"는 폐기된 규칙을 말하고 있었다.

- `비공식 경기` 배지 **제거**. `UNOFFICIAL_BADGE` 상수도 없앴다
- `MatchListItem.official` 을 **공개 계약에서 제거** — 남겨 두면 언젠가 다시 그려진다
- 남는 사용자 표기는 **`래더 미반영`** 하나뿐이다 (판정: `participant_completeness === '5v5'`)
- **DB 의 `Match.official` 은 지우지 않았다.** 출처(provenance)·관리자 화면에서 계속 쓴다

집계 기준도 `official` 이 아니라 **`Match.redRatingUpdate != null`** 이다.
D-148 에서 승패·평균킬을 고쳤고, D-149 에서 **무기별 누적**도 같은 기준으로 맞췄다
(그전에는 무기 집계만 `official: true` 로 걸러 25명만 잡혔다 → 104명 → 최종 259명).

### 6. `알수없음` 을 원인별로 나눴다

| 상황 | 전 | 후 |
|---|---|---|
| 래더 미반영 경기의 증감 | `알수없음` | **`미반영`** |
| 래더 미반영 경기의 경기당시 클랜 점수 | `알수없음` | **`미반영`** |
| K/D 를 모르는 참가자 | 큰 `알수없음` | **`- / - / -`** |
| 딜량·헤드샷 결측 | `알수없음` | 그대로 (`CLAUDE.md` 6장 원본 규칙) |

래더 미반영은 **모르는 게 아니라 없는 것**이다. `알수없음` 은 값을 정말 모를 때만 쓴다.

### 7. 클랜 구성 보정 — 경기별 배율이 아니다

엔진은 이미 D-145 대로였다. 화면이 그것을 말하지 못하고 있었을 뿐이다.

```
평균 1명 +0 · 2명 +10 · 3명 +20 · 4명 +35 · 5명 +50   (사이는 선형, 상한 +50)
최종 클랜 점수 = 내부 Elo + 구성 보정 − 활동 페널티
```

- 정상 5v5 면 **클1용4 든 클5 든 Elo 는 똑같이 정상 반영**된다
- 클랜원 수는 그 경기의 증감을 깎지 않는다. 최근 20경기 **평균**으로만 들어간다
- 반영률(100/70/40/0%) UI 는 복구하지 않는다. `clan_weight` 필드도 계약에서 **없앴다**

화면에 추가한 것 — 경기 상세의 팀별 `본클랜 N / 용병 M` 옆에
`구성 보정 +35 · 최근 20경기 평균 본클랜 4.0명`.
값은 **DB 계산값을 그대로** 쓴다. 그래서 `LeagueClan.compositionMembers` 를 새로 저장한다
(보정값만 있으면 "왜 +35 인가"를 설명할 수 없고, UI 가 되짚어 계산하면 엔진과 어긋난다).

### 8. 3rd.supply weapon 값 실측

스냅샷 라인업 7,522행의 weapon 필드 distinct 값은 **정수 `0`(6,022) 과 `1`(1,500) 뿐**이다.
`CLAUDE.md` 6장의 `0 = 라이플 / 1 = 스나이퍼` 와 일치한다. 문자열도 unknown 도 없다.
그래서 매핑에 추측이 들어갈 자리가 없다. 무기가 `null` 인 참가자는 unknown 으로 남긴다 —
억지로 라이플에 넣지 않는다.

### 9. 결과 (Beta 실데이터)

| | 값 |
|---|---|
| 래더 반영 경기 | 98 |
| 전적·통계 모집단 | 98 (동일 — 불일치 0) |
| 래더 경기 참가행 | 980 |
| weapon 있음 | 980 |
| weapon + KDA known | 810 |
| weapon + KDA unknown | 170 |
| 스나 집계 선수 | 66 |
| 라플 집계 선수 | 220 |
| 둘 다 | 27 |

### 10. 모바일 축소는 viewport 버그가 아니다

`<meta name="viewport" content="width=device-width, initial-scale=1">` 은 **정상적으로 있다**.

원인은 `.pc-container { width: 80rem }` (1120px 고정폭)이다.
이건 실수가 아니라 **원본 3rd.supply 의 관측 동작을 그대로 재현한 것**이다 —
원본도 1120px 아래로 줄어들지 않고 넘쳐서 가로 스크롤이 생긴다 (`styles.css` 주석 참조).
모바일 브라우저가 그 폭에 맞춰 축소해 보여 주는 것이다.

반응형으로 바꾸는 것은 **원본과 다르게 만드는 일**이라 V1 원칙상 사용자 승인이 필요하다.
이번 범위에서는 원인만 확인하고 손대지 않았다.

### 11. 하지 않은 것

- **624경기 import 하지 않았다.** 별도 승인 사항이다 (경기 136 → 760 이 되는 대규모 작업)
- rating formula 를 고치지 않았다. replay 결과는 그대로다 (경기 98 · 선수 260 · 클랜 22)
- `null` KDA 를 0으로 backfill 하지 않았다

### 명령

```bash
pnpm --filter @sacloud/worker nexon lineup-complete --confirm      # 무기 채움 (idempotent)
pnpm --filter @sacloud/worker nexon weapon-rebuild --league supply --confirm
```

`weapon-rebuild` 는 `MatchPlayerStat` 만 읽어 처음부터 다시 만든다. 같은 입력이면 같은 결과다.
rating 을 읽기만 하고 **쓰지 않는다** — 이 잡이 D-145 래더를 바꾸면 그건 결함이다.

---

## D-150 — 624경기 적재 감사 (2026-08-27 · 승인 대기)

### 판정

**B — 590건 적재 가능 / 34건 제외.** 가오픈 **후** 적재를 권고한다.
상세 수치는 `docs/SUPPLY_SNAPSHOT_IMPORT_AUDIT.md`.

### 방법 — 공식을 복제하지 않는다

래더 예측을 별도 시뮬레이터로 만들면 언젠가 갈라지고, 갈라진 예측은 예측이 아니다.
`runRate` 에 `extraMatches` 주입 지점만 더해 **실제 replay 코드**로 돌린다.
`ctx.dryRun` 이 아니면 주입 자체를 거부한다.

**하네스를 먼저 검증한다.** 아무것도 주입하지 않고 dry-run 을 돌려 현재 DB 래더와
대조했고 260명 전원 일치했다. 이 검증이 없으면 예측에 근거가 없다.

### 확정된 사실

- 미수입 경기는 **정확히 624건** (matchId 중복 0 · 형식 오류 0)
- 624건 전부 필드가 완비돼 있고 **넥슨 증거도 하나 이상 있다** (스냅샷만 있는 경기 0건)
- 무기 값은 정수 `0`/`1` 뿐 (라이플 5,014 · 스나이퍼 1,248 · 미상 0)
- 투영 590건은 전부 10명 · 5대5 · 참가자 중복 0
- 결정적 · NaN 0 · D-145 위반 0 · **DB 쓰기 0건**

### 팀 식별 — 라인업의 `clanId` 를 쓰면 안 된다

라인업 행의 `clanId` 는 **그 선수 개인의 클랜**이다. 팀 식별에 쓰려고 다수결로 모았더니
양 팀이 같은 클랜으로 판정되는 경기가 **54건** 나왔다.
`perspectives[0].clan_id` / `opponent_clan_id` 로 바꾸니 **54 → 0**.

### ⚠ 별건 결함 — 현재 래더의 팀 판정

기존 126경기를 perspective 방식으로 대조하니 98건 일치 · **28건 어긋남**.
어긋난 24건(래더 반영분)을 참가자 원소속으로 다시 판정한 결과
**스냅샷 10 : 우리 DB 2 (동점 12)** 로 스냅샷 쪽이 더 맞았다.

즉 **현재 래더 98경기 중 24건의 팀 판정이 의심스럽다.**
이번 범위 밖이라 손대지 않았다. 590건 적재 **전에** 판단이 필요하다.

### 알아 둔 결함 (고치지 않았다)

`side_clan_mismatch` 로 래더에서 빠지는 경기의 승패가 **클랜 누적에는 들어간다.**
가드가 누적 뒤에 있기 때문이다 (D-148 에서 내가 그 위치에 넣었다).
어긋난 경기가 2건일 때는 안 보이다가 투영에서 86건이 되니 `15전 21승11패` 로 드러났다.

**고치지 않았다.** 가드를 앞으로 옮기면 그 경기의 개인 래더 반영도 함께 빠져
**현재 래더 값이 바뀐다** (실제로 40명이 달라졌다). 투영은 "실제로 일어날 일"을
예측해야 하므로 HEAD 의 의미를 그대로 두었다. 수정은 별도 승인 사항이다.

### 스냅샷 신뢰 범위 (재확인)

허용 — 경기 발견 · 10명 라인업 · 닉네임 · **경기 당시** 클랜 · 무기 · 승패 · 팀 식별
금지 — **K/D/A 를 스냅샷에서 만들어 내는 것** · 현재 클랜 소급 · fuzzy 신원 확정 ·
라인업 `clanId` 를 팀 식별에 쓰는 것

### 명령

```bash
pnpm --filter @sacloud/worker nexon snapshot-audit --league supply
```

`--confirm` 이 없다. 적재 도구(`snapshot-import`)는 **아직 만들지 않았다** — 승인 후에 만든다.

---

## D-151 — Vercel 프로덕션 배포 (2026-08-27)

첫 프로덕션 배포를 성공시키면서 **네 가지**를 결정했다. 전부 "운영에서만 죽는" 종류라
개발 PC 에서는 끝까지 드러나지 않았다.

근거: `pnpm-workspace.yaml` · `apps/web/next.config.ts` · `apps/web/scripts/copy-prisma-engine.mjs` ·
`packages/contract/src/config.ts` · 커밋 `5d50960` `f19605f` `941780d` `fc8d846` `ba5aeab`

### 1. `allowBuilds` 에 **`false` 도 명시한다**

pnpm 11 은 목록에 없는 빌드 스크립트를 **경고가 아니라 오류**로 만든다 (`ERR_PNPM_IGNORED_BUILDS`,
`pnpm install` exit 1). `@embedded-postgres/windows-x64` 만 `true` 로 적어 뒀더니
개발 PC(Windows)는 통과하고 **Vercel(Linux)에서 `linux-x64` 가 목록에 없어 설치가 죽었다.**
배포가 build 단계에 들어가지도 못했다.

그래서 `@embedded-postgres/*` 를 **플랫폼별로 전부 적는다** — 개발 PC 플랫폼만 `true`,
나머지는 **`false`(실행하지 않는다고 명시)** 다. `false` 는 "안 쓴다"가 아니라
"안 쓴다는 것을 pnpm 에게 말해 둔다"는 뜻이다.

> Linux 재현법 — 매니페스트와 lockfile 만 빈 폴더에 복사하고 `pnpm-workspace.yaml` 에
> `supportedArchitectures: {os: [linux], cpu: [x64]}` 를 넣은 뒤 `pnpm install --frozen-lockfile`.

Windows 외 OS 에서 개발한다면 자기 플랫폼 항목만 `true` 로 바꾼다. 그 변경은 커밋하지 않는 편이 낫다.

### 2. Prisma 쿼리 엔진을 **`apps/web/generated/client/` 로 복사한다**

생성된 클라이언트는 `packages/db/generated/client` 에 있는데, Next 가 그것을 번들하면
모듈이 원래 위치를 잃는다. 런타임에 Prisma 는 네이티브 엔진(`.so.node`)을 **정해진 몇 군데**
에서만 찾고, **첫 번째가 `/var/task/apps/web/generated/client`** 다.

`outputFileTracingIncludes` 는 파일의 **상대 경로를 유지**하므로 `packages/db/...` 를 그대로
넣으면 Prisma 가 보지 않는 곳에 떨어진다 — 실제로 그렇게 한 번 더 실패했다.
그래서 `scripts/copy-prisma-engine.mjs` 가 엔진을 첫 번째 검색 경로로 먼저 옮기고,
`next.config.ts` 가 `outputFileTracingIncludes: { '/**': ['./generated/client/**'] }` 로 번들에 넣는다.
`outputFileTracingRoot` 는 모노레포 루트다 — 그래야 번들 루트가 저장소 루트가 되어
`apps/web/...` 상대 경로가 런타임 검색 경로와 맞는다.

`binaryTargets = ["native", "rhel-openssl-3.0.x"]` 도 함께 필요하다.
개발 PC 가 Windows 라 `native` 만 두면 **Linux 엔진이 아예 생기지 않는다.**

`apps/web/generated/` 는 커밋하지 않는다. 매 빌드마다 다시 만드는 산출물이다.

### 3. `resolveApiBaseUrl` 은 **Mock 주소로 떨어지지 않는다**

`NEXT_PUBLIC_API_BASE_URL` 이 없으면 예전에는 `DEFAULT_API_BASE_URL`(`https://api.sacloud.local`,
MSW 가 가로채는 가짜 주소)로 떨어졌다. 그래서 운영 배포에서 **서버 API 는 멀쩡한데 브라우저만
존재하지 않는 도메인으로 요청을 보내 화면이 "불러오지 못했습니다"로 죽었다.**

D-116 이 `resolveApiMode` 에 적용한 것과 같은 원칙 — **안전한 쪽으로 실패한다.**
미설정이면 모드를 보고, `mock` 일 때만 Mock 주소를 쓴다. 그 외에는 같은 오리진(`/api`)이다.
빈 문자열도 미설정으로 본다(환경변수를 만들어 두고 값을 비우는 실수가 잦다).
`NEXT_PUBLIC_*` 은 빌드 시점에 인라인되므로 되돌리려면 **재빌드가 필요하다** — 그래서 기본값이 중요하다.

### 4. Deployment Protection 을 껐다

Vercel Authentication 이 켜져 있으면 전 경로가 302 로 SSO 로 튀어 아무도 못 본다.
공개 사이트이므로 껐다. **지금 배포는 공개 상태다.**

### 배포 설정 (Vercel 대시보드)

- Root Directory `apps/web`
- Build Command
  `pnpm --filter @sacloud/db exec prisma generate && node scripts/copy-prisma-engine.mjs && next build`

### 남은 것 · 알아 둔 것

- `DATABASE_URL` — Supabase **transaction pooler(6543)** 는 사용자명이 `postgres` 가 아니라
  **`postgres.<project-ref>`** 여야 한다. `pgbouncer=true` 를 빼면 Prisma 가 prepared statement
  오류를 낸다. 값이 Sensitive 라 CLI 로 읽을 수 없어 **사람이 직접 고쳐야 한다.**
  (자격증명 자체는 문서에 적지 않는다.)
- 스키마 drift — 로컬 DB 에만 `MatchPlayerStat(matchTimeLeagueClanId)` 인덱스가 있다.
  고칠 때는 forward-only · `IF NOT EXISTS`.
- Vercel 빌드마다 `@embedded-postgres/linux-x64` 타르볼을 내려받는다(스크립트 실행은 막았다).
  설치를 `--filter @sacloud/web...` 로 좁히면 없앨 수 있다.

---

## D-152 — 경기 인원과 선수 포지션은 **세어서** 정한다 (2026-08-27)

근거: `packages/ui/src/common/format.ts` · `packages/ui/src/record/weaponCopy.ts` ·
`packages/ui/src/__tests__/team-counts.test.ts` · `packages/ui/src/__tests__/position.test.ts` ·
커밋 `fc0d9e8` `514d866`

### 1. `5 vs 5` — 총원을 양쪽에 쓰지도, 2로 나누지도 않는다

운영에서 **정상 5대5 경기가 `10 vs 10` 으로 나갔다.** `player_count`(총원 10)를 양쪽에
그대로 썼기 때문이다.

- **총원을 그대로 쓰지 않는다** — 위 사고 그대로다.
- **총원을 2로 나누지도 않는다** — 인원이 어긋난 경기(5대4)를 반올림해 5대5 인 척하게 된다.
  라인업 배열의 길이가 사실이고, **어긋나면 어긋난 대로 보인다** (`5 vs 4`).
- 비어 있으면 `0 vs 0` 이다. 숫자를 지어내지 않는다.

`formatTeamCounts(redCount, blueCount)` 하나만 쓴다. 회귀 테스트로 고정했다.

### 2. 포지션은 **그 무기로 뛴 경기 수**로만 정한다

킬 수·킬뎃·현재 클랜으로 정하지 않는다. 그건 판단이지 사실이 아니다.
"스나로 몇 판 뛰었나"는 경기마다 기록된 사실이고, 세면 끝난다.

| 조건 | 결과 |
|---|---|
| 스나 경기 > 라플 경기 | `스나이퍼` |
| 라플 경기 > 스나 경기 | `라이플` |
| 동률 (0판 대 0판 제외) | **반드시 `멀티`** |
| 둘 다 0판 | `집계 없음` — 뛴 적이 없는데 "멀티로 뛴다"고 말할 수는 없다 |

동률을 한쪽으로 몰아 주면 **근거 없이 포지션을 지어내는 것**이라 하지 않는다 (3장 7번).
분모는 `games`(그 무기로 뛴 경기 전부)다. `knownGames`(K/D 를 아는 경기)가 아니다 —
K/D 를 모른다고 그 경기를 안 뛴 것으로 칠 수는 없다 (D-034).

표기는 원본 용어 그대로 `스나이퍼 / 라이플 / 멀티 / 집계 없음` 이다 (CLAUDE.md 6장).

---

## D-153 — 3rd.supply 미러링 수집 · 원본 점수를 우리 계산과 **분리 보관** (2026-08-27)

근거: `packages/db/prisma/schema.prisma`(`sourceRating` / `sourceRatingDelta`) ·
`packages/db/prisma/migrations/20260827090000_d153_source_rating/migration.sql` ·
`packages/db/ops/supplyRollup.ts` · `packages/db/ops/supplyMirrorParse.ts` ·
`apps/worker/src/jobs/supplyMirror.ts` · `apps/worker/src/lib/jsonlStore.ts` ·
커밋 `eb3b8a7` `c302815` `f42cd48` `e800d9f`

### 왜 컬럼을 따로 두는가

기존 `ratingBefore` / `ratingUpdate` 는 **우리가 계산한** 값이다(D-145).
여기에 원본 점수를 덮어쓰면 어느 쪽 숫자인지 알 수 없게 되고, 한 번 섞이면 되돌릴 수 없다.
`CLAUDE.md` 3-A 2번 — **기존 `rating_update` 를 추정 공식으로 덮어쓰지 않는다.**

그래서 원본값 전용 컬럼을 새로 만들었다.

| 표 | 컬럼 |
|---|---|
| `MatchPlayerStat` | `sourceRating` · `sourceRatingDelta` |
| `Match` | `redSourceRating` · `blueSourceRating` · `redSourceRatingUpdate` · `blueSourceRatingUpdate` |

전부 **NULL 허용 · 기본값 없음**이다. 마이그레이션이 **기존 행을 하나도 바꾸지 않는다.**
되돌리려면 컬럼만 지우면 된다 — 파괴적 변경이 아니다.

시즌7 베타 운영 화면은 **이 source 값을 쓴다.** 우리 공식은 코드와 컬럼을 그대로 두되
화면에 적용하지 않는다.

### `sourceRating` 은 "경기 당시" 값이 **아니다** (실측)

한 선수의 5,978경기에서 이 값의 distinct 가 **3개뿐**이었다(초기 표본 162경기에서는 전부 같았다).
원본은 **수집 시점의 현재 래더**를 모든 행에 그대로 붙인다. `opponent.rating` / `opponent.division`
도 클랜별로 값이 하나뿐이라 같은 성격이다.

따라서 **래더 재현의 입력으로 쓰면 안 된다.** 원본 화면을 그대로 재현하려고 담아 둘 뿐이다.
집계에서는 **가장 최근 행의 값 = 그 선수의 현재 원본 래더**로 보고 그것 하나만 옮긴다.
평균을 내거나 증감을 누적하면 원본과 어긋난다.

`sourceRatingDelta` 는 다르다 — **경기마다 값이 다르고 진짜 증감이 맞다.** 원본값 그대로
보관하고 재계산하지 않는다 (3-A 2번).

근거가 아예 없는 선수(모든 행이 `null`)는 **rating 을 건드리지 않는다.** 기본값 3000 을
써 넣으면 "원본 점수가 3000이다"라는 거짓이 남는다 (3-A 8번).

### 수집 방식

사람이 승인한 뒤(2026-08-27), 웹 클라이언트 공개 앱 헤더(`SP-APP-*`)를 붙여 사이트 자신의
공개 API 를 **페이지와 같은 속도로** 불러 받았다. 커서를 끝까지 따라간다 — 예전 스냅샷은
클랜당 첫 20건뿐이었다. 이는 `packages/db/legacy/collect-snippet.js` 의 "헤더 위조 없음"
원칙과는 다르며, **사용자 승인 사항**이다. 체크포인트 파일의 `note` 에 그대로 적어 두었다.

수집 범위(`floor`)는 **2024-05-24 ~ 현재**다.

### 저장은 줄 단위(JSONL)

한 리그가 수십만 경기다. 단일 JSON 으로는 읽고 쓸 때마다 통째로 메모리에 올라가
실제로 파일이 깨졌다(`supply-mirror-supply.json.corrupt` 가 그 흔적이다).
그래서 `packages/db/data/supply-mirror-<slug>.matches.jsonl` / `.details.jsonl` 로 나누고,
체크포인트(`.json`)에는 클랜 목록·커서·실패만 남긴다. **중단 후 재개 가능**하다 (3-A 4번).

---

## D-154 — 배치고사 완료는 되돌아가지 않는다 · 랭킹의 세는 집합과 보여 주는 집합 (2026-08-27)

근거: 커밋 `31c9642` · `packages/db/ops/supplyRollup.ts` ·
`apps/web/tests/clanRankPopulation.test.ts` · `packages/db/ops/__tests__/supplyRollup.test.ts`

### 1. 랭킹 필터 자체는 결함이 아니었다

1부리그에 6개만 뜨던 이유를 수치로 확정했다.

```
LeagueClan 48 (1부 26 / 2부 22)
placement=true 26 · clan.active=false 4
division=1 · placement=false · active → 6   ← 화면과 정확히 일치
```

필터는 `placement: false` + `clan.active` 둘뿐이고, `CLAUDE.md` 6장(배치고사 진행 중이면
랭킹 미표시)과 원본 안내 문구를 그대로 지킨다. 고칠 게 없었다.

### 2. 원본은 배치고사 완료를 **되돌리지 않는다** (근거 셋)

`rate.ts` 의 `untouchedClans` 는 이번 시즌 창에 경기가 없는 클랜을 전부 `placement=true` 로
되돌린다. 베타는 `placementMatches=0`(D-112)이라 베타 창에 래더경기 1건이라도 있어야 랭킹에 오른다.
**원본은 그렇게 하지 않는다.**

1. 원본 클랜랭킹을 커서 끝까지 받으니 1부 22개가 전부 올라와 있다. 우리 1부 활성 클랜 22개와
   `sourceClanId` 로 **22/22 일치**.
2. 그중 3개(`smite` · `gaIactico-` · `supremacy-`)는 원본 자신의 4,891경기 캡처에도
   베타 창 경기 흔적이 없다. **그런데도 원본 랭킹에 있다.**
3. 캡처한 4,891경기 중 `placement:true` 가 **0건**이다.

그래서 미러 집계(`supplyRollup`)는 **미러 경기가 있는 선수를 `placement = false` 로 둔다.**
클랜은 D-157 대로 **수집 파일 클랜 목록에 있으면 `false`**, 없으면 `true` 다.
`placement = true` 는 **랭킹에서만 빠지는 표시**이지 경기를 지우는 것이 아니다 —
기록실에는 그대로 남는다.

> **[미확인]** 원본이 시즌 경계에서 배치 완료를 **유지**하는지, 아니면 리그 최초 가입 때
> **한 번만** 배치고사를 보는지는 관측으로 갈리지 않았다. 임의로 정하지 않았고 `rate.ts` 도
> 건드리지 않았다.

### 3. 세는 집합과 보여 주는 집합을 맞췄다

D-147 이 같은 파일에서 이미 금지한 패턴인데 두 군데가 빠져 있었다.

| 함수 | 증상 |
|---|---|
| `rankOfFirstClan()` | `ACTIVE_CLAN` 누락 → 순위 번호에만 비활성 클랜이 더해진다 |
| `clanRankOf()` | `ACTIVE_CLAN` 누락 → `rankCount`(분모)가 목록보다 커진다 |

"목록엔 6개인데 3 / 7 위", "1위인데 rank=2" 로 드러난다. 지금은 비활성 1부 4개가 전부
배치고사라 안 보이지만, 그중 하나가 배치를 통과하는 순간 드러난다.

개인 랭킹(`rankOfFirstPlayer`)에는 **일부러 넣지 않았다.** `getPlayerRanks` 가 클랜으로
거르지 않고, 무소속 선수는 `clanId` 가 null 이라 넣으면 통째로 빠진다(D-107).

---

## D-155 — 같은 경기를 **여러 리그에 기록**한다 (2026-08-27)

근거: `packages/db/prisma/migrations/20260827180000_d155_match_per_league/migration.sql` ·
`packages/db/ops/supplyMirrorImport.ts` · `apps/web/lib/server/queries/matches.ts` ·
`apps/web/tests/matchPerLeague.test.ts` · `packages/contract/src/entities/match.ts`

### 문제

클랜은 리그를 겸한다. `e2stro-` 와 `The|vub` 가 둘 다 공식리그·열산리그 소속이면
그 경기는 **양쪽 리그에 다 찍혀야 한다.** 그런데 `unique(origin, sourceMatchId)` 이
한 경기를 한 리그에만 묶어서, **먼저 들어간 리그에만 남고 나머지는 버려졌다.**

실측 — 공식리그 **1,828건** · 열산리그 **28건**이 `exists_in_other_league` 로 스킵됐다.

### 결정 — 리그마다 행을 둔다

리그마다 값이 다르다. 부리그·클랜 래더·증감은 리그별이라 **한 행을 공유할 수 없다.**

```
unique(origin, sourceMatchId)  →  unique(leagueId, origin, sourceMatchId)
Match.id                       =  <18자리 경기번호>@<리그slug>
Match.sourceMatchId            =  18자리 경기번호 (원본 대조 키)
+ index Match(sourceMatchId)      "이 경기가 어느 리그들에 있나"
```

제약을 **좁히는 것이 아니라 넓히는** 것이라 기존 행은 하나도 바뀌지 않고 새 제약도 그대로
만족한다. 되돌리려면 인덱스만 되돌리면 된다 — 데이터 손실이 없다.
원본 대조 키(`sourceMatchId`)를 잃지 않는다 (3-A 3번).

### 밖으로 나가는 경기 번호는 `sourceMatchId` 다 — `id` 가 아니다

`<번호>@<리그slug>` 는 **우리 저장 사정**이라 사용자 URL 이나 API 응답에 새어 나가면 안 된다.
계약상 경기 id 는 **원본 그대로 18자리**다. 옛 행은 `id === sourceMatchId` 라 그대로 떨어지고,
mock 시드처럼 `sourceMatchId` 가 없는 행도 `match.sourceMatchId ?? match.id` 로 기존 동작이 유지된다.

### 조회할 때는 **리그를 반드시 함께 건다**

리그 없이 경기 번호로만 찾으면 **엉뚱한 리그의 기록**이 나온다 — 공식리그와 열산리그에
같은 번호의 행이 1,828건 겹친다. `getMatch` 는
`where: { leagueId, OR: [{ sourceMatchId }, { id }] }` 로 찾는다.
적재 쪽 중복 판정도 **그 리그 안에서만** 본다 — 다른 리그에 같은 경기가 있는 것은 **정상**이다.

회귀 테스트: `apps/web/tests/matchPerLeague.test.ts`.

---

## D-156 — 운영 전송은 **로컬 적재 후 대량 push** (2026-08-27)

근거: `apps/worker/src/jobs/supplyPush.ts` · `apps/worker/src/cli.ts`

### 왜

미러 적재를 운영(Supabase)에 **직접** 돌렸더니 **초당 9.3경기**였다. 공식리그 12.8만 경기면
**3시간 49분**이다. 경기마다 개별 INSERT 를 네트워크 너머로 보내기 때문이다.

그래서 적재는 **로컬에서** 하고(왕복이 없어 빠르다), 결과만 옮긴다.
`createMany` 로 1000행씩 묶으면 왕복이 12.8만 번에서 **1,400번쯤**으로 준다.
로컬 읽기는 5,000행 커서 단위다 — 130만 행을 통째로 메모리에 올리지 않는다.

`pg_dump` 를 쓰지 않는다 — 이 저장소의 로컬 DB 는 `embedded-postgres` 인데 서버 바이너리만
들어 있고 클라이언트 도구(`pg_dump` / `psql`)가 없다.

### 무엇을 옮기는가

**미러가 만든 것만** 옮긴다 (`origin = '3rd.supply'`). 개발 시드(`origin='mock'`)나 넥슨
수집분은 건드리지 않는다 — 운영에 개발 데이터가 새어 나가면 안 된다 (D-116).
FK 순서대로 `League → GameMap → LeagueMap → Clan → LeagueClan → Player → Match → MatchPlayerStat`.

### id 재매핑 — 이것이 이 잡의 핵심이다

`Clan` 은 `slug` 와 `sourceClanId` 가 각각 unique 다. 같은 클랜이 운영에 **다른 id 로** 이미
있으면 `skipDuplicates` 가 조용히 건너뛰는데, `LeagueClan` 은 **로컬 id** 를 가리키고 있어
**FK 가 깨진다.** 실제로 그렇게 실패했다 (`Clan 62 읽음 · 0 씀`).

그래서 로컬 id → 운영 id 대응표를 만들고 **참조하는 쪽을 옮길 때 바꿔 넣는다.**
선수도 `sourcePlayerId` / `nexonOuid` 가 unique 라 같은 재매핑이 필요하다.
`LeagueClan.sourceLeagueClanId` 는 전역 unique 라 다른 리그 것과 부딪힐 수 있어 **비운다.**

**운영에 이미 있는 행을 고치지 않는다.** 우리 쪽 참조만 맞춘다.

### 안전 · 한계

전부 `skipDuplicates` 다. 여러 번 돌려도 같은 결과이고 **삭제하지 않는다** (3-A 4번).

그 대가로 **이 잡은 create-only 다.** 운영에 이미 있는 행의 값은 갱신되지 않는다 —
특히 집계표가 그렇다. `LeagueClan` 은 있어도 새로 만들 때만 값이 들어가고,
`LeaguePlayer` 는 아예 옮기지 않는다. **운영 집계는 `supply-rollup` 을 운영 DB 에 대고
직접 돌려서 맞춘다.**

---

## D-157 — 클랜 랭킹 값은 **수집 파일 클랜 목록**에서 온다 (2026-08-27)

근거: `packages/db/ops/supplyRollup.ts` · `packages/db/ops/__tests__/supplyRollup.test.ts` ·
`apps/worker/src/jobs/supplyMirror.ts`

### 선수와 클랜을 다르게 다룬다

- **선수** — 경기 기록을 되짚어 승패·K/D/A 를 만든다. 점수만 원본값을 옮긴다 (D-153).
- **클랜** — 점수·승패·부리그를 **되짚지 않는다.** 수집 파일 체크포인트의 **클랜 목록 값을
  그대로 쓴다.** 그 값이 원본 클랜랭킹 화면이 실제로 보여 주는 숫자다.

### 되짚기가 틀린다는 증거

| 클랜 | 되짚은 값 | 원본 |
|---|---|---|
| `saint` | 1,525 | **1,561** |
| `One.PoinT` | 1,117 | **1,079** |
| `smite` | 3,000 (기본값 — 자기 진영 점수가 담긴 경기가 하나도 없다) | **1,718** |

원본은 경기 화면에 **한쪽 진영의 점수만** 실어 준다. 그래서 되짚기로는 채워지지 않는 클랜이 생기고,
채워지는 클랜도 어긋난다.

### 랭킹 **모집단**도 그 목록이 정한다

- 상대 클랜으로만 등장한 클랜은 리그 등록 클랜이 아니어서 원본 클랜랭킹에 없다. 우리도 뺀다 —
  supply 1부가 22개여야 하는데 되짚으니 **31개**가 됐다(부리그를 몰라 기본값 1로 들어간 9개가 섞였다).
- 반대 방향도 마찬가지다. **경기가 한 건도 없는 등록 클랜도 원본은 랭킹에 띄운다.**
  랭킹 값이 전부 목록에서 오므로 경기가 0건이어도 값은 완전하다. 막고 있던 것은
  "경기가 없으면 `Clan`·`LeagueClan` 행이 아예 안 만들어진다" 하나뿐이라 **집계에서 만든다.**
  daerule 1부가 15개여야 하는데 9개였다.
- 목록에 없는데 경기에는 나온 클랜은 `placement = true` 로 **랭킹에서만 뺀다.**
  점수·승패를 건드리지 않고 경기도 그대로 남는다.

선수는 다르다 — **선수는 경기가 있어야 존재하는 것이 맞다.** 그대로 둔다.

### 값을 지어내지 않는다

`rating` 이 `null` 이면 그 칸을 아예 쓰지 않는다(기존 값 보존). `win`/`lose` 가 `null` 이면
0으로 채우지 않는다. 부리그는 목록이 항상 준다. 우리 공식값(`internalRating` ·
`compositionScore` · `activityPenalty` · `lastRatedAt`)은 **건드리지 않는다** (D-145).

집계는 누적하지 않고 **처음부터 다시 계산해 덮어쓴다.** 두 번 돌려도 값이 두 배가 되지 않는다.

### 대조 결과 (2026-08-27~28)

세 리그 모두 **"수집 파일 클랜 수 == 랭킹 반영 클랜 수"** 다.

| 리그 | 클랜 | 부리그 |
|---|---|---|
| supply (서플라이공식리그) | 49 | 1부 22 · 2부 27 |
| daerule (대룰리그) | 45 | 1부 15 · 2부 30 |
| sanply (열산리그) | 96 | 1부 96 (단일리그) |

supply 는 원본 클랜랭킹과 **순위·점수 전부 일치**를 확인했다.

---

## D-158 — 스냅샷 감사 안전장치 테스트의 **전제** 갱신 (2026-08-28)

`apps/worker/src/__tests__/snapshotAuditSafety.test.ts` 3건이 실패하고 있었다.
전부 **세상이 바뀌어 전제가 깨진 것**이지 결함이 아니었다. 단언이 지키려던 **성질**은 그대로 두고
**전제 요구만** 걷어냈다.

### 1. `audit.set.missing > 0` → `audit.set.snapshotTotal > 0`

D-150 당시에는 스냅샷 파일의 624경기가 아직 DB 에 없었다. 지금은 **미러 적재로 전부 들어와
`missing = 0`** 이다 (D-155). 그건 결함이 아니라 목표를 달성한 상태다.

이 테스트가 지키는 것은 **"감사를 돌려도 DB 가 안 바뀐다"** 이지 "감사할 거리가 남아 있다"가
아니다. 그래서 전제를 요구하지 않고, **감사가 실제로 무언가를 읽었다**는 것만 확인한다.
`before`/`after` 스냅샷 비교는 그대로다.

### 2. `baselineMatchesDb.mismatched === 0` → **미러링 리그에서는 단언하지 않는다**

하네스는 D-145 공식으로 replay 한 값을 내는데, `LeaguePlayer.rating` 에는 **3rd.supply
원본 점수**가 들어 있다 (D-153). **서로 다른 것이 정상**이고, 같기를 요구하면
"원본값을 그대로 보존한다"(3-A 2번)는 원칙과 **정면으로 충돌한다.**
실제로 207건이 어긋났는데 그건 설계다.

미러링 여부는 **`Match.origin = '3rd.supply'` 가 그 리그에 존재하는가**로 판정한다.
미러링 리그에서는 `compared > 0`(하네스가 돌았는가)만 보고, 미러링이 아닌 리그에서는
기존 `mismatched === 0` 단언이 그대로 작동한다.

### 3. `unknown.length > 0` 요구 삭제

지키려는 성질은 **"모르는 값을 0으로 채우지 않는다"**(D-034)이지 "모르는 값이 존재한다"가 아니다.
미러 적재로 스냅샷의 경기가 전부 DB 에 들어와 **투영 대상 자체가 0건**이 됐다 (D-155).
대상이 없으면 어길 것도 없다. 대상이 생기면 `kill === null` 인 행에 대한
`death`/`assist` 도 `null` 이라는 단언이 그대로 작동한다.

### 결과

**8/8 통과.** 코드(단언)만 고쳤고 감사 도구·투영 로직·DB 는 건드리지 않았다.

---

## D-159 — 매치 상세 표기 정정: `미반영` 폐기 · 무기 칸 축약 · 안내 문구 제거 (2026-08-28)

사용자 지시로 매치 상세 화면 표기를 원본에 맞춰 고쳤다. **D-149 의 결론 하나를 뒤집는다.**

### 1. 인라인 `미반영` → `알수없음` (D-149 를 뒤집는다)

D-149 는 "래더에 반영되지 않는다는 **사실을 아는** 것이라, 모른다는 뜻의 `알수없음` 은 틀린 말"
이라며 인라인 표기로 `미반영` 을 썼다. 논리 자체는 지금도 맞다.

그러나 사용자가 원본 화면 기준으로 **`알수없음` 하나로 통일**하라고 지시했다.
원본이 두 표기를 구분하지 않는다면 우리 쪽 논리적 정합성보다 **원본 재현이 우선**이다
(`CLAUDE.md` 2장 V1 최우선 목표).

- 적용: `packages/ui/src/record/MatchCard.tsx` L209-214 · L311-316 · L499-505
- 상수 `NOT_RATED_INLINE` 삭제. 툴팁용 `NOT_RATED_INLINE_TITLE` 은 유지
- **접힌 매치 카드의 상태 배지 `NOT_RATED_BADGE = '래더 미반영'` 은 그대로 둔다.**
  그건 매치 상세 표의 값 칸이 아니라 기록실 카드의 상태 배지다.
  `래더 알수없음` 으로 바꾸면 오히려 틀린 말이 된다. **[미확인]** — 원본에 이 배지가
  어떻게 표기되는지는 확인되지 않았다

### 2. 무기 칸은 축약형 `스나` / `라플`

`matchWeaponLabel` (`packages/ui/src/record/matchDetailView.ts:136-140`).
선수 상세 사이드바의 포지션 표기(`weaponCopy.positionLabel`)는 **여러 경기를 센 결과**라
`스나이퍼` / `라이플` 을 그대로 쓴다. 문맥이 다르므로 한쪽을 고칠 때 다른 쪽을 따라 고치지 않는다.
무기를 모르면 여전히 `null` → 호출부가 `알수없음` (D-034). `null` 을 라플로 떨어뜨리지 않는다.

### 3. 팀바 `선레드` / `선블루` 를 항상 표시

전에는 `blue_team === null` 이면 라벨이 통째로 사라졌다. 넥슨이 진영을 주지 않아
**대부분의 경기에서 사라졌다.** 이제 팀 블록의 진영 기준으로 항상 그린다.
**[미확인]** — 우리가 저장한 red/blue 가 원본의 **선공** 진영과 같은지는 검증되지 않았다.

### 4. 설명 문구 전면 제거

`ladderNotice()` 와 `COMPOSITION_NOTICE`(`클랜원 수는 이 경기의 증감을 깎지 않습니다 …`) 삭제.
매치 상세 패널에 렌더되는 문장형 안내는 **0건**이다.
데이터 표기인 `확인 5v5 · 일부` 칩(D-068)은 안내 문구가 아니라서 유지했다.

### 5. MVP 표기는 `★`

글자 배지 `MVP` → `★` (title/aria-label 은 `MVP` 유지).
**[미확인]** — 원본 경기 상세 펼침 관찰에 실패해(`docs/UI_PARITY_AUDIT.md` 7장)
원본의 실제 모양은 모른다.

### 검증

`npx vitest run packages/ui/src/__tests__/` → **17 파일 / 162 테스트 통과**.
`pnpm typecheck` → 8개 패키지 전부 통과. 감독관이 독립 재실행해 같은 결과를 확인했다.

---

## D-160 — 선수의 **현재 소속 클랜**은 가장 최근 경기에서 온다 (2026-08-28)

근거: `packages/db/ops/supplyRollup.ts` · `packages/db/ops/__tests__/supplyRollup.test.ts` ·
`apps/worker/src/jobs/supplyRollup.ts`

### 무엇이 비어 있었나

개인랭킹 표가 닉네임 옆에 클랜 마크를 하나도 띄우지 못했다. 화면 질의
(`apps/web/lib/server/queries/leagues.ts` 의 `getPlayerRanks`)는 `LeaguePlayer.clanId` 를 읽는데
**적재도 집계도 그 칸을 채우지 않았다.**

| 칸 | 채워짐 |
|---|---|
| `LeaguePlayer.clanId` (supply) | 113 / 10,388 |
| `LeaguePlayer.clanId` (sanply · daerule) | 0 |
| `Player.clanId` (`origin='3rd.supply'`) | 0 / 21,107 |

### 규칙 — `sourceRating` 과 같다

원본이 참가 기록에 붙여 주는 `player.clan` 은 **경기 당시가 아니라 수집 시점의 현재 소속**이다
(실측: 3,322명 중 경기별로 소속이 달라진 사람 0명 — D-153 · `supplyMirrorParse.ts` 머리말).
그래서 **가장 최근 경기의 값 하나**를 현재 소속으로 옮긴다.

- 값은 `MatchPlayerStat.matchTimeClanSlug` 에서 읽는다. **`MatchPlayerStat` 은 고치지 않는다** —
  과거 경기 화면은 그 경기에 박힌 스냅샷을 계속 쓴다 (D-131).
- 래더와 딱 하나 다르다. 래더는 **값이 있는 행 중** 최신을 고르지만, 소속은 **경기 자체의 최신**을
  고른다. `null` 이 "무소속" 이라는 실제 정보라, 값이 있는 행만 고르면 클랜을 나온 선수가
  영영 예전 클랜에 남는다.
- 시각이 같으면 `matchId` 로 순서를 고정한다.

### 지어내지 않는다

- 최신 경기에 클랜이 없으면 **무소속**이다 → 칸을 아예 쓰지 않는다(새 행은 `null` 로 남는다).
- 클랜 slug 는 있는데 `Clan` 표에 행이 없으면 **클랜을 만들지 않는다** → 역시 쓰지 않는다.
- `Player.clanId` 는 **`origin='3rd.supply'` 인 선수만** 건드린다. 조건을 읽을 때와 쓸 때 양쪽에
  걸어, 넥슨 경로로 들어온 선수의 소속을 미러 값으로 덮지 않는다.
- `Player` 는 전역 표라 리그마다 따로 쓰면 마지막에 돌린 리그가 이긴다. 리그별 근거를
  `mergePlayerClanPicks` 로 합쳐 **한 번만** 쓴다 — `--league` 를 무엇으로 주든 값이 흔들리지 않는다.

### 결과 (로컬 · 2026-08-28)

| 리그 | `LeaguePlayer.clanId` | 최신 경기에 클랜 있음 | `Clan` 표에서 찾음 |
|---|---|---|---|
| supply | 2,635 / 10,388 | 2,978 / 10,324 | 2,622 |
| sanply | 3,719 / 15,312 | 4,420 / 15,312 | 3,719 |
| daerule | 820 / 4,157 | 1,124 / 4,157 | 820 |

`Player.clanId` = **4,419 / 21,107**. 다른 `origin` 선수 1,041명은 손대지 않았다.

두 번 돌려 값이 같았다 — 2회차는 `Player.clanId` 갱신 0건 · "이미 같음" 4,419건이었다.

수집 파일(`.matches.jsonl`)만 보고 선수별 최신 clan 을 **DB 없이 다시 구해** 대조했다.
29,798행 중 **9행(0.03%)만 달랐다.** 전부 설명된다 —
5행은 그 선수의 DB 최신 행이 미러가 아닌 다른 3rd.supply 경로로 들어와 클랜이 없는 경우이고,
4행은 **이미 값이 있어 지우지 않은** 행이다.

### 남은 것 — 원본이 우리보다 소속을 많이 알고 있다 [미확인]

미러 수집 파일 자체가 선수의 **71%를 무소속으로 준다.** 참가 기록 행 기준으로는 60%에 클랜이
붙어 있는데, 선수 기준으로는 21,342명 중 5,674명(26.6%)만 한 번이라도 클랜이 찍힌다.
supply 래더 상위 20명 중 클랜이 붙은 사람은 1명뿐이다 — 3,065경기를 뛴 `chococake` 는
supply·sanply 합쳐 **5,768행 전부** `clan: null` 이다.

그런데 3rd.supply 클랜원 목록(`supply-clan-rosters.json`)은 같은 선수를 `smite` 소속으로 싣는다.
**원본 개인랭킹의 마크가 참가 기록의 `player.clan` 이 아닌 다른 칸에서 올 가능성이 있다.**
supply 기준 대조: 클랜원 목록에 있는 1,084명 중 707명은 우리 값과 같고, 139명은 다르며(그 목록은
"1경기 이상 뛴 이력" 이라 한 선수가 여러 클랜에 실린다), 238명은 우리가 무소속으로 둔다.
반대로 목록에 없는데 우리가 채운 선수가 1,789명이다.

추가로, 원본이 준 클랜 slug 인데 우리 `Clan` 표에 행이 없는 클랜이 **439개**(선수 1,090명)다.
이 리그들에서 팀으로 뛴 적이 없어 행이 만들어지지 않았다. 수집 파일에는 이름·slug·마크 URL 이
전부 있으므로 만들 수는 있으나, **이번 작업 범위가 아니라 만들지 않았다.**

---

## D-165 — 무소속리그: **티어 = 부리그(division)** · 새 축을 만들지 않는다 (2026-08-28)

> **번호 정정(감독관 2026-08-28 12:10)**: `D-161` 을 세 작업이 동시에 가져갔다 —
> 선수 포지션/프로필(코드 인용 23곳) · 이 항목(8곳) · 래더 조회 조건(3곳).
> 인용이 가장 많은 선수 포지션이 `D-161` 을 유지하고, 이 항목은 `D-165`,
> 래더 조회 조건은 `D-164` 로 옮겼다. 코드 주석과 `CLAUDE.md` 도 함께 고쳤다.

사용자 지시로 만든 **신규 기능**이다 (`CLAUDE.md` 3장 3번 "임의 기능 추가 금지"의 예외 —
원본 복제가 아니라 소유자가 직접 요구했다).

> "리그 카테고리를 따로 만들어라. 거기에 공식리그 무소속리그 열산리그 대룰리그를 집어넣어라.
>  무소속리그 페이지는 지금부터 만든다. 1티어부터 5티어까지의 칸을 만들고
>  관리자 권한으로 각 티어에 클랜을 등록할 수 있게 만들어라."
>
> "등록된 무소속 클랜끼리의 기록들은 공식리그와 같은 방식으로 각 개인과 클랜에 기록한다.
>  유일한 하나의 차이점은 무소속리그는 킬데스를 기록하지 않는다.
>  킬데스를 계산하긴 하지만 킬데스를 숨겨라. 그 외에는 전부 공식리그와 동일하다."

### 결정 1 — 티어 1~5 는 `LeagueClan.division` 1~5 다. **새 축이 아니다**

`League.divisionCount = 5` 인 무소속리그를 만들고, 화면 **표기만** `1부리그` → `1티어` 로 바꾼다.

| | 공식리그 | 무소속리그 |
|---|---|---|
| 저장 | `LeagueClan.division` | `LeagueClan.division` (같은 칸) |
| 표기 | `1부리그` · `2부리그` | `1티어` … `5티어` |
| 승강 | 운영자 | 운영자 (**자동 아님** — D-104 ①) |

왜 이렇게 하나
  클랜랭킹 탭 · 커서 페이지네이션 · `getClanRanks(leagueId, division, …)` · 순위 계산이
  전부 이미 `division` 으로 돈다. 티어를 새 축으로 만들면 그 네 가지를 전부 복제해야 하고,
  두 벌이 되는 순간 반드시 어긋난다.

표기 변환은 **한 곳**이다 — `packages/ui/src/league/divisionLabel.ts`.
조건은 `league.category === 'independent'` 하나뿐이라 **공식리그 표기는 바뀔 수 없다**
(회귀 `packages/ui/src/__tests__/division-label.test.ts` 5건이 그것만 본다).

### 결정 2 — 값이 두 곳에 있다. **항상 같이 쓴다**

`LeagueClan.division` (리그 안의 티어) 과 `Clan.tier` (D-104 무소속 래더 질의가 읽는 값)가
따로 있다. 등록·이동은 **둘을 한 번에** 쓴다. 한쪽만 고치면 부리그 탭과 무소속 전체 래더가
서로 다른 답을 낸다.

- 기준은 **`LeagueClan.division`** 이다. `Clan.tier` 가 그것을 따라간다
- 어긋난 것을 되맞추는 명령이 있다 — `independent-league --sync` (재실행해도 결과가 같다)
- 티어 이동은 `division` 만 바꾼다. rating·승패·placement 는 건드리지 않는다

### 결정 3 — 킬뎃 숨김은 **D-107 그대로**다. 새로 만들지 않았다

사용자 지시가 D-107 과 정확히 같아서 기존 장치를 그대로 쓴다.

- 계약 `LeagueSummary.hides_cumulative_kd` · `PlayerRankRow.kd_rate` (nullable)
- 조회 `apps/web/lib/server/queries/visibility.ts`
- 화면 `RankTable` 이 `kd_rate === null` 이면 칸을 비운다

**킬·데스는 계산해서 저장한다.** DB 에 `null` 로 넣지 않는다 — 그건 "원본이 안 줬다" 는
뜻이라 의미가 다르다 (D-034 · D-106). 감추는 것은 **응답에 넣는가** 하나뿐이고,
감추는 대상은 **누적** kill/death/킬뎃이다. 경기 한 판의 K/D/A 는 그대로 보인다.

래더 공식은 하나뿐이다 (`CLAUDE.md` 3-B 1번). 무소속용 공식·무소속용 K값을 만들지 않았고,
`rate.ts` 는 예전부터 무소속 클랜을 계산에서 빼지 않는다 (D-102 정정분).

### 계약 변경 (추가만)

- `LeagueSummary.category` — `official` | `independent`, 기본값 `official`.
  화면이 **표기**를 고르는 값이다. `hides_cumulative_kd`(무엇을 감추는가)와 뜻이 달라 합치지 않았다
- `leagueClanRegister` — `POST /leagues/:leagueSlug/clans`.
  초대(`leagueInvite`)는 클랜 마스터가 링크로 수락하는 흐름이고,
  이것은 **운영자가 티어를 정해 직접 넣는** 흐름이다

### 스키마 변경 — **없다**

필요한 칸이 전부 이미 있었다: `League.category`(D-107) · `League.divisionCount` ·
`Clan.category` · `Clan.tier` · `LeagueClan.division`. 마이그레이션을 만들지 않았다.

리그 행은 마이그레이션이 아니라 **재실행 가능한 명령**으로 만든다.

```bash
pnpm --filter @sacloud/worker nexon independent-league --confirm
pnpm --filter @sacloud/worker nexon independent-league --register <클랜slug> --tier 3 --confirm
pnpm --filter @sacloud/worker nexon independent-league --sync --confirm
```

`--confirm` 이 없으면 한 줄도 쓰지 않는다. `slug = nolink` · `category = independent` ·
`divisionCount = 5` · `origin = sacloud`(`mock` 이면 공개 화면에서 걸러진다 — D-116) ·
`official = false`(공식 배지는 공식리그의 것이다).

### 권한

새 경로도 **기존 리그 관리자 검사**(`requireLeagueAdmin`)를 그대로 받는다.
비인증 401 · 리그 관리자가 아니면 403. `apps/web/tests/adminApi.test.ts` 에 회귀를 추가했다.
관리자 화면의 **티어 등록 칸은 무소속리그에서만** 나온다 — 공식리그 관리 화면은 원본 그대로다.

### `[미확인]`

1. **같은 클랜이 공식리그와 무소속리그에 동시에 있을 수 있는가.** 원본에서 확인되지 않았다.
   막지 않고 `warnings` 로 알린다 — 없는 규칙을 지어내지 않는다.
   등록하면 `Clan.category` 가 `independent` 로 바뀌므로 운영자가 알아야 한다
2. **경기별 kda 까지 감추는가.** D-107 은 **누적**만 감춘다고 정했고 그 범위를 넓히지 않았다
3. **무기별 누적**(`sniper_kill` · `rifle_kill` · `sniper_kd_rate` …, D-149)은 지금
   무소속리그에서도 그대로 나간다. 이것도 누적 킬·데스라 D-107 의 뜻과 어긋날 수 있으나,
   D-107 이 정한 범위가 아니라 **넓히지 않았다.** 판단이 필요하다
4. **무소속리그에 시즌이 없다.** 시즌 카드·시즌 범위 replay 가 필요하면 운영자가 연다
   (`nexon season --start`). 래더 계산 자체는 시즌 없이도 돈다

---

## D-161 — 선수 `포지션` 은 **원본에 있다**. 잘못 지운 줄을 되살린다 (2026-08-28)

### 무엇이 틀렸나

2026-08-27 UI 대조 감사(`docs/UI_PARITY_AUDIT.md` 6-2)에서 선수 기록실 사이드 `상세정보` 의
`포지션` 줄을 **"원본에 없다"** 고 판정하고 지웠다. **그 판정이 틀렸다.**

원본 `상세정보` 는 이 순서다 (2026-08-28 원본 모바일 실측).

```
상세정보
래더        3260점
포지션      A 숏        ← 있다. 래더 바로 아래다
승률        525승 367패  58.9%
킬뎃        9,371킬 6,228데스  60.1%
평균킬      판당  10.5킬
MVP         246회
랭킹        5,599명중  6위
소속        없음
```

감사에서 못 본 이유는 **값이 있는 선수에게만 그 줄이 나오기 때문**이다.
표본으로 본 선수들은 값이 `null` 이라 줄이 아예 없었다.
`null` 이면 `-` 도 `알수없음` 도 아니고 **줄째로 사라진다** (D-099 · D-106 과 같은 규칙).

### 두 `포지션` 은 서로 다른 것이다

| | 원본의 `포지션` | 우리가 만들었던 `포지션` |
|---|---|---|
| 출처 | **선수가 직접 설정**하는 프로필 값 | 무기별 **경기 수를 세어 계산** |
| 값 | `A 숏` 같은 맵 포지션 | `스나이퍼` / `라이플` / `멀티` / `집계 없음` |
| 코드 | `data.player.position` | `packages/ui/src/record/weaponCopy.ts` `resolvePlayerPosition` |

계산식 블록을 화면에서 뺀 것은 **옳았다.** 원본에 없는 개념이다.
잘못은 그것과 함께 **원본에 있는 줄까지 지운 것**이다.
`resolvePlayerPosition` 은 지우지 않았지만 **어떤 화면에도 쓰지 않는다.**
그 파일 주석에 "원본의 `포지션` 과 다른 개념" 을 명시했다.

### 원본 응답 (실측 2026-08-28)

```
GET /players/1896093983
GET /leagues/{supply|sanply|daerule}/players/1896093983   ← .data.player 가 같다
{"id":1896093983,"name":"Yolloanswag","clan":null,"note":null,
 "position":3,"me":false,"renewed_at":"2026-08-05 06:53:00"}
```

- **`position` 은 문자열이 아니라 숫자 코드다.** 화면이 코드를 한글 표기로 바꿔 그린다.
- 세 값(`position` · `note` · `renewed_at`)은 **리그와 무관한 전역 선수 값**이다.
  세 리그에서 같은 선수를 조회하면 셋 다 같은 값이 온다 — 그래서 선수당 **1요청**만 한다.
- `renewed_at` 은 ISO 가 아니라 `YYYY-MM-DD HH:mm:ss` 다. 시간대 표기가 없어
  기존 `parseSupplyDateTime`(KST → UTC) 규칙을 따른다.
- `/clans/{slug}/players` 도 `position` 을 준다 (클랜원 목록용 · 이번 수집에는 쓰지 않았다).

### 코드 → 표기 매핑은 **대부분 `[미확인]` 이다**

원본이 코드를 주므로 표기는 원본 화면을 봐야 안다. 우리가 화면에서 **직접 확인한 것은 하나뿐이다.**

| 코드 | 표기 | 근거 |
|---|---|---|
| 3 | `A 숏` | 원본 모바일 실측 (선수 `Yolloanswag`) |
| 0 · 1 · 2 · 4 · 5 · 6 | `[미확인]` | 코드의 존재는 확인, 표기는 못 봄 |

**지어내지 않는다** (3장 7번 · 3-A 8번). `SUPPLY_POSITION_LABELS`
(`packages/db/ops/supplyPlayerProfiles.ts`)에는 확인한 것만 넣는다.
표기를 모르는 코드는 `Player.position` 을 비워 두고 **몇 명인지 센다.**
`supply-player-profiles-import` 가 코드별 인원과 **확인용 대표 선수 URL** 을 함께 낸다.

원본 코드는 수집 JSONL 에 그대로 남아 있으므로(3-A 1번), 표기가 확인되면
**네트워크 없이** 표만 고쳐 다시 적재하면 된다.

> 표기를 알아내려고 원본 **웹페이지 HTML** 을 받아 보려 했으나 AWS WAF 봇 차단이 걸린다.
> `CLAUDE.md` 3-A 5번에 따라 **우회하지 않고 멈췄다.** 공개 API 에는 코드 목록 엔드포인트가 없다
> (`/positions` · `/codes` · `/configs` 전부 404).

### 수집이 값을 버리고 있었다

`apps/worker/src/jobs/supplySeasons.ts` 가 이미 `/leagues/{slug}/players/{id}` 를 부르면서
응답에서 `player_id` · `league_player_id` · `player_name` 만 뽑고 **세 값을 버렸다.**
`CLAUDE.md` 3-A 1번(원본 응답을 버리지 않는다) 위반이었다. 두 가지를 했다.

1. 그 잡의 색인 레코드에 `position` · `note` · `renewed_at` 을 **함께 남기게** 고쳤다.
   이미 받은 줄에는 세 칸이 아예 없으므로(`undefined`) `?` 로 두어
   **"값이 null 이다" 와 "물어보지 않았다" 를 구분**한다.
2. 이미 받은 29,798줄은 되살릴 수 없어 새 잡으로 한 번 더 받았다 —
   `supply-player-profiles` (선수당 1요청 · 중단 후 재개 · `--dry-run` 은 무요청).

적재는 `supply-player-profiles-import` 다. **네트워크를 쓰지 않고**,
`origin='3rd.supply'` 선수만 건드리며(넥슨 경로·개발 시드를 덮지 않는다),
값이 이미 같으면 쓰지 않는다. `--confirm` 없이는 한 줄도 쓰지 않는다.

### 계약 · 화면

- `LeaguePlayerProfile`(`packages/contract/src/entities/detail.ts`) —
  `PlayerSummary` + `position` · `note`. 둘 다 `nullable`, 기본값 `null` 이라
  이 필드가 없던 응답과도 호환된다. 계약은 **표기 문자열만** 받는다 —
  숫자 코드가 그대로 흘러 화면에 `3` 이 뜨는 것을 막는다.
- `note` 는 원본이 **어느 화면에 쓰는지 확인하지 못했다** `[미확인]`.
  계약에는 담되 화면에는 붙이지 않는다.
- `PlayerStatSidebar` — `래더` 바로 아래에 `포지션` 줄. `null` 이면 렌더하지 않는다.
- Mock 도 같은 자리에 같은 모양으로 내보낸다 (픽스처는 40%만 값을 가진다).

---

## D-162 — 병영수첩 **주소를 붙여 넣으면** 그 선수로 간다 (2026-08-28)

근거: `packages/contract/src/barracksUrl.ts` · `apps/web/lib/server/queries/search.ts` ·
`packages/mock/src/store.ts` · `apps/web/tests/barracksUrl.test.ts`

> 문서가 없이 코드만 이 번호를 인용하고 있어 **감독관이 코드 주석의 근거만 옮겨 적었다**
> (2026-08-28 · `CLAUDE.md` 3장 11번). 새로 정한 사실은 없다.

### 무엇이 문제였나

검색창에 병영수첩 주소를 **그대로 붙여 넣으면** 주소 전체가 닉네임으로 취급돼 아무것도 안 나왔다.
클랜 쪽에는 이미 같은 기능이 있었다 — `leagueAdmin.ts` 의 `clanSlugFromBarracksUrl()` 이
`barracks.sa.nexon.com/clan/{slug}` 에서 slug 를 뽑는다. **선수용만 없었다.**

### 경로 형식은 `[미확인]` 이다 — 그래서 단정하지 않는다

클랜 주소(`/clan/{slug}/clanMatch`)는 관측됐지만 **선수 주소의 정확한 경로는 관측하지 못했다.**
그래서 한 가지 형식을 정해 놓고 그것만 받지 않는다.

- 주소에서 식별자가 될 만한 조각을 **전부 뽑아 순서대로 시도**하고, **DB 에 있으면** 그것으로 본다
- 식별자가 아닌 조각(`clan` · `record` · `profile` · `ko` 같은 화면 이름·언어 코드)은
  `KNOWN_PLAYER_SEGMENTS` 로 걸러낸다. 목록이 모자라면 후보가 늘 뿐이고 **DB 대조에서 걸러진다**
- 넥슨 계정 번호(ouid)는 **긴 16진 문자열**(`/^[0-9a-f]{24,}$/i`)로 알아본다.
  `ouid` 는 확정 키이고 `nickname` 은 동명이인이 있을 수 있어 확정 키가 아니다

이렇게 해서 "이 형식일 것이다" 라고 지어내지 않는다 (`CLAUDE.md` 3장 7번).
**형식이 확인되면 `KNOWN_PLAYER_SEGMENTS` 만 좁히면 된다.**

### 자동완성에서는 주소 결과만 준다

주소를 붙여 넣는 도중에 부분일치가 엉뚱하게 끼어들면 오히려 방해가 된다.
`isBarracksUrl(keyword)` 이면 주소로 찾은 결과가 있을 때 **그것만** 돌려주고, 없으면 빈 배열이다.
주소가 아니면 기존 닉네임 조회로 그대로 내려간다. Mock(`packages/mock/src/store.ts`)도 같은 규칙이라
두 모드의 응답이 어긋나지 않는다.

---

## D-164 — "래더에 반영된 경기" 판정을 **한 곳으로 모은다** (2026-08-28)

근거: `apps/web/lib/server/queries/ladderScope.ts` · `apps/web/lib/server/queries/records.ts` ·
`apps/web/lib/server/queries/leagues.ts`

> 문서가 없이 코드만 이 번호를 인용하고 있어 **감독관이 코드 주석의 근거만 옮겨 적었다**
> (2026-08-28). 새로 정한 사실은 없다. 원래 `D-161` 로 적혀 있었으나 번호가 겹쳐 옮겼다
> (`docs/TASK_LEDGER.md` 12절).

### 무엇이 틀어졌나

여러 화면이 각자 `redRatingUpdate: { not: null }` 을 적어 두고 있었다.
그 칸은 **우리 공식(D-145)이 계산해 넣는 값**이라, 3rd.supply 에서 미러링해 들여온 경기에는
들어 있지 않다 (D-153 — 원본 점수는 `redSourceRatingUpdate` 에 따로 있다).

실측: **supply 리그 경기 130,022건 중 `redRatingUpdate` 가 있는 것 98건.**

그래서 미러 적재 뒤에 세 군데가 한꺼번에 틀어졌다.

- 개인랭킹 평균킬이 전부 `0.0킬`
- 선수·클랜 상세정보의 평균킬이 `0킬` (869킬을 기록한 선수가 판당 0킬)
- 최근매치 요약(`20전 10승 10패`)과 연승/연패가 비어 보임

**같은 조건을 세 번 적어 두었기 때문에 한 번 고쳐도 나머지가 남았다.**
한 곳(`ladderScope.ts`)에서 정의하고 셋이 같이 쓴다.

### 미러 경기를 래더 경기로 보는 근거

원본은 **래더 경기만** 준다. 그래서 `origin = '3rd.supply'` 이면 래더 경기다.
추측이 아니라 **수집 범위에서 나오는 사실**이다 (`docs/NEXON_INGEST_SPEC.md`).

### 배치고사는 여기서 거르지 않는다

배치고사 경기는 `rating_update` 가 **0이지 `null` 이 아니다** (`CLAUDE.md` 3-B 7번).
"래더에 반영됐는가" 와 "점수가 움직였는가" 는 다른 질문이다.

### `OR` 충돌 주의

`ladderMatchWhere()` 는 `OR` 를 쓰므로 **다른 `OR` 와 같은 객체에 펼치면 덮어쓴다.**
그럴 때는 `withLadderMatch(where)` 로 감싸 `AND` 로 붙인다.
(감독관이 전 호출부 3곳을 점검해 `OR` 동시 사용 0건임을 확인했다.)

---

## D-166 — 지난시즌 카드는 **가져오고**, 그리는 방식은 **표에서 카드로** 바꾼다 (2026-08-28)

### 무엇을 했나

3rd.supply 의 **선수별 지난시즌 카드**(시즌 1~6)를 수집해 우리 `LeaguePlayerSeason` 에 넣고,
선수 상세 `지난시즌` 탭을 원본과 같은 **카드**로 다시 그렸다.
**과거 경기(Match)는 가져오지 않았다.** 이 작업의 범위가 아니다.

### 실측한 원본 경로 (2026-08-28)

```
GET /leagues/{leagueSlug}/players/{playerId}   → data.id 가 leaguePlayerId
GET /leagueplayers/{leaguePlayerId}/seasons    → 시즌 카드 배열
```

- 우리 계약(`packages/contract/src/endpoints.ts`)의 `/leagueplayers/:id/seasons` 가
  **원본과 같았다.** 계약을 고칠 필요가 없었다.
- 원본 화면 URL 은 `/league/{slug}/player/{playerId}/season` 이라 **playerId → leaguePlayerId**
  를 먼저 풀어야 한다. 그래서 선수당 요청이 2번이다.
- `/leagues/{leagueId}/ranks/players` 는 leaguePlayerId 를 **주지 않는다**(확인함).
  `/leagues/{slug}/clans/{clanSlug}/players` 는 주지만 **현재 클랜원만** 나와 무소속을 못 덮는다.
- `/leagues/{leagueId}/players/{playerId}` 는 404 다. **slug 를 써야 한다.**

### 실측한 응답

```json
{"message":"success","data":[
 {"id":51730,"season":6,"rank":2501,"rank_count":6934,
  "win":82,"lose":123,"win_rate":40,"kill":1396,"death":1864,"kd_rate":42.8},
 {"id":34547,"season":4,"rank":593,"rank_count":29991,
  "win":null,"lose":null,"win_rate":52.8,"kill":null,"death":null,"kd_rate":48.7}]}
```

**시즌마다 주는 필드가 다르다** — D-099 가 맞았다. 수집 14,441줄로 확인했다.

| 리그 | 시즌 | 줄 | `win/lose/kill/death` |
|---|---|---|---|
| supply | 6 | 4,628 | 있음 |
| supply | 5 | 3,468 | 있음 |
| supply | 4 | 989 | **전부 `null`** |
| supply | 3 | 866 | **전부 `null`** |
| supply | 2 | 419 | **전부 `null`** |
| supply | 1 | 294 | **전부 `null`** |
| daerule | 6 | 2,155 | 있음 |
| sanply | 6 | 1,622 | 있음 |

`rating`(래더)·`division`·소속·평균킬·MVP 는 **어느 시즌에도 없다.** 전부 `null` 로 둔다.

### `rank: 0` 은 **배치고사**다

원본은 `rank: null` 을 주지 않는다. 대신 **`0`** 을 준다 (14,441줄 중 `null` 0건).
숫자로 대조했다 —

| | 판수 최소 | 중앙값 | 최대 | 건수 |
|---|---|---|---|---|
| `rank = 0` (sanply) | 1 | 3 | **9** | 922 |
| `rank > 0` (sanply) | 10 | 28 | 598 | 700 |
| `rank = 0` (supply) | 1 | 4 | **9** | 1,280 |

`rank = 0` 은 **10판 미만**에서만 나온다. 배치고사 미완료다.

- **DB 에는 원본값 `0` 을 그대로 저장한다** (3-A 2번). 해석은 화면에서만 한다.
- 화면은 `rank === 0` 과 `rank === null` 을 똑같이 **`배치고사`** 로 그린다.
  그 줄에 `{모수}명중` 을 함께 쓰는지는 `[미확인]` — 순위가 없으므로 쓰지 않는다.

### 카드 표기 (원본 관측 · 모바일)

```
┌──────────────────────────────────────────┐
│ 서플라이공식리그                  시즌 6  │
│ ──────────────────────────────────────── │
│                     6,934명중     1위    │
│  967승 578패          승률     62.6%     │
│  16,875킬 10,605데스   킬뎃     61.4%    │
└──────────────────────────────────────────┘
```

- 어두운 남색(`--color-side` #1e293b) 배경 · 흰 글자 · 구분선 `--color-side-line`
- **PC 2열 격자 · 모바일 1열 전폭.** `.pc-container` 폭은 건드리지 않았다
- **없는 값은 줄 자체를 없앤다.** 0 이나 `-` 로 채우지 않는다 (D-099 · D-106)
- 승률·킬뎃 색은 기존 `rateClass` 를 그대로 쓴다.
  관측 대조 — 62.6%·63% 파랑(`rate-3`) / 58.4%·56.9%·55.8%·59.2% 주황(`rate-2`) **일치**
- **순위 색은 `[미확인]`** 이다. `1위`·`17위`·`30위`·`31위` 노랑 / `122위` 파랑으로 보였으나
  경계를 모른다. 임의로 100 같은 값을 만들지 않고 기본색으로 둔다
- 원본 카드에 **래더 줄이 없다.** SACLOUD 가 계산한 카드에는 있으므로 값이 있을 때만 붙인다

### 시즌 표기를 `Season N` → **`시즌 N`** 으로 고쳤다 (D-098 정정)

원본 카드가 `시즌 6` 이었다. `Season N` 은 관측 전의 추정이었다.
표기는 여전히 `seasonLabel()` **한 곳**에서만 정한다. 베타는 원본에 없는 우리 시즌이라
이름도 우리 것(`Beta Season`)을 그대로 둔다.

### `Season.startedAt` 은 **합성값**이다 `[미확인]`

원본은 과거 시즌의 시작·종료 날짜를 주지 않는다. `Season.startedAt` 은 NOT NULL 이고
화면 정렬이 이 값을 쓴다(D-098). 그래서 **명백히 합성인** 고정 기준점을 쓴다 —
`2000-01-01 + (N-1)년`. 추측한 날짜처럼 보이지 않게 일부러 현실과 먼 값을 골랐다.
순서(1 → 6 → 현재)만 맞으면 화면은 정확하고, 날짜는 어디에도 표시되지 않는다.

### 명령

```bash
pnpm --filter @sacloud/worker nexon supply-seasons [--league <slug>] [--limit N] [--dry-run]
pnpm --filter @sacloud/worker nexon supply-seasons-import [--league <slug>] [--confirm]
```

- 수집은 **중단 후 재개**된다. `<base>.leagueplayers.jsonl`(색인) ·
  `<base>.seasons.jsonl`(원본 응답) 에 덧붙이고, 이미 있는 선수는 건너뛴다
- `--dry-run` 은 **요청을 한 건도 보내지 않고** 예상 요청 수만 낸다
- import 는 **네트워크를 쓰지 않는다.** `--confirm` 없이는 한 줄도 쓰지 않고,
  `DATABASE_URL` 이 로컬이 아니면 거부한다
- 같은 자리에 `imported = false` 인 행(우리가 계산한 카드)이 있으면 **덮어쓰지 않는다**

### 수집 실적 (2026-08-28)

| 리그 | 대상 선수 | 카드 있는 선수 | 시즌 줄 | 실패 |
|---|---|---|---|---|
| supply | 10,329 | 5,514 | 10,664 | 0 |
| daerule | 4,157 | 2,155 | 2,155 | 0 |
| sanply | 15,312 | 1,622 | 1,622 | 0 |
| **합** | **29,798** | **9,291** | **14,441** | **0** |

요청 59,596건 · 동시성 10 · 요청 간격 130ms. 429·5xx 0건.
`DB 에 없음` **0명** — 수집 대상 전원이 우리 `LeaguePlayer` 와 이어졌다.

---

## D-162 — 프로필 수집 대상은 `origin` 이 아니라 **`sourcePlayerId` 가 있는가** (2026-08-28)

### 무엇이 틀렸나

D-161 의 프로필 수집을 **`origin='3rd.supply'` 인 선수만** 대상으로 잡았다.
그래서 넥슨 경기 수집이 먼저 만든 행(`OBS-` · `origin='nexon'`)이 통째로 빠졌고,
그 선수들은 **닉네임이 옛것이고 소속이 비어** 있었다.

사용자가 운영 화면에서 지적한 것이 그 결과다 — 선수 `huwho`(원본 id 1561236212).

```
원본     huwho · 소속 Valiant · 공식 262전 · 열산 1,459전
우리     후후시치 · 소속 없음
```

기록이 없는 게 아니었다. **이름이 옛것이라 다른 사람처럼 보인 것**이다
(그 행에는 참가 기록이 1,699건 있었다).

기준은 출처가 아니라 **원본에 물어볼 id 가 있는가** 다.
`playerProfileTargets` 와 `applySupplyPlayerProfiles` 에서 `origin` 필터를 걷어냈다.
개발 시드(`origin='mock'`)만 명시적으로 뺀다 — 시드 값을 원본 값으로 덮지 않기 위해서다.

> **`OBS-` 963명 중 실제로 받을 수 있는 사람은 236명뿐이다.**
> 나머지 727명은 `sourcePlayerId` 가 아예 없다 — 넥슨 상세가 닉네임만 주기 때문이다(D-132).
> 물어볼 id 가 없으므로 이 잡의 범위가 아니다. 잇는 것은 `supply-players`(D-132) 의 일이다.

### 닉네임 — 원본은 **두 개**를 들고 있다 (실측 2026-08-28)

이 부분은 지시받은 전제와 실측이 달라, 재어 보고 적는다.

| 원본 경로 | 주는 이름 |
|---|---|
| `/players/{id}` · `/clans/{slug}/players` | **현재** 닉네임 |
| `/leagues/{id}/matches/{matchId}` 라인업 | **그 경기 당시** 닉네임 (바뀌지 않는다) |

실측: 선수 `1006751513` 은 프로필이 `@이재모피자@` 인데
2026-07-09 경기 라인업을 지금 다시 불러도 여전히 `미운정고윤정` 이다.

우리 `Player.name` 은 미러 수집이 **라인업에서** 가져온 값이라 옛 이름이 많았다
(21,343명 중 **11,457명(54%)** 이 현재 닉네임과 달랐다).

**그래서 `Player.name` 을 현재 닉네임으로 맞춘다.** 근거는 두 가지다.

1. 사용자가 보는 곳(프로필 헤더 · 검색 · 소속)은 현재 이름이어야 한다.
2. `Player.name` 은 어차피 **한 개**라 모든 경기의 당시 이름이 될 수 없다.
   1,699경기를 뛴 선수의 이름이 그중 임의의 한 경기 값으로 박혀 있었다.

**대신 남는 문제를 숨기지 않는다** — 경기 상세 라인업이 이제 현재 이름을 그린다.
원본은 그 자리에 **당시 이름**을 그린다. `MatchPlayerStat` 에는 닉네임 스냅샷 칸이 없다
(클랜은 `matchTimeClan*` 로 있는데 닉네임은 없다 — D-131 이 클랜만 다뤘다).
칸을 만들려면 마이그레이션이 필요해 **이번 범위 밖**이다.
당시 이름은 미러 수집 파일(`supply-mirror-*.matches.jsonl`)에 그대로 남아 있으므로
칸이 생기면 네트워크 없이 채울 수 있다. **후속 과제로 남긴다.**

옛 이름 자체는 `NexonNickname`(닉네임 관측 이력)에도 남아 있어 사라지지 않는다.

### 소속 클랜 — 프로필 값이 **1순위**, D-160 이 보조

`raw.clan` 이 그 선수의 **현재 소속**이다. 리그와 무관한 전역 값이다.

- 프로필에 클랜이 있으면 그 값을 쓴다. **D-160**(최신 경기의 참가자 clan 에서 되짚는 것)보다 정확하다.
- 프로필에 클랜이 없으면 **기존 값을 그대로 둔다.** D-160 을 지우지 않는다.
  원본의 `clan: null` 이 "무소속" 인지 "모름" 인지 응답이 말해 주지 않기 때문이다 `[미확인]`.
  이 때문에 원본은 `소속 없음` 인데 우리는 클랜을 보여 주는 선수가 **26명** 생긴다.
  (프로필이 클랜을 알려 준 5,655명은 **전원 우리 값과 일치**했다 — 어긋난 사람 0명.)

### 클랜 행이 없으면 만든다 — 마크까지

선수가 열산리그에만 등록된 클랜 소속일 수 있다. 그 클랜이 우리 리그에서 팀으로 뛴 적이
없으면 `Clan` 행이 아예 없고, 그러면 소속이 `없음` 으로 나온다.
**수집 파일이 준 값만으로** 행을 만든다 — `id`→`sourceClanId` · `name` · `slug` ·
`mark_bg` · `mark_front`. `establishedAt` · `notice` · `masterPlayerId` 는 주지 않으므로 비운다.

**마크를 반드시 함께 넣는다.** `toClanSummary` 는 `sourceClanId` 가 있는 클랜만 실제 마크를
내보내고(D-146), 마크가 비면 화면이 우리 fallback 을 그린다 — 사용자가 지적한 그 상태다.

이미 있는 클랜은 **이름을 바꾸지 않는다.** 비어 있는 칸(`sourceClanId` · 마크)만 채운다.
클랜 이름·랭킹 집계는 `supplyRollup` 의 몫이다.

### 화면은 고치지 않았다

`ProfileHeader` · `LeagueRecordHeader` 는 **원래부터 `ClanMark` 와 클랜명을 그리고 있었다.**
비어 있던 것은 데이터뿐이다. 기록실 사이드 `상세정보` 의 `소속` 은 이름만 쓰는데,
2026-08-27 대조에서 원본과 같다고 확인된 모양이라 마크를 덧붙이지 않는다 (3장 3번).

### 숫자 (2026-08-28)

```
프로필 수집     21,107 → 21,343   (+236 · 실패 0)
닉네임 갱신     11,457명
소속 채움        1,163명   (Player.clanId 있음 5,462 → 6,604)
클랜 생성          438개   (Clan 495 → 933 · 마크 있는 클랜 462 → 922)
표본 10명 대조   이름·소속·마크 9/10 일치
                 (1건은 프로필이 `clan: null` 이라 D-160 값을 남긴 위 26명 중 하나)
재실행           값 변경 0 · 이미 같음 21,343  (idempotent)
```

### 남은 것

- **경기 라인업의 당시 닉네임** — 위 참조. 스냅샷 칸이 없다.
- **같은 이름을 쓰는 행 385쌍(780행)** — 대부분 신원 분리다.
  `huwho` 도 `sourcePlayerId` 없는 짝(참가 3건)이 따로 있어 검색에 둘이 잡힌다.
  서든어택 닉네임은 유일하므로 같은 이름 두 행은 같은 사람일 가능성이 높다.
  **여기서 합치지 않는다** — 근거로 잇는 것은 `supply-players`(D-132)의 일이다.
- `huwho` 의 supply `LeaguePlayer` 가 `-2전 -1승 -1패` 다. **이 작업과 무관하다**
  (프로필 적재는 `LeaguePlayer` 를 건드리지 않는다). 집계 쪽에서 봐야 한다.

---

## D-168 — 신규 경기 반영 **30분 → 5분**: 적응형 폴링 + 증분 집계 (2026-08-28)

> **번호 정정**: 처음에 `D-167` 로 적었는데 폼그래프 작업이 같은 번호를 가져갔다.
> 그쪽은 `packages/ui` · `packages/contract` · `apps/web` 여러 파일에서 이미 그 번호를
> 인용하고 있어, **인용이 적은 이쪽이 `D-168` 로 옮겼다** (D-165 때와 같은 규칙).
> 새로 정한 사실은 없다.

사용자 요구: *"30분마다 들어오는건 너무 오래걸리는거 아닌가?"* → *"5분내로 뜨면 좋겠는데"*

주기만 6배로 당기면 원본을 6배로 두드리게 된다. 그건 3-A 5번 위반이다.
**주기를 당기면서 요청은 줄이는** 것이 이 작업의 조건이었다.

### 병목 두 개

| | 무엇이 | 왜 낭비인가 |
|---|---|---|
| 1 | 사이클마다 등록 클랜 **190개 전부**의 경기목록 + 클랜 show 190건 | 찾는 새 경기는 회당 **1.5건** |
| 2 | 집계가 **전수 재계산** | 새 경기 1건 때문에 36만 경기를 다시 훑는다 |

### 고친 것

**① 요청 낭비 — 적응형 폴링 (`apps/worker/src/lib/supplyPollingPolicy.ts`)**

개념을 새로 만들지 않았다. 넥슨 수집의 적응형 폴링(D-049)과 **같은 개념**이다 —
활동량으로 티어를 나누고 티어마다 주기를 다르게 준다. 이름도 그대로 쓴다.
다른 점은 둘이다.

1. 활동량을 **조회해서 배우지 않는다.** 클랜의 마지막 경기 시각을 우리 DB 가 이미 안다.
2. **상태를 저장하지 않는다.** GitHub Actions 는 매번 빈 작업공간이라 남는 것이 없다.
   대신 시계에서 계산한다 — `사이클번호 % (주기/사이클) == 해시(slug) % (주기/사이클)`.

`/clans/{slug}/show` 190건도 없앴다. 그 값(`league_clan` 원본 id)은
`LeagueClan.sourceLeagueClanId` 에 이미 있다 (3-A 3번).

**② 전수 재계산 — 증분 집계 (`packages/db/ops/supplyRollup.ts`)**

위험한 것은 "증분 = 더하기" 로 만드는 것이다. 같은 경기가 두 번 들어오면 값이 두 배가 되고
한 번 어긋나면 원인을 못 찾는다. 그래서 **더하지 않는다** —
영향받은 선수를 골라내기만 하고, 그 선수의 값은 **그 리그 전 경기에서 처음부터 다시 만든다.**
누적 함수는 전수 경로와 **같은 것 하나**를 쓴다. 그래서 증분 결과는 전수와 같을 수밖에 없다.

전수 경로는 **없애지 않았다.** 하루 1회 별도 워크플로(`supply-rollup-full.yml`)로 남겨
정합성 보정만 맡긴다. `ingestedAt` 이 없거나 뒤로 밀린 행, 적재 없이 바뀐 경기처럼
증분이 못 보는 경우를 되돌려 놓는 길이다.

### 이번 작업에서 **새로 고친** 결함

**적응형이 CI 에서 통째로 무효였다.** `--adaptive` 는 "이번에 볼 클랜" 을 클랜랭킹을 받기
**전에** 정하는데, 빈 작업공간에서는 그 시점의 클랜 목록이 비어 있다. 그래서

- 고를 후보가 0개 → **한 클랜도 훑지 않는다**
- `clanFactsFromDb` 도 빈 목록을 받아 → `/clans/{slug}/show` 190건이 그대로 나간다

즉 두 절약이 **둘 다** 무효였다. 워크플로에 `--adaptive` 를 켜는 순간 수집이 0건이 됐을 것이다.
등록 클랜 모집단을 `LeagueClan.placement=false` 에서 되살려 고쳤다.
**값**(점수·승패·부리그)은 되살리지 않는다 — 출처는 여전히 클랜랭킹 응답 하나뿐이다 (D-157).
클랜랭킹에서 **처음 본 클랜**은 차례를 기다리지 않고 그 사이클에 바로 훑는다.

**원본 `league_clan` id 를 이미 있는 행에는 버리고 있었다** (3-A 3번 위반).
클랜랭킹 응답은 사이클마다 그 값을 들고 오는데, 집계는 행을 **만들 때만** 넣고
이미 있는 행에는 쓰지 않았다. 그 칸이 비면 수집이 클랜마다 `/clans/{slug}/show` 를
다시 물어야 한다 — 실측으로 **`supply` 등록 클랜 49개 중 44개가 비어 있었다.**
비어 있을 때만 채우도록 고쳤다(값이 있는 행은 덮어쓰지 않는다). 로컬 실측 5/49 → **49/49**.
운영도 첫 사이클이 한 번 `/clans/show` 를 더 쓰고 나면 집계가 채워 **스스로 낫는다.**

**증분이 큰 창에서 죽었다.** 고른 선수의 리그 전 경기를 500명씩 한 번에 읽는데,
오래 뛴 선수는 한 명이 수천 경기다. 실측으로 프로세스가 죽었다 —
`daerule` 창에 걸린 경기 29,085건 · 영향 선수 4,094명 →
`memory allocation of 1605648 bytes failed`. 배치를 500 → 100 으로 내렸다.
평소 사이클(영향 선수 수십 명)에서는 어차피 한 배치라 차이가 없다.
창에 리그의 1/4 이상이 걸리면 "이 규모면 `--full` 이 낫다" 고 경고한다.

### 왜 cron `*/5` 가 아닌가

GitHub Actions 예약은 정시성이 나쁘다 — 밀리고, 심하면 그 회차가 버려진다.
게다가 회차마다 체크아웃·`pnpm install`·`prisma generate` 에 1~2분이 든다.
5분 주기면 그 고정비가 전체의 30%다.

그래서 **한 번 실행 안에서 5분 간격으로 6사이클**을 돈다 (30분마다 시작, 27분 예산).
준비는 한 번만 하고 사이클 간격은 GitHub 이 아니라 우리 시계가 정한다.
예약이 밀려도 데이터가 새지 않는다 — 수집은 늘 목록 맨 앞부터 보고, 집계 창은 24시간이라 겹친다.

사이클 하나가 실패해도 **루프를 죽이지 않는다.** 죽이면 남은 25분치 신선도를 통째로 버린다.
파이프라인이 idempotent 라 다음 사이클이 같은 일을 다시 한다.
대신 실패를 세어 **잡은 마지막에 실패시킨다** — 결과까지 삼키지는 않는다.

### 실측 — 요청량 (로컬 DB · 최근 14일 · 등록 클랜 190 · 경기 2,209건 · 5분 사이클 4,032회)

`apps/worker/src/dev/pollSim.ts` 로 쟀다. 실제 경기 시각을 그대로 쓴 시뮬레이션이다.

| 요청 종류 | 지금 (30분) | 새 설계 (5분) |
|---|---:|---:|
| 클랜 경기목록 | 380 /시간 | **328** /시간 |
| 클랜 show | 380 /시간 | **0** /시간 |
| 클랜랭킹 페이지 | 28 /시간 | 168 /시간 |
| 경기 상세 | 7 /시간 | 7 /시간 |
| **합계** | **795 /시간** | **502 /시간** (−37%) |

- 훑는 클랜: 190/사이클 → **평균 27.3** · p95 46 · 최대 56
- 사용자가 말한 "190건/30분 = 380/시간" 은 **클랜 경기목록만** 센 숫자다.
  그 항목끼리 비교해도 380 → 328 로 줄었다. 전체로 보면 795 → 502 다.
- 클랜랭킹만 늘었다(28 → 168). 그 목록이 클랜 점수·승패·부리그의 **유일한 출처**라
  (D-157) 사이클마다 받지 않으면 클랜랭킹이 그 자리에 얼어붙는다. 늘어난 140건은
  없어진 380건 안에서 상쇄된다.
- "클랜 show 0" 은 `LeagueClan.sourceLeagueClanId` 가 채워져 있어야 성립한다.
  운영에 처음 올릴 때 비어 있으면 **첫 사이클만** 그만큼 더 쓰고, 그 사이클의 집계가
  값을 채운 뒤부터 0 이 된다. 배포 전 `apps/worker/src/dev/corruptCheck.ts` 로 확인한다.
- `League.sourceLeagueId` 가 비면 리그 id 를 원본에 묻는다. 다만 그 값은 첫 사이클이
  체크포인트 파일에 적어 두므로 **실행당 1건**(시간당 2건)이고, 표에서는 무시했다.
  실측: `supply` 만 비어 있다(값은 `1`). 한 줄 갱신이라 **운영 데이터 변경**이므로 손대지 않았다.

### 실측 — 조용한 클랜을 놓치지 않는 보장

티어는 **굶기지 않는다.** 위상이 slug 해시로 고정돼 어떤 클랜이든 자기 주기마다
**정확히 한 번** 차례가 온다. 가장 낮은 `dormant` 도 24시간에 한 번은 반드시 본다.

> 시뮬레이션 실측: **최장 미조회 24.0시간** (세 리그 전부). 상한에 걸려 미룬 사례 0건.

상한(`SUPPLY_POLL_MAX_CLANS`)에 걸려 미뤄져도 다음 차례에 다시 온다 — 영원히 빠지지 않는다.

### 실측 — 실제로 5분 안에 뜨는가

경기 2,209건 각각에 대해 "언제 발견되는가" 를 쟀다. 경기는 클랜 **둘**이 뛰므로
어느 쪽을 훑든 발견된다. 티어는 **발견된 경기**로만 매겼다 — 아직 못 찾은 경기가
그 클랜을 `hot` 으로 만들어 주는 순환 낙관을 피하려는 것이다.

| | 값 |
|---|---:|
| 5분 이내 | **94.7%** |
| 10분 이내 | 96.5% |
| 30분 이내 | 99.5% |
| 중앙 | 2.6분 |
| p90 | 4.8분 |
| p99 | 24.4분 |
| 최대 | 118분 |

**5분을 넘기는 5%가 무엇인가** — 몇 달 조용하던 클랜(`cold`/`dormant`)이 다시 뛰기
시작하는 **그 첫 경기** 하나다. 그 경기를 찾는 순간 두 클랜 다 `hot` 이 되고,
그 뒤로는 5분마다 본다. 즉 **뒤따르는 경기는 전부 5분 안**이다.

더 당길 수는 있지만 요청이 는다. 같은 도구로 잰 대안 —

| 설정 | 요청/시간 | 5분 이내 | 최대 지연 |
|---|---:|---:|---:|
| **warm 30 / cold 360 (채택)** | **502** | **94.7%** | 118분 |
| warm 30 / cold 60 | 533 | 95.1% | 58분 |
| warm 15 / cold 360 | 612 | 96.6% | 118분 |
| warm 15 / cold 60 | 643 | 96.9% | 58분 |
| warm 10 / cold 60 / dormant 360 | 764 | 98.1% | 58분 |

채택한 값은 **지금(795)보다 확실히 적으면서** 목표를 만족하는 쪽이다.
꼬리를 더 줄이고 싶으면 `SUPPLY_POLL_COLD_MINUTES=60` 하나만 바꾸면 된다(+6% 요청).
값은 전부 `SUPPLY_POLL_*` 환경변수로 덮어쓴다 — 코드에 흩뿌리지 않았다 (3-B 6번과 같은 취지).

### 검증 — 증분과 전수가 **같은 값인가** (이 작업의 핵심)

`apps/worker/src/dev/rollupParity.ts`. 순서는
`전수 → 창에 걸리는 선수 값을 일부러 훼손 → 증분 → 체크섬 대조 → 증분 2회 더`.
체크섬은 `LeaguePlayer`(rating·win·lose·kill·death·assist·headshot·clanId·placement) +
`LeagueClan`(rating·win·lose·division·placement) + `Player.clanId` 전부를 md5 로 묶은 것이다.

| 리그 | 미러 경기 | 전수 | 증분 | 체크섬 | idempotent |
|---|---:|---:|---:|---|---|
| supply | 130,022 | 121.4초 | **15.4초** | `2960042d…` = `2960042d…` **일치** | PASS |
| daerule | 29,697 | 45.8초 | **7.5초** | `67711592…` = `67711592…` **일치** | PASS |
| sanply | 202,727 | 149.1초 | **39.1초** | `6ccadcf8…` = `6ccadcf8…` **일치** | PASS |

훼손 체크섬은 세 리그 모두 기준선과 **달랐다** — 즉 시험이 실제로 값을 망가뜨렸고
증분이 그것을 되살렸다. 증분을 두 번 더 돌려도 체크섬이 변하지 않았다.

> 창은 `--since` 로 **시각을 직접** 준다. 시간 창은 호출 시각 기준이라 같은 명령을 두 번
> 돌려도 경계가 밀리는데, 대조 실험에서는 그 밀림이 값 차이로 보인다 —
> 실제로 그렇게 거짓 FAIL 을 두 번 봤다.

### 반영까지 실제로 몇 분인가

`발견(중앙 2.6분) → 적재 → 증분 집계(리그당 8~40초)`.
세 리그를 순서대로 도는 사이클 하나가 새 경기가 있을 때 **1~3분**이다.
**대부분의 새 경기는 5분 안에 화면에 뜬다.** 남은 병목은 두 가지다.

1. 조용하던 클랜의 **첫 경기** — 위 표의 5%. 티어 주기 문제이고, 값으로 조절한다.
2. GitHub Actions 예약이 밀리는 구간 — 한 실행 안에서 6사이클을 돌아 영향을 줄였지만
   **없앤 것은 아니다.** 27분 예산이 끝나고 다음 실행이 늦게 뜨면 그 사이가 빈다.
   완전히 없애려면 상주 프로세스가 필요하다. 지금 구조에서는 못 없앤다.

### 손대지 않은 것

- **원본 응답·원본 id 를 그대로 보존한다** (3-A 1·3번). 수집 파일 구조를 바꾸지 않았다.
- 동시 요청 3 · 간격 250ms · exponential backoff 그대로다.
- **D-164 함정 점검**: 집계 경로(`packages/db/ops/supplyRollup.ts`)는
  `redRatingUpdate` 를 조건으로 쓰지 않는다. 모집단을 `origin='3rd.supply'` 로 잡으므로
  미러 경기 36만 건이 빠지는 결함은 **없다.**
  다만 그 반대 방향은 **의도된 경계**다 — 우리 공식이 만든 경기(`origin != '3rd.supply'`)는
  이 집계에 들어오지 않는다. 시즌7 은 전부 미러 경기라 지금은 문제가 없고,
  그 리그에 우리 경기를 섞게 되면 그때 다시 봐야 한다 (D-153).

---

## D-167 — 선수 프로필: 승률 도넛을 **최근 6개월 월별 킬뎃 그래프**로 교체 (2026-08-28)

### 요구 (사용자 지시 원문)

> "6개월 단위로 폼이 상승중인지 하락중인지 그래프로 보여줘. 저 원 그래프 빼버리고
> 최근 6개월간의 폼을 한달 단위로 보여주고, 최근 10경기 폼을 보고
> '최근 퍼포먼스가 하락중입니다' / '최근 꾸준한 퍼포먼스를 보여줍니다' /
> '최근 폼이 급상승중입니다' 이렇게 세개로 나눠서 멘트쳐줘.
> 그래프는 한달단위의 킬뎃으로 보여줘."

**원본(3rd.supply)에는 이 블록도 이 판정도 없다.** 아래 전부에
"원본과 동일함이 검증되지 않음" 이 붙는다 (CLAUDE.md 3장 7번).

### 무엇을 바꿨나

- **선수 기록실**(`/league/{slug}/player/{id}`)의 승률 도넛을 **뺐다.**
  그 자리를 `최근 폼`(6개월 월별 킬뎃 꺾은선 + 판정 한 줄)이 대신한다.
- **클랜 기록실의 도넛은 그대로 둔다.** 사용자가 바꾸라고 한 것은 선수 프로필뿐이다.
  `RecentMatchSummary` 에 `form` 을 넘긴 쪽(선수)만 도넛이 사라진다.
- 도넛이 보여 주던 승률 숫자는 바로 옆 `20전 16승 4패 (80%)` 에 그대로 있다.
  **사라지는 정보는 없다.**

### 두 기준이 다르다 — 통일하지 않는다

| | 기준 | 답하는 질문 |
|---|---|---|
| 그래프 | 최근 **6개월** · 한 달 단위 | 어떻게 흘러왔나 |
| 판정 문구 | 최근 **10경기** vs 직전 **30경기** | 지금 어떤가 |

사용자가 그렇게 지시했다. 하나로 맞추면 둘 중 하나의 질문이 사라진다.

### 판정 경계값 — `FORM_TREND_THRESHOLD_PP = 5` (%p)

`packages/contract/src/form.ts` **한 곳**에만 있다. 코드에 흩뿌리지 않았다.

비교 대상은 **최근 10경기 바로 앞 30경기**다. 전체 누적과 견주지 않는다 —
3년 치 평균과 비교하면 그 선수의 "예전" 이 아니라 "평생" 과 비교하는 것이 되어,
최근 반년간 실력이 변한 선수가 영원히 상승/하락으로 굳는다.

**근거 (2026-08-28 로컬 DB 실측 · `서플라이공식리그` 상위 400명)**

`(최근 10경기 킬뎃) − (직전 30경기 킬뎃)` 의 분포:

```
평균 -0.61%p · 표준편차 5.33%p
백분위  5%:-9.2   25%:-4.1   50%:-1.1   75%:+2.8   95%:+8.9
```

그런데 10경기는 표본이 작다. 한 경기 20교전으로 잡으면 킬뎃의 이항 표준오차가
최근 구간 ≈4.0%p, 비교 구간 ≈2.3%p → 두 구간 차이의 표준오차 ≈4.6%p 다.
**관측된 5.33%p 의 거의 전부가 표본 흔들림**이라는 뜻이다.
그보다 작은 차이를 "상승/하락" 이라 부르면 난수를 읽어 주는 것이 된다.

경계별로 400명이 이렇게 갈린다:

```
±3.0 → 급상승 23.0% / 꾸준 43.5% / 하락 33.5%
±4.0 → 급상승 18.0% / 꾸준 55.8% / 하락 26.3%
±5.0 → 급상승 14.8% / 꾸준 65.8% / 하락 19.5%   ← 채택 (관측 표준편차 1개)
±6.0 → 급상승 11.3% / 꾸준 74.0% / 하락 14.8%
```

`급상승` 은 강한 말이라 흔하면 안 된다. 15% 가 그 말에 맞는 빈도다.

### 경기가 부족한 선수 — **셋 중 하나로 밀어 넣지 않는다**

사용자가 준 문구는 셋이지만 **네 번째 경우가 실제로 존재한다.**
3경기 뛴 선수에게 `꾸준한 퍼포먼스` 라고 말하면 그건 거짓이다 (D-106).

```
최근 10경기 미만            → unknown → "최근 폼을 판정할 경기가 부족합니다"
직전 구간 10경기 미만        → unknown  (상수 FORM_MIN_BASELINE_GAMES)
구간의 킬+데스가 0           → unknown  (0% 라고 말하지 않는다)
```

`unknown` 일 때는 판정 근거 숫자도 붙이지 않는다. 근거가 없기 때문이다.

### 그래프의 결측 — 0 으로 채우지 않는다 (D-106)

- 경기가 없던 달은 `kd_rate = null` 이다. **점을 찍지 않고 선을 끊는다.**
  빈 달을 건너뛰어 이으면 "쉬는 동안에도 꾸준했다" 는 없는 사실이 그려진다.
- 눈금에는 `알수없음` 이라고 쓴다. `0%` 도 `-` 도 아니다.
- 여섯 달이 **통째로** 비면 그래프 대신 `최근 6개월간 기록된 경기가 없습니다.` 한 줄만 쓴다.
  (예: 최근 6개월에 경기가 없지만 그 이전 기록으로는 판정이 되는 선수가 실제로 있다.
  그때 그래프는 없고 판정 문구는 나온다.)
- K/D 를 모르는 참가 기록은 **집계에서 아예 뺀다** (D-148). `kill ?? 0` 으로 더하면
  판수만 늘고 킬뎃이 0 쪽으로 끌려 내려간다. 미러 경기에 그런 기록이 섞여 있다.

### 모집단은 `ladderMatchWhere()` 다 (D-164)

`redRatingUpdate: { not: null }` 로 잡으면 **미러링해 온 3rd.supply 경기가 통째로 빠진다.**
그 칸은 우리 공식이 채우는 것이라 미러 경기에는 없다
(실측: supply 리그 경기 13만 건 중 98건). 이 함정으로 예전에 전 선수 평균킬이
`0.0킬` 로 나갔다. 폼 그래프는 **미러 경기 위에서 그려지므로** 여기 걸리면 6개월이 통째로 빈다.

달 경계는 **KST** 다. UTC 로 나누면 매달 1일 00:00~09:00 경기가 전달로 샌다.

### 실측 검증 (2026-08-28 · 로컬 DB · `서플라이공식리그`)

```
[피존]      누적 40,867킬 40,303데스 = 킬뎃 50.3%
  3월 51.8%(207판) 4월 55.1%(176판) 5월 53.5%(133판)
  6월 52.5%(100판) 7월 55.2%(19판)  8월 67.5%(6판)
  최근10전 60.3% vs 직전30전 55.2% → Δ+5.1%p → "최근 폼이 급상승중입니다"

[현누]      누적 22,166킬 23,537데스 = 킬뎃 48.5%
  3월 49.4%(164판) 4월 48.5%(123판) 5월 52.6%(33판)
  6월 52.5%(36판)  7월 52.5%(40판)  8월 46.9%(28판)
  최근10전 43.6% vs 직전30전 50.5% → Δ-6.9%p → "최근 퍼포먼스가 하락중입니다"

[bom]       누적 49,749킬 48,087데스 = 킬뎃 50.8%
  3월 50.3%(381판) 4월 48.8%(220판) 5월 50.7%(210판)
  6월 51.1%(112판) 7월 45.9%(16판)  8월 알수없음(0판)   ← 빈 달
  최근10전 47.1% vs 직전30전 47.9% → Δ-0.8%p → "최근 꾸준한 퍼포먼스를 보여줍니다"

[건뭉]      가운데가 빈 사례 (선이 끊긴다)
  3월 47.0%(178판) 4월 47.6%(115판) 5월 50.9%(49판)
  6월 알수없음(0판) 7월 44.9%(39판)  8월 42.5%(7판)
  최근10전 40.9% vs 직전30전 46.0% → Δ-5.1%p → "최근 퍼포먼스가 하락중입니다"

[4bf567w3]  6개월 전부 빈 사례 — 그래프 없음, 판정은 나온다
  최근10전 46.8% vs 직전30전 51.3% → Δ-4.5%p → "최근 꾸준한 퍼포먼스를 보여줍니다"

[천부적인꼼띡이] 4경기뿐 → unknown → "최근 폼을 판정할 경기가 부족합니다"
[제니]           1경기(KD 있는 경기 0) → unknown
```

월별 값이 그 선수의 누적 킬뎃과 어긋나지 않는다 —
`bom` 은 매달 45~51% 이고 누적 50.8%, `피존` 은 51~67% 이고 누적 50.3%
(누적은 3년 치라 최근 상승분이 희석된다).

### 그리는 방법

**차트 라이브러리를 새로 깔지 않았다** (CLAUDE.md 7장 인프라 설치 원칙).
꺾은선 하나에 라이브러리는 과하다.

이 칸은 모바일 ~330px, PC ~800px 로 폭이 두 배 넘게 차이 난다.
`preserveAspectRatio` 를 맞춰 두면 PC 에서 세로가 같이 늘어나 배너처럼 커진다.
그래서 좌표계를 `0~100` 으로 두고 가로세로를 따로 늘린다(`none`).
그러면 원은 타원이 되고 선 두께가 방향에 따라 달라지므로 —

- **선만** SVG(`<polyline>` + `vector-effect="non-scaling-stroke"`)
- **점과 50% 기준선**은 HTML 을 절대 위치로 얹는다

높이는 예전 도넛과 같은 `h-28` 이다. **새 크기를 만들지 않았다.**
색도 새로 만들지 않았다 — 선/점은 매치 카드가 쓰는 `--color-win-bar`,
월별 킬뎃 숫자는 랭킹 표와 같은 `rateClass`(50/55/60/65),
판정 문구는 연승연패 문구와 같은 `text-win` / `text-lose` 다.

### 코드 위치

```
packages/contract/src/form.ts              상수 · judgeFormTrend · KST 달 키
packages/contract/src/entities/detail.ts   PlayerForm 스키마 · LeaguePlayerDetail.form
apps/web/lib/server/queries/playerForm.ts  DB 조회 (모집단 = ladderMatchWhere)
packages/mock/src/store.ts                 mock 쪽 같은 계산
packages/ui/src/record/formChart.ts        좌표 계산 (테스트 가능한 순수 함수)
packages/ui/src/record/formCopy.ts         네 문구 · 달 표기
packages/ui/src/record/PlayerFormPanel.tsx 렌더
packages/ui/src/record/RecordPanels.tsx    form 이 있으면 도넛을 그리지 않는다
```

`LeaguePlayerDetail.form` 은 `nullable().default(null)` 이다. 이 필드가 없던 응답과도
맞고, `null` 이면 화면이 폼 블록을 **그리지 않는다** — 도넛도 되살리지 않는다.
빈 자리를 무언가로 메우지 않는다.

### 광고

이 작업으로 광고 슬롯·placeholder·광고용 여백을 **어떤 형태로도 만들지 않았다**
(CLAUDE.md 4장).

### 바꾸려면

경계값을 바꾸려면 `FORM_TREND_THRESHOLD_PP` 와 **이 문서를 함께** 고친다.
회귀 테스트는 `packages/contract/src/__tests__/form.test.ts` ·
`packages/ui/src/__tests__/form-chart.test.ts` 에 있다.

---

## D-169 — 개인랭킹을 **통합 / 스나 / 라플** 로 나누고, 각 탭 위에 **폼 TOP3** 를 둔다 (2026-08-28)

### 배경

사용자가 명시적으로 지시한 **신규 기능 두 가지**다. 원본 3rd.supply 에는 둘 다 없다.
`CLAUDE.md` 3장 3번("임의 기능 추가 금지")의 예외다 — 임의가 아니라 지시받은 것이다.

> "개인랭킹 차트는 스나수와 라플수를 나누어 랭킹을 한다.
>  통합랭킹 / 스나랭킹 / 라플랭킹"
> "가져온 시즌7의 랭킹도 통합 스나 라플 로 분류할거라는것도 인지해줘"

적용 범위는 **모든 리그**다. 미러링해 온 시즌7 기록에도 그대로 적용된다.
(무소속리그의 누적 킬뎃 비공개는 별개 축이다 — D-107. 여기서 바뀌지 않는다.)

### 정한 것 ①  무기 축은 **기록만** 나눈다

`CLAUDE.md` 3-B 1번을 그대로 지킨다. **스나용·라플용 공식을 따로 만들지 않았다.**
통합 공식이 이미 계산해 둔 경기별 증감을 무기에 따라 나눠 담은
`LeaguePlayerWeaponStat.ratingDelta` 를 **읽기만** 한다.

| 탭 | 모집단 | 정렬 | 래더 칸에 쓰는 값 |
|---|---|---|---|
| 통합 | 배치 종료 선수 전원 | `LeaguePlayer.rating` 내림차순 | 통합 래더 (`rating`) |
| 스나 | 배치 종료 + `weapon=1` 기록 있음 + `knownStatGames>0` | `ratingDelta` 내림차순 | **그 무기 래더증감** |
| 라플 | 배치 종료 + `weapon=0` 기록 있음 + `knownStatGames>0` | `ratingDelta` 내림차순 | **그 무기 래더증감** |

통합 탭은 기존 `getPlayerRanks` 를 **그대로** 부른다. 코드 한 줄도 바꾸지 않았다.
`weapon` 파라미터가 없거나 모르는 값이면 `all` 로 떨어져 **기존 동작 그대로**다.

정렬 기준을 `ratingDelta` 로 잡은 것은 선수 프로필의 무기별 순위(`playerWeaponRankOf`, D-149)와
**같아야 하기 때문**이다. 기준이 다르면 프로필의 `3위 / 1,204명` 과 랭킹 목록의 3번째 줄이 어긋난다.

### 정한 것 ②  무기별 **절대 점수를 만들지 않는다**

`baseRating + ratingDelta` 같은 "스나 래더 점수" 를 지어내지 않았다. 두 가지 이유다.

1. 통합 래더와 **다른 축의 숫자**가 생긴다. 3-B 2번이 금지하는 방향이다.
2. `baseRating` 은 선수마다 다르다. 그러면 정렬 기준(`ratingDelta`)과 표시값(`base+delta`)이
   어긋나 표가 **뒤죽박죽 정렬된 것처럼** 보인다.

그래서 무기 탭의 마지막 칸은 머리글부터 `래더증감` 이고 값은 `+1,836점` 처럼 부호를 붙인다.
같은 자리에 다른 뜻의 숫자를 넣으면서 이름만 `래더` 로 두면 거짓말이 된다.

### 정한 것 ③  폼 TOP3 규칙 (사용자와 확정)

- **그날 하루 동안 얻은 래더 증감의 합**이 큰 순서로 3명
- **최소 3경기** 이상 한 선수만 후보
- 동점이면 **경기 수가 많은 쪽**이 위 (그 다음은 `playerId` 로 고정 — 화면이 흔들리지 않게)
- 표시 형식 `+48점 (5경기)`
- 랭킹 탭(통합/스나/라플)을 따라간다 — 탭을 바꾸면 폼 TOP3 도 그 축으로 다시 계산된다

### [미확인] — 원본이 알려주지 않아 우리가 정한 것

1. **"그날"의 기준 시각.** 한국 서비스이므로 **KST 자정**을 하루 경계로 정했다.
   원본과 동일함이 검증되지 않았다. (`kstDayRange` · `apps/web/lib/server/queries/rankings.ts`)
2. **대상 날짜는 "가장 최근에 경기가 있었던 날"** 이다. 오늘 경기가 있으면 곧 오늘이다.
   오늘 경기가 하나도 없을 때 빈 칸을 남기지 않으려는 폴백이고, 물러섰다는 사실은
   응답의 `is_today = false` 와 화면의 날짜 표기로 **드러낸다** — 감추지 않는다.
3. 무기 탭·폼 TOP3 의 **레이아웃**. 원본에 대응 화면이 없어 새로 만들었지만,
   모양은 부리그 탭(`DivisionTabs`)과 랭킹 표(`RankTable`)의 실측 치수를 **그대로** 재사용했다.
   새 크기를 추측하지 않았다 (`packages/ui/src/league/rankStyles.ts`).
4. 탭 상태를 URL 이 아니라 컴포넌트 상태로 둔다. 원본에 이 축의 URL 규칙이 없기 때문이다.

### 함정 — 미러 경기의 증감은 다른 칸에 있다 (D-164 · D-153)

`MatchPlayerStat.ratingUpdate` 는 **우리 공식(D-145)이 계산한 경기에만** 있다.
3rd.supply 에서 미러링해 온 시즌7 경기에는 비어 있고, 그 증감은 `sourceRatingDelta` 에 있다.
폼 TOP3 는 모집단이 거의 전부 미러 경기라, 폴백을 빠뜨리면 **화면이 통째로 빈다.**
그래서 집계는 `SUM(COALESCE(ratingUpdate, sourceRatingDelta, 0))` 이다.
Prisma `groupBy` 로는 두 칸을 합쳐 더할 수 없어 이 한 곳만 raw SQL 이다.
회귀 테스트(`apps/web/tests/weaponRanking.test.ts`)는 픽스처 경기를 **전부 미러 경기로** 만들어
이 함정을 고정한다 — 폴백이 빠지면 그 테스트가 먼저 깨진다.

### 항등식 실측 — `rating = baseRating + 스나 + 라플`

3-B 2번의 항등식을 운영 DB 전량으로 확인했다 (2026-08-28, **조회만**).

| 리그 | 대상 | 성립 | `baseRating = 3000` |
|---|---:|---:|---:|
| officialmain · secondline · friendly01 · tourney2026 (시드) | 1,350 | **1,350 (100%)** | 0 |
| supply | 10,388 | 11 | 10,134 |
| daerule | 4,157 | 0 | 4,157 |
| sanply | 15,312 | 0 | 15,312 |

시드 리그는 **한 명도 어긋나지 않는다** — 시드가 `baseRating = rating − Σdelta` 로 맞춰 넣기 때문이다.

미러 리그에서 어긋나는 것은 **이 작업 이전부터의 데이터 상태**이고 원인이 분명하다.
미러 적재는 `LeaguePlayer.rating` 에 **3rd.supply 가 보여 주던 값을 그대로** 넣고
(D-153), `baseRating` 은 기본값 3000 으로 둔다. 즉 그 `rating` 은 우리 공식이 만든 값이 아니라
원본 표시값이라 우리 증감 합과 이어지지 않는다.

**이 작업은 `rating` 도 `baseRating` 도 쓰지 않는다.** 무기 축은 읽기 전용이다.
통합 랭킹 상위 5명(`근면 3432 · chococake 3376 · 으어어어 3367 · kinder 3312 · mozz'a' 3303`)은
무기 축 도입 전후가 **완전히 같다** — 무기 분리가 통합 래더 값을 바꾸지 않았다.

미러 리그의 항등식을 실제로 성립시키려면 적재 쪽에서 `baseRating` 을 보정해야 한다.
그건 `apps/worker` 의 일이고 **여기서 하지 않았다.** 별도 과제로 남긴다.

### 계약 · 코드 위치

- 계약: `packages/contract/src/entities/league.ts` — `RankWeapon` · `RANK_WEAPON_CODE` ·
  `parseRankWeapon` · `FormTop` · `FormTopRow` · `FORM_TOP_MIN_GAMES` · `FORM_TOP_SIZE`.
  `PlayerRankRow` 에 `weapon` · `rating_delta` 를 **선택 필드**로 더했다
  (클랜원 목록 등 기존 소비자를 깨지 않기 위해서다)
- 엔드포인트: `leagueRankPlayers` 에 `weapon` 쿼리 추가 · `leagueRankForm` 신설(`designed`)
- 조회: `apps/web/lib/server/queries/rankings.ts`
- API: `apps/web/app/api/leagues/[league]/ranks/players` · `.../ranks/form`
- 화면: `packages/ui/src/league/RankWeaponTabs.tsx` · `FormTop3.tsx` · `rankStyles.ts` ·
  `apps/web/app/league/[leagueSlug]/rank/player/page.tsx`
- Mock: `packages/mock/src/store.ts` (`getPlayerRanksByWeapon` · `getFormTop`) · `handlers.ts`
- 테스트: `apps/web/tests/weaponRanking.test.ts` · `packages/mock/src/__tests__/rankWeapon.test.ts`

### 바꾸려면

정렬 기준을 바꾸면 `playerWeaponRankOf`(D-149)도 **같이** 바꾼다. 한쪽만 고치면
프로필의 순위와 랭킹 목록이 어긋난다. 폼 TOP3 의 상수는 `FORM_TOP_MIN_GAMES` ·
`FORM_TOP_SIZE` 하나뿐이니 하드코딩을 새로 만들지 않는다.

---

## D-170 — 미러가 덮는 기간은 **원본 점수가 정본**이다. 우리 공식으로 다시 계산하지 않는다 (2026-08-29)

### 배경

`supply` 리그에는 두 종류의 경기가 있다. 3rd.supply 에서 미러해 온 경기(`origin='3rd.supply'`)와
넥슨 재구성 경기(`origin='nexon'`)다. 그런데 넥슨 재구성 136건이 **전부 미러 구간 안**이었다.
같은 기간을 두 체계가 각각 채점하니 한 시즌에 서로 다른 두 점수가 섞였다.

실측 (2026-08-29 · `supply`)

| 점수 출처 | 인원 | 범위 | 평균 |
|---|---|---|---|
| 원본(3rd.supply) | 10,329 | 0 ~ 3,432 | 834 |
| 우리 공식 | 59 | 2,940 ~ 3,049 | 3,001 |

기준점이 3000 이라 **2판짜리 행이 원본 상위권을 통째로 밀어냈다** —
개인랭킹 상위 100 중 61명이 10판 미만이었다.

### 정한 것

미러 마지막 경기 시각까지는 **원본을 정본**으로 두고 우리 공식을 돌리지 않는다.
그 **뒤**의 넥슨 경기는 진짜 신규이므로 평소대로 계산한다.

`CLAUDE.md` 3-A 2번(기존 `rating_update` 를 추정 공식으로 덮지 않는다) ·
3-B 5번(과거 경기 재계산 금지)을 그대로 지키는 것이고, 새 정책이 아니라
**지켜지지 않던 것을 코드로 강제한 것**이다.

`rate` 가 이미 만들어 둔 잔재 집계 행은 `ratingGhostCleanup` 이 지운다.
되돌리기 대상은 **미러 뒤 넥슨 경기에 실제로 참가한 선수·클랜으로 한정**한다 —
범위를 넓히면 원본 점수를 건드리게 된다.

### 코드 위치

`apps/worker/src/jobs/rate.ts` (미러 구간 경계 · `skipped.mirror_covered`) ·
`apps/worker/src/jobs/ratingGhostCleanup.ts`

### 바꾸려면

미러 경계를 없애려면 **원본 점수를 무엇으로 대체할지 먼저 정해야 한다.**
경계만 풀면 두 체계가 다시 섞인다.

---

## D-171 — 시즌0: 미러 구간을 우리 공식으로 **읽기 전용 재계산**한다 (2026-08-29)

### 배경

D-170 으로 미러 구간을 계산 대상에서 뺐지만, 사용자는 **가져온 시즌7 기록도 우리 점수로**
보고 싶어 했다. 원본 점수는 판수 인플레가 있어(많이 뛴 사람이 위로 간다) 실력순이 아니다.

### 정한 것 ①  덮어쓰기가 아니라 **replay 후 별도 반영**이다

`runRate` 에 `matchScope`(origin + 기간)를 더했다. **`dryRun` 에서만 쓸 수 있다** —
아니면 예외를 던진다. 재계산은 DB 를 건드리지 않고 결과만 돌려주고,
화면 반영은 `season0Apply` 가 따로 한다. 원본 `Match.redRatingUpdate` 등은 **그대로 남는다.**

시즌0 창은 `2026-01-01 ~ 2026-07-01`(미러 구간)이고 origin 은 `3rd.supply` 다.
공식을 복제한 별도 시뮬레이터를 만들지 않았다 — 복제하면 언젠가 갈라진다 (D-150 과 같은 원칙).

### 정한 것 ②  범위를 지정한 replay 는 **시즌 종류 예외를 쓰지 않는다**

Beta 예외(1경기부터 래더 계산 · D-112)는 그 시즌을 **운영할 때**의 규칙이다.
임의의 창을 다시 재는 것은 운영이 아니므로 정식 규칙(배치고사 10경기)으로 잰다.

### 정한 것 ③  무기 분리는 **경기별 참가행에서** 나눈다

`collectStats: true` 면 replay 가 경기별 참가행 계산 결과를 함께 돌려준다.
그 행의 `MatchPlayerStat.weapon` 으로 `ratingUpdate` 를 스나/라플로 나눠 담는다.
**무기별 공식은 여전히 없다** (`CLAUDE.md` 3-B 1번). 자기 점검으로
`무기 증감 합 == 통합 증감` 을 세고(`weaponSplitMismatch`), 어긋나면 보고한다.

### 코드 위치

`apps/worker/src/jobs/rate.ts` (`matchScope` · `collectStats`) ·
`apps/worker/src/jobs/season0.ts` (창 정의 · 무기 분리 · 자기 점검) ·
`apps/worker/src/dev/season0Scope.ts`

### 바꾸려면

`matchScope` 의 dry-run 강제를 풀지 마라. 그것이 원본 점수를 지키는 마지막 방어선이다.

---

## D-172 — 점수 시스템 **v2** — 실데이터 백테스트로 갈아탄다 (2026-08-29)

### 배경

D-145 로 잠근 공식을 실데이터 14,408경기에 돌려 보니 두 가지가 걸렸다.
**강팀에 얹혀 간 선수가 더 받고**(개인 기대승률을 "내 점수 vs 상대팀 평균" 으로 재서),
**판수가 순위를 밀어 올렸다**(표시 점수에 신뢰도를 곱해서).

### 정한 것

| 항목 | v1 (D-145) | v2 |
|---|---|---|
| 개인 기대승률 | 내 점수 vs 상대팀 평균 | **팀 평균 대 팀 평균** · 증감은 출전 10명에게 동일 분배 |
| 일방적 경기 억제 | 0.80~0.86 ramp | **없앤다** — "이겼는데 0점" 을 만들지 않는다 |
| 표시 점수 신뢰도 | `×min(1,√(g/150))` | **곱하지 않는다** |
| 승률 자격선 | 밴드별 · 부족분 1%p 당 −20 | **없앤다** (억제를 빼면 필요 없다) |
| 개인 K | 50 고정 | **판수 구간별** 0판 40 · 30판 28 · 100판 20 |
| 클랜 구성 보정 | 가산 (상한 +50) | **곱셈** — 본클랜원 5명 100% · 4명 90% · 3명 75% · 2명 55% · 1명 30% |

**본클랜원 가중치는 클랜 증감에만 곱한다. 개인 점수에는 걸지 않는다** —
용병으로 뛰어도 잘하면 잘하는 것이다. 근거는 데이터다: 그 경기 본클랜원이
0명일 때 예측 정확도 57.1% → 4명일 때 70.3% 로 **단조 증가**했다.
이 방식이면 열산 같은 리그를 따로 분류하지 않아도 구성으로 자동 감쇠한다.

### 검증 (백테스트 · `ratingBacktest`)

같은 코드로 상수만 바꿔 나란히 돌렸다 (`V2_RATING_CONSTANTS`). **전 항목에서 v2 가 우세**다.

| 지표 | v1 | v2 |
|---|---|---|
| 클랜 승패 예측 | 53.72% | **54.06%** |
| 개인 승패 예측 | 57.48% | **58.22%** |
| 판수 ↔ 순위 상관 | 0.155 | **0.113** |
| 제로섬 오차 | 12,290 | **7,632** |

### 같이 고친 것 — 평균킬 분모

평균킬이 `0.29킬` 처럼 나왔다. 분자는 시즌0 집계인데 **분모가 시즌0 밖 경기까지** 세고 있었다.
분모는 **분자와 같은 집계에서 나온 판수**를 쓴다.

### 코드 위치

`packages/rating/src/constants.ts`(`RatingV2Flags` · `V2_RATING_CONSTANTS`) ·
`formula.ts` · `match.ts` · `apps/worker/src/jobs/ratingBacktest.ts` ·
`apps/web/lib/server/queries/leagues.ts`(평균킬 분모)

### 바꾸려면

v2 는 **스위치**다. 플래그를 다 끄면 D-145 그대로 동작한다. 두 시스템을 비교할 때
공식을 복제하지 말고 상수만 바꿔 같은 코드로 돌린다 — 복제한 시뮬레이터는 언젠가 갈라진다.

---

## D-173 — 미참여 감점 정상화 · 무기 랭킹은 **주무기만** · 감점은 무기 증감에도 (2026-08-29)

### ① 미참여 감점이 **한 번도 작동한 적이 없었다** (버그)

감점 커서를 매 경기 시각으로 밀고 있었다. 경기가 하루 86건씩 촘촘해 호출 간격이 몇 분뿐이라
**"하루가 지났는가" 가 영원히 거짓**이었다. 실측: 181일치를 재생했는데 **진행한 날 0일 ·
감점 받은 선수 0명**. 커서를 **넘긴 하루 경계까지만** 옮기고 자투리는 다음 호출에 넘기도록 고쳤다.

강도도 바꿨다 (사용자 지시 — "한 달 안 하면 거의 기본점수").

| | v1 (D-145) | v2 |
|---|---|---|
| 대상 | 표시 4000 이상만 | **전원** |
| 유예 | 7~10일 | 7일 (`decayGraceDays`) |
| 속도 | 구간별 주 −20~−60 | **30일이면 기준점(3000)** (`decayToBaselineDays`) |
| 복귀 회복 | 경기당 8점 고정 | **남은 감점의 1/5 씩** (`decayRecoveryGames: 5`) |

고정 8점이면 1,900점짜리 감점을 지우는 데 237판이 걸려 **영구 낙인**이 된다.
비율 회복으로 바꾸되 한 판에 다 사라지지는 않게 했다 — "한 판 던지고 초기화" 는 여전히 막는다.
감점만으로 기준점 아래로 내려가지도 않는다.

### ② 무기 랭킹은 **주무기만** 오른다

`LeaguePlayerWeaponStat.isMain` 을 더했다 — 그 무기로 뛴 판수가 **전체의 절반 이상**이면 참이다.
없으면 라플수가 어쩌다 든 스나 몇 판으로 스나 랭킹에 올라온다.
기록 자체(판수·킬데스·증감)는 주무기가 아니어도 **그대로 남긴다.** 모집단만 좁힌다.

마이그레이션 `20260829000000_weapon_is_main` 은 **추가만** 한다 (`IF NOT EXISTS` · 기본값 false).

### ③ 감점을 **무기 증감에도 같은 비율**로 반영한다

무기 랭킹은 증감 순으로 줄 세운다. 그런데 감점은 표시 점수에만 걸려 있어서
**5달 전에 그만둔 선수가 라플 1위**로 남았다 (실측).

내부 증감을 배율(3.5)로만 늘리지 않고, **내부 증감의 합이 표시 증감이 되도록** 한 번에 축소한다.

```
shrink = (표시 − 3000) / (내부 − 3000)
스나증감 = 내부스나증감 × shrink · 라플증감 = 내부라플증감 × shrink
기본 = 표시 − 스나증감 − 라플증감          ← 통합 = 기본 + 스나 + 라플 이 그대로 성립
```

### ④ 집계 잡이 점수를 **되돌려 쓰던 것**을 막았다

30분마다 도는 `supply-rollup` 이 매번 원본 점수로 덮어써 **시즌0 랭킹이 30분 만에 사라졌다.**
`SACLOUD_RATING_OWNER=formula` 면 `rating` · `placement` 두 칸을 쓰지 않는다.
승패·킬데스 같은 **기록은 계속 갱신한다.** 두 워크플로 모두 이 값을 세운다.

### 검증 (운영 DB · 2026-08-29 · 읽기만)

- 감점이 있는 선수 **773명**
- 라플 TOP10 전원 마지막 경기가 **2026-06-18 ~ 06-30** (시즌0 창 끝이 07-01)
- 3~5월에 멈춘 선수(1,892 · 1,630 · 1,549점 감점)는 **표시 3000 · 무기 증감 합 0**
- 운영 API `/api/leagues/supply/ranks/players?weapon=rifle` 1위 = 로컬 replay 결과와 동일
  (`4619` · 증감 `+1623`) — 로컬 · 운영 DB · 화면 세 곳이 같다

### 코드 위치

`apps/worker/src/jobs/rate.ts`(감점 커서 · `dailyDecay`) ·
`packages/rating/src/constants.ts`(`decayGraceDays` · `decayToBaselineDays` · `decayRecoveryGames`) ·
`apps/worker/src/jobs/season0Apply.ts`(무기 증감 축소 · 주무기 판정) ·
`apps/worker/src/jobs/weaponRebuild.ts` · `packages/db/ops/supplyRollup.ts`(`ratingOwnedByFormula`) ·
`apps/web/lib/server/queries/rankings.ts` · `leagues.ts`(`isMain` 필터) ·
`.github/workflows/supply-incremental.yml` · `supply-rollup-full.yml`

### 바꾸려면

- 감점 커서를 다시 `until` 로 밀지 마라. 그러면 감점이 **조용히** 멈춘다 (로그도 정상으로 보인다)
- `isMain` 필터를 랭킹 질의에서 뺄 때는 선수 프로필의 무기 순위(`playerWeaponRankOf`)도 **같이** 뺀다
- `SACLOUD_RATING_OWNER` 를 지우면 다음 rollup 이 점수를 원본으로 되돌린다

---

## D-174 — 배틀로그 **좌표**를 저장하고, 포지션을 자동으로 정한다 (2026-08-29)

### 배경

사양은 `docs/PLAYER_TRAITS_SPEC.md`(2026-08-28 사용자 지시)에 이미 있었는데
**코드도 데이터도 저장소에 없었다.** 구역 지도는 작업용 임시 폴더에만 있었고,
좌표 분석에 쓴 배틀로그 원문은 어디에도 남아 있지 않았다.
사양의 "원문을 버리지 않는다" 를 사양 자신이 못 지키고 있던 셈이다.

### 정한 것 ①  표를 나눈다 — 클랜 단위와 선수 단위는 다른 응답이다

| 표 | 무엇을 담나 | 키 |
|---|---|---|
| `BarracksRawImport` (기존 · D-114) | **클랜 단위** 응답. 무기 판정용 | `matchKey + clanNo` |
| `BarracksBattleLogRaw` (신설) | **선수 단위** 응답. 좌표까지 원문 그대로 | `matchKey + subject` |

선수 단위 응답은 한 경기에 최대 10줄이 생긴다. 키가 다르므로 같은 표에 넣지 않는다.
`subjectKind`(`user`/`clan`)로 어느 단위인지 남긴다 — 섞이면 참가자 수가 거짓이 된다.

**가공한 값을 원문 표에 넣지 않는다.** 구역·포지션은 언제든 다시 계산한다.

### 정한 것 ②  포지션은 "최다 구역" 으로 정하지 않는다

**2층은 A(숏)의 위층이라 미니맵 x/y 가 겹친다.** 좌표만으로는 둘을 가를 수 없고,
실제로 2층 선수 5명 전원이 "숏이 최다 구역" 으로 나왔다.

| 방법 | 정확도(실측 · 정답 20명) |
|---|---|
| 손으로 그린 3구역의 최다 구역 | 45% |
| 격자 히스토그램 학습 | 75% |
| + 스나 든 판 제외 | 80% |
| + B와 리베 통합(3분류) | **85%** |

그래서 구역 비율이 아니라 **격자 분포 전체**를 정답 표본의 중심과 코사인 유사도로 견준다.
검증은 **한 명씩 빼고 학습해 그 사람을 맞히는 방식**이다 — 자기를 중심에 넣고 자기를
맞히는 것은 검증이 아니다.

### 정한 것 ③  중심이 없으면 **찍지 않는다**

정답 라벨이 없으면 분포만 만들고 `position` 은 `null` 로 둔다.
분포를 저장해 두므로 라벨이 생기면 **원문 재수집 없이** 다시 고른다.
정답이 한 명뿐인 포지션은 그 사람을 빼면 중심이 사라져 맞힐 수 없는데,
그 경우도 **틀린 것으로 센다** — 표본이 모자란다는 사실을 감추지 않는다.

### 정한 것 ④  본인 좌표만 쓴다

`kill` 이벤트는 그 선수가 죽인 것이므로 `kill_x/kill_y` 가 본인 자리이고,
`death` 는 그 선수가 죽은 것이므로 `death_x/death_y` 가 본인 자리다.
폭파(`bomb`)·수류탄사(`g_death`)는 위치의 뜻이 달라 **쓰지 않는다.**

> `[미확인]` `kill_x` 가 **언제나** 죽인 사람 위치인지는 원본이 알려 주지 않았다.
> 필드 이름과 산점도 모양으로 그렇게 읽었다. 원본과 동일함이 검증되지 않았다.

### 정한 것 ⑤  선수와 잇는 규칙은 무기 판정과 **같다**

병영수첩 계정 번호(`sourcePlayerId`)로만 잇는다. 닉네임으로 전역에서 합치지 않는다 (D-036).
못 찾으면 `playerId` 를 비우고 분포는 그대로 남긴다.
자동 판정한 포지션은 원본이 준 `Player.position`(선수가 **직접 등록**한 값)과 **다른 값이다.
섞지 않는다.**

### 수집은 여전히 브라우저가 한다

서버에서 병영수첩을 부르면 **403**(봇차단)이다. UA 를 위조해 뚫지 않는다
(`CLAUDE.md` 3-A 5번). 사용자의 로그인된 브라우저가 정상 요청으로 받고,
워커는 그 결과 파일을 **읽기만** 한다. D-114 에서 정한 방식 그대로다.

### 코드 위치

- 순수 함수: `packages/nexon/src/position.ts` (좌표 추출 · 구역 · 격자 분포 · 코사인 · leave-one-out)
- 적재·판정 잡: `apps/worker/src/jobs/battlelog.ts`
- CLI: `nexon battlelog-import --file <수집.json> [--confirm]` ·
  `nexon position-build [--labels <라벨.json>] [--min-games N] [--cell N] [--confirm]`
- 스키마: `BarracksBattleLogRaw` · `PlayerPositionProfile`
  (마이그레이션 `20260829100000_barracks_battlelog_position` — **추가만** · `IF NOT EXISTS`)
- 입력 자료: `data/barracks/zonemap.json` · `sniper-lane.json` · `zones-raw-pixels.json`
- 테스트: `packages/nexon/src/__tests__/position.test.ts`(22) ·
  `apps/worker/src/__tests__/battlelog.test.ts`(7)

### 아직 없는 것 (정직하게)

- **정답 20명의 라벨.** 사양에 정확도만 적혀 있고 라벨 자체가 파일로 남지 않았다.
  다시 받아 `data/barracks/position-labels.json` 으로 저장해야 판정이 돈다
- **배틀로그 원문.** 좌표 분석에 쓴 수집분이 저장되지 않았다. 다시 받아야 한다
- 뱃지(선수 특성) 계산은 이번 범위 밖이다 (`docs/PLAYER_TRAITS_SPEC.md` 4절)

---

## D-176 — 선수 프로필 `상세정보` 는 **경기에서 그 자리에서 센다** (2026-08-29)

근거: `apps/web/lib/server/queries/playerTotals.ts` (신설) ·
`apps/web/lib/server/queries/records.ts` · `packages/contract/src/entities/league.ts`

### 무엇이 틀어졌나

운영 화면(2026-08-29 · `/league/supply/player/OBS-4d31283ebf60773a1296254f`)에서
**같은 카드 안의 두 숫자가 어긋나** 있었다.

```
본문 `최근매치`     20전 11승 9패 (55%) · 3시간 전 경기까지 보임
오른쪽 `상세정보`   0승 0패 0% · 0킬 0데스 · 평균킬 0 · MVP 0회 · 래더 `배치고사`
```

**둘이 서로 다른 모집단을 세고 있었다.**

| | 무엇을 세는가 |
|---|---|
| 본문 `최근매치` (`buildRecordSummary`) | `Match` 를 그 자리에서 센다 — 조건은 `ladderScope.ts` 하나 (D-164) |
| 오른쪽 `상세정보` (예전) | `LeaguePlayer` 의 **누적 칸**(`win/lose/kill/death/mvpCount`)을 읽었다 |

누적 칸은 화면이 아니라 **배치 집계가 채우는 값**이다. 그래서 집계가 훑는 기간
(시즌 창) 밖의 경기는 한 판도 들어가지 않는다. 실측(로컬 DB · 운영과 같은 미러):

```
supply 리그에서 래더 경기가 있는 선수      10,324명
그중 `LeaguePlayer.win + lose = 0` 인 선수  7,370명
지적된 선수(OBS-4d31283e…)                래더 경기 66건 · 누적 칸 0승 0패 0킬 0데스
```

경기가 없어서가 아니라 **집계 창에 갇혀 있었다.**

### 결정

**상세정보의 승·패·킬·데스·어시·헤드샷·MVP·평균킬·무기별 기록은
`MatchPlayerStat` 에서 그 자리에서 센다.** 조건은 본문과 **똑같이**
`ladderScope.ts` 의 "래더에 반영된 경기" 하나뿐이다 (D-164).

- **시즌 창을 화면에 복제하지 않는다.** 창
  (`apps/worker/src/lib/season0Window.ts` · D-175)은 래더·시즌 누적 집계에만 걸리고
  기록실은 창으로 거르지 않는다 — 그 파일이 그렇게 적어 두었다.
  날짜 상수를 복제하지 않으므로 창이 바뀌어도 이 조회는 고칠 곳이 없다
- **래더 점수(`rating`) · 순위(`rank`) · 배치고사(`placement`) 는 그대로 엔진 값을 쓴다.**
  선수끼리 비교하는 값이라 화면에서 다시 계산하면 랭킹 목록과 모집단이 어긋난다
- **무기별도 같다** — 기록은 여기서 세고, **순위만** `LeaguePlayerWeaponStat` 에서 온다

### 모르는 값을 0으로 채우지 않는다

`games`(그 무기·그 리그에서 뛴 래더 경기 전부)와
`knownGames`(그중 K/D 를 아는 경기)를 나눈다 — D-149 와 같은 정의다.
`knownGames === 0` 이면 킬·데스·킬뎃은 `0` 이 아니라 **`null`** 이다 (D-034 · D-106).
평균킬의 분모도 `knownGames` 다 (분자와 출처를 같게 — D-172).

### 계약 변경 (추가만 · 넓히기만)

`sniper_kill` · `sniper_death` · `sniper_assist` · `rifle_kill` · `rifle_death` ·
`rifle_assist` 를 `Count` → `Count.nullable().default(null)` 로 넓혔다.
필드를 새로 만들지 않았다 — 이름과 개수는 그대로다. 숫자를 넣던 기존 생산자
(`packages/mock/src/store.ts`)는 계속 유효하다.

`games` · `known_games` 는 **세는 값**이라 항상 숫자다.

### 남은 것 · `[미확인]`

- **개인랭킹 행 · 클랜원 목록은 여전히 누적 칸을 읽는다.** 그래서 프로필의 누적(전 기간)과
  랭킹 행의 누적(시즌 창)이 다를 수 있다. 어느 쪽에 맞출지는 사용자 판단이 필요하다
- **화면 래더 판정과 엔진 집계 대상이 완전히 같지 않다.** 실측(시즌0 창 안 · supply):
  화면 래더 판정 9,981건 · 엔진 집계 대상 10,019건. 차이는 `origin='nexon'` 인데
  `Match.redRatingUpdate` 가 비어 있는 **38건**이다 (D-171 이 원본 칸을 덮지 않기 때문).
  `ladderMatchWhere()` 를 넓힐지는 D-164 를 다시 여는 일이라 **여기서 정하지 않았다**

---

## D-175 — 시즌0 을 **2026-04-01(KST) ~ 현재(열린 구간)** 로 다시 정하고, 마감 시 카드를 만든다 (2026-08-29)

### 시즌0 이 무엇인가 (사용자 지시 · 원문)

> "현재 우리는 시즌0이야. 24년도5월부터의 기록이 아니라 2026년 4월부터 현재까지의 기록까지만
> 기록하고 그 외 남은 26년3월까지의 기록은 다른곳에 둬. 시즌0이란 시즌 시작전 테스트 시즌이야.
> 시즌1오픈 날은 내가 정한다. 그전까지는 계속 시즌0. 내가 시즌1 오픈하면 자동으로 시즌0 마무리 후
> 각 선수 개인기록에 카드로 만들어서 저장."

- 시즌0 = **시즌1 오픈 전의 테스트 시즌**이다. `Season` 표에서 번호 `0` · `seasonType='beta'`
- **끝나는 날은 사용자가 정한다.** 코드가 날짜로 자동 전환하지 않는다 (D-077 그대로)
- 마감하면 각 선수의 시즌0 성적이 **지난시즌 카드**(`LeaguePlayerSeason`)로 남는다

### 배경 — 창 끝이 박혀 있어서 새 경기가 통째로 빠졌다

`season0.ts` 에 `2026-01-01 ~ 2026-07-01` 이 **하드코딩**돼 있었고 대상 origin 은 미러뿐이었다.
그래서 7월 이후 경기는 시즌0 집계에 하나도 들어가지 않았다.

실측 (로컬 · `OBS-4d31283ebf60773a1296254f` = 서든러#001088394):
`supply` 리그 참가 기록이 **2026-07 에 7판 · 2026-08 에 63판** 뿐이었다.
전부 옛 창 **밖**이라 `season0Apply` 가 "시즌0 무경기" 로 판정해 기준점(3000)으로 되돌렸고,
화면은 최근매치에 경기가 있는데 상세는 승률 0% · 0킬 0데스 · 래더 `배치고사` 로 나왔다.

### 정한 것 ①  창을 **한 곳에서만** 정의한다. 끝을 박지 않는다

`apps/worker/src/lib/season0Window.ts` 하나가 창·origin·시즌 번호를 전부 가진다.

| | |
|---|---|
| 시작 | `2026-04-01 00:00 KST` = `2026-03-31T15:00:00Z` |
| 끝 | **`null`** — 상한 없음(열린 구간) |
| origin | `3rd.supply` + `nexon` **둘 다** (앞이 우선) |

끝에 `new Date()` 를 쓰지 않는다. 돌릴 때마다 값이 달라지면 결정적 replay 가 깨진다.
상한이 없으면 "DB 의 마지막 경기까지" 가 자연히 끝이 되고, 같은 DB 에서는 같은 결과가 나온다.

### 정한 것 ②  창 밖 기록은 **지우지 않는다**

2024-05 ~ 2026-03 의 `Match` · `MatchPlayerStat` 은 그대로 있다.
기록실(`apps/web/lib/server/queries/records.ts`)은 창으로 거르지 않으므로 경기·매치 상세·
개인 기록실에서 계속 보인다. 창이 거는 곳은 **래더·시즌 누적 집계뿐**이다.
실측(로컬): `supply` 미러+넥슨 130,158건 중 창 안 10,019 · **창 밖 120,139건 보존**.

### 정한 것 ③  D-170 미러 가드는 `matchScope` replay 에 걸지 않는다

가드의 목적은 "한 리그에 원본 점수와 우리 점수가 섞이는 것" 을 막는 것이다.
시즌0 replay 는 창 **전체**를 우리 공식 하나로만 재고, `dryRun` 강제라 원본
`Match.redRatingUpdate` 등을 한 줄도 건드리지 않는다 — 섞일 것이 없다.
반대로 가드를 그대로 두면 창 안 넥슨 경기가 통째로 빠져 위 증상이 그대로 남는다.

판정 기준을 `scopeOrigin === 'nexon'` 에서 **`matchScope` 유무**로 바꿨다.
범위를 지정하지 않은 평소 계산에서는 가드가 **그대로** 걸린다. D-170 은 살아 있다.

### 정한 것 ④  중복은 `sourceMatchId` 로 뺀다 — 미러를 남긴다

미러와 넥슨은 **같은 경기를 각자의 `Match.id` 로** 저장한다. PK 로는 안 걸리고
`sourceMatchId` 만 같다. 남기는 쪽은 **미러**다 — 라인업 10명·무기·원본 점수를 다 갖고 있고
넥슨 재구성분은 무기가 전부 `null` 이다 (D-034).

실측 (2026-08-29 · 로컬):

| | |
|---|---|
| **한 리그 안**에서 `sourceMatchId` 중복 | **0건** (전 리그 통틀어) |
| 넥슨 136건 중 다른 리그(`sanply`) 미러와 겹치는 것 | 30건 |

리그별로 계산하므로 **지금은 이중 계산이 없다.** 그래도 제거를 넣어 둔다 —
두 수집이 같은 리그에서 만나는 순간 조용히 두 번 세게 되기 때문이다.
`sourceMatchId` 가 없는 경기는 대조할 근거가 없으므로 **그대로 둔다.** 추측하지 않는다.

### 정한 것 ⑤  시즌0 마감은 **기존 `closeSeason` 을 그대로 쓴다**

새 축을 만들지 않았다. 카드(`LeaguePlayerSeason` · `LeagueClanSeason`)와 랭킹 스냅샷은
`packages/db/ops/season.ts` 의 `closeSeason` 이 이미 한 트랜잭션으로 만든다 (D-101 · D-166).
`season0Close.ts` 는 그 앞뒤만 맡는다 —

1. 열려 있는 시즌이 **정말 시즌0(번호 0 · beta)인지** 확인한다. 아니면 거부한다
2. `Season.startedAt` 을 창 시작(2026-04-01 KST)에 맞춘다
3. `closeSeason` 을 부른다
4. 카드가 실제로 생겼는지 **DB 를 다시 세서** 확인한다 (보고 로그를 믿지 않는다)
5. `--open --number N` 이면 `startSeason` 으로 다음 시즌을 연다

### 같이 고친 것 — `closeSeason` 이 만 명 리그에서 **죽고 있었다**

한 줄씩 `upsert` 를 돌려서 `LeaguePlayer` 10,393행짜리 리그에서
`Transaction API error: Transaction not found` 로 통째로 실패했다 (실측).
`createMany({ skipDuplicates: true })` 로 바꿨다 — `update: {}` 와 뜻이 같아
**이미 확정된 카드(과거 이관분)를 덮어쓰지 않는다.** 트랜잭션 `timeout` 도 명시했다.

**그 시즌에 아무것도 하지 않은 행은 카드를 만들지 않는다.** 기존 주석이 스스로
"배치고사라도 카드는 남긴다 — **전적 자체는 있었기 때문**" 이라고 근거를 댔는데,
판수·킬·데스·MVP 가 전부 0 인 행에는 그 전적이 없다. 실측: 10,393행 중
시즌0 에 한 판이라도 뛴 선수는 **2,382명**. 나머지 8,011명에게 `0승 0패` 카드를 만들면
뛰지도 않은 시즌이 프로필에 생긴다.

### 같이 고친 것 — `SEASON_BASELINE` 이 1500 으로 남아 있었다

D-145 에서 기준점을 3000 으로 올렸는데 `packages/db/ops/season.ts` 의 상수만 1500 이었다.
그대로 두면 **시즌1 을 여는 순간 전원이 1500 에서 시작**하고, 그 뒤 경기를 3000 기준으로
계산하는 우리 공식과 어긋난다. 3000 으로 맞추고 `seasonIsolation.test.ts` 가 지키게 했다.

### 검증 (로컬 DB · 2026-08-29)

| | |
|---|---|
| 창 안 경기 | 미러 9,883 + 넥슨 136 = **10,019** (중복 제거 0건) |
| 래더 반영 | 8,609 · 제외 `side_clan_mismatch` 1,390 · 불완전 스쿼드 20 |
| 집계된 선수 · 클랜 | **2,382명** · 47곳 |
| 랭킹에 뜨는 선수(배치고사 아님) | **1,096명** · 배치고사 보류 1,286명 |
| 자기 점검 | 무기분리 불일치 0 · 무기 미상 참가행 0 · NaN 0 |
| 불변식 `통합 = 기본 + 스나 + 라플` | 어긋난 선수 **0** |
| 서든러#001088394 | 70전 34승 36패 · 승률 48.6% · 573킬 584데스 · 평균킬 8.19 · MVP 5 · 래더 2,873 |
| 시즌0 마감(로컬 시연) | 개인 카드 **2,382** · 클랜 카드 47 · 랭킹 스냅샷 3 → 검증 후 되돌림 |

> 제로섬 오차가 `3,453` 이다. 창이 넓어지면서 커졌다. 창 안에서 시작한 선수의
> 초기 점수가 기준점이라 생기는 값으로 보이지만 **원인을 숫자로 규명하지 않았다.**
> `[미확인]` 으로 남긴다.

### 코드 위치

`apps/worker/src/lib/season0Window.ts`(창의 단일 정의) ·
`apps/worker/src/jobs/rate.ts`(`matchScope.origins` · 중복 제거 · 가드 조건) ·
`apps/worker/src/jobs/season0.ts` · `season0Apply.ts` · `season0Close.ts`(신설) ·
`apps/worker/src/dev/season0Scope.ts` · `decayProbe2.ts` ·
`apps/worker/src/cli.ts`(`season0-finish`) ·
`packages/db/ops/season.ts`(`closeSeason` · `SEASON_BASELINE`) ·
`apps/worker/src/__tests__/season0Window.test.ts`

### `[미확인]` · 남은 판단

- **"시즌1" 의 번호를 우리가 정할 수 없다.** `Season` 표에 3rd.supply 에서 이관한
  시즌 1~7 이 이미 있어 다음 번호는 8 이다. 사용자가 부르는 `시즌1` 과 숫자가 다르다.
  그래서 `season0-finish --open` 은 **`--number` 를 반드시 받아야** 진행한다. 추측하지 않는다
- 화면 표기도 같은 문제다. `seasonLabel` 은 번호 0 · beta 를 **`Beta Season`** 으로 그린다
  (D-098). 사용자는 `시즌0` 이라고 부른다. 표기를 바꿀지는 화면 쪽 판단이라 손대지 않았다
- ~~`side_clan_mismatch` 1,390건(창 안 경기의 14%)이 래더에서 빠진다~~ → **D-179 에서 규명했다.**
  원인은 참가자 원소속 다수결이 저장된 진영 클랜을 덮는 것이고, **개인 래더가 아니라 클랜 래더만**
  빠진다. 전체 조사는 `docs/SIDE_CLAN_MISMATCH_AUDIT.md`
- ~~시즌0 은 `supply` 리그에만 적용해 확인했다~~ → **D-179**: `sanply`(24,995) 로컬 반영 완료 ·
  `daerule`(1,591) 은 사용자 지시로 접었다(데이터는 보존)
- 마감 명령은 **로컬에서만** 시연하고 되돌렸다. 운영 반영은 사용자가 한다

### 바꾸려면

- **창 끝을 다시 고정값으로 박지 마라.** 그 순간 그 뒤 경기가 조용히 빠진다.
  `season0Window.test.ts` 가 `SEASON0_TO === null` 을 지킨다
- 날짜·origin 을 다른 파일에 다시 적지 마라. 흩어지면 한쪽만 고쳐져 두 값이 갈라진다
- `matchScope` 의 dry-run 강제를 풀지 마라. 그것이 원본 점수를 지키는 마지막 방어선이다 (D-171)

---

## D-179 — 열산리그 시즌0 재계산 · 대룰리그는 접는다 · `side_clan_mismatch` 원인 규명 (2026-08-29)

사용자 지시 세 건을 한 세션에서 처리했다. **로컬 DB 에만 반영했고 운영 DB 는 읽지도 쓰지도 않았다.**

### 정한 것 ①  `sanply`(열산리그)를 `supply` 와 **같은 창·같은 공식**으로 재계산했다

창·origin·시즌 번호는 그대로 `apps/worker/src/lib/season0Window.ts` 한 곳에서 온다 (D-175).
`sanply` 전용 상수를 만들지 않았고, 공식도 하나뿐이다 (CLAUDE.md 3-B 1번).

```bash
pnpm --filter @sacloud/worker exec tsx src/jobs/season0.ts       --leagues sanply
pnpm --filter @sacloud/worker exec tsx src/jobs/season0Apply.ts  --leagues sanply --confirm
```

| | |
|---|---|
| 창 안 경기 | **24,995** (미러 24,995 + 넥슨 0 · `sourceMatchId` 중복 **0**) |
| 클랜 래더 반영 | 22,318 · 제외 `side_clan_mismatch` 2,671 · `incomplete_squad_6v6` 6 |
| 개인 증감이 계산된 경기 | **24,989** — `side_clan_mismatch` 는 개인 래더를 빼지 않는다 (아래 ③) |
| 집계된 선수 · 클랜 | **4,315명** · **120곳** |
| 랭킹에 뜨는 선수(배치고사 아님) | **2,266명** · 배치고사 보류 2,049명 |
| 시즌0 무경기라 기준점(3000)으로 되돌린 선수 · 클랜 | 10,997 · 236 |
| 자기 점검 | 무기분리 불일치 **0** · 무기 미상 참가행 **0** · NaN **0** |
| 불변식 `통합 = 기본 + 스나 + 라플` | 어긋난 선수 **0** |
| 백업 | `apps/worker/backups/season0/applied-2026-08-29T03-30-31-012Z.json` (`--revert` 로 복구) |

개인 통합 TOP 10 (DB 반영 후 실측) —
`pit` 5189 · `피존` 5011 · `글썽` 4695 · `코딱지파는중` 4607 · `1rock` 4601 ·
`꿀뽀뽀` 4548 · `xek` 4531 · `goa` 4431 · `쭈잉` 4430 · `[h].blanc` 4430.

> 제로섬 오차 **10,966.5**. `supply`(3,453)보다 크지만 경기 수도 2.5배다.
> D-175 와 **같은 성격의 `[미확인]`** 으로 남긴다 — 원인을 숫자로 규명하지 않았다.

### 정한 것 ②  `daerule`(대룰리그)은 **재계산하지 않는다**

- **사유: 사용자 지시.** 대룰리그는 **서비스 준비중**이라 시즌0 집계를 하지 않는다.
- **데이터는 지우지 않는다.** 창 안 1,591 · 창 밖 28,106 = 총 29,697경기가 그대로 있다.
  기록실·경기 상세는 창으로 거르지 않으므로 계속 보인다 (D-175 정한 것 ②).
- `season0.ts` 의 기본 리그 목록에는 여전히 `daerule` 이 있다. **목록에서 빼지 않았다** —
  `--leagues` 로 대상을 지정하는 것이 이 명령의 정상 사용법이고, 목록을 건드리면
  나중에 대룰리그를 열 때 왜 빠져 있는지 모르게 된다. **부를 때 지정하지 않는 것으로 막는다.**
- 화면 안내 문구는 이 작업 범위가 아니다 (다른 담당).

### 정한 것 ③  `side_clan_mismatch` 는 **원인만 규명하고 고치지 않았다**

전체 조사는 `docs/SIDE_CLAN_MISMATCH_AUDIT.md` 에 있다. 요지만 —

- 원인은 경기 기록이 아니라 **판정 우선순위**다. `evaluateEligibility` 는 참가자
  원소속 다수결이 양쪽에서 서로 다른 클랜을 뽑으면 저장된 진영 클랜(`sideEvidence`)을
  **보지도 않는다** (D-133). 그 다수결의 입력은 **41%가 비어 있고**, 어긋난 쪽의 절반은
  **단 한 명**이 팀 이름을 정한다. `supply` 에서 5명 전원이 기록과 다른 경우는 **0건**이다.
- 유형별: `home_absent` 1,068 · `mercenary_plurality` 310 · `opponent_clan` 12 ·
  `dup_league_clan` **0** (supply). `sanply` 는 2,005 / 610 / 56 / 0.
- **개인 래더는 빠지지 않는다.** `rate.ts` 의 `side_clan_mismatch` 검사가 개인 점수 반영
  **뒤**에 있어서다. 빠지는 것은 클랜 점수와 판수뿐이고, 그 결과 `LeagueClan` 은
  **승·패에는 세어졌는데 판수에는 안 세어진 경기**를 갖고 있다
  (`supply` 47곳 중 43곳 · `sanply` 120곳 중 110곳).
- 권고 1(미러 경기는 저장된 진영이 정본) 또는 권고 2(다수결 3명 이상)를 받으면
  어긋남이 1,390 → 0 또는 → 56 이 된다. **판정 규칙 변경은 사용자 승인 사항**이라
  코드를 한 줄도 바꾸지 않았다.

### 코드 위치

`apps/worker/src/dev/sideClanMismatchAudit.ts`(신설 · 읽기 전용) ·
`apps/worker/src/dev/sideClanMismatchImpact.ts`(신설 · 읽기 전용) ·
`apps/worker/src/dev/season0Verify.ts`(`--league` 인자 추가) ·
`docs/SIDE_CLAN_MISMATCH_AUDIT.md`(신설)

### `[미확인]`

- 미러(3rd.supply) 원본 응답이 **`RawImport` 에 없다.** 로컬 `RawImport` 는 전부 `nexon` 이다.
  진영 클랜을 원본과 직접 대조하려면 미러 수집이 남긴 스냅샷 파일을 따로 찾아야 한다
- 권고안을 받았을 때 **클랜 점수가 얼마나 움직이는지**는 재지 않았다.
  반영 경기 수 변화만 측정했다 — 점수 변화는 규칙을 바꿔 replay 해야 알 수 있다
- `sanply` 제로섬 오차 10,966.5 의 원인 (D-175 와 같은 미확인)
- `sanply` 는 `Season` 행이 없어 replay 가 "시즌이 없다" 로 돈다. `supply` 는 시즌0 이 있다.
  시즌 카드(`LeaguePlayerSeason`)를 만들려면 `sanply` 에도 시즌0 을 열어야 한다 — **판단 필요**

### 바꾸려면

- `sanply` 를 다시 돌릴 때도 `season0Window.ts` 를 통해서만 창을 잡아라. 날짜를 다시 적지 마라
- `daerule` 을 열 때는 같은 두 명령을 `--leagues daerule` 로 부르면 된다. 데이터는 그대로 있다
- `side_clan_mismatch` 판정을 바꾸기 전에 `docs/SIDE_CLAN_MISMATCH_AUDIT.md` 6장의
  영향표를 먼저 읽어라. 개인 래더는 안 변하고 클랜 래더만 변한다


---

## D-178 — 화면의 **성적 수치**를 시즌0 창으로 통일한다 · 래더 판정을 엔진과 맞춘다 · `시즌0` 표기 · 대룰리그 접기 (2026-08-29)

근거: `apps/web/lib/server/queries/season0Scope.ts`(신설) · `ladderScope.ts` · `playerTotals.ts` ·
`records.ts` · `leagues.ts` · `playerForm.ts` · `rankings.ts` ·
`packages/contract/src/season.ts`(신설) · `packages/ui/src/site-config.ts` ·
`packages/ui/src/league/LeaguePreparing.tsx` · `preparingText.ts`(신설) ·
`apps/web/app/league/[leagueSlug]/layout.tsx`

사용자 지시 4건(2026-08-29)을 화면 쪽에서 반영한 기록이다. **로컬까지만 고쳤고 커밋하지 않았다.**

---

### ① 화면 래더 판정을 **엔진 집계 대상과 같게** 넓혔다 (D-164 를 다시 연다)

같은 선수를 **엔진은 70판, 화면은 66판**으로 셌다.

`ladderMatchWhere()` 는 `redRatingUpdate != null` **또는** `origin='3rd.supply'` 둘만 봤다.
그런데 시즌0 재계산은 **읽기 전용 replay** 라 원본 칸을 채우지 않는다 (D-171 정한 것 ①).
그래서 `origin='nexon'` 경기가 화면에서만 통째로 빠졌다.

실측 (2026-08-29 · 로컬 · `supply`):

| | |
|---|---|
| 시즌0 창 안 엔진 집계 대상 | **10,019** (미러 9,883 + 넥슨 136) |
| 시즌0 창 안 화면 래더 판정 (고치기 전) | **9,981** |
| 차이 | **38건** — 전부 `origin='nexon'` · `redRatingUpdate` 가 `null` |
| `서든러#001088394` | 엔진 70판 · 화면 66판 |

**정한 것** — 판정에 넥슨 origin 을 더한다. 문자열을 새로 적지 않고
엔진이 쓰는 `SEASON0_ORIGINS`(`apps/worker/src/lib/season0Window.ts`)를 **그대로 가져다 쓴다.**
`'nexon'` 을 화면에도 적으면 두 곳이 갈라진다.

```ts
OR: [
  { redRatingUpdate: { not: null } },      // 우리 공식이 원본 칸에 써 넣은 경기 (D-145)
  { origin: { in: [...SEASON0_ORIGINS] } } // 미러 + 넥슨 = 엔진 집계 대상 (D-175)
]
```

**넥슨 경기를 래더 경기로 보는 근거**는 미러와 같은 종류다 — 넥슨 수집은 클랜전 배틀로그만
훑고, 엔진도 그 origin 을 집계 대상으로 잡는다. 추측이 아니라 수집 범위에서 나오는 사실이다.

**주의** — 엔진은 `sourceMatchId` 로 미러/넥슨 중복을 제거하지만 **화면은 하지 않는다.**
실측상 한 리그 안 중복은 **0건**이라 지금은 차이가 없다 (D-175 정한 것 ④). 두 수집이
같은 리그에서 만나면 화면만 두 번 세게 된다 — 그때 다시 봐야 한다. `[미확인]`

---

### ② 화면의 **성적 수치**를 전부 시즌0 창 기준으로 통일했다 (사용자 선택 A안)

D-176 이 상세정보를 경기에서 직접 세게 만들었지만 **창을 걸지 않아 전 기간**을 셌다.
랭킹 표는 엔진 집계(시즌0 창)라 같은 선수의 두 숫자가 어긋났다.
사용자 선택은 **프로필을 시즌0 기준으로 맞추는 것**(A안)이다.

**창이 걸리는 곳**

| 화면 | 함수 |
|---|---|
| 선수 상세정보 누적(승·패·킬·데스·어시·헤드샷·MVP·무기별) | `playerLadderTotals` |
| 선수·클랜 `최근매치` 요약 · 연승연패 · 상대별 · 최근 같이한 플레이어 | `buildRecordSummary` |
| 개인랭킹·클랜원 목록의 **평균킬 분모** | `matchCountByPlayer` |
| 선수 프로필 `최근 폼` 6개월 그래프 · 최근 10경기 판정 | `buildPlayerForm` |
| 폼 TOP3 의 대상 날짜 | `getFormTop` |

**창이 걸리지 않는 곳** — 기록실(경기 목록) · 매치 상세 · 지난시즌 카드.
2026-03 이전 기록은 **지우지 않았고** 거기서는 계속 보인다 (D-175 정한 것 ②).

**창 안에 경기가 없는 선수가 빈 성적 · `배치고사` 로 보이는 것은 정상이다** (사용자 확인).

폼 그래프는 "최근 6개월" 과 창 중 **늦은 쪽**에서 시작한다. 6개월을 그대로 두면
그래프만 창 밖(2026-03)을 세어 같은 프로필 안에서 또 어긋난다.

---

### ③ 창 값을 **화면에 복제하지 않는다** — worker 정의를 경로로 읽는다

`apps/web/lib/server/queries/season0Scope.ts` 가 창의 유일한 진입점이고,
그 안에서 **`apps/worker/src/lib/season0Window.ts` 를 상대 경로로 import** 한다.

조사한 대안과 판단 —

| 방법 | 판단 |
|---|---|
| 날짜를 web 에 다시 적는다 | **금지.** 창이 바뀌면 한쪽만 고쳐진다. D-176 이 그 사고였다 |
| 창 정의를 `packages/contract` 로 옮긴다 | worker 를 고쳐야 한다. 이번 작업 범위 밖이다 |
| `@sacloud/worker` 를 web 의 의존성으로 건다 | 락파일이 바뀌고 DB·넥슨 클라이언트까지 딸려 온다 |
| **상대 경로로 그 파일만 읽는다** | **채택.** `season0Window.ts` 는 **import 가 하나도 없는 순수 상수 파일**이라 이것만 딸려 온다 |

`tsc`(web) 통과를 확인했고 `next.config.ts` 의 `transpilePackages` 도 건드리지 않았다.
**읽기만 한다.** worker 쪽에 값을 되쓰지 않는다.

`SEASON0_TO` 는 `null`(열린 구간)이므로 `where` 에 상한을 **붙이지 않는다.**
`new Date()` 로 채우면 요청마다 값이 달라져 같은 DB 에서도 숫자가 흔들린다.

---

### ④ 화면 표기 `Beta Season` → **`시즌0`**

사용자 지시다. 문자열은 `packages/contract/src/season.ts` 한 곳에만 있고
(`BETA_SEASON_LABEL` · `seasonDisplayLabel()`), 화면(`apps/web`)과 공용 컴포넌트
(`packages/ui`)가 **둘 다** 그 값을 읽는다.

- `@sacloud/db/ops` 의 `seasonLabel()` 은 **건드리지 않았다** — CLI·관리도구 로그가 쓰는 값이고
  `packages/db` 는 이번 작업 범위 밖이다. 화면은 계약의 `seasonDisplayLabel()` 로 갈아탔다
- 정식·과거 시즌은 원본 관측대로 `시즌 6` 이다 (D-166). 띄어쓰기가 다른 것은
  사용자가 지시한 표기(`시즌0`)를 그대로 쓴 결과다
- 베타 안내 문구의 `정식 Season 8` 도 지웠다 — **시즌1 의 번호가 `[미확인]`** 이다 (D-175).
  모르는 번호를 화면에 쓰지 않는다 (CLAUDE.md 3장 7번) → `다음 정식 시즌`

---

### ⑤ 대룰리그(`daerule`)를 접는다 — **`서비스 준비중`**

사용자 지시다. **데이터는 지우지 않았다.** DB 의 리그·클랜·경기·시즌 카드는 그대로 있다.
정하는 것은 "화면에 그리는가" 하나다 (`publicScope.ts` 와 같은 성격).

- 판정은 `packages/ui/src/site-config.ts` 의 **`PREPARING_LEAGUE_SLUGS`** 한 배열이다.
  다시 열 때 slug 하나만 빼면 된다 — 화면 코드에 slug 가 없다
- 막는 자리는 **`app/league/[leagueSlug]/layout.tsx` 한 곳**이다. 리그홈·클랜랭킹·개인랭킹·
  클랜/선수 상세가 전부 이 레이아웃을 지나므로 랭킹·집계가 어느 경로로도 나가지 않는다.
  페이지마다 조건을 뿌리면 새 경로가 생길 때 빠진다
- **리그 조회 요청도 보내지 않는다** (`enabled: ready && !preparing`).
  그려 줄 것이 없는데 응답을 받으면 참가 클랜 수·랭킹 링크가 클라이언트 캐시에 남는다
- **GNB 링크는 그대로 둔다.** 지우면 눌렀을 때 빈 화면·404 가 된다 —
  사용자가 원한 것은 **안내가 나오는 것**이다. 서브내비 탭은 그리지 않는다
- **API 는 막지 않았다.** `/api/leagues/daerule/...` 는 그대로 응답한다.
  화면에서 그 경로로 가는 링크가 없어졌을 뿐이다 — 서버 차단이 필요하면 별도 판단이 필요하다 `[미확인]`

---

### 검증 (로컬 DB · 2026-08-29)

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| `supply` 시즌0 창 안 화면 래더 판정 | 9,981 | **10,019** (엔진과 일치) |
| `서든러#001088394` 상세정보 판수 | 66 | **70** |
| 같은 선수 `LeaguePlayer` 누적(랭킹 표가 읽는 값) | 34승 36패 = 70 | 그대로 |

회귀 테스트 `apps/web/tests/season0Scope.test.ts`(신설 · 8건) ·
`packages/ui/src/__tests__/preparing-league.test.ts`(신설 · 4건).

같이 고친 테스트 —

| 파일 | 왜 |
|---|---|
| `officialIndependence.test.ts` | "래더 밖" 표본이 `origin='nexon'` 이었다. 이제 그건 래더 경기다 → `sacloud` 로 |
| `weaponRanking.test.ts` | 폼 TOP3 픽스처가 `2026-03-10`(창 밖)이었다 → `2026-05-10` |
| `playerProfileTotals.test.ts` | 대조 질의에도 같은 창을 건다. 안 걸면 화면과 다른 것을 센다 |
| `beta-notice.test.ts` | `Beta Season` → `시즌0` |

**알려진 간헐 실패**(이번 변경과 무관 · HANDOFF 에 이미 적혀 있다): 로컬 DB 가 테스트 도중
연결을 끊어 `independentLeague` · `playerProfileIdentity` · `health` 가 가끔 죽는다.
같은 명령을 다시 돌리면 전부 통과한다 — 실제로 그렇게 확인했다.

---

### 바꾸려면

- **창 날짜를 `apps/web` 에 적지 마라.** `season0Scope.ts` 만 고쳐라. 값은 worker 에만 있다
- `ladderMatchWhere()` 에서 넥슨을 뺄 거면 **엔진 집계 대상도 같이** 좁혀야 한다.
  한쪽만 바꾸면 화면과 랭킹이 다시 갈라진다 — 그것이 이 항목이 생긴 이유다
- 성적 수치에 창을 새로 거는 곳이 생기면 `seasonWindowWhere()` 를 써라. 조건을 베껴 적지 마라
- 대룰리그를 다시 열려면 `PREPARING_LEAGUE_SLUGS` 에서 slug 를 빼라. 데이터는 그대로 있다

---

## D-180 — 미러 경기는 **저장된 진영 클랜이 정본**이다 (2026-08-29)

`docs/SIDE_CLAN_MISMATCH_AUDIT.md` 의 **권고 1** 을 사용자 승인으로 실행했다.
**로컬 DB 에만 반영했다. 운영 DB 는 읽지도 쓰지도 않았다.** `daerule` 은 건드리지 않았다 (D-179).

### 정한 것 ①  `origin = '3rd.supply'` 경기는 `Match` 의 진영 클랜을 그대로 쓴다

`evaluateEligibility` 는 참가자 원소속 다수결을 저장된 진영 클랜보다 **우선**했다 (D-133).
그 다수결의 입력은 미러 참가행의 **41%가 비어 있고**(클랜명 없음 31.6% + 리그 미등록 9.2%),
`plurality()` 는 동률만 아니면 그대로 반환하므로 **5명 중 한 명이 팀 이름을 정했다.**
어긋난 쪽의 절반(supply 721/1,630)이 한 명이 정한 것이었고,
**5명 전원이 기록과 다른 경기는 supply 에 0건**이었다. 얇은 추론이 두꺼운 기록을 이기고 있었다.

`Match.redLeagueClanId` / `blueLeagueClanId` 는 3rd.supply 가 자기 화면에 "A vs B" 로
**적어 둔 값**이다. 추론이 아니라 원본의 판정이므로 다수결이 이길 수 없다.

**넥슨(`origin = 'nexon'`) 재구성 경기의 규칙은 그대로 둔다.** 그쪽 진영 클랜은
애초에 라인업 근거로 정해진 값이라 성격이 다르다. D-133 은 넥슨 경기에서 살아 있다.

### 정한 것 ②  판정이 갈리는 곳을 **한 파일**로 모았다

| 파일 | 역할 |
|---|---|
| `apps/worker/src/lib/sideEvidencePolicy.ts` (신설) | **어느 경기의 저장된 진영이 정본인가**를 정하는 유일한 곳. `MIRROR_ORIGIN` · `storedSideEvidence()` |
| `packages/rating/src/eligibility.ts` | `SideEvidence.authority: 'primary' \| 'fallback'` 를 받아 우선순위를 바꾼다. **origin 을 모른다** (순수 패키지) |

`rate.ts` · `ratingBacktest.ts` · `dev/sideClanMismatchAudit.ts` 가 전부 그 함수를 부른다.
`'stored-match'` 문자열을 직접 만드는 곳은 이제 정책 파일 하나뿐이다 (테스트 제외).
`packages/db/ops/supplySnapshotAudit.ts` 의 `toRateMatchRows` 도 `origin: '3rd.supply'` 를
붙여, 투영 경기가 저장 경기와 **같은 판정**을 받게 했다.

`authority` 기본값은 `fallback` 이다 — 넣지 않으면 D-133 그대로다.
`primary` 라도 **한쪽이 비었거나 양쪽이 같은 클랜이면** 정본으로 쓰지 않고 다수결로 내려간다.

### 정한 것 ③  원본 `Match.*RatingUpdate` 는 한 줄도 건드리지 않았다

시즌0 재계산은 여전히 `matchScope` + dry-run 이다 (D-171). 반영은 `season0Apply` 가
`LeaguePlayer` · `LeagueClan` · `LeaguePlayerWeaponStat` 에만 한다.

### 실측 (로컬 DB · 시즌0 창 `2026-04-01 KST ~ 열린 구간`)

| | `supply` 전 | `supply` 후 | `sanply` 전 | `sanply` 후 |
|---|---:|---:|---:|---:|
| `side_clan_mismatch` | 1,390 | **2** | 2,671 | **0** |
| 클랜 래더 반영 경기 | 8,609 | **9,997** (+16.1%) | 22,318 | **24,989** (+12.0%) |
| `승 + 패 ≠ 판수` 인 클랜 | 43 | **4** | 110 | **0** |
| 잘못 더해진 클랜-경기 | 2,780 | **4** | 5,337 | **0** |
| 집계된 클랜 | 47 | 47 | 120 | **121** |

**남은 `supply` 2건은 전부 `origin = 'nexon'`** 이다 — 규칙을 그대로 둔 쪽이라 예상된 잔여다.
그 2경기가 클랜 4곳에 `승 + 패 = 판수 + 1` 을 남긴다
(`dravelior` · `Iatency-` · `ThelVub` · `One.PoinT` 각 1).
원인은 `rate.ts` 의 클랜 승패 누적(③)이 어긋남 검사(⑤)보다 **앞**에 있는 것이다
(권고 4). **이번에는 고치지 않았다** — 넥슨 경기의 판정을 어떻게 할지와 함께 판단할 일이다.

### 개인 래더는 **변하지 않았다** (숫자 증거)

반영 직전 백업(`applied-2026-08-29T04-19-50-935Z.json`)의 `LeaguePlayer` **25,705행**을
반영 후 DB 와 전 칸 대조했다.

```
rating 바뀐 선수 0 · baseRating 바뀐 선수 0 · 승패 바뀐 선수 0 · 판수 바뀐 선수 0
클랜 418행 중 — rating 바뀐 클랜 134 · 판수 바뀐 클랜 154 · 승패 바뀐 클랜 1
```

당연한 결과다. `side_clan_mismatch` 검사는 개인 반영 **뒤**에 있어서 개인 점수를 빼지 않았고,
이 변경은 팀의 **이름표**만 바꾼다. 승/패 그룹·참가 인원·5v5 판정·상대 평균은 그대로다.
(승패 바뀐 클랜 1곳은 `sanply` 에서 새로 집계 대상이 된 121번째 클랜이다.)

### 클랜 랭킹 상위 10 이동

| 순위 | `supply` 전 | `supply` 후 | `sanply` 전 | `sanply` 후 |
|---:|---|---|---|---|
| 1 | -tsAr.nTc 3132 | -tsAr.nTc 3139 | saint 3176 | **-tsAr.nTc 3152** |
| 2 | afterpray 3100 | afterpray 3093 | -tsAr.nTc 3160 | **saint 3150** |
| 3 | 鬼神。 3086 | 鬼神。 3091 | afterpray 3145 | **sovereignwc 3134** |
| 4 | PokerFace. 3076 | **MiraGe. 3049** | sovereignwc 3134 | **afterpray 3129** |
| 5 | MiraGe. 3046 | **PokerFace. 3034** | 너구리마을 3133 | 너구리마을 3123 |
| 6 | recent.wct- 3040 | recent.wct- 3024 | stylecIan 3103 | stylecIan 3116 |
| 7 | Iatency- 3030 | Iatency- 3019 | 또마토 3096 | **melody 3103** |
| 8 | Castle 3019 | Castle 3015 | highway 3094 | **또마토 3101** |
| 9 | mercedes- 3014 | **saint 3002** | melody 3087 | **highway 3087** |
| 10 | ＃chaseplay 3010 | **mercedes- 3002** | LulIaby 3064 | **鬼神。 3067** |

`supply` 1위는 그대로고 4·5위가 맞바뀌었다. `saint` 가 새로 들어오고 `＃chaseplay` 가 빠졌다.
`sanply` 는 1·2위가 뒤집혔다. 점수보다 **판수**가 크게 늘었다 —
`afterpray` 2,234 → 2,523 · `melody` 2,626 → 2,911 로, 이제 승+패와 정확히 같다.

### 자기 점검

| | `supply` | `sanply` |
|---|---:|---:|
| 무기분리 불일치 | 0 | 0 |
| 무기 미상 참가행 | 0 | 0 |
| NaN | 0 | 0 |
| 불변식 `통합 = 기본 + 스나 + 라플` 어긋난 선수 | **0** | **0** |
| 제로섬 오차 | 3,453.59 | 10,966.53 |

제로섬 오차는 **바뀌지 않았다** (D-175 · D-179 와 같은 값). 개인 증감이 그대로라는
또 하나의 증거다. 원인은 여전히 `[미확인]` 이다.

### 되돌리는 법

```bash
pnpm --filter @sacloud/worker exec tsx src/jobs/season0Apply.ts \
  --revert apps/worker/backups/season0/applied-2026-08-29T04-19-50-935Z.json
```

백업에는 두 리그의 `LeaguePlayer` · `LeagueClan` · `LeaguePlayerWeaponStat` 전량이 들어 있다.
코드까지 되돌리려면 `sideEvidencePolicy.ts` 의 `storedSidesAreCanonical()` 이
항상 `false` 를 돌려주게 하면 된다 — 그 함수 하나가 규칙 전체다.

### 회귀 테스트

| 파일 | 고정하는 것 |
|---|---|
| `packages/rating/src/__tests__/storedSideAuthority.test.ts` (신설 · 12건) | `primary` 면 다수결이 무엇을 뽑든 저장된 진영이 이긴다 · 기본값은 D-133 그대로 · 반쪽/동일 클랜이면 정본이 아니다 · 승패 그룹과 인원은 안 변한다 |
| `apps/worker/src/__tests__/sideEvidencePolicy.test.ts` (신설 · 5건) | 미러만 `primary` · 넥슨과 origin 미상은 `fallback` · `winnerSide` 로 이름표가 뒤집힌다 |
| `packages/rating/src/__tests__/sideEvidencePrecedence.test.ts` (기존) | 그대로 통과 — 넥슨 경기 규칙이 안 바뀌었다는 증거다 |

### 새 도구

`apps/worker/src/dev/ladderSnapshot.ts` (신설 · 읽기 전용) —
`승 + 패 ≠ 판수` 클랜 수 · 클랜/개인 상위 N · 개인 점수 합계를 찍는다. 전/후 대조용이다.

```bash
pnpm --filter @sacloud/worker exec tsx src/dev/ladderSnapshot.ts --leagues supply,sanply --out ladder-after.json
```

### `[미확인]`

- 넥슨 경기 2건의 어긋남을 어떻게 할지 (다수결 최소 인원? 클랜 승패 누적 순서?)
- 3rd.supply 의 진영 클랜 **자체가 틀린 경기**가 있는지. 미러 원본 응답이 `RawImport` 에
  없어 직접 대조하지 못했다 (D-179 와 같은 미확인). 다만 5명 전원 불일치 0건이라 근거는 없다
- 제로섬 오차 (D-175 · D-179 와 같은 미확인)

### 바꾸려면

- **규칙은 `sideEvidencePolicy.ts` 에서만 고쳐라.** 조건을 다른 파일에 다시 적으면
  replay 와 조사 도구가 조용히 갈라진다 — 그것이 이 파일이 생긴 이유다
- 넥슨 경기까지 정본으로 올리려면 `storedSidesAreCanonical()` 하나만 고치면 된다.
  다만 넥슨 진영 클랜은 **라인업 근거로 만들어진 값**이라 성격이 다르다. 먼저 재라
- `authority` 기본값을 `primary` 로 바꾸지 마라. 그러면 origin 을 모르는 호출까지
  정본으로 올라간다 — 판단은 정책 파일이 해야 한다

---

## D-181 — IPL(무소속리그) 기록 이관 · 랭킹 · 포지션 사양을 받아 적는다 (2026-08-29)

사양 본문은 **`docs/IPL_SPEC.md`** 에 있다. 여기에는 어긋나면 안 되는 것만 남긴다.
**아직 한 줄도 구현하지 않았다.** 클랜 명단만 실제 조사로 채웠다.

### 정한 것 ①  IPL 은 공식리그·열산리그와 다른 리그다

- 공식리그 **1·2부 등록 클랜은 IPL 에 소속될 수 없다**
- 그러나 **열산리그에 참가 중인 IPL 클랜은 중복 등록·기록을 허가한다**

배타는 `공식 1·2부 ↔ IPL` 한 방향뿐이다. 열산리그와는 겹쳐도 된다.

### 정한 것 ②  티어 이동은 **점수가 아니라 관리자가 정한다**

점수가 더 높다고 3티어가 2티어로 자동으로 올라가지 않는다.
**승격·강등 자동화를 만들지 마라.** 우리는 1~40위 순위표를 **보고**하고, 티어는 사용자가 정한다.

### 정한 것 ③  IPL 개인랭킹은 **킬데스만** 숨긴다

통합/스나/라플 3분할 · 점수 시스템 · 표 · 커서 · 배치고사 표기 — **전부 서플라이와 같다.**
다른 것은 킬데스 노출 하나뿐이다. 이것은 **D-107 그대로**이고 새 규칙이 아니다.
킬·데스는 계산해서 저장한다. 감추는 것은 **누적 킬뎃 표시**뿐이고 경기 한 판의 K/D/A 는 보인다.

### 정한 것 ④  포지션 판정 대상이 리그마다 다르다

| 리그 | 기간 | 대상 | 배틀로그 |
|---|---|---|---|
| IPL | 2026-01-01 ~ 08-29 | 30판 이상 | 인당 100판 |
| 공식(supply) | 4월 ~ 현재 | 30판 이상 | 인당 100판 |
| 열산(sanply) | — | **하지 않는다** | **하지 않는다** — 킬데스와 승률만 뜨게 한다 |

100판은 등록 클랜끼리 한 경기로 고르는 편이 좋지만, 안 되면 **제3보급창고이기만 하면** 써도 된다.

### 정한 것 ⑤  이관 기간은 시즌0 창과 **다르다**

IPL 은 `2026-01-01 ~ 2026-08-29` 이고 8/30 부터 끊지 않고 이어 간다.
시즌0 창은 `2026-04-01 KST ~ 열린 구간`(D-175)이다. **섞지 마라.**

### 클랜 명단 조사 (2026-08-29) — **39곳 전부 확정**

조사로는 36곳만 확실했다. 나머지 3곳은 **추측으로 메우지 않고 미확인으로 남겼고**,
그러자 사용자가 답을 주었다. **명단의 정본은 사용자다.**

| 조사 결과 | 사용자 지시 | 확정 |
|---|---|---|
| `nightbloom` 미확인 | nightbloom → pIacebo | `pIacebo` · `/clan/ytsys` |
| `swell` 미확인 | swell 제거 후 romantico 등록 | `romantico` · `/clan/zzim1` |
| `Major-` 추정 | major 이 병영으로 등록 | `Major-` · `/clan/jjangkangsu` |

`pIacebo` 는 조사 단계에서 리그 클랜들의 **상대 명단에 이미 잡혀 있던 이름**이다.
`nightbloom` 과 글자가 전혀 겹치지 않아 우리 쪽에서는 이어 붙일 수 없었다.
**이것이 "미확인으로 남긴다" 가 옳은 이유다** — 이름이 비슷한 빈 클랜을 답으로 냈다면
틀린 클랜의 기록을 이관할 뻔했다.

> ⚠ **병영수첩 클랜명은 동형문자를 즐겨 쓴다.** 소문자 `l` 자리에 대문자 `I`,
> 라틴 `P` 자리에 키릴 `Р`, `B` 자리에 그리스 `Β`. 검색 API 는 이 차이를 구분하므로
> 사용자가 적어 준 철자로는 **절대 안 나온다.** 확인된 리그 클랜의 클랜전 목록에서
> 상대 클랜명을 그대로 긁어 재검색하는 편이 훨씬 확실했다.

### 알아 둘 것 — 병영수첩 클랜전 목록에는 **양 팀이 함께 온다**

`GetClanMatchList` 응답 한 건에 `red_clan_name` 과 `blue_clan_name` 이 **둘 다** 있다.
넥슨 Open API 가 한 경기에 양 팀을 주지 않는 제약(D-044)이 여기엔 **없다.**
팀 판정 근거로 쓸 수 있다는 뜻이다. 반면 `match_time_date` 는 항상 `0001-01-01` 로
쓸모없고, 절대시각은 `match_key` 앞 12자리(`YYMMDDHHMMSS`)에서 뽑아야 한다.

### 정한 것 ⑥  IPL = 기존 무소속리그(`nolink`)다. **6단으로 간다**

사용자 확정(2026-08-29): "같은 리그가 맞다. 무소속 리그의 다른 이름이다. IPL 은 6단으로 진행하라."

**새 리그를 만들지 않았다.** `slug = nolink` · `category = independent` 그대로이고
`League.divisionCount` 만 5 → 6 이다 (D-165 는 5단이었다).

상한의 단일 출처는 `packages/db/ops/independentLeague.ts` 의 `INDEPENDENT_TIER_COUNT` 다.
`updateClan()` 이 박아 두었던 `1~5` 숫자를 걷어내고 이 상수를 읽게 했다 —
**숫자를 다시 박지 마라.** `independentTier.test.ts` 의 "범위 밖" 단언도
`INDEPENDENT_TIER_COUNT + 1` 로 바꿨다. 그대로 뒀으면 티어 수가 바뀌는 순간
그 테스트가 "정상 티어를 거절하라" 로 조용히 뒤집혔을 것이다.

관리자 화면 티어 select 는 `division_count` 에서 만들어지므로 코드 변경이 없다.
표기 함수(`divisionLabel`)도 상한을 모르므로 그대로 돈다.

**이 값을 줄이지 마라.** 이미 그 티어에 등록된 클랜이 갈 곳을 잃는다.

### 미확정

`docs/IPL_SPEC.md` 8장에 모아 두었다. 특히 **등록 클랜이 39곳인지 40곳인지**
(사용자 목록 39 · 지시문 40)는 착수 전에 확인해야 한다.

---

## D-182 — 화면에는 **우리 MVP** 를 쓴다 · 플레이스타일 바의 블루·레드는 **수비·공격 진영**이다 (2026-08-29)

`docs/PLAYER_TRAITS_SPEC.md` 6절·8절에 `[미확인]` 으로 남아 있던 두 가지를 사용자가 확정했다.
**아직 한 줄도 구현하지 않았다.** 관련 코드는 저장소에 0건이다(확인함).

### 정한 것 ①  MVP 는 **우리 판정을 보여준다**

> "원본이 3rd.supply 를 말하는 거라면 당연히 원본과 다른 방식으로 MVP 를 선정하기 때문에
> 우리가 선정한 MVP 를 보여줘야 한다."

| | |
|---|---|
| 화면 | **우리 판정.** 원본 MVP 는 화면에서 쓰지 않는다 |
| 저장 | **둘 다.** 원본값(`Match.mvpPlayerId` · `MatchPlayerStat.mvp`)은 한 줄도 안 고친다 |

우리 규칙은 `기본 = 그 경기 최다 킬` 이고, **매치의 사나이**(20분 초과 경기의 마지막 라운드
최다 킬)가 있으면 그 선수가 MVP 를 받고 이름 옆에 `매치의 사나이` 가 붙는다.

**우리 값이 없는 경기를 원본값으로 대신 채우지 마라.** 라운드 복원이 안 되는 경기는
`알수없음` 이다 (D-034 · D-106 과 같은 원칙). 두 기준을 한 칸에 섞으면
그 칸이 무슨 뜻인지 아무도 모르게 된다 — 과거 13만 경기가 여기 해당한다.

### 정한 것 ②  블루 = 수비 · 레드 = 공격

> "플레이스타일 바는 블루 레드가 수비 공격이다."

```
블루 = 수비   안전함  ↔  변칙적
레드 = 공격   느린전개 ↔  빠른전개
```

바 두 줄의 색 이름이 아니다. **그 진영으로 뛴 라운드만 골라서 잰다.**
수비 라운드의 움직임으로 공격 성향을 재면 거짓이 된다.

### 이 결정이 새로 만든 `[미확인]` — 라운드별 진영

폭파미션은 전반·후반에 진영이 바뀐다. `Match` 의 red/blue 클랜은 **경기 단위** 값이고
배틀로그 이벤트에는 진영 표시가 없다. 즉 **어느 라운드에 어느 진영이었는지 지금은 모른다.**

후보는 셋이다 — ① 라운드 수의 절반에서 바뀐다고 보기 ② 스코어 진행에서 되짚기 ③ 포기.

**추측해서 절반으로 자르지 마라.** 연장·조기종료가 있으면 어긋나고, 그러면 네 축이 전부
조용히 틀린 값을 낸다. 먼저 실데이터로 재고 결정한다.

### 지금 만들 수 있는 것과 없는 것 (4절 육각형 기준)

| 축 | 상태 |
|---|---|
| 3 캐리력 · 2 샷싸움(라플 딜량) | ✅ 재료가 DB 에 있다 |
| 5 원어택 성공률(라플) | ⏸ 포지션 자동 판정(3장) 선행 |
| 2 스나싸움 · 5 작업 성공률(스나) | ⏸ 배틀로그 추가 수집 (지금 23명 786경기분) |
| 1 세이브 · 4 매치의사나이 · 6 소수싸움 | ⛔ 그 경기 **10명 전원** 배틀로그 필요 |
| 8절 플레이스타일 바 4축 | ⛔ 구역 이름 + 라운드 복원 + **라운드별 진영** |

10절 **오늘 퍼포먼스는 재료가 전부 있다.** 맵도 배틀로그도 없이 지금 만들 수 있다.

---

## D-183 — 플레이스타일 구역은 **지도를 맞추지 않고** 좌표 위에 직접 칠해서 정했다 (2026-08-29)

`docs/PLAYER_TRAITS_SPEC.md` 8절이 요구하는 여섯 구역
(`컨뒤` · `A설대` · `달방` · `ㄱ자` · `벙커` · `비롱`)을 좌표로 확정했다.
결과는 **`data/barracks/style-zones.json`** (칸 196개 · 격자 10단위).

### 정한 것 ①  인벤 미니맵 정렬은 **포기했다.** 필요하지도 않았다

배틀로그 좌표를 인벤 미니맵 그림에 맞추는 시도를 **여섯 번** 했고 전부 실패했다.

| 시도 | 왜 실패했나 |
|---|---|
| 1차 | 그림을 줄일수록 유리해지는 함정 |
| 2차 | 마스크가 틀렸다 — 노란 글씨·배경 격자까지 맵으로 잡았다 |
| 3차 | **맞추는 대상이 틀렸다.** 좌표를 벽 *위에* 얹으려 했다. 좌표는 방 *안*에 있다 |
| 4차 | 바깥 테두리에 끊긴 곳이 있어 flood fill 이 새어 "맵 안쪽"이 그림의 12.5% 로 잡혔다 |
| 5차 | 마스크는 고쳤지만(31.8%) **지표가 아무것도 못 가렸다** — 네 방향 전부 90~92% |
| 6차 | 무작위 대비 이득으로 바꿨더니 그림 가장자리로 도망갔다 |

5차의 실패가 핵심이다. **벽이 얇고 열린 공간이 31.8% 라, 구름을 아무 빈터에 던져도
90% 가 나온다. 90% 가 곧 무작위 기준선이었다.** 사람에게 후보 그림을 보여 고르게 해도
"전부 틀린 것 중 하나"를 고르는 일이 됐다.

**정렬은 구역 이름을 얻으려는 우회로였을 뿐이다.** 우회로가 막혔으면 원래 길로 간다.

### 정한 것 ②  원래 길 — 사용자가 **좌표 산점도 위에 직접 칠한다**

기존 `zonemap.json`(B · 2층 · 숏)도 그렇게 만들어졌고 지금 포지션 판정 85% 를 내고 있다.
같은 방식을 그대로 썼다. **지도가 틀리든 말든 결과가 틀리지 않는다 — 좌표 위에 칠하니까.**

### 정한 것 ③  칠한 그림은 **눈으로 읽지 않는다**

사용자가 보낸 이미지(1125×1219)에서 **격자선을 찾아** 변환을 먼저 정했다.

```
세로선 5개  1 · 246 · 490 · 735 · 979      → 픽셀 = 0.70 + 2.4460 * x
가로선 5개  8 · 253 · 497 · 742 · 986      → 픽셀 = 7.70 + 2.4460 * y
```

가로·세로 배율이 **똑같이 2.4460** 으로 나온 것이 이 변환이 맞다는 증거다
(따로 맞춘 값이 아니라 각각 최소제곱으로 뽑은 값이다).

### 정한 것 ④  산점도 점을 색칠로 착각하지 않는다

그림에는 빨강·파랑·초록·회색 **점**이 흩어져 있다. 색만 보면 그것들도 걸린다 —
실제로 첫 추출에서 `ㄱ자` 가 `x -0~459` 로 그림 전체를 덮었다.

칠한 자리는 **큰 덩어리**고 점은 몇 픽셀이다. 이어진 덩어리를 찾아 900픽셀 이상만 남겼다.
덩어리 155개 중 **정확히 6개**가 남았다 — 색 하나에 덩어리 하나다. 이게 검산이다.
화면 캡처의 위·아래 검은 띠도 검정으로 잡히므로 가장자리 14픽셀을 잘라냈다.

### 결과

| 구역 | 색 | x | y | 칸 |
|---|---|---|---|---:|
| 비롱 | 보라 | 132~185 | 169~404 | 97 |
| 벙커 | 파랑 | 143~189 | 403~451 | 25 |
| ㄱ자 | 검정 | 169~228 | 247~297 | 25 |
| 달방 | 빨강 | 170~205 | 299~328 | 15 |
| A설대 | 초록 | 365~406 | 297~319 | 15 |
| 컨뒤 | 노랑 | 400~423 | 259~311 | 19 |

### 지켜야 할 것

- **`zonemap.json` 을 덮어쓰지 않았다.** 그쪽은 포지션 판정용(B · 2층 · 숏)이고
  이쪽은 플레이스타일용이다. `sniper-lane.json` 과 같은 관계다 — 쓰임이 다르면 파일도 다르다
- `data/barracks/map-align.json` 의 `status: 미해결` 은 **그대로 둔다.** 그 값은 여전히 쓰면 안 된다
- 구역을 고치고 싶으면 **다시 칠해서 다시 뽑는다.** 숫자를 손으로 고치지 마라 —
  어느 칸이 왜 그 구역인지 설명할 수 없게 된다

### ⚠ 유보 — 칠한 사람이 스스로 단 단서 (2026-08-30 추가)

구역 경계는 **사용자가 눈으로 칠한 것**이고, 사용자 본인이 이렇게 말했다.

> "근데 나도 정확하게 다 맞춘것같진 않아"

그러므로 이 경계는 **정답이 아니라 현재 최선의 근사**다. 방식(좌표 위에서 정한다)은 흔들리지
않지만 **경계선 위치는 흔들릴 수 있다.** 경계 근처에서 갈리는 판정을 확정처럼 보고하지 마라.
자세한 것은 D-192.

### 이걸로 풀린 것과 안 풀린 것

8절 네 축 중 **블루(수비) 두 축의 구역 조건이 풀렸다.**
남은 것은 여전히 **라운드별 진영**(D-182)과 **그 경기 10명 전원 배틀로그**다.

---

## D-184 — 배틀로그는 **클랜 단위로 받는다** · 진영은 `team_no` 가 아니라 **폭탄**이 말해 준다 (2026-08-29)

D-182 가 남긴 `[미확인]`("라운드별 진영을 어떻게 아는가")을 실측으로 풀었다.
사용자 가설("레드였다가 블루가 된 라운드가 교대 지점")에서 출발했고, **가설의 방향은 맞았으나
필드가 달랐다.**

### 정한 것 ①  `team_no` 를 진영으로 읽지 마라 — 그건 **클랜 번호**다

클랜 단위 응답에 `team_no` 가 있어서 이걸로 될 줄 알았다. 아니었다.
같은 응답의 `teamList` 가 짝을 알려 준다.

```
teamList: [ { team_no: "0", clan_no: "070716026783" },
            { team_no: "1", clan_no: "060503000068" } ]
```

실측 경기 `260820162642124001` 에서 **14라운드 내내 선수 13명 전원의 `team_no` 가
한 번도 바뀌지 않았다.** 이 값을 레드/블루로 읽으면 경기 절반이 뒤집힌다.
`clanByTeamNo()` 가 이 짝을 만들어 준다 — 확인하지 않고 쓰지 마라.

### 정한 것 ②  진영은 **폭탄**으로 되짚는다

폭파미션이므로 규칙 자체가 진영을 말해 준다.

```
C4 설치(c4-install)   → 그 라운드에 그 팀은 공격
C4 해체(c4-dismantle) → 그 라운드에 그 팀은 수비
```

실측(같은 경기 · team 1): 4라운드 **해체**(수비) · 10·11라운드 **설치**(공격).
14라운드 경기이므로 전·후반 교대와 앞뒤가 맞는다.

**모르는 라운드를 채우지 않는다.** 교대가 한 번뿐이라는 성질로 앞뒤를 넓히되,
근거가 있는 마지막 전반 라운드와 첫 후반 라운드 **사이는 비워 둔다**(위 예에서 5~9라운드).
근거가 어긋나면(같은 라운드에 설치·해체가 둘 다, 또는 진영이 두 번 바뀜)
**아무것도 확정하지 않는다.** 다수결하지 않는다 (D-106).

### 정한 것 ③  라운드 승패는 `win_flag` 에서 나온다 — 단 **폭탄 줄은 빼고**

한 라운드의 줄들은 대개 같은 `win_flag`(조회 클랜 기준)를 갖는다.
그런데 **폭탄 줄만 기준이 다르다** — 폭탄을 만진 사람 기준이라 조회 클랜이 진 라운드에서도
`"win"` 으로 온다. 실측에서 값이 갈린 라운드는 4·10·11 이었고 **그 셋이 정확히 폭탄이 있는
라운드**였다. 그래서 폭탄 줄을 빼고 본다. 빼고도 갈리면 그 라운드는 `null` 이다.

### 정한 것 ④  선수 단위 수집으로는 **라운드 복원이 영원히 안 된다**

| | 선수 단위 (`GetBattleLog`) | 클랜 단위 (`GetBattleLogClan`) |
|---|---|---|
| 한 번에 오는 것 | 그 선수가 얽힌 이벤트만 | 그 클랜 전원 + 상대까지 |
| 실측 | 경기 616건 중 **485건이 1명분** (최대 4명분) | 한 번 호출에 이벤트 135건 · 선수 13명 · 1~14라운드 |
| 필드 | 21개 | **32개** |

선수 단위 응답에 **없는** 필드 11개:
`team_no` · `target_team_no` · `win_team_no` · `win_flag` · `myself_tag` ·
`target_event_type` · `target_event_category` · `target_weapon` ·
`team_name` · `target_team_name` · `teamList`

라운드 복원(육각형 1 세이브 · 4 매치의사나이 · 6 소수싸움)에는 그 경기 10명이 필요한데
선수 단위로는 **한 경기도 채우지 못했다.** 클랜 단위면 경기당 두 클랜으로 채워진다.

### 정한 것 ⑤  클랜 응답을 **선수 로그로 둔갑시키지 않는다**

기존 적재 코드는 원문의 첫 `str_usn` 을 주인으로 삼았다. 클랜 응답에 그대로 넣으면
**그 클랜의 첫 선수 개인 로그**가 되고, 그 선수 혼자 한 경기에서 10명분 좌표를 가진 것이 되어
포지션 판정(`subjectKind: 'user'` 만 읽는다)이 통째로 오염된다.

가르는 기준은 **응답 자신이 가진 표시**다 — `teamList` 가 있으면 클랜 응답이다.
수집기가 붙인 이름표를 믿지 않는다 (`isClanResponse()`).
스키마는 바꾸지 않았다 — `BarracksBattleLogRaw.subjectKind` 가 이미 `clan` 을 받는다 (D-174).

### 알아 둘 것 — 목록 응답에 **상대 클랜 번호는 없다**

`GetClanMatchList` 가 주는 것은 조회 클랜의 `clan_no` 하나와 양 팀의 **이름**뿐이다
(`red_clan_no`/`blue_clan_no` 는 존재하지 않는다 · 실측). 그래서 한 클랜을 조회하면
그 클랜 쪽 응답만 받는다. **39곳을 다 돌면 양쪽이 채워진다.**
`red_clan_name`/`blue_clan_name` 은 그 경기에서 어느 클랜이 어느 진영으로 **시작**했는지라,
폭탄 판정을 **교차 검산**하는 데 쓴다.

### 코드 위치

- 순수 함수: `packages/nexon/src/roundSide.ts`
  (`bombEvidenceOf` · `roundSidesOf` · `roundResultsOf` · `clanByTeamNo`)
- 적재 판정: `apps/worker/src/jobs/battlelog.ts` 의 `isClanResponse()`
- 수집 스니펫: `packages/db/legacy/barracks-clan-battlelog-snippet.js` (브라우저에서 돌린다)
- 테스트: `packages/nexon/src/__tests__/roundSide.test.ts`(17) ·
  `apps/worker/src/__tests__/battlelog.test.ts`(11 · 클랜 판정 4건 추가)

### 3경기 실측 (2026-08-29 · `evermore` 최근 제3보급창고 경기)

| 경기 | 라운드 | 이벤트 | `str_usn` | **등장 선수** | 폭탄 근거 | 좁혀진 교대 구간 |
|---|---:|---:|---:|---:|---|---|
| `...172701124001` | 13 | 104 | 5 | **10** | 0팀 설치 6·8 → 1팀 설치 11·12 | 8 ~ 11 |
| `...171553124001` | 17 | 129 | 5 | **10** | 0팀 설치 3·5 → 0팀 해체 10 | 5 ~ 10 |
| `...170447124001` | 17 | 128 | 5 | **10** | 0팀 설치 2·4·5·7·8 → 1팀 설치 12·14·16 | 8 ~ 12 |

**한 번 호출에 양 팀 10명이 다 나온다.** `str_usn`(그 로그의 주인)은 조회 클랜의 5명이지만
킬·데스 상대로 나머지 5명이 함께 실려 온다. 킬은 반드시 양 팀이 한 명씩 얽히므로
**경기당 한 번만 불러도 사실상 전원이 채워진다** — 두 번 부르는 것은 교차 검산용이다.

> `[미확인]` 세 경기 모두 좁혀진 구간이 **10라운드**를 품는다(8~11 · 5~10 · 8~12).
> "전반 9라운드 뒤 교대" 로 보이지만 **표본 3건이다. 규칙으로 박지 마라.**
> `roundSidesOf()` 는 경기마다 폭탄 근거로 따로 판정하고, 근거가 없으면 비워 둔다.
> 대규모 수집 뒤에 이 가설을 숫자로 검증한다.

### 아직 없는 것 (정직하게)

- ~~**클랜 단위 수집분 자체.** 스니펫은 만들었고 사용자 브라우저에서 돌려야 한다~~
  → **2026-08-30 갱신: 들어왔다. 아래 참조**
- 라운드 복원(누가 언제까지 살아 있었나)은 아직 코드가 없다. 재료가 들어온 뒤 일이다
- `round_result` 필드는 목록 응답에서 **항상 `null`** 이었다. 다른 경로가 있는지 미확인

### 2026-08-30 갱신 — 클랜 단위 수집분은 **들어와 있다** (파일 실측)

`apps/worker/reports/clan-battlelog/` 를 전수 파싱한 결과다. 위의 "아직 없다" 는 **낡았다.**

| | 실측값 |
|---|---:|
| 파일 | **75개** (`part` 1~75 · 중복 없음) |
| 용량 | 188,531,800 B (**약 180MB**) |
| 수집 행 | 3,000 행 |
| **고유 경기** | **2,615건** (385경기는 양 클랜 시점에서 두 번 잡혔다) |
| **수집 주체 클랜** | **12곳** |
| 로그에 등장한 클랜 이름(상대 포함) | 253개 |
| **이벤트 줄** | **251,375개** (고유 경기 기준 중복 제거 시 219,600) |
| 경기 기간 (`rows[].matchTime` · KST) | **2026-07-03 22:17 ~ 2026-08-29 18:42** (약 58일) |
| 수집 시각 (`collected_at`) | 2026-08-29 09:27~10:11 Z (약 44분) |
| 맵 | 3,000행 전부 `제3보급창고` — **단일 맵** |

파일 모양은 `{ collected_at, part, note, rows[] }` 이고, 행 키는
`source · endpoint · matchKey · clanNo · subject · matchTime · mapName · listed · raw` 다.
이벤트는 `raw.battleLog[]` 에 있고 `round · kill_x/kill_y · death_x/death_y · str_usn ·
event_type · weapon · team_no · win_flag` 를 포함한다.

> ⚠ **이 75개 파일 안에는 `teamList` 배열이 없다.** 위 "정한 것 ①" 이 말한
> `clanByTeamNo()` 짝짓기를 **이 수집분에는 적용할 수 없다.** 여기서는 `team_no` 가
> `"0"`/`"1"` 두 값뿐이고, 클랜 신원은 `subject`/`clanNo` + `listed.redClanName`/
> `blueClanName` 에서 와야 한다. **`team_no` 를 진영으로 읽지 말라는 원칙은 그대로다** —
> 진영은 여전히 폭탄(정한 것 ②)으로 되짚는다.

### 그러나 **DB 적재는 아직이다** (2026-08-30 · 로컬 DB 실측)

| `BarracksBattleLogRaw` | 행 |
|---|---:|
| 전체 | **1,174** |
| distinct `matchKey` | 984 |
| `endpoint = GetBattleLog` (선수 단위) | 786 |
| `endpoint = GetBattleLogClan` (클랜 단위) | 440 |

**파일에는 2,615경기가 있는데 DB 에는 984 경기뿐이다.** 클랜 단위 수집분의 대부분이
아직 적재되지 않았다. `nexon battlelog-import --file <수집.json> --confirm` 은 멱등이므로
75개 파일을 그대로 다시 넣어도 안전하다.

---

## D-185 — 전투력 육각형은 **모르는 축을 0으로 그리지 않는다** · `상세정보` 를 육각형 옆으로 옮겼다 (2026-08-29)

`docs/PLAYER_TRAITS_SPEC.md` 4절(육각형) · 8절(플레이스타일 바) · 9절(카드 배치) · 10절(오늘
퍼포먼스)을 화면으로 만들었다. **원본 3rd.supply 에 없는 화면**이고 `CLAUDE.md` 3장 3번의
명시적 예외다 — 무소속리그(D-165) · 오늘 퍼포먼스(D-182)와 같은 취급이다.

핵심은 하나다. **재료가 없는 축을 그럴듯하게 메우지 않는다** (D-106 · D-034 와 같은 원칙).
육각형은 넓이로 실력을 말하는 그림이라, 모르는 축을 0으로 찍으면 그 넓이가 곧 "못한다" 는
**거짓말**이 된다.

### 정한 것 ①  값 없는 축은 `percentile: null` 이고, **왜 못 재는지**를 함께 적는다

`측정중` 만 적으면 무엇이 없어서 못 재는지 아무도 모른다. 이유를 5종으로 못 박았다
(`packages/contract/src/traits.ts` · `TRAIT_PENDING_KEYS`).

| `pending` | 화면 문구 | 무엇이 없나 |
|---|---|---|
| `rounds` | `라운드 복원 필요` | 그 경기 **10명 전원**의 배틀로그가 있어야 라운드별 생존자가 나온다 |
| `battlelog` | `배틀로그 필요` | 킬로그 자체가 없다 (상대가 그 판에 무엇을 들었나) |
| `position` | `포지션 판정 필요` | 사양 3절의 **포지션 자동 판정**이 먼저다 |
| `games` | `경기 부족` | 표본이 모자라 백분위가 난수가 된다 |
| `weapon` | `주무기 미정` | **누구와 견줄지**를 모른다 |

화면 아래 한 줄은 이유를 중복 없이 모아 적는다 — `측정중 5항목 — 라운드 복원 필요 ·
배틀로그 필요 · 포지션 판정 필요` (`pendingSummary()`).

### 정한 것 ②  육각형은 **여섯 축을 다 쟀을 때만** 잇는다

세 가지 모습이 있다.

```
1. 여섯 축 전부 있다  →  백분위를 이은 도형을 채운다. 그 넓이가 곧 전투력이다
2. 일부만 쟀다        →  잰 축만 점으로 찍고 **도형을 그리지 않는다.**
                         못 잰 축은 축선을 점선으로 둔다 — 그림만 봐도 갈린다
3. 하나도 못 쟀다     →  작은 정육각형이 **심장처럼 뛴다** + `전투력 측정중`
```

사양(4절)이 정한 것은 1과 3이다. **2는 우리가 더했다** — 지금 재료가 두 축뿐이라
1도 3도 아닌 상태가 생겼고, 이미 잰 값을 버리기 아까워서다. 배틀로그가 들어오면 저절로
1로 넘어간다. 원본과 동일함이 검증되지 않았다.

`3` 의 심장박동은 `--scale 1 → 1.06 → 1` 을 1.6초에 왕복한다(`.trait-heartbeat`).
**데이터처럼 읽히면 안 되므로** 폭을 6%로 뒀고, `prefers-reduced-motion` 이면 멈춘다
(그래도 `측정중` 문구가 남아 뜻이 사라지지 않는다).

### 정한 것 ③  모집단은 **같은 주무기 선수들**이고, 주무기 판정은 **과반**이다

스나가 라플보다 킬뎃이 높은 것은 무기의 성질이지 실력이 아니다(사양 4절). 그래서
스나는 스나끼리, 라플은 라플끼리 줄 세운다.

```
mainWeaponOf(rifleGames, sniperGames)
  rifleGames * 2 > total   →  0 (라플)
  sniperGames * 2 > total  →  1 (스나)
  그 밖                    →  null
```

⚠ **`LeaguePlayerWeaponStat.isMain`(D-173)과 딱 한 경우만 다르다.**
`isMain` 은 `절반 이상`(≥)이라 **정확히 반반인 선수를 양쪽 다 참**으로 둔다. 여기서는
견줄 무리를 **하나만** 골라야 하므로 반반이면 `null` 이고 **어느 무리에도 넣지 않는다.**
반반인 사람을 라플로 세면 그 사람의 절반짜리 스나 판이 라플 무리 안에서 견줘진다.

무기를 아는 판이 하나도 없어도 `null` 이고, 그때는 여섯 축 전부 `주무기 미정`이다.

### 정한 것 ④  모집단도 값도 **`상세정보` 와 같은 경기**를 센다

`withLadderMatch()` + `seasonWindowWhere()` (D-164 · D-178). 다른 모집단을 세면
**같은 카드 안에서** `판당 8.3킬` 인 선수가 `캐리력 상위 3%` 같은 어긋난 숫자를 갖는다.
D-176 이 고친 사고(`최근매치 20전 11승 9패` ↔ `상세정보 0승 0패`)와 정확히 같은 함정이고,
이번에는 두 숫자가 **나란히 붙어** 있어 어긋나면 바로 눈에 띈다.

그 선수 자신의 값도 **분포와 같은 캐시**에서 읽는다. 값과 분포를 다른 시점에서 가져오면
같은 어긋남이 다시 생긴다.

판당 킬의 분모는 `_count.kill` — **K/D 를 아는 판수**다 (D-148). 딜량은 결측 양상이 달라
분모를 따로 센다. 딜량이 통째로 결측인 선수는 `damagePerGame`이 `null`이고 **0으로 채우지
않는다** (D-034).

### 정한 것 ⑤  지금 잴 수 있는 축은 **둘뿐**이다

| # | 축 | 라플수 | 스나수 |
|---|---|---|---|
| 3 | 캐리력 (판당 평균 킬) | ✅ 잰다 | ✅ 잰다 |
| 2 | 샷싸움(라플) / 스나싸움(스나) | ✅ 판당 평균 딜량 | ❌ `배틀로그 필요` |
| 5 | 원어택 성공률(라플) / 작업 성공률(스나) | ❌ `포지션 판정 필요` | ❌ `배틀로그 필요` |
| 1 · 4 · 6 | 세이브 · 매치의 사나이 · 소수싸움 | ❌ `라운드 복원 필요` | ❌ `라운드 복원 필요` |

**스나수는 잴 수 있는 축이 캐리력 하나뿐이다.** 딜량으로 `스나싸움`을 대신 채우지 않는다 —
사양이 말하는 스나싸움은 상대 스나와의 교전 성과이고, 그건 킬로그의 상대가 그 판에 무엇을
들었는지를 봐야 나온다.

축 이름은 주무기를 따라 2·5번만 바뀐다(`TRAIT_AXIS_LABEL`). 주무기를 몰라도 이름은 붙이고
(라플 표기가 기본) 값만 `null` 이다.

### 정한 것 ⑥  리그 분포를 **10분 캐시**한다

백분위는 **그 선수 하나만 봐서는 낼 수 없다.** 같은 무기 선수 전원의 값이 있어야 "몇 %" 가
나온다. 그 스캔 비용을 쟀다.

| | 열산리그(`sanply`) 시즌0 창 · 래더 반영 경기 |
|---|---|
| 훑는 참가기록 | **249,962 행** |
| `groupBy(playerId, weapon)` 결과 | 5,931 조합 |
| 구현 당시 실측 (2026-08-29) | 약 **0.5초** |
| 문서화 시점 재측정 (같은 날 · 3회 연속) | **2,403 / 2,385 / 2,474 ms** |

두 실측이 다르다. **같은 쿼리인데 머신 부하에 따라 0.5초에서 2.4초까지 흔들린다** —
어느 쪽이든 프로필을 열 때마다 물기에는 비싸다. 그래서 리그 단위로 한 번 세서
`DISTRIBUTION_TTL_MS` 동안 재사용하고, 같은 순간에 여러 요청이 와도 **진행 중인 약속을
나눠 써서 한 번만** 센다. 실패한 약속은 캐시에서 지운다(TTL 동안 같은 오류를 되돌려 주지
않게).

**대가는 백분위가 최대 10분 낡는 것이다.** 순위표처럼 실시간이어야 하는 값이 아니라
**분포 안의 위치**라 감수한다.

표본이 `TRAIT_MIN_GAMES` 에 못 미치는 선수는 **모집단에도 넣지 않는다.** 두세 판짜리 값이
분포 안에 섞이면 남의 백분위까지 흔들린다.

### 정한 것 ⑦  플레이스타일 바는 **가운데(`정석`)로 채우지 않는다**

두 줄 다 아직 못 잰다. 네 축(스나/라플 × 블루/레드)이 전부 **라운드별 진영**(D-184 의 폭탄
판정) + 라운드 복원 + 맵 구역 이름을 요구하는데, **클랜 단위 배틀로그 수집분이 아직 0건**이다.

> **2026-08-30 정정 — "수집분 0건" 은 더 이상 사실이 아니다.**
> `apps/worker/reports/clan-battlelog/` 에 파일 75개(약 180MB) · **고유 경기 2,615건** ·
> 수집 주체 클랜 12곳 · 이벤트 251,375개(경기 기간 2026-07-03 ~ 08-29)가 들어와 있다
> (실측 표는 D-184 의 `2026-08-30 갱신` 절).
> **막고 있는 것은 이제 수집이 아니라 적재와 라운드 복원이다** — `BarracksBattleLogRaw` 에는
> 1,174행(고유 경기 984)만 적재돼 있고, 라운드 복원 코드는 아직 없다.
> 이 절의 **결론(점을 찍지 않는다)은 그대로 유효하다.** 이유만 바뀌었다.

```
value =  0     "재 봤더니 가운데"   ← 실제 판정
value =  null  "아직 못 잰다"       ← 지금 상태
```

이 둘을 섞으면 그 칸이 무슨 뜻인지 아무도 모르게 된다. 그래서 지금은 눈금과 양끝 문구만
그리고 점을 찍지 않는다. 사양 8절이 "화면 자리와 측정중 표시는 먼저 만들 수 있다" 고 한
그 상태다. 블루·레드는 색 이름이 아니라 **진영**(수비·공격)이므로(D-182) 헤더에
`블루 = 수비 · 레드 = 공격` 을 한 번 밝혀 둔다.

### 정한 것 ⑧  `상세정보` 는 **새로 만든 게 아니라 옮긴 것**이다 (사양 9절)

```
┌ 육각형 ─────────────────┬ 상세정보 ┐   ← 상세정보가 사이드에서 여기로 올라왔다
├ 플레이스타일 바 2줄 ──────┴─────────┤
├ 오늘 퍼포먼스 한 줄 ────────────────┤
└ 경기 상세기록 (아래 · 기존 그대로) ──┘
```

같은 `PlayerStatSidebar` 를 그대로 옮겼다. **항목·순서·표기는 한 줄도 바뀌지 않았다** —
래더·등수 / 승률 / 킬뎃 / 판킬 / MVP / 소속, 그리고 `포지션` 줄(D-161)까지 그대로다.

그래서 두 가지가 따라 바뀌었다.

- 오른쪽 칸에는 **`최근 같이한 플레이어` 만 남았다**
- 모바일 `order` 뒤집기를 **걷어냈다.** 2026-08-28 지시("첫번째 카드가 최상단, 그 밑에부터
  기록")를 지키려고 `상세정보` 를 `order-1` 로 끌어올려 뒀는데, 이제 그 패널이 위 카드로
  올라갔으므로 뒤집기를 남겨 두면 **`최근 같이한 플레이어` 가 경기 기록 위로** 올라와
  같은 지시를 거스른다

### `--color-side-meta` 토큰을 새로 만들었다

어두운 사이드 패널 위의 **보조 글자**(`측정중` · `상위 N%` · 양끝 문구) 색이다.

> `[미확인]` **원본에 이 블록 자체가 없어서 실측할 값이 없다.** 밝은 배경용
> `--color-meta`(#374151)를 어두운 패널에 쓰면 거의 안 보인다. 이미 쓰고 있는 slate 계열
> (`--color-side` #1e293b · `--color-side-line` #334155)의 다음 단계인 **#94a3b8** 을 골랐다.
> **원본과 동일함이 검증되지 않았다.**

### `[미확인]` — 우리가 고른 숫자 둘

| 값 | 어디 | 왜 미확인인가 |
|---|---|---|
| `TRAIT_MIN_GAMES = 10` | `packages/contract/src/traits.ts` | 사양 4절은 "데이터가 모자란 선수" 라고만 적고 **숫자를 정하지 않았다.** 10은 우리가 골랐다 |
| `DISTRIBUTION_TTL_MS = 10분` | `apps/web/lib/server/queries/playerTraits.ts` | 사양에 없는 값이다. 짧으면 조회가 자주 스캔을 물고, 길면 새 경기가 늦게 반영된다 |

둘 다 **원본과 동일함이 검증되지 않았다** (`CLAUDE.md` 3장 7번). 사용자 확정 전까지
상수 하나만 고치면 되도록 분리해 뒀다.

### 코드 위치

| 무엇 | 어디 |
|---|---|
| 판정·라벨·문구 (단일 진실 원천) | `packages/contract/src/traits.ts` **(새 파일)** |
| 응답 스키마 | `packages/contract/src/entities/detail.ts` (`PlayerTraits` · `PlayerPlaystyle` · `LeaguePlayerDetail.traits`/`playstyle`) |
| 실제 서버 — 재료만 센다 | `apps/web/lib/server/queries/playerTraits.ts` **(새 파일)** |
| 응답에 물린 곳 | `apps/web/lib/server/queries/records.ts` (`getLeaguePlayerDetail` 의 `Promise.all`) |
| Mock | `packages/mock/src/store.ts` (`traitCohortsOf` · `buildTraits`) |
| 화면 | `packages/ui/src/record/TraitHexagon.tsx` · `PlaystyleBars.tsx` · `TodayPerformance.tsx` · `traitCopy.ts` **(전부 새 파일)** |
| 토큰·애니메이션 | `packages/ui/src/styles.css` (`--color-side-meta` · `@keyframes trait-heartbeat`) |
| 배치 | `apps/web/app/league/[leagueSlug]/player/[playerId]/page.tsx` |

**Mock 과 실제 서버가 같은 `buildPlayerTraits()` 를 부른다.** 두 곳에서 따로 판정하면
mock↔live 대조가 조용히 어긋난다(오늘 퍼포먼스 D-182 와 같은 구조다). 양쪽은 **재료를 세는
방법만** 다르다 — 픽스처는 무기·K/D·딜량이 항상 함께 있어 결측 처리가 필요 없고, 결정적이라
TTL 없이 한 번 세서 들고 있는다.

### 검증

| | |
|---|---|
| typecheck | 통과 |
| lint | 통과 (`pnpm verify` 가 test 단계까지 진행) |
| test | **1,342 passed · 3 failed · 55 skipped / 1,400** |
| 새 테스트 | `packages/contract/src/__tests__/traits.test.ts`(45) · `packages/ui/src/__tests__/trait-copy.test.ts`(43) · `apps/web/tests/playerTraits.test.ts`(9) |

실패 3건은 **D-185 와 무관한 기존 간헐 실패**다 — `playerPosition.test.ts` 2건 ·
`playerProfileIdentity.test.ts` 1건이 전부 병렬 실행 중 5초 타임아웃이고, 세션재개2 가
같은 증상을 이미 기록해 두었다(단독 실행하면 통과, DB 프로세스는 살아 있음).

> ⚠ **`apps/web/tests/playerTraits.test.ts` 가 전체 실행에서 `afterAll` 훅 10초 타임아웃으로
> 스위트째 실패하는 경우가 있다.** 단독 실행은 9/9 · 7초에 통과한다(그중 `상세정보의 평균킬과
> 같은 경기를 센다` 가 526ms). 이 테스트는 `T185-` 접두사 픽스처를 DB 에 만들었다 지우므로
> 병렬 부하에서 정리가 10초를 넘긴다. **잔여물은 확인했고 0건이다**(league/player/clan/match/map
> 전부 0) — 훅이 타임아웃으로 보고돼도 삭제 자체는 끝난다. 그래도 **훅 타임아웃을 올리든
> 픽스처를 줄이든 손봐야 할 빚**이다.

### 아직 확인하지 못한 것 (정직하게)

- **화면 렌더를 아직 보지 못했다.** 이 실행 환경에서 `listen` 이 `EFAULT` 로 죽어
  (`listen EFAULT: bad address in system call argument 127.0.0.1:3999` · 실측)
  dev 서버를 띄울 수 없었다. `CLAUDE.md` 3장 10번이 요구하는 **실제 렌더 스크린샷 확인이
  빠져 있다.** 육각형·바·오늘 줄·옮긴 상세정보는 **"구현했다" 이지 "검수했다" 가 아니다**
- 사양 6절 **MVP 규칙(최다 킬 · 매치의 사나이)은 손대지 않았다.** 육각형 4번 축 이름
  말고는 관련 코드가 없다
- 임시 점검 스크립트 `apps/web/probe-traits.mts` 는 **커밋 대상이 아니다** (파일 첫 줄에
  그렇게 적어 뒀다)

---

## D-186 — `최근 폼` 을 빼고 그 자리에 **오늘 기록**을 넣는다 (2026-08-29)

사용자가 확정했다. 원문 그대로 옮긴다.

> "그 날 하루 승률과 킬데스를 보여줘 만약 그 날 하루에 한 경기가 없다면 미접속 으로 문구
> 띄우고 오늘 게임을 했으면 실시간으로 기록해 몇전 몇승 몇패인지 오늘의 기준은 오전7시부터
> 다음날 오전7시까지의 기록이 그 날 하루의 기록인거야 그러니까 만약 오늘 8/22일이면 내가
> 오전10시부터 그날 새벽 1시까지 쭉 게임을 했으면 그 기록들을 실시간으로 기록해 그리고
> 8/23 오전 7시에 다시 초기화 하고 다시 기록 겜 안한상태면 미접속"

**원본(3rd.supply)에 없는 표시다.** `CLAUDE.md` 3장 3번의 명시적 예외이고,
무소속리그(D-165) · 오늘 퍼포먼스(D-182) · 육각형(D-185)과 같은 취급이다.

### 정한 것 ①  하루의 경계는 **자정이 아니라 오전 7시 KST** 다

`DAY_START_HOUR_KST = 7` (`packages/contract/src/todayPerformance.ts`).

```
2026-08-22 07:00 KST  ─────────────────────────  2026-08-23 07:00 KST
        └ 오전 10시 ~ 다음날 새벽 1시에 뛴 판이 전부 "8/22 하루" 안에 있다
```

**자정으로 자르면 안 되는 이유가 실제 사용 패턴에 있다.** 클랜전은 밤에 열려 새벽까지
이어진다. 자정에 자르면 **한 번 앉아서 한 판들이 이틀로 쪼개지고**, 그러면 어느 쪽도
그 사람의 "오늘" 이 아니다.

`kstDayStart(now)` 는 지금이 오전 7시 전이면 **어제 오전 7시**를 시작으로 돌려준다.
서버는 UTC 로 도는데 경기 시각 표기는 전부 KST 라(`toKstIso` · `formMonthKey`),
UTC 로 자르면 시차 9시간만큼 통째로 어긋난다.

화면에는 경계를 한 줄로 밝혀 둔다 — `오전 7시 기준 · 다음날 오전 7시에 초기화`
(`packages/ui/src/record/TodayPerformance.tsx`). 이걸 안 적으면 새벽 3시에 뛴 판이
왜 어제로 잡히는지 아무도 모른다.

### 정한 것 ②  오늘 경기가 없으면 문구는 **`미접속`** 이다

`NO_GAMES_TODAY = '미접속'`.

```
0전 0승 0패   ← 쓰지 않는다. "지지도 이기지도 않았다" 는 **결과**처럼 읽힌다
미접속        ← 말하려는 것은 그날 아예 안 왔다는 **사실**이다
```

이때 폼은 판정하지 않는다(`trend: 'unknown'`). 없는 것을 `유지중` 으로 뭉개지 않는다
(D-106).

### 정한 것 ③  `최근 폼`(D-167)을 **화면에서 뺐다.** 코드는 남겨 뒀다

6개월 꺾은선과 판정 문구가 있던 자리를 오늘 기록이 그대로 이어받는다.
그 자리는 원래 원본의 **승률 도넛**이었다.

```
승률 도넛 (원본)  →  최근 폼 6개월 꺾은선 (D-167)  →  오늘 기록 (D-186)
```

**컴포넌트(`PlayerFormPanel`)와 계약 필드(`form`)는 지우지 않았다.**
`WeaponStatPanel` 과 같은 처리다 — 그쪽도 원본에 없어서 화면에서만 빼고
컴포넌트·계약 필드(`weapon_stats` · `sniper_*` · `rifle_*`)는 그대로 두었다
(`apps/web/app/league/[leagueSlug]/player/[playerId]/page.tsx` 주석).

`today` 를 넘긴 쪽이 선수 기록실이다. **클랜 기록실은 이 값을 넘기지 않으므로 도넛이
그대로 남는다** — 사용자가 바꾸라고 한 것은 선수 프로필뿐이다(D-167과 같다).
`today` 가 `null` 이면(응답에 값이 없음) 블록을 그리지 않고 도넛을 되살리지도 않는다.
**오늘 경기가 없는 것과 다르다** — 그때는 값이 있고 문구가 `미접속` 이다.

### 왜 뺐나 — **두 숫자를 나란히 적어 놓고 `꾸준` 이라 부르던 모순**

사양 9절 카드 배치를 사용자가 그려 줄 때 **그 자리에 폼 그래프가 없었다.**
그때 "오늘 퍼포먼스가 최근 폼을 대체합니까?" 라고 물었으나 **답을 받지 못해**
`todayPerformance.ts` 에 `[미확인]` 으로 남겨 둔 채 코드가 둘 다 그리고 있었다.

> `[미확인]` 이 줄이 기존 `최근 폼` 을 **대체하는지 나란히 두는지** 확정되지 않았다.

그 상태에서 사용자가 화면을 보고 지적했다.

> "4퍼나 차이나는데 어케 꾸준한 폼?"

```
최근 10경기  45.4%
직전 30경기  49.2%   →  delta = -3.8%p
FORM_TREND_THRESHOLD_PP = 5  →  판정 `steady` = "최근 꾸준한 퍼포먼스를 보여줍니다"
```

경계가 5%p 라 -3.8%p 는 `꾸준` 으로 떨어진다. **판정 자체는 규칙대로였다.**
문제는 그 판정 바로 위에 근거 숫자 둘이 그대로 적혀 있었다는 것이다 — 보는 사람은
4%p 차이를 눈으로 보면서 `꾸준` 이라는 말을 읽는다. 블록을 빼면서 이 모순도 같이 사라졌다.
**`FORM_TREND_THRESHOLD_PP` 는 손대지 않았다** (D-167 의 값이다).

### 배치 — 오늘 기록은 **따로 카드를 두지 않는다**

```
┌ 육각형 ─────────────────┬ 상세정보 ┐
├ 플레이스타일 바 2줄 ──────┴─────────┤
├ 최근매치 요약 + **오늘 기록** ──────┤   ← 카드 안, 예전 폼 자리
└ 경기 상세기록 (기존 그대로) ────────┘
```

사용자가 그 자리를 지목했다. 구분선은 카드가 이미 쓰는 것(`border-t-divider`)을 그대로 쓴다.

### 판정 규칙은 D-182 에서 바뀌지 않았다

폼은 **킬데스만으로** 정하고(승률은 문구에만 들어간다), 기준은 **시즌 평균 킬데스**,
경계는 `TODAY_FORM_THRESHOLD_PP = 2`(%p), 최소 판수는 `TODAY_MIN_GAMES = 3` 이다.
D-186 이 바꾼 것은 **하루의 경계 · 무경기 문구 · 자리** 셋이다.

### 코드 위치

| 무엇 | 어디 |
|---|---|
| 하루 경계 · 무경기 문구 · 문장 조립 | `packages/contract/src/todayPerformance.ts` (`DAY_START_HOUR_KST` · `kstDayStart` · `NO_GAMES_TODAY`) |
| 한 줄 화면 | `packages/ui/src/record/TodayPerformance.tsx` |
| 폼 자리를 넘겨받은 곳 | `packages/ui/src/record/RecordPanels.tsx` (`RecentMatchSummary` 의 `today` prop) |
| 남겨 둔 컴포넌트 | `packages/ui/src/record/PlayerFormPanel.tsx` · `packages/ui/src/index.ts` 에서 계속 export |
| 남겨 둔 계약 필드 | `packages/contract/src/entities/detail.ts` (`PlayerForm` · `form`) |
| 배치 | `apps/web/app/league/[leagueSlug]/player/[playerId]/page.tsx` |
| 테스트 | `packages/contract/src/__tests__/todayPerformance.test.ts` (`하루의 경계는 오전 7시 KST 다` · `오늘 경기가 없으면 미접속 이다`) |

### `[미확인]`

> **"실시간으로 기록해" 의 실시간 정도가 정해지지 않았다.**
> 화면은 **조회 시점** 기준으로 그날 경기를 센다. 방금 끝난 판이 여기 뜨는 시점은
> 증분 수집 주기(D-168)에 달려 있고, 사용자가 말한 "실시간" 이 그 정도로 충분한지
> 확인받지 못했다. **원본과 동일함이 검증되지 않았다** (`CLAUDE.md` 3장 7번).

### 아직 확인하지 못한 것

**화면 렌더를 아직 보지 못했다.** D-185 와 같은 이유다 — 이 컴퓨터에서 dev 서버가
뜨지 않는다(D-187). `CLAUDE.md` 3장 10번이 요구하는 실제 렌더 스크린샷 확인이 빠져 있다.
`구현했다` 이지 `검수했다` 가 아니다.

---

## D-187 — 로컬 DB 커넥션 상한 · **5433 간헐 실패의 진짜 원인은 이 컴퓨터의 소켓 계층**이다 (2026-08-30)

테스트 전체 실행에서 매번 1~3건이 `Can't reach database server at 127.0.0.1:5433` 로
실패했고 **실패하는 파일이 실행마다 바뀌었다.** 인수인계 문서에 **"원인 미상 · 미해결"**
로 며칠째 남아 있던 그 건이다.

### 원인 — 저장소가 아니라 **컴퓨터**다

생 TCP 로 쟀다. 프로젝트 코드도 Prisma 도 vitest 도 거치지 않는 node `net.connect` 다.

| 대상 | 실측 |
|---|---|
| `127.0.0.1:5433` (열린 포트) | **200회 중 3회 EFAULT** |
| `127.0.0.1:5433` (다른 시점) | **100회 중 21회 EFAULT** |
| `127.0.0.1:59999` (**닫힌** 포트) | **60회 중 3회 EFAULT** · 나머지는 정상 `ECONNREFUSED` |

**닫힌 포트에 붙을 때도 EFAULT 가 난다.** 상대가 PostgreSQL 인지와 무관하다는 뜻이다.
연결 간격을 **0ms · 5ms · 20ms** 로 벌려도 실패율이 그대로였다 — 몰림 문제도 아니다.

`EFAULT`(WSAEFAULT)는 네트워크 오류가 아니라 **"시스템 호출 인자가 잘못됐다"** 는 뜻이라
정상적인 소켓 사용에서는 나올 수 없는 값이다. 같은 이유로 `next dev` 의 `listen()` 도
EFAULT 로 죽어 **이 환경에서는 개발 서버가 아예 뜨지 않는다**
(`listen EFAULT: bad address in system call argument 127.0.0.1:3999` · D-185 실측).

> **고칠 곳은 저장소가 아니라 컴퓨터다** — `netsh winsock reset` + 재부팅,
> 네트워크 필터 드라이버(백신·VPN·방화벽 LSP) 점검.

### 저장소에서 한 것은 **완화**뿐이다

노출 횟수를 줄인다. 근본 해결이 아니다.

```
Prisma 기본 풀 = CPU × 2 + 1  (이 컴퓨터에서 17)
        ↓
LOCAL_CONNECTION_LIMIT = 5    ← 여는 횟수가 줄어 그 확률에 덜 노출된다
```

실측으로 **5가 가장 안정적이었다.** `8` · `12` 는 첫 연결부터 실패해 **스위트가 통째로
skip 되는 일이 잦았다.**

### 루프백에만 붙인다 — **운영은 건드리지 않는다**

```
호스트가 127.0.0.1 · localhost · ::1 · [::1] 이고
URL 에 connection_limit 이 아직 없을 때만  →  connection_limit=5 를 붙인다
```

운영은 Supabase **transaction pooler(6543)** 라 성격이 완전히 다르다. 거기 값을 여기서
정하면 안 된다. 6543 설정 점검은 기존 `warnIfPoolerMisconfigured()` 가 **경고만** 하고
고쳐 주지 않는 것과 같은 태도다. **URL 은 로그에 찍지 않는다** (비밀번호가 들어 있다).

### 코드 위치

| 무엇 | 어디 |
|---|---|
| 실측·판단이 전부 적혀 있는 곳 | `packages/db/src/index.ts` (파일 주석) |
| 상수 | `LOCAL_CONNECTION_LIMIT = 5` · `LOOPBACK_HOSTS` |
| 붙이는 함수 | `localDatasourceUrl()` → `new PrismaClient({ datasources })` |

커밋은 `e9b1f2a` 하나이고 **바뀐 파일은 `packages/db/src/index.ts` 뿐이다**(+58줄).

### `[미확인]`

| 무엇 | 왜 미확인인가 |
|---|---|
| `LOCAL_CONNECTION_LIMIT = 5` | 사양에 없는 우리 값이다. 실측으로 골랐을 뿐 5가 최적이라는 근거는 없다 |
| 이 컴퓨터를 고친 뒤에도 이 완화가 필요한가 | `netsh winsock reset` 이후 재측정하지 않았다. 원인이 사라지면 상한도 필요 없을 수 있다 |

원인을 컴퓨터에서 실제로 제거해 보지 못했으므로 **이 항목은 "완화했다" 이지
"해결했다" 가 아니다.**

---

## D-188 — UI 는 **모든 기능이 끝난 뒤 통째로 바꾼다.** 지금 껍데기는 임시다 (2026-08-30)

> ⚠ **이 항목은 `CLAUDE.md` 1장의 위치를 다시 잡는다.** 프로젝트의 최종 모습에 관한
> 결정이므로, 화면 작업을 시작하기 전에 반드시 읽는다.

사용자가 **두 세션에 걸쳐** 같은 말을 했는데 문서에 한 줄도 남지 않았다. 그 결과 이후 세션들이
"3rd.supply 를 충실히 재현하는 것이 최종 목표" 라고 믿고 일했다. 그건 사실이 아니다.

### 사용자 원문 (다듬지 않았다)

> "ui만 나중에 바꿀 수 있어 전체적으로? 버튼 기능이나 그런건 바꾸지 말고 약간 고급스럽게
>  만들고싶은데 전반적 분위기를 근데 이건 마지막에 하고싶어 왜냐면 기능과 컨텐츠 설계가 먼저야.
>  그래서 일단 ui는 서플라이로 하라고 한거였어 나중에 모든 기능과 작업 다 마치고
>  ui디자인이나 분위기 통째로 바꿔줘"

> "Ui분위기는 배경은 우선 올블랙으로 할거야. 다른 버튼이나 그런것들은 진짜 고급지게 만들고싶어
>  사이트가 너무 복잡하지 않았으면 좋겠고 서플라이 정도의 간결함이면 좋겠어
>  한 화면에 뭔가 꽉 채우는게 너무 답답하고 싫은거같아"

### 정한 것 ①  **3rd.supply 재현은 최종 목표가 아니라 임시 껍데기다**

재현을 부정하는 것이 아니다. **순서**를 말하는 것이다.

```
지금        3rd.supply 껍데기에 기능·컨텐츠를 담는다   ← 재현은 여기서 유효하다
마지막 단계  껍데기만 통째로 갈아 끼운다               ← 예정돼 있다. 즉흥이 아니다
```

"일단 ui는 서플라이로 하라고 한거였어" 가 핵심이다. 원본 UI 는 **기능·컨텐츠 설계를 담아 둘
그릇으로 고른 것**이지, 도달하고 싶은 결과물이 아니다.

### 정한 것 ②  **지금은 여전히 마음대로 바꾸지 않는다** (3장 2번은 살아 있다)

`CLAUDE.md` 3장 2번(임의 디자인 변경 금지)을 **폐기하지 않는다.** 뜻이 이렇게 갈린다.

| | 지금 | 마지막 단계 |
|---|---|---|
| 색·간격·타이포를 "더 예쁘게" | ❌ 금지 | ✅ 그때 한다 |
| 레이아웃 위계 손보기 | ❌ 금지 | ✅ 그때 한다 |
| 버튼이 하는 일 | ❌ 안 바꾼다 | ❌ **그때도 안 바꾼다** |

"버튼 기능이나 그런건 바꾸지 말고" 는 마지막 단계에도 적용된다. **바꾸는 것은 분위기이지
동작이 아니다.**

### 정한 것 ③  방향은 네 마디로 못 박는다

| 방향 | 사용자 표현 |
|---|---|
| 배경 | **올블랙** ("배경은 우선 올블랙으로 할거야") |
| 질감 | **고급스럽게** ("진짜 고급지게") |
| 밀도 | **간결하게** ("서플라이 정도의 간결함") |
| 여백 | **한 화면을 꽉 채우지 않는다** ("꽉 채우는게 너무 답답하고 싫은거같아") |

넷 중 **밀도와 여백이 서로를 설명한다** — 간결함의 기준선이 3rd.supply 라는 뜻이고, 그보다
더 채우면 안 된다는 뜻이다.

### 착수 조건 — "모든 기능과 작업 다 마치고"

착수 시점을 우리가 정하지 않는다. **사용자가 "이제 UI 바꾸자" 라고 말할 때** 시작한다.
그 전까지 이 항목은 계획에만 존재한다. `docs/IMPLEMENTATION_PLAN_1.md` 6절 `V2 후보` 에도
한 줄 넣어 두었다.

### `[미확인]`

- 올블랙의 실제 값(#000 인가, 거의 검정인가), 강조색, 타이포 — **아무것도 정해지지 않았다**
- "고급스럽게" 를 무엇으로 구현할지(여백·선 굵기·모션·질감) 도 정해지지 않았다
- 이 교체가 어느 Phase 뒤에 오는지 — `Phase 10 이후` 인지 별도 단계인지 미정

**추측해서 미리 만들지 마라.** 지금 시안을 그리는 것도 3장 2번 위반이다.

---

## D-189 — 포지션은 **4개**다. `리베` 를 없앴다 (2026-08-30)

### 사용자 원문

> "리베 포지션 없애자 그냥. 간단하게 우리의 포지션은 총 4개야 스나 숏 2층 B
>  굳이 리베랑 비를 구분하지마."

### 정한 것 — 포지션은 이 넷뿐이다

```
스나 · 숏 · 2층 · B
```

`리베` 는 **B 에 흡수한다.** 별도 포지션으로 두지 않고, 화면에도 이 이름을 내지 않는다.

### 코드는 이미 따랐다. 낡은 것은 **문서**였다

`apps/worker/src/jobs/battlelog.ts` 의 `POSITION_ALIAS` 가 이미 이렇게 돼 있다.

```
RIBE:   'B'
LIBERO: 'B'
```

수집분마다 코드 표기가 달랐던 것(`RIBE` / `LIBERO`)을 한곳에서 흡수한다.
**모르는 코드는 바꾸지 않고 그대로 둔다** — 조용히 다른 뜻으로 만들지 않기 위해서다.

### 근거 — 숫자도 같은 편이다

`docs/PLAYER_TRAITS_SPEC.md` 3절 실측이 이미 이 방향이었다.

| 조건 | 정확도 |
|---|---|
| 격자 히스토그램 4분류 (리베 분리) | 80% |
| **B 와 리베 통합 (3분류 + 스나는 주무기로)** | **85%** |

리베를 갈라 두면 정확도가 **떨어진다.** 리베 정답 표본도 3~4명뿐이었다.

### 같이 고친 문서

| 파일 | 무엇이 낡았나 |
|---|---|
| `docs/PLAYER_TRAITS_SPEC.md` 3절 "B vs 리베 — 구분 가능하다" | 리베를 별도 포지션으로 다루고 "표본 100경기로 늘려 재검증" 을 예고했다 → **폐기 표시** |
| `docs/TASK_LEDGER.md` B17 | 표시 목록에 `리베` 가 있었다 → 4개로 정정 |

### 아직 안 고친 것 — **데이터 파일**

`data/barracks/position-labels.example.json` 이 아직 `LIBERO(리베)` 를 유효 값으로 안내한다.

> 이 항목을 쓰는 작업의 범위가 **문서 전용**이라 손대지 않았다.
> **고쳐야 한다** — `note` 문구에서 `LIBERO(리베)` 를 빼고, 예시 줄
> `{ "userNexonSn": "000000003", "playerName": "예시-리베", "position": "LIBERO" }` 도 지운다.
> 지금은 별칭이 `B` 로 흡수해 주므로 **틀린 라벨이 들어와도 결과는 맞는다.** 그래서 급하지는
> 않지만, 안내 문구가 없앤 개념을 계속 가르치고 있다.

---

## D-190 — 일하는 방식: **착수 전 보고 · 조사는 서브에 · 응대는 본체가** (2026-08-30)

사용자가 여러 번, 여러 표현으로 지적한 것인데 `MEMORY.md` 한 줄 말고는 저장소에 없었다.
행동 규칙이므로 여기에 박는다.

### 사용자 원문

> "작업순위 전부 좋아 근데 작업 진행하기 전에 나한테 항상 보고해.
>  이 작업을 하려는데 이거 이렇게 하려는거 맞냐 변경사항 있으면 알려줘라 이런식으로"

> "서브한테 조사 시키고 넌 커밋진행해야지 인마"

> "서브레이던트 한테 돌리라고 해줘 나 너무 힘들어서 못움직이겠어"

> "Db 반영도 병렬로 하고 있니?"

> "너는 나랑 이야기해"

> "자꾸 나한테 시킬래 너 ㅡㅡ"

> "앞으로 할 모든 작업 순서대로 나한테 보고해"

### 정한 것 ①  **착수 전에 보고하고 답을 기다린다**

에이전트를 띄우기 전 · 파일을 고치기 전 · 재계산이나 마이그레이션을 돌리기 전에 세 줄로 적는다.

```
① 무엇을      ② 어떤 방식으로      ③ 무엇이 바뀌는지
```

"이 작업을 하려는데 이렇게 하려는 것이 맞습니까? 변경사항 있으면 알려주세요" 형식이다.
**계획 전체를 한 번 승인받았다고 개별 작업의 확인이 면제되지 않는다.**
이미 손대던 작은 수정은 마무리하되 다음 단계는 반드시 확인을 받는다.

### 정한 것 ②  조사·검증은 **서브에이전트에 병렬로** 넘기고 본체는 계속 진행한다

"서브한테 조사 시키고 넌 커밋진행해야지" 가 정확히 이 뜻이다. 본체가 조사에 붙들려 있으면
사용자가 기다리는 시간이 곧 조사 시간이 된다.

| 서브에 넘긴다 | 본체가 한다 |
|---|---|
| 코드·문서 전수 조사, 파일 통계, 대조 검증 | 사용자와의 대화 |
| 여러 파일을 훑어야 답이 나오는 질문 | 커밋 · 실제 변경 |
| 오래 걸리는 계측 | 판단과 결정 |

DB 반영·수집 같은 **오래 걸리는 일도 병렬로 돌린다** ("Db 반영도 병렬로 하고 있니?").
독립적인 일을 순서대로 돌려 사용자를 기다리게 하지 않는다.

### 정한 것 ③  **사용자 응대는 본체가 직접 한다**

> "너는 나랑 이야기해"

서브에이전트의 결과를 그대로 붙여 넘기지 않는다. 본체가 읽고 **필요한 것만 추려** 답한다.

### 정한 것 ④  **사용자가 할 수 있는 일을 사용자에게 떠넘기지 않는다**

> "자꾸 나한테 시킬래 너 ㅡㅡ"

도구로 되는 일은 직접 한다. 사람 손이 꼭 필요한 것 — 로그인된 브라우저에서만 되는 수집,
사람만 아는 정답 라벨, 눈으로 칠해야 하는 구역 — **만** 부탁한다.

부탁할 때는 **무엇을 어떻게 해야 하는지 구체적으로** 적는다. 실제로 이런 지적이 있었다.

> "너가 그 격자모양 그림 두개 준걸로 뭘 하라는지도 모르겠음"

파일을 던지고 "확인해 주세요" 로 끝내지 않는다. ①이게 무엇인지 ②무엇을 해야 하는지
③결과를 어떤 형태로 돌려주면 되는지를 적는다.

### 정한 것 ⑤  **할 일은 순서대로 보고한다**

> "앞으로 할 모든 작업 순서대로 나한테 보고해"

목록을 순서와 함께 내놓는다. 순서를 바꾸게 되면 바뀐 사실도 알린다.

### 관련

- `MEMORY.md` → `confirm-before-starting-work.md` (같은 지시의 메모리 판)
- 긴 보고는 **평문 코드블록 하나**로 준다 (D-193 ③ · `phone-friendly-reports.md`)

---

## D-191 — 운영 DB 반영은 **보류다.** 로컬↔운영 대조 관측값을 기록으로 남긴다 (2026-08-30)

세션재개2 마지막에 나온 대조 결과인데 문서에 한 줄도 없었다. 이 숫자가 사라지면 다음 세션이
같은 대조를 처음부터 다시 하거나, 모르고 반영해 사고를 낸다.

### 관측값 (세션재개2 · 사용자 보고)

| 무엇 | 값 |
|---|---|
| 운영이 로컬보다 더 가진 경기 | **352건 더 많다** |
| 로컬과 운영의 관계 | **로컬은 운영의 부분집합**이다 |
| 로컬을 운영에 반영하면 열산리그 랭킹 노출 | **15,318명 → 2,266명** (약 **13,052명** 이 빠진다) |

### 사용자가 고른 것 — **"A" = 미리보기만**

`--confirm` 없이 미리보기만 돌리는 쪽을 골랐고, **세션이 거기서 끊겼다.**
그러므로 **반영은 실행되지 않았다.**

### 정한 것 — 규명 전에는 **반영 금지**

> `[미확인]` **빠지는 약 1만3천명이 정상인지 오류인지 아직 규명하지 않았다.**
> 가설은 있다 — 시즌0 창(D-175: 2026-04-01 KST ~ 현재) 밖의 선수라 랭킹에서 빠지는
> 정상 동작일 수 있다. **그러나 확인하지 않았다.** 가설을 결론으로 쓰지 마라.

셋 중 무엇인지 답이 나오기 전에는 `--confirm` 을 붙이지 않는다.

```
① 시즌0 창 밖이라 정상적으로 빠지는 것        → 반영해도 된다
② 로컬이 운영보다 적어서(352건) 생기는 누락   → 로컬을 먼저 채워야 한다
③ 반영 로직이 지우면 안 될 것을 지우는 것      → 코드를 고쳐야 한다
```

**규명 방법은 빠지는 13,052명을 표본으로 뽑아 "마지막 경기 일시" 를 보는 것이다.**
전원이 2026-04-01 이전이면 ①, 시즌0 창 안에 경기가 있는 사람이 섞여 있으면 ② 또는 ③ 이다.

### 지켜야 할 것

- 로컬을 운영에 **덮어쓰지 않는다.** 로컬이 부분집합인 이상 덮어쓰기는 곧 삭제다
- 운영 데이터를 hard delete 하지 않는다 (`CLAUDE.md` 3-A 7번)
- 반영하게 되더라도 **미리보기 숫자를 먼저 남기고** 실행 후 같은 숫자를 다시 센다

---

## D-192 — 착수 게이트("맵 공부가 우선")는 **해제됐다.** 다만 구역 경계에는 유보가 붙는다 (2026-08-30)

### 사용자 원문

> "아직 작업 진행하지마 이 모든게 너가 맵에 대한 이해도가 높아졌을때 진행해야할거같아.
>  맵을 정말 자세히 잘 알려줄게 맵공부가 우선이야 일단. 맵공부 필요없는 ui작업같은건 해도 돼"

이 게이트가 걸려 있던 동안에도 **맵과 무관한 UI 작업은 허용**이었다.

### 정한 것 — 게이트는 **해제됐다**

게이트의 해제 조건은 "맵 이해" 였고, 그것은 **D-183 으로 끝났다.**
인벤 미니맵 정렬을 포기하고 **좌표 산점도 위에 직접 칠하는** 방식으로 여섯 구역을 확정했다
(`data/barracks/style-zones.json` · 칸 196개). 지도를 맞추지 않으므로 지도 이해가 더는
선결 조건이 아니다.

따라서 **맵 관련 작업도 진행할 수 있다.** 다만 아래 유보를 달고 간다.

### 유보 — 구역 경계는 **사용자가 눈으로 칠한 것**이다

사용자 본인의 말이다.

> "근데 나도 정확하게 다 맞춘것같진 않아"

그러므로 구역 경계는 **정답이 아니라 현재 최선의 근사**다.

| 흔들려도 되는 것 | 흔들리면 안 되는 것 |
|---|---|
| 구역 칸 몇 개가 이쪽이냐 저쪽이냐 | 구역을 **좌표 위에서** 정한다는 방식 |
| `비롱`/`벙커` 경계선 위치 | 고치고 싶으면 **다시 칠해서 다시 뽑는다** (D-183) |

경계 근처에서 갈리는 판정을 **확정처럼 보고하지 마라.** 숫자를 손으로 고치지도 마라 —
어느 칸이 왜 그 구역인지 설명할 수 없게 된다.

이 유보는 **D-183 본문에도 추가해 두었다.**

---

## D-193 — 사용자가 지적했으나 기록이 없던 결함 3건 (2026-08-30)

셋 다 사용자가 화면을 보고 지적한 것이다. 하나만 고쳐졌고 둘은 살아 있다.

### ① 판킬 48.3 — **원인 규명됨 · 수정됨**

`상세정보` 의 판당 킬이 **48.3** 으로 나왔다. 사람이 한 판에 48킬을 할 수는 없다.

```
분자 = 누적 킬 (전 기간)
분모 = 래더에 반영된 판수  ← 훨씬 작다
```

**분자와 분모가 서로 다른 모집단을 세고 있었다.** 지금은 같은 경기를 센다
(`withLadderMatch()` + `seasonWindowWhere()` · D-176 · D-185 정한 것 ④).
같은 함정이 D-176 에서도 한 번 났다 — **분자와 분모가 같은 창을 보는지 항상 확인해라.**

### ② 화면에 `알수없음` 이 아직 남아 있다 — **조사 안 됨**

사용자가 화면에서 `알수없음` 표기를 봤다고 지적했다.

> `[미확인]` **어느 화면의 어느 칸인지 특정되지 않았다.** 결측 표기 자체는 정당한 동작이라
> (상대팀 딜량·헤드샷 등 · `CLAUDE.md` 6장) **버그가 아닐 수도 있다.**
> 그러나 D-034 이후 넥슨이 주지 않는 값이 많아 **원본보다 많이 뜨고 있을 가능성**이 있다.
> 조사하지 않았다. 정상인지 결함인지 판정되지 않은 상태다.

조사할 때는 ①어느 화면 ②어느 칸 ③원본 3rd.supply 는 그 자리에 무엇을 띄우는지를 함께 본다.

### ③ 아티팩트가 사용자 **휴대폰에서 깨진다** — **미해결**

사용자 원문:

> "이상하게 다 깨져있는디"

> "계속 다시시도하래"

원인은 규명되지 않았다.

### 그래서 정한 것 — **보고는 아티팩트가 아니라 평문 코드블록으로 준다**

③ 때문이다. 사용자가 못 읽는 형식으로 보고하면 보고를 안 한 것과 같다.

```
긴 보고 · 완료 보고 · 작업 목록  →  코드 블록 하나에 평문
```

표·굵은 글씨·중첩 목록을 쓰지 않는다. 휴대폰에서 그대로 복사해 갈 수 있어야 한다.
`MEMORY.md` → `phone-friendly-reports.md` 와 같은 규칙이고, 이 결정이 그 이유다.

---

## D-194 — 라운드 복원 — **누가 언제 죽었나**로 라운드 상황을 되짚는다 (2026-08-30)

막혀 있던 것이 이것 하나였다. `docs/PLAYER_TRAITS_SPEC.md` 4절 육각형의 **세 축**과
6절의 **우리 MVP** 가 전부 라운드 복원을 기다리고 있었다(D-185 정한 것 ⑤ 의
`라운드 복원 필요`).

```
1 세이브        혼자 남은 라운드 중 이긴 비율
4 매치의 사나이  20분 초과 경기의 마지막 라운드 최다 킬     ← 6절 우리 MVP 도 이걸 쓴다
6 소수싸움      본인 포함 둘이 남아 이겨낸 횟수
```

### 정한 것 ①  **줄마다 죽은 사람이 한 명** 나온다 — 그게 복원의 전부다

재료는 클랜 단위 배틀로그(`GetBattleLogClan`)의 이벤트다 (D-184).
이벤트 한 줄이 **킬 하나를 양쪽에서** 적는다.

```
event_type = death  →  주체가 죽었다 (상대가 죽였다)
event_type = kill   →  반대다
```

그래서 줄마다 죽은 사람이 **정확히 한 명** 나온다. 그 죽음을 라운드별로 시각순으로
세우면 "그 시점에 몇 명 살아 있었나" 가 복원된다(`roundStatesOf`).

한 킬이 **두 줄로 올 수 있다** — 죽인 쪽과 죽은 쪽이 둘 다 조회 클랜이면 그렇다.
`라운드 + 죽은 사람 + 시각` 으로 같은 죽음을 하나로 본다.

`event_time` 은 `"MM:SS"` 이고 **경기 전체 누적 시간**이다(라운드 기준이 아니다).
형식이 아니면 `null` 이고 **`0` 으로 만들지 않는다** — 0초는 "경기 시작 순간" 이라는
실제 값이라, 못 읽은 줄을 0으로 두면 그 줄이 그 라운드의 **첫 죽음**으로 올라간다.

### 정한 것 ②  **양 팀 5명씩 확인된 경기만** 판정한다

`rosterOf` → `isRestorable(roster, 5)`. 팀이 정확히 둘이고 양 팀이 각각 5명이어야 한다.

**한 명이라도 모자라면 그 사람의 죽음을 놓치고, 그러면 "혼자 남았다" 가 거짓이 된다.**
빠진 이벤트는 살아 있는 사람을 더 많게 보이게 하므로 세이브·소수싸움을 **적게** 세는
쪽으로 틀린다. **없는 것을 만들어 내지는 않는다.**

복원 불가면 `roundTallyOf` 가 `null` 이다. **`0` 을 돌려주지 않는다** — 0회는
"겪었는데 없었다" 이고 지금은 "셀 수 없다" 이다 (D-106). 그 축은 화면에서 `측정중` 이다.

같은 사람이 두 팀으로 나오면(응답이 어긋났다는 뜻) 그 사람을 **버린다.**
다수결로 정하지 않는다.

### 정한 것 ③  **2:1 은 소수싸움에 넣지 않는다** (사양 4절 · 사용자 지시)

우리가 유리한 상황이라 능력의 증거가 아니다. `roundTallyOf` 는 둘이 남은 시점에
**상대 생존자가 2명 이상**일 때만 센다(`foesAlive < 2` 면 버린다).

혼자 남은 경우(세이브)는 **1대1 이든 1대5 든 전부 센다.** 넥슨의 `save_cnt` 는
1대3 이상만 세므로 쓰지 않는다(사양 4절).

승패를 모르는 라운드는 분모(`alone` · `outnumbered`)에는 남고 이긴 쪽으로는 세지 않는다 —
**그 상황이 있었던 것은 사실**이기 때문이다.

### 정한 것 ④  매치의 사나이는 **동률이면 `null`** 이다

`lastRoundTopKiller` 는 마지막 라운드 최다 킬 한 명을 돌려주고, 동률이거나 그 라운드에
킬이 하나도 없으면 `null` 이다. **찍지 않는다**(사양 6절).

20분 초과 판정은 여기서 하지 않는다. 플레이시간은 이벤트가 아니라 경기 정보에 있고
넥슨이 안 주는 경기도 있다(D-034). 부르는 쪽이 판단해서 넘긴다.

### 실측 — 수집분 **2,615경기**

`apps/worker/src/dev/roundStateProbe.ts` (운영 코드가 아니다).

| | 실측값 |
|---|---:|
| 수집된 고유 경기 | **2,615** |
| 10명이 다 나온 경기 | **2,606 (99.7%)** |
| 복원 가능(5:5 확인) | **2,606 (99.7%)** |
| 마지막 라운드 최다킬 판정 | **1,609 (61.5%)** — 나머지는 동률이거나 킬 없음 |
| 복원된 라운드 | **29,608** |
| 승패까지 아는 라운드 | **24,780** |
| 혼자 남은 라운드 | **28,777** · 그중 승 **4,164 (14.5%)** |
| 둘이 남은 라운드 | **34,869** · 그중 승 **10,150 (29.1%)** |

팀 인원이 5가 아닌 경우가 **14건**(1명 · 4명 · 6명) 있었다. **전부 판정에서 빠진다.**

### 조회 클랜이 어느 팀인가 — **두 방법으로 교차검산했다**

라운드 승패는 조회 클랜 기준이라, 조회 클랜이 어느 `team_no` 인지 틀리면 승패가 통째로
뒤집힌다. 그래서 서로 독립인 두 방법으로 각각 구해 대조했다.

```
A. teamList 짝짓기 —  team_no ↔ clan_no (D-184)
B. str_usn 소속    —  그 로그의 주체는 조회 클랜 선수들이다. 전부 한 팀이면 그 팀이다
```

**2,221건 전부 일치 · 불일치 0.** 어긋나면 그 경기의 승패를 읽지 않게 해 뒀다 —
다수결하지 않는다 (D-106). 두 클랜이 같은 경기를 각각 보낸 경우는 기준이 갈리므로
**한 클랜이 보낸 것만 있을 때에 한해** 승패를 읽는다.

### D-184 가 남긴 `[미확인]` 중 **해소된 것과 아닌 것**

> D-184: "표본 3건이다. 규칙으로 박지 마라."

- ✅ **해소** — "이 재료로 라운드가 복원되는가". 3건이 아니라 2,615경기로 쟀고
  **99.7% 에서 된다.** D-184 의 "라운드 복원은 아직 코드가 없다" 도 이제 낡았다
- ❌ **미해소** — **"전반 9라운드 뒤 교대" 가설은 여전히 검증되지 않았다.**
  D-184 의 세 경기가 10라운드를 품는 구간(8~11 · 5~10 · 8~12)으로 좁혀졌다는 관측은
  그대로 표본 3건이다. 이번 복원은 **진영 교대 지점을 재지 않았다**

**이 둘을 섞지 마라.** `roundSidesOf()` 는 여전히 경기마다 폭탄 근거로 따로 판정하고,
근거가 없으면 비워 둔다.

### 코드 위치

| 무엇 | 어디 |
|---|---|
| 복원·집계 (순수 함수 · DB 도 네트워크도 모른다) | `packages/nexon/src/roundState.ts` **(새 파일)** |
| `rosterOf` · `isRestorable` | 양 팀 5명씩 확인 |
| `roundStatesOf` | 라운드별 죽은 순서 (중복 제거) |
| `roundTallyOf` | 세이브 · 소수싸움 |
| `lastRoundTopKiller` | 매치의 사나이 |
| export | `packages/nexon/src/index.ts` |
| 실측 도구 | `apps/worker/src/dev/roundStateProbe.ts` **(새 파일 · 운영 코드 아님)** |

진영 판정(`roundSide.ts` · D-184)의 형제 모듈이다.

```
pnpm --filter @sacloud/worker exec tsx src/dev/roundStateProbe.ts
```

### 아직 안 한 것 (정직하게)

- **집계를 DB 에 저장하지 않는다.** 지금은 훑어서 숫자를 찍어 볼 뿐이다
- **육각형 축에 물리지 않았다.** 1 · 4 · 6번 축은 화면에서 여전히 `라운드 복원 필요` 다
- 우리 MVP(6절)도 `lastRoundTopKiller` 를 아직 부르지 않는다
- 지금 확인한 것은 **재료가 복원된다는 것까지**다

---

## D-195 — 스나싸움 · 작업 성공률: **누가 누구를 죽였나**로 총싸움을 가른다 (2026-08-30)

육각형 여섯 축 중 ③번(`스나싸움/작업 성공률`)의 재료다.

### 무엇을 세나

| 지표 | 정의 |
|---|---|
| 스나싸움 | **스나 대 스나** 교전에서 이긴 비율 |
| 작업 | 상대 **스나를 라플로** 잡은 횟수 (= 스나 작업) |

### 무기를 어디서 읽나 — **죽인 쪽의 칸이다**

배틀로그 킬 이벤트는 두 칸에 무기를 담는다.

```
weapon         이벤트 주체(subject)의 무기
target_weapon  대상(target)의 무기
```

주체가 죽였으면 `weapon` 이 죽인 총이고, 대상이 죽였으면 `target_weapon` 이 죽인 총이다.
둘을 뒤집으면 스나수가 라플수로 뒤바뀐다. `packages/nexon/src/duel.ts` 의 `killsOf` 가
이 방향을 한 곳에서만 정한다.

### 무기 코드

넥슨이 내려주는 값은 문자열이다.

```ts
const KILL_KEY = { rifle: 'riple', sniper: 'sniper' }
```

`riple` 은 오타가 아니라 **넥슨이 실제로 쓰는 철자**다. 우리가 고쳐 쓰면 하나도 안 맞는다.

### 우리 DB 의 `weapon` 과 다르다

`MatchPlayerStat.weapon`(0=라이플 · 1=스나이퍼)은 **그 판의 주무기**고,
여기서 세는 것은 **킬 한 건 한 건의 무기**다. 한 판 안에서도 총을 바꿔 든다.
그래서 `weaponAgreementOf` 로 둘이 얼마나 맞는지 재 두었다 — 어긋나면 어느 쪽이
틀렸는지 사람이 판단할 근거가 된다. 자동으로 덮어쓰지 않는다.

### 표본이 얇으면 값을 내지 않는다

`TRAIT_MIN_GAMES`(10판) · `TRAIT_MIN_ROUNDS`(10라운드)에 못 미치면 `null` 이고
화면은 `측정중` 이다. 0% 로 채우지 않는다 (D-106).

---

## D-196 — 원어택: **혼자 남아 뒤집은 판**을 센다 (2026-08-30)

육각형 ②번 축이다. `packages/nexon/src/duel.ts` 의 `oneAttackTallyOf`.

### 정의

그 라운드에서 **우리 팀 마지막 생존자**가 된 뒤, 남은 상대를 **혼자 다 잡아** 라운드를 딴 것.

- 상대가 몇 명 남았는지도 함께 센다 (1대1 · 1대2 · 1대3 …)
- 소수싸움(D-194)과 다르다 — 소수싸움은 **숫자가 밀린 상황**이고,
  원어택은 **혼자 남은 뒤 실제로 뒤집은 것**이다. 소수싸움에서 이겨도
  마지막 생존자가 아니었으면 원어택이 아니다

### 한계 — 정직하게

라운드 복원이 되는 경기에서만 셀 수 있다. 복원이 안 되는 경기(라인업이 10명이
아니거나 이벤트가 끊긴 것)는 분모에서도 뺀다. 표본 수를 함께 저장한다.

---

## D-197 — 하루 성적: **7시가 하루의 경계다** (2026-08-30)

사용자 지시로 `최근 폼 그래프`를 버리고 **그 날 하루 성적**으로 바꿨다.

> "폼 그래프 없애고 오늘 몇퍼 쐈는지 그 날 하루 승률이랑 킬뎃 보여준다"

### 왜 자정이 아니라 7시인가

클랜전은 밤에 한다. 자정으로 끊으면 **한 판이 어제와 오늘로 갈린다.**
새벽 2시 경기는 사람 감각으로 "어젯밤" 이다. 그래서 **KST 오전 7시**를 경계로 삼는다.

```ts
export const DAY_START_HOUR_KST = 7
```

> `[미확인]` 7시는 우리가 고른 값이다. 원본과 무관하다.

### 경기가 없으면 `미접속`

0전 0승 0패로 적지 않는다. `미접속` 이라고 쓴다 — 0% 는 "졌다" 로 읽힌다 (D-106).

---

## D-198 — 최근 3일 기록 — **오늘은 언제나 첫 줄이다** (2026-08-30)

사용자 지시:

> "최근 매치 없애고 해당 유저가 가장 최근에 한 3일치의 기록을 보여줘
>  ex 오늘-미접속 / 8/16 n전n승n패 승률 킬뎃 판킬 / 8/8 n전n승n패 …"

`apps/web/lib/server/queries/recentDays.ts`.

- 하루 경계는 D-197 (오전 7시)
- **오늘 줄은 경기가 없어도 그린다.** 없으면 `미접속`
- 그 아래로 **경기가 있었던 날** 두 개를 최근 순으로 붙인다. 빈 날은 건너뛴다
  (달력 3일이 아니라 **기록 3일**이다 — 사용자 예시가 `8/16` 다음이 `8/8` 이다)
- 판킬 = 판당 킬. 킬뎃과 따로 적는다

---

## D-199 — 포지션은 **선수의 고유 자리**다. 그 판에 든 총과 다르다 (2026-08-30)

사용자 확정:

> "숏 이층 스나 B리베 총4개의 포지이고 B리베는 팀당 두명이다"
> "경기별 포지션은 버리고 유저 개인의 포지션을 확정후 경기 상세에 해당유저의 포지션을 적어준다"
> "그 판에 스나를 누가 들었는지는 잘 파악해 **스나수가 무조건 스나를 드는것만은 아니야**"

### 네 자리와 정원

| 코드 | 표기 | 한 팀 정원 |
|---|---|---|
| `SNIPER` | 스나수 | 1 |
| `2F` | 2F | 1 |
| `B` | B리베 | 2 |
| `SHORT` | 숏포지 | 1 |

### 누가 이기나

```
1. 선수가 직접 등록/수정한 값   ← 언제나 이긴다
2. 주무기가 스나면 `스나수`
3. 좌표로 판정한 자리 (2F · B리베 · 숏포지)
4. 아무것도 없으면 비운다 — 지어내지 않는다
```

사용자 지시가 "유저가 바꿔달라고 하거나 직접 바꾸면 **유저권리먼저 보장**" 이다.
자동 판정이 사람을 덮어쓰지 않는다.

### 애매하면 **비운다** — `POSITION_MIN_MARGIN = 0.17`

사용자가 "이 사람 2층인데 왜 숏이냐" 고 지적했다. 까 보니
`숏 0.9398 · 2층 0.8215 · B 0.7052` 로 1등·2등 격차가 **0.118** 이었다
(전체 분포 중앙값 0.282 · 하위 17%).

우연이 아니다. **2층은 A 의 위층이라 미니맵 x/y 가 겹친다.**
정답 23명 검증에서 틀린 2명도 **둘 다 숏으로** 갔다 — 판정기는 헷갈리면 숏 쪽으로 쏠린다.

그래서 격차가 0.17 미만이면 그 판정을 쓰지 않는다. 틀린 포지션보다 빈칸이 낫다.

> `[미확인]` 0.17 은 실측 분포의 하위 25% 지점이다. 우리가 고른 값이고
> 정답 표본이 늘면 다시 재야 한다.

### `(S)` 는 따로다

포지션 옆의 `(S)` 는 **그 판에 스나를 들었다**는 뜻이고 `MatchPlayerStat.weapon === 1`
로만 붙인다. `weapon` 이 `null` 이면 아무것도 붙이지 않는다.
포지션으로 무기를 추측하지 않는다 — 사용자가 못박은 지점이다.

---

## D-200 — 병영수첩 배틀로그를 **브라우저로 직접 받는다** (2026-08-30)

### 문제

병영수첩 배틀로그(`POST /api/BattleLog/GetBattleLogClan/{경기키}/{클랜번호}`)는
**서버에서 부르면 403** 이다 (AWS WAF 봇차단 · 실측). 브라우저에서 부르면 200 이다.
로그인도 필요 없다.

그런데 브라우저는 파일을 못 쓴다.

### 우회가 아니다

브라우저에서 부르는 것은 **브라우저가 하는 정상 요청**이다.
rate limit 을 우회하지 않고(요청 간격 320ms), CAPTCHA 를 풀지 않고,
비공개 영역에 들어가지 않는다. `CLAUDE.md` 3-A 5번을 지킨다.

### 흐름

```
브라우저 (barracks.sa.nexon.com 에서 실행)
  └ fetch → 원문을 모아 두었다가 60건마다 파일로 내려받는다
       ↓  (다운로드 폴더)
  nexon battlelog-import --file <받은파일> --confirm   ← 원문 그대로 적재 (멱등)
       ↓
  nexon clan-number --confirm    ← 응답의 `teamList` 로 클랜번호를 배운다
       ↓                            받을수록 부를 수 있는 경기가 넓어진다
  battlelogWorklist.ts           ← 아직 안 받은 경기 목록을 다시 뽑는다
```

`apps/web/app/api/dev/barracks-ingest/route.ts` 도 같은 목적의 창구인데,
크롬의 Private Network Access 차단 때문에 실제로는 파일 경유가 더 확실했다.
그 라우트는 **운영에서 404** 이고 로컬 DB 를 볼 때만 열린다.

### 클랜번호를 어떻게 아나 — **참가 선수로 맞춘다**

`apps/worker/src/jobs/clanNumber.ts`. 응답의 팀번호에 속한 선수들이
우리 DB 에서 그 경기의 어느 진영이었는지 보고 짝을 짓는다.
한쪽으로 **8할 이상 몰릴 때만** 잇는다. 다수결로 억지로 밀어 넣지 않는다.

### 실측 (2026-08-30)

```
받은 원문(클랜)   3,845 건
아는 클랜번호        61 개  (35 → 49 → 61 로 늘었다)
포지션 판정         771 명  (517 → 771)
정답 23명 대조     91.3%
```

원문은 버리지 않는다 (`CLAUDE.md` 3-A 1번). 집계는 언제든 다시 만든다.
