# RATING_FINAL_SPEC — SACLOUD 레이팅 최종 사양

> **이 문서는 운영 구현의 source of truth 다.**
> 시뮬레이션 과정·근거는 `docs/RATING_DESIGN_VERDICT.md` 에 있다. 여기에는 **결론만** 적는다.
>
> 확정: 2026-08-25 · **D-145 최종 승인 · FINAL LOCK**
> 이력: D-142 → D-143 → **D-145** (D-144 는 시간축 측정으로 사양 변경 없음)
> D-145 가 D-143 을 supersede 하는 범위는 **신뢰도 · 일방적 경기 억제 두 가지뿐**이다.
> 검증: `scripts/rating-simulation` · 판정 PASS
>
> **formula version: `sacloud-d145`**

---

## 0. 설계 철학 (한 문단)

제3보급창고 클랜전에는 **자동 매치메이킹이 없다.** 상대를 사람이 직접 고른다.
그래서 점수 공식이 매칭 필터 역할까지 해야 한다. 단순 승률로는 안 된다.

> **강한 상대들과 많이 싸우고, 그 강한 상대들을 실제로 많이 이긴 사람이 가장 높이 간다.**

중요도 순서는 다음과 같고 **뒤집지 않는다.**

```
상대 강도 → 승패 → 강한 상대를 상대로 만든 승리 → 충분한 경기 수 → 지속적 활동 → (KD/MVP 는 점수에 없음)
```

---

## 1. 개인 레이팅

### 1-1. 내부 Elo (실력 추정치)

```
기대 승률   E = 1 / (1 + 10^((상대 - 나) / 400))
증감        Δ = K × (실제 − E)              실제: 승=1, 패=0
K = 50 · 기준점 3000 · 제로섬 · 바닥 1000
```

- **제로섬이다.** 내가 얻은 만큼 상대가 잃는다. 점수 총량은 늘어나지 않는다.
- 배치고사(1~10경기) 구간의 Δ 는 **0** 이다.
- KD · 데미지 · 헤드샷 · MVP · 탈주 · 연승연패는 **식에 들어가지 않는다.**

### 1-2. 일방적인 경기 억제 ★ (D-143 · 차단선을 대체한다)

```
그 결과가 얼마나 예상됐는가  h = 이겼으면 E, 졌으면 1 − E
반영 비율                  m = 1                      (h <= 0.80)
                           m = 1 − (h − 0.80) / 0.06  (0.80 < h < 0.86)
                           m = 0                      (h >= 0.86)
delta = K × (실제 − E) × m
```

> **D-145 에서 끝점을 0.88 → 0.86 으로 내렸다.**
> 0.88 에서는 약팀 600판 전승(4,774)이 강한 일정 55%(4,705)를 **이겼다.**
> 0.86 에서 4,684 < 4,705 로 뒤집힌다. 정직한 일정의 점수 변화는 **0** 이다
> (강일정 55% 4,705 → 4,705 · 58% 4,239 → 4,239 · 60% 5,076 → 5,074).

**이변은 언제나 만점이다.** 약한 쪽이 이기면 h = E 가 작으므로 m = 1 이다.
억제는 **예상대로 끝난 일방적인 경기**에만 걸린다.

**왜 필요한가.** 순수 Elo 는 "3100 상대 95% 승" 과 "3500 상대 60% 승" 을 같게 본다.
매치메이킹이 있으면 맞는 말이지만 제3보급에는 없다. 약팀만 골라 잡아 4,876 까지 올라갔다 —
강한 상대를 **한 번도 이기지 않고서**.

**왜 계단(E ≥ 0.90 → 0)이 아니라 경사인가.** 계단에는 두 가지 문제가 있었다.

1. 기대 89.9% 는 제값을 받고 90.0% 부터 갑자기 0 이 된다.
2. **이긴 쪽만** 0 이 되고 진 쪽은 그대로 잃는다 — 점수가 사라져 제로섬이 깨지고,
   강자에게 사냥당한 약팀만 일방적으로 손해를 본다.

경사는 양쪽에 함께 걸리므로 제로섬이 유지되고, 약팀도 일방적으로 깎이지 않는다.

**왜 0.80 ~ 0.88 인가.**

| 변형 | 시즌 실력상관 | 일정 승리의 질 | 시즌 최고 | 약팀 600판 전승 | OUTLIER |
|---|---|---|---|---|---|
| 계단 0.90 (D-142) | 0.9066 | 0.987 | 4,781 | **4,945** | 5,638 |
| 0.65 ~ 0.80 | 0.8760 | 0.873 | **4,145** | 4,423 | 5,295 |
| 0.75 ~ 0.85 | 0.8920 | 0.935 | 4,453 | 4,627 | 5,455 |
| **0.80 ~ 0.88** | **0.8988** | **0.960** | **4,618** | **4,774** | **5,536** |
| 0.80 ~ 0.90 | 0.8995 | 0.965 | 4,652 | **4,873** | 5,550 |

- **끝점(0.88)** 이 양학의 상한을 정한다 — "사냥하는 팀 중 가장 센 팀 + diff(0.88)".
  0.90 이면 4,873 으로 역사적 영역(4900)에 닿아 실패다.
- **시작점(0.80)** 은 정상 상위권이 얼마나 깎이는지를 정한다. 0.65 로 내렸더니
  시즌 최고가 4,145 로 무너지고 4300+ 가 0명이 됐다 — 밴드 의미 자체가 붕괴한다.
  SACLOUD 는 모집단이 작아 상위권이 **자기 수준의 상대를 만나기 어렵다**는 점을 감안해야 한다.

정상 일정(강일정 55% · 500판 55%)의 점수 변화는 **1% 이내**다 (회귀 테스트로 고정).

### 1-2-B. 랭커 승률 자격선 ★ (D-143)

> **강한 상대와 많이 붙었다는 사실은 랭커 자격이 아니다.
> 랭커가 되려면 최소 48% 이상 실제로 이겨야 한다.**

```
표시 점수는 아래 표의 자격을 만족하는 밴드까지만 올라간다.
자격 미달이면 그 밴드 바로 아래로 내려가고, 부족한 승률 1%p 당 20점씩 더 내려간다.
내부 Elo(실력 추정치)는 건드리지 않는다.
```

| 표시 점수 | 최소 시즌 승률 |
|---|---|
| 4000 이상 | **48%** |
| 4300 이상 | 50% |
| 4500 이상 | 52% |
| 4700 이상 | 55% |
| 4800 이상 | 58% |
| 4900 이상 | 60% |

**왜 필요한가.** 순수 Elo 는 "최상위(3400~3600) 상대에게 30% 승" 을 내부 3,318 로 평가한다.
수학적으로는 맞다 — 그 정도 상대에게 30% 를 따내면 평균보다 확실히 위다.
그런데 표시 4,115 는 "확실한 랭커" 구간이다. **10판 중 7판을 지는 사람이 랭커가 된다.**
상대 강도는 승리의 **가치**를 정하는 것이지, 패배의 **면죄부**가 아니다.

**왜 48% 인가.** 4000 문턱만 바꿔 가며 비교했다.

| 문턱 | 최상위 30% | 최상위 45% | 최상위 48% |
|---|---|---|---|
| 45% | 3,699 | **4,199** ← 45% 가 랭커가 된다 | 4,259 |
| 47% | 3,659 | 3,959 | 4,259 |
| **48%** | **3,639** | **3,939** | **4,259** |
| 49% | 3,619 | 3,919 | **3,979** ← 48% 가 랭커가 못 된다 |
| 50% | 3,599 | 3,899 | 3,959 |

47% 와 48% 만 두 조건("45% 는 랭커 아님" · "48% 는 랭커 가능")을 동시에 만족한다.
**48%** 는 그중 높은 쪽이다 — "거의 반반은 이겨야 한다" 는 기준에 가장 가깝다.

**왜 밴드마다 다른가.** 4000 하나만 막으면 47% 선수들이 전부 3,999 에 몰린다.
그리고 "점수가 높을수록 실제 승률도 높아야 한다" 는 요구를 4000 한 줄로는 표현할 수 없다.

**왜 자르지 않고 더 내리는가(soft).** 그냥 자르면 승률 30% 와 47% 가 **똑같이 3,999** 가 된다.
부족한 만큼 더 내리면 30% → 3,639, 47% → 3,979 로 갈린다.

### 1-3. 신뢰도 (표시 점수에만)

```
신뢰도 = min(1, √(경기수 / 150))
```

| 경기 수 | 신뢰도 | 경기 수 | 신뢰도 |
|---|---|---|---|
| 1 | 8.2% | 75 | 70.7% |
| 10 | 25.8% | 90 | 77.5% |
| 20 | 36.5% | 100 | 81.6% |
| 30 | 44.7% | 120 | 89.4% |
| 40 | 51.6% | 135 | 94.9% |
| 50 | 57.7% | 149 | 99.7% |
| 60 | 63.2% | **150** | **100%** |

- 내부 Elo 는 신뢰도의 영향을 **받지 않는다.** 표시 점수를 만들 때만 곱한다.
- **150경기에서 정확히 100% 가 되고 그 뒤로는 완전히 평평하다.**
  판수 자체로 주는 보너스는 없다. 많이 하는 이점은
  "실력을 더 확실히 증명할 기회가 많다" 뿐이다 (1000판 41% → 3,022).

> **D-145 에서 계단식을 대체했다.** 기존 표(40/55/70/85/95/100%)는 한 경기로
> 신뢰도가 **15%p** 뛰는 구간이 있었다 — 내부 편차 450 인 선수는 30→31 · 60→61 · 90→91
> 경계에서 한 판에 **+236점**이 움직였다.
> √ 곡선의 한 경기당 최대 변화는 **0.74%p**, 표시 점수로 **+12점**이다.
>
> | 경계 | 계단식 | √ 곡선 |
> |---|---|---|
> | 30 → 31 | +236 | **+12** |
> | 60 → 61 | +236 | **+8** |
> | 90 → 91 | +236 | **+7** |
> | 120 → 121 | +158 | **+6** |
> | 149 → 150 | +79 | **+5** |

### 1-4. 표시 점수

```
① 표시(기본)  = 3000 + (내부 − 3000) × 신뢰도 × 3.5
② 승률 자격선 적용        (1-2-B)
③ 활동 페널티 차감        (1-6)
```

**이 순서를 반드시 지킨다.** 바꾸면 잠수로 4000 아래로 내려간 사람이 자격선을 우회한다.

- **percentile 앵커를 쓰지 않는다.** "상위 0.5% 는 자동 4300" 같은 상대평가는 금지다.
  압도적인 시즌도 4300 이 되고 약한 시즌의 1등도 4300 이 되어, 점수를 **버는** 구조가 사라진다.
- 고정 선형식이다. piecewise · convex 와 비교했을 때 실력 재현도가 **셋 다 같았고**,
  convex 는 4900/5000 희귀성을 무너뜨렸다. 정확도가 같으면 설명 가능한 쪽을 고른다.
- **hard cap 이 없다.** 5000 을 넘을 수 있다.

### 1-5. 점수대 의미

| 점수 | 의미 | 9시즌 관측 |
|---|---|---|
| 3000 | 시작 / 기준 | — |
| 3500 | 꽤 잘하는 선수 | — |
| 4000 | 확실한 랭커 · **최소 승률 48%** | 9/9 시즌 (6~22명) |
| 4100 | "41층" · 분명한 상위권 | 9/9 시즌 (4~17명) |
| 4300 | 매우 강한 상위권 · 최소 50% | 8/9 시즌 (0~9명) |
| 4500 | 최상위권 · 최소 52% | 6/9 시즌 (0~5명) |
| 4700 | 극상위 · 최소 55% | 1/9 시즌 (0~4명) |
| 4800 | 시즌 압도 · 최소 58% | 0/9 — 정상 시즌에서는 안 나온다 |
| 4900 | 역사적인 시즌 · 최소 60% | 0/9 |
| 5000+ | hard cap 아님 · 역대급만 | 0/9 · 합성 outlier 는 **5,536** 도달 |

고점이 실제로 닿는지는 실험실이 보여 준다 —
최상위(3400~3600) 상대 60% 300판 = **5,076**, 65% = **5,125**, HISTORIC_OUTLIER = **5,536**.

### 1-6. 미참여 감점 (활동 페널티)

**내부 Elo 를 깎지 않는다.** 한 달 쉬었다고 실력이 사라진 것은 아니다.
사라진 것은 **현재 왕좌를 지킬 자격**이다. 그래서 실력 추정치와 활동성을 분리한다.

```
활동 페널티는 표시 점수에서만 뺀다.
구간은 "감점 전 표시 점수" 기준. 하루 단위로 (주간 감점 ÷ 7) 씩 쌓인다.
```

| 감점 전 표시 점수 | 유예 | 주당 감점 |
|---|---|---|
| 4900 이상 | 7일 | −60 |
| 4800 ~ 4899 | 7일 | −50 |
| 4600 ~ 4799 | 7일 | −40 |
| 4300 ~ 4599 | 7일 | −30 |
| 4000 ~ 4299 | 10일 | −20 |
| 4000 미만 | — | **없음** |

- 구간이 표시 점수 기준이라 **내려갈수록 감점 속도도 느려진다** — 자동으로 자기 제한이 걸린다.
- **바닥은 3000.** 감점만으로 기준점 아래로 내려가지 않는다.
- 4000 미만 유저는 아무리 쉬어도 **0점** 깎인다.

**실측 (잠수 일수별 손실)**

| 시작 | 1주 | 2주 | 3주 | 4주 | 6주 |
|---|---|---|---|---|---|
| 4200 | −0 | −14 | −34 | −54 | −94 |
| 4500 | −4 | −34 | −64 | −94 | −154 |
| 4800 | −7 | −47 | −87 | −127 | −206 |
| 4900 | −9 | −59 | −107 | −147 | −227 |

### 1-7. 복귀 — "1판 던지기" 로 초기화되지 않는다 ★

```
경기 1판당 활동 페널티 −8 회복. 그 이상은 회복되지 않는다.
```

경기를 하면 페널티가 **더 쌓이는 것은** 멈추지만, 이미 쌓인 것은 남는다.

| 상황 | 쌓인 페널티 | 완전 회복에 필요한 경기 |
|---|---|---|
| 4800 · 28일 잠수 | 127 | **16경기** |
| 4800 · 42일 잠수 | 206 | **26경기** |
| 4900 · 35일 잠수 | 187 | **24경기** |
| 4500 · 28일 잠수 | 94 | **12경기** |

한 판만 던져서 왕좌를 지키는 경로는 없다.

### 1-8. 개인 퍼포먼스 — **0%** (넣지 않는다)

시드 3개로 0% / ±2% / ±5% 를 비교했다.

| 비중 | 실력 재현도 | 일정 감안 승리의 질 |
|---|---|---|
| 0% | 0.9066 / 0.9173 / 0.8915 | **0.987 / 0.982 / 0.979** |
| ±2% | 0.9076 / 0.9179 / 0.8929 | 0.973 / 0.969 / 0.973 |
| ±5% | 0.9087 / 0.9191 / 0.8938 | 0.903 / 0.915 / 0.921 |

±2% 는 실력 재현도를 **+0.001** 올리는 대신 1순위 지표를 **−0.011** 내린다.
세 시드 모두 같은 방향이다. **1순위 지표를 더 많이 깎으면서 사는 거래**이므로 채택하지 않는다.

KD · MVP 는 **프로필 통계로만** 보여 준다. 덕분에 설명이 한 문장으로 끝난다 —
**"킬뎃은 점수에 들어가지 않는다."**

---

## 2. 클랜 레이팅

### 2-1. Clan Elo

개인과 같은 식이다. 상대는 **상대 클랜의 레이팅**이다.

```
E = 1 / (1 + 10^((상대클랜 − 우리클랜) / 400))
Δ = 50 × (실제 − E)     제로섬 · 기준점 3000
```

### 2-2. 구성 보정 (상한 있음)

```
최근 20경기의 평균 본클랜원 수 → 보정 점수
클랜 최종 점수 = Clan Elo + 구성 보정 − 클랜 활동 페널티
```

| 최근 20경기 평균 본클랜원 | 보정 |
|---|---|
| 1.0 | **+0** |
| 2.0 | +10 |
| 3.0 | +20 |
| 4.0 | +35 |
| 5.0 | **+50** (상한) |

사이 값은 선형 보간.

- **판수로 누적되지 않는다.** 몇 판을 하든 최대 +50 이다.
- 상한 스윕 결과 (클랜 실력 재현도): 상한 0 → 0.8832 · 30 → 0.8762 · **50 → 0.8726** ·
  70 → 0.8672 · 100 → 0.8644. 상한이 클수록 정확도가 떨어진다.
  **50** 은 유도 강도와 정확도의 균형점이고, 실력 차이를 뒤집을 만큼 크지 않다.
- 창 크기 10 / 20 / 30 / 50 은 결과 차이가 거의 없어 **설명하기 쉬운 20** 으로 고정한다.

### 2-2-B. 클랜 상위 자격선 ★ (D-143)

개인의 48% 를 **그대로 복붙하지 않는다.** 클랜은 조직이라 로스터가 돌고 일정도 덩어리진다.
그래서 더 느슨하게 잡는다. 클랜 점수는 내부 Elo 스케일(3000 ± 400)이라 구간도 다르다.

| 클랜 점수 | 최소 시즌 승률 |
|---|---|
| 3150 이상 | 45% |
| 3300 이상 | 50% |

부족한 승률 1%p 당 **6점**씩 더 내려간다 (개인 20점의 1/3.5 — 스케일 비율).

**왜 필요한가.** 방치했더니 승률 **42%** 클랜이 강팀만 상대했다는 이유로
100팀 중 **7위**에 올랐다. 개인과 같은 이유로 이상하다.
자격선 적용 후 상위 20팀의 최저 승률은 38~42% → **51~53%** 로 올라갔다.

### 2-3. 클랜 미참여 감점

개인과 **다른 표**를 쓴다. 클랜은 한 사람이 아니라 조직이라 기준이 짧아도 된다.

| 무경기 기간 | 주당 감점 |
|---|---|
| 0 ~ 6일 | 없음 |
| 7 ~ 13일 | −10 |
| 14 ~ 20일 | −20 |
| 21일 이상 | −30 |

- 경기 1판당 **−5** 회복. 바닥은 3000.
- 목적은 점수 파괴가 아니라 **active leaderboard 의 의미 유지**다.

---

## 3. 대상 판정 (eligibility) — official 게이트 **폐기**

### 3-1. 확정 정책

```
정상적인 제3보급창고 5v5 + 실제 참가자 10명  →  레이팅 대상.
```

- **`homeClanMembers >= 3 OR awayClanMembers >= 3` 게이트를 폐기한다.**
- **`unofficial 이라 rating 0` 은 없앤다.**
- **클랜원 가중치 `{full:1, two:0.7, one:0.4, none:0}` 을 폐기한다.** 전부 1.0 이다.
- official / unofficial 은 **UI·통계용 historical label 로만** 남길 수 있다.

**왜.** 제3보급 인원이 많지 않은 상황에서 "클랜원 3명 못 채우면 점수 없음" 은
사용자를 랭킹에서 이탈시킨다. **열빡을 죽이지 않는다. 진짜 클랜전을 더 가치 있게 만든다.**

### 3-2. 점수 처리 예시

| 경기 | 개인 점수 | 클랜 점수 | 구성 보정 |
|---|---|---|---|
| 클1용4 vs 클1용4 | **지급** | **지급** | 양쪽 +0 |
| 클2용3 vs 클3용2 | 지급 | 지급 | +10 / +20 |
| 클5 vs 클5 | **지급** | **지급** | 양쪽 **+50** |
| 클5 vs 클1용4 | 지급 | 지급 | +50 / +0 |

같은 Elo 라면 클5 클랜이 클1 클랜보다 **최대 50점 위**에 있다. 그뿐이다.

---

## 4. 반복 대진 / 파밍 대응

**자동 감쇠를 넣지 않는다. 탐지만 한다.**

멸망전 20 / 50 / 100 연전과 재편 시나리오에서 **생성된 점수 총합은 전부 0.00** 이었다.
Elo 가 제로섬이라 반복 대전으로 점수가 새로 생기지 않는다.
후반 평균 |Δ| 는 23.6 → 20.2 → 14.4 로 수렴한다.

| 시나리오 | 점수 총합 증가 | 평균 본클랜원 |
|---|---|---|
| 고정팀 (클5 vs 클5) 100판 | 0.00 | 5.00 |
| 20판마다 재편 | 0.00 | 3.40 |
| 5판마다 재편 | 0.00 | 3.35 |
| 소속 분산 열빡 | 0.00 | 2.00 |

열빡 재편은 **구성 보정만** 자연히 낮아진다 (5.00 → 2.00 → 보정 +50 → +10).
탐지 알고리즘 없이 구조적으로 나오는 결과다.

→ **정상적인 라이벌전까지 벌하는 복잡한 규칙을 넣지 않는다.**
   같은 상대와의 대전 횟수는 **관리자 탐지 플래그**로만 집계한다.

---

## 5. 결정적 재현 (deterministic replay)

- 정렬: `startAt` 오름차순 → 같으면 `matchId` 오름차순 (안정 정렬).
  `startAt` 은 `matchId` 앞 12자리(KST)에서 유도한다.
- 레이팅은 **경기 시각 순서대로** 재생한다. 수집 시각·저장 순서를 쓰지 않는다.
- 경기 당시 소속(`match-time affiliation`)으로 판정한다. 현재 소속을 쓰지 않는다.
- 미참여 감점 tick 은 **하루 단위**로 돌리고, 경기 재생과 시간순으로 섞는다.
- 반올림: 내부 Elo 는 **실수로 유지**하고, 표시 점수만 `roundHalfUp` 으로 정수화한다.
  (경기마다 반올림하면 오차가 누적되어 제로섬이 깨진다.)
- `formulaVersion` 을 모든 `rating_update` 에 남긴다. 최종안은 **`sacloud-v1`**.

---

## 6. 엣지 케이스

| 상황 | 처리 |
|---|---|
| 참가자가 10명이 아님 | 레이팅 제외. 기록은 남긴다 |
| 같은 선수가 양 팀에 | 레이팅 제외 · 관리자 플래그 |
| 배치고사(1~10경기) | Δ = 0 · 랭킹 미표시 · 래더 자리에 `배치고사` |
| 상대 클랜을 알 수 없음 | 개인 레이팅만 반영 · 클랜 레이팅 제외 |
| 탈주 | 레이팅에 반영하지 않는다 (승패만 본다) |
| 무기(라이플/스나이퍼) | **계산에 영향 없음.** `sniper_rating_delta` / `rifle_rating_delta` 로 **기록만** 분리 |
| 통합 래더 | `base + sniper_delta + rifle_delta`. 무기 분리가 통합값을 바꾸면 안 된다 |
| 과거(3rd.supply) 경기 | `rating_update` 를 **재계산하지 않는다.** 원본값 보존 |
| 신규 선수 | 3000 에서 시작 · 신뢰도 40% |
| 시즌 전환 | 개인·클랜 모두 3000 에서 다시 시작 (soft reset 없음) |

---

## 7. 사용자에게 하는 설명 (이 이상 복잡해지면 다시 단순화한다)

> 강한 상대를 이기면 많이 오르고, 약한 상대를 이기면 적게 오른다. 지면 반대로 떨어진다.
> **뻔한 경기는 이겨도 져도 점수가 거의 움직이지 않는다.** 대신 **이변은 온전히 반영된다.**
> **랭커(4000)가 되려면 시즌 승률이 최소 48% 는 돼야 한다.** 상대가 강했다는 것은
> 승리의 값어치를 올려 주는 것이지, 패배를 덮어 주지 않는다. 점수대가 높을수록 요구 승률도 올라간다.
> 판수만 많다고 오르지 않는다.
> 킬뎃과 MVP 는 점수에 들어가지 않는다 — 프로필 기록으로만 남는다.
> 150경기까지는 아직 검증 중이라 점수가 완전히 열리지 않는다.
> 높은 점수를 찍은 뒤 오래 안 하면 미참여 감점이 있고, 한 판만 해서는 되돌릴 수 없다.
> 클랜은 용병을 많이 써도 점수를 받는다. 다만 자기 클랜원을 꾸준히 데리고 나오면 조금 더 높게 평가한다.

---

## 8. 운영 코드와의 차이 — REMOVE / CHANGE / KEEP

**실제 코드를 읽고 확인한 목록이다** (추측 아님). 근거 파일·줄 표기.

### REMOVE

| 대상 | 위치 | 현재 동작 | 이유 |
|---|---|---|---|
| official 게이트 | `match.ts` `if (!eligibility.official) return {…players: [], clans: []}` | 비공식이면 증감 0 | 3장 — 폐기 확정 |
| `minConfirmedPerSide: 3` | `constants.ts` | `home>=3 OR away>=3` 판정 | rating 게이트로 쓰지 않는다 |
| `clanWeightByMembers` | `constants.ts` `{full:1,two:0.7,one:0.4,none:0}` | 클랜 증감에 곱함 (`match.ts` `roundHalfUp(result.ratingUpdate * side.clanWeight)`) | 구성은 **상한 있는 보정**으로 대체 |
| `personalK` 가변 K | `formula.ts` `max(kFloor, kBase − rating/kSlope)` | 점수가 높을수록 K 감소 | **고정 K=50** |
| `rewardCapFactor` | `formula.ts` · `rewardCapStart 600 / rewardCapFull 1200` | 승자만 깎는 선형 램프 | **일방적 경기 억제**로 대체 (양쪽 대칭) |
| `repeatDecayFactor` 일체 | `formula.ts` · `repeatDecay/Floor/WindowDays/MinGap` | 이미 꺼져 있음(1) | 탐지만 · 코드 정리 |
| `lineupBlend` / `lineupMinConfirmed` | `constants.ts` · `lineup.ts effectiveOpponentRating` | 상대 클랜 래더와 라인업 평균을 0.5 혼합 | 클랜 상대는 **상대 클랜 레이팅 하나** |
| `personalWinMultiplier` | `constants.ts` · `formula.ts` | 승리 증감에 배수 | 제로섬을 깬다 |
| `minWinReward` | `constants.ts` | 승리 최소 보장 1점 | 억제가 0을 만들어야 하는 구간과 충돌 |

### CHANGE

| 대상 | 현재 | 최종안 |
|---|---|---|
| `initialRating` / `seasonBaseline` | 1500 / 1500 | **3000 / 3000** |
| `ratingFloor` | 0 | **1000** |
| 개인 K | 가변 | **고정 50** |
| `clanK` | 16 | **50** |
| 증감식 | `승: k(1−E)·mult·cap·repeat` / `패: −k·E·repeat` (비대칭) | **`K(actual−E)·m`** (양쪽 대칭 · 제로섬) |
| 반올림 | 경기마다 `roundHalfUp` | 내부는 **실수 유지** · 표시만 정수화 |
| 표시 점수 | 내부값 그대로 노출 | **3000 + (내부−3000)×신뢰도×3.5 → 자격선 → 감점** |
| replay 대상 필터 | `rate.ts` `where: { official: true }` | **제거** — complete 5v5 전부 |
| formula version | `sacloud-p1-PA` | **`sacloud-d145`** |

### KEEP

| 대상 | 이유 |
|---|---|
| `expectedScore` 400 스케일 | 동일 |
| `rate.ts` 정렬 `[{startAt:'asc'},{id:'asc'}]` | 결정적 tie-break 이미 올바름 |
| `placementMatches: 10` · 배치 Δ=0 | 동일 |
| Beta 예외 `constantsForSeason` (배치 0경기) | 정책 스위치 유지 |
| 시즌 soft reset 폐기 (D-064) | 동일 |
| `roundHalfUp` 구현 | 표시 점수에 그대로 쓴다 |
| 개인은 용병도 100% (`match.ts` 주석대로) | 동일 |
| `MatchPlayerStat.matchTime*` 경기 당시 소속 | 동일 — 클랜 귀속 판정에 쓴다 |
| 과거 `rating_update` 보존 원칙 | CLAUDE.md 3-A |

### 신규 구현 필요

- 일방적 경기 억제 `m = ramp(h, 0.80 → 0.86)`
- 신뢰도 `min(1, √(games/150))`
- 표시 점수 변환 + **승률 자격선** + **활동 페널티** (개인 · 클랜)
- 구성 보정 (최근 20경기 · 상한 +50)

### 스키마 — 추가만 한다 (destructive 없음)

| 모델 | 추가 컬럼 | 이유 |
|---|---|---|
| `LeaguePlayer` | `internalRating Float @default(3000)` | 실수 내부 Elo (기존 `rating Int` 는 **표시 점수**로 유지 — 랭킹 인덱스 그대로) |
| | `activityPenalty Float @default(0)` | 표시에서만 빼는 활동 페널티 |
| | `lastRatedAt DateTime?` | 미참여 일수 계산 |
| `LeagueClan` | `internalRating Float @default(3000)` | 동일 |
| | `compositionScore Float @default(0)` | 최근 20경기 구성 보정 |
| | `activityPenalty Float @default(0)` | |
| | `lastRatedAt DateTime?` | |

기존 컬럼은 **하나도 지우거나 타입을 바꾸지 않는다.**

## 9. 운영 이식 · replay 결과 (2026-08-25 완료)

**실행했다.** 아래는 실제 결과다.

### 백업

| 항목 | 값 |
|---|---|
| 파일 | `apps/worker/backups/rating/supply-d145-pre.json` (git 미추적) |
| checksum | `e16c100f49739bd2aa79579db1df8043` |
| 보존 행 | LeaguePlayer 58 · LeagueClan 48 · MatchPlayerStat 1,084 · Match 136 |
| 복원 | `pnpm --filter @sacloud/worker nexon rating-restore --file <경로>` |
| 검증 | replay → restore 왕복으로 **원래 값 복귀 확인 (PASS)** |

`pg_dump` 는 이 환경에 없다 (embedded-postgres 에 포함돼 있지 않다).
JSON 스냅샷이 rollback 수단이다. **한계**: 값은 되돌리지만 replay 가 새로 만든
행(LeaguePlayer 등)은 삭제하지 않는다 — 다시 replay 하면 덮어써지므로 문제되지 않는다.

### replay 결과 (`supply` 리그 · Beta 시즌 0)

| 항목 | 값 |
|---|---|
| 시즌 범위 경기 | 109 |
| **래더 반영** | **29** |
| **새로 포함 (구 정책에서 제외됐던 경기)** | **24** |
| 제외 | 5v4 20 · 5v1 19 · 5v2 18 · 5v3 14 · 4v1 6 · 3v1 2 · 1v1 1 (전부 5v5 아님) |
| 선수 | 155 |
| 클랜 | 17 |
| formula version | `sacloud-d145` (290 stat 행) |

### 검증 결과

| 검사 | 결과 |
|---|---|
| 결정적 replay (dry-run 2회 해시 비교) | **PASS** — 동일 |
| idempotent (실제 replay 2회 DB 해시 비교) | **PASS** — 동일 |
| NaN / Infinity | **0** |
| 래더 반영됐는데 5v5 아닌 경기 | **0** |
| 승률 48% 미만인데 표시 4000+ | **0** |
| 경기별 증감 합 (제로섬) | **0** |
| 옛 공식(`sacloud-p1-PA`) 잔존 | **0** — 대상 아닌 경기의 옛 증감을 지웠다 |
| official 게이트 잔존 | **0** |
| 클랜원 가중치(100/70/40/0) 잔존 | **0** |

### 후속 처리

- 래더 대상이 아닌 경기에 남아 있던 **옛 공식의 증감을 지운다.**
  안 지우면 화면에서는 점수가 오른 것처럼 보이는데 실제 래더에는 없는 상태가 된다.
- 래더 경기가 하나도 없는 선수·클랜은 **기준점 3000 으로 되돌린다.**
  안 하면 예전 1500 기준 값이 남아 랭킹에 섞인다 (실제로 최저 1,476 이 찍혔다).

### 아직 하지 않은 것

- **incomplete 88건은 그대로 제외**다. raw/staging 은 보존돼 있고, 넥슨 할당량이
  회복되면 backfill 후 다시 replay 하면 된다. KDA 를 0으로 채워 complete 처리하지 않았다.

## 10. 남은 미확정

**없다.** 이번 작업에서 4가지(표시 함수 · 감점 · 구성 상한 · 퍼포먼스)를 모두 확정했다.

운영 반영 시 재보정이 필요할 수 있는 값은 아래 하나뿐이며, 이는 미확정이 아니라
**실데이터 확인 항목**이다.

- 시뮬레이션 모집단에는 매치메이킹이 없어 상위 승률이 88~95% 로 극단적이다.
  실제 Beta 데이터로 replay 한 뒤 **밴드 절대값(4000/4500/4800)이 의도한 인원 비율과 맞는지**
  한 번 확인한다. 어긋나면 **표시 배율 3.5 하나만** 조정한다. 다른 상수는 건드리지 않는다.
