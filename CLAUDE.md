# CLAUDE.md — sacloud (서플라이 재현 프로젝트)

이 문서는 이 저장소에서 작업하는 Claude Code(및 모든 기여자)가 **작업 시작 전에 반드시 읽어야 하는 최상위 규칙**이다.
아래 원칙과 `docs/`의 기준 문서가 충돌하면, 먼저 사용자에게 확인한다. 임의로 판단해 진행하지 않는다.

---

## 1. 프로젝트 정의

`3rd.supply`(서든어택 클랜전 기록 / 리그 / 래더 사이트)의 **사용자에게 보이는 결과물**을 새 코드로 재현하는 프로젝트다.

**V1 최우선 목표**

> 3rd.supply를 개선하거나 재해석하지 않는다.
> 현재 3rd.supply의 **페이지 구조 · UI · 기능 · 동작 · 사용자 흐름**을 최대한 충실하게 새 코드로 재현한다.

기존 기능의 삭제·변경, 신규 콘텐츠·기능 추가는 **V1 완성 이후**에만 시작한다.

### ⛔ 2026-08-30 — **껍데기를 갈아 끼웠다. 이 절은 끝났다** (D-204)

아래 D-188 의 서술은 **역사 기록**이다. 예정돼 있던 "마지막 단계" 가 왔고, 실제로 교체했다.

```
지금부터   3rd.supply 를 재현하지 않는다. 화면은 `적진` 톤이 기준이다
```

사용자 지시: *"처음 말했던 서플라이의 형식은 이제 모두 버린다. 형식에 맞추려고 하지말고
데이터들을 유저들에게 가장 편하고 직관적으로 복잡하지 않게 보여줄 수 있게끔 ui를 설계하라"*

시안 14개를 보이고 사용자가 **`적진`**(검정 `#060505` + 진홍 `#d92b2b` + Black Han Sans)을 골랐다.
확정 토큰과 규칙은 **`docs/DECISIONS.md` D-204** 에 있다. 화면을 만들기 전에 그것부터 읽는다.

**여전히 유효한 것**: 원본 소스·자산·문구 복사 금지(3장 4번), 광고 전면 제외(4장),
없는 데이터 지어내기 금지(3장 7번), 버튼이 하는 일은 바꾸지 않는다.

<details><summary>D-188 원문 (2026-08-30 이전)</summary>

위의 재현 원칙은 **지금 유효하고, 지금 지킨다.** 그러나 도달하려는 결과물은 3rd.supply가 아니다.

> "일단 ui는 서플라이로 하라고 한거였어 나중에 모든 기능과 작업 다 마치고
>  ui디자인이나 분위기 통째로 바꿔줘" — 사용자

```
지금        3rd.supply 껍데기에 기능·컨텐츠를 담는다   ← 재현은 여기서 유효하다
마지막 단계  껍데기만 통째로 갈아 끼운다               ← 예정돼 있다. 즉흥이 아니다
```

**기능과 컨텐츠 설계가 먼저다.** 원본 UI 는 그것을 담아 둘 그릇으로 고른 것이지, 우리가
만들려는 모습이 아니다. 전면 교체의 방향은 **올블랙 · 고급스럽게 · 간결하게 · 한 화면을 꽉
채우지 않게**이며, **버튼이 하는 일은 그때도 바꾸지 않는다.**

그래서 두 가지가 따라온다.

- 3장 2번(임의 디자인 변경 금지)은 **그대로 살아 있다.** "어차피 나중에 바꿀 거니까" 는
  지금 마음대로 바꿔도 되는 이유가 아니다. 교체는 **그 단계에 가서 한 번에** 한다
- 착수 시점은 우리가 정하지 않는다. **사용자가 "이제 UI 바꾸자" 라고 말할 때** 시작한다.
  색값·강조색·타이포는 아직 아무것도 정해지지 않았다 — **미리 시안을 만들지 마라**

자세한 것은 `docs/DECISIONS.md` **D-188**.

</details>

---

## 2. 기준 문서 (Source of Truth)

| 문서 | 역할 |
|---|---|
| `docs/3rd-supply-structure.md` | 원본 사이트 구조 분석. 라우팅·도메인 모델·API 계약·화면별 정보 구조의 기준 |
| `docs/IMPLEMENTATION_PLAN_1.md` | 실행 계획(Phase 0~10), 범위, 순서, 완료 조건, 미확인 목록 |
| `docs/DECISIONS.md` | 원본 관측으로 정해지지 않아 **우리가 결정한** 사항의 기록. 구현 중 판단이 필요하면 먼저 확인 |
| `docs/POST_V1_REQUIREMENTS.md` | **V1 이후** 확정 요구사항(관리자·시즌 스냅샷·래더 분리·유료상품·소유권 인증). **V1에서는 구현 금지**, 스키마 설계 시 확장 지점만 참고 |
| `docs/HANDOFF_CURRENT.md` | **새 세션은 이 파일을 가장 먼저 읽는다.** 현재 Phase · DB · Legacy 상태 · 열린 결정 |
| `docs/LEGACY_MIGRATION.md` | 3rd.supply 과거 기록 이관 계획 · 도구 · 차단 상태 |
| `docs/NEXON_INGEST_SPEC.md` | **넥슨 Open API 수집 사양.** 엔드포인트 실측 · 수집 흐름 · 신원 규칙 · 신선도 정책. **Phase 8 작업 전에 읽는다** |
| `CLAUDE.md` (이 문서) | 작업 원칙. 위 문서들보다 상위의 행동 규칙 |

- 계획 문서 본문에는 기준 문서 경로가 `claude/3rd-supply-structure.md`로 적혀 있으나 **실제 경로는 `docs/3rd-supply-structure.md`** 이다.
- 계획 문서의 실제 파일명은 `docs/IMPLEMENTATION_PLAN_1.md` 이다. 대화에서 "IMPLEMENTATION_PLAN.md"로 불려도 이 파일을 가리킨다.

---

## 3. V1 절대 원칙

작업 중 아래 항목은 예외 없이 지킨다.

1. **임의 UX 개선 금지**
   더 나은 흐름이 보여도 V1에서는 원본 흐름을 그대로 재현한다. 개선안은 구현하지 말고 `docs/IMPLEMENTATION_PLAN_1.md`의 `V2 후보`에 기록만 한다.

2. **임의 디자인 변경 금지** — 기준이 바뀌었다 (2026-08-30 · D-204)
   색·간격·타이포를 "더 예쁘게" 바꾸지 않는다는 원칙은 그대로다.
   다만 **기준은 이제 원본 관찰값이 아니라 `적진` 토큰**이다 (`docs/DECISIONS.md` D-204).
   `packages/ui/src/styles.css` 의 토큰을 쓰고, 화면마다 색을 새로 지어내지 않는다.
   **진홍(`--color-accent`)은 아껴 쓴다** — 가는 선 · 1위 · 활성 표시 · 링크 · 중요한 숫자 하나.

3. **임의 기능 추가/삭제 금지**
   원본에 없는 기능을 넣지 않고, 원본에 있는 기능을 빼지 않는다. 범위 변경이 필요하면 먼저 사용자에게 확인한다.

4. **원본 소스코드·자산 복사 금지**
   원본의 JS/CSS 번들, 이미지, 클랜마크, 아이콘, 약관·규정·안내 문구를 그대로 가져오지 않는다.
   **관찰된 동작과 정보 구조만** 재현하고, 코드와 문구는 새로 작성한다.

5. **초기에는 Mock 데이터로 화면과 흐름부터 복원**
   Phase 0에서 API 계약(Zod 스키마)을 먼저 확정하고, Phase 1~6은 Mock(MSW) 기반으로 전 화면·전 흐름을 완성한다.

6. **실제 데이터 연결은 별도 Phase**
   DB·실제 API는 Phase 7, 전적 수집은 Phase 8, 레이팅/랭킹 엔진은 Phase 9에서 진행한다.
   Mock 단계에서 실제 데이터 연동을 앞당겨 시작하지 않는다.

7. **불확실한 내용은 추측하지 않는다**
   확인되지 않은 사양은 그럴듯하게 메우지 말고 `[미확인]`으로 표기한다.
   기존 미확인 목록은 `docs/IMPLEMENTATION_PLAN_1.md` 5장에 있으며, 새로 발견한 미확인 사항은 그곳에 추가한다.
   부득이하게 자체 정책으로 결정했다면 **"원본과 동일함이 검증되지 않음"을 코드 주석과 문서에 명시**한다.

8. **구현 후 Chrome으로 직접 렌더 검수** — 비교 대상이 바뀌었다 (2026-08-30 · D-204)
   **원본 3rd.supply 와 나란히 비교하는 절차는 끝났다.** 이제 원본에 맞추지 않는다.
   대신 실제 브라우저로 확인한다: 글자가 바닥색에 묻히지 않는가 / 가로 스크롤이 없는가 /
   진홍이 넓은 면에 칠해지지 않았는가 / 숫자가 `--font-num` 으로 정렬되는가 /
   상태 표기(배치고사·알수없음·MVP·익명·측정중·포지션 미정)가 남아 있는가 / 이동 흐름이 그대로인가.
   **데이터가 사라졌으면 그것은 결함이다.** 화면 위계를 바꾸는 것과 값을 없애는 것은 다르다.

9. **큰 구조 변경 전 IMPLEMENTATION_PLAN 확인**
   폴더 구조, 패키지 경계, 라우팅 방식, 상태 관리, API 계약을 바꾸기 전에 `docs/IMPLEMENTATION_PLAN_1.md`를 다시 확인한다.
   계획과 어긋나는 변경은 사용자 확인 후, 계획 문서를 함께 갱신한다.

10. **TypeScript 타입 오류 · lint · 테스트 확인 후 완료 처리**
    `typecheck`, `lint`, `test`, `build`가 통과해야 "완료"라고 보고한다.
    실패했으면 실패했다고 그대로 보고한다. 통과하지 않은 작업을 완료로 표시하지 않는다.

    **화면 단위 작업은 여기에 더해 실제 렌더 스크린샷을 반드시 확인한다.**
    `HTTP 200`과 `getComputedStyle` 수치만으로 완료 판정하지 않는다.
    둘 다 통과하는데도 화면이 무스타일일 수 있다 (`docs/DECISIONS.md` D-014).
    CSS가 안 먹으면 `pnpm clean` 후 dev 서버 재시작 + 3000번 포트 좀비 프로세스 확인.
    **404나 "옛날 화면"이 보이면 dev 서버가 여러 개 떠 있는지부터 의심한다.**
    Next는 3000이 점유되면 3001…로 밀려 뜨고, 3000번에는 옛 서버가 남는다 (`docs/DECISIONS.md` D-021).
    `pnpm dev:clean` 으로 정리한다.

11. **중요한 프로젝트 지식과 결정은 문서화**
    사양 판단, 자체 정책 결정, 원본과의 차이, 새로 확인/미확인된 사항은 `docs/`에 기록한다.
    대화에만 남기고 넘어가지 않는다.

---

## 3-A. 최우선 신규 요구사항 — 기존 3rd.supply 데이터 마이그레이션

SACLOUD는 **빈 데이터베이스에서 새로 시작하지 않는다.**
합법적으로 접근 가능한 공개/API 범위에서 누적된 기록을 최대한 보존해 이전한다.

```
기존 기록 보존 → SACLOUD 이전 → 현재 시즌 최종 스냅샷 → 현재 시즌 종료
  → 새 시즌 시작 → 과거 시즌은 계속 조회 가능
```

이 요구사항은 **선택사항도 후순위도 아니다.** 상세 계획은 `docs/IMPLEMENTATION_PLAN_1.md` Phase 8-M,
접근 가능성 조사 결과는 `docs/MIGRATION_GAPS.md`, 실행 결과는 `docs/MIGRATION_REPORT.md`.

**절대 규칙**

1. **원본 응답(Raw)을 버리지 않는다.** `source / endpoint / source_id / fetched_at / raw / migration_version`
   을 보존해, 변환 로직이나 래더 로직에 오류가 나와도 원본에서 재변환·재검증할 수 있어야 한다.
2. **기존 `rating_update`를 추정 공식으로 덮어쓰지 않는다.**
   과거 기록은 원본값 그대로, 신규 경기만 SACLOUD 공식으로 계산한다.
3. **원본 ID를 버리지 않는다.** `source_*_id`를 보존해 나중에 원본과 대조할 수 있어야 한다.
4. **중단 후 재개 가능한 idempotent 파이프라인**으로 만든다. 일회성 스크립트 금지.
   중복 저장 금지 · 실패 기록 · exponential backoff · rate limit 준수.
5. **접근 통제를 우회하지 않는다.** rate limit 우회, CAPTCHA/봇차단 우회, 비공개 영역 접근 금지.
   접근 불가한 데이터는 우회하지 말고 `docs/MIGRATION_GAPS.md`에 사유·영향·대안을 기록한다.
6. **검증 없이 완료 처리하지 않는다.** "수집 완료" 로그가 아니라 **숫자 대조**로 판정한다.
7. **검증·스냅샷 전에 시즌을 종료하지 않는다.** 과거 시즌 데이터는 hard delete 하지 않는다.
8. **원본에 없는 값을 추측해서 만들지 않는다.**

### V1 범위로 승격된 항목

`docs/POST_V1_REQUIREMENTS.md`의 아래 항목은 **V1 필수**로 승격됐다.

- 관리자 시스템
- 시즌 시스템 · 시즌 스냅샷 · 과거 시즌 조회
- 스나이퍼 / 라이플 기록 분리 및 통합 래더 일관성

나머지(유료상품 등)는 여전히 V1 제외다.

---

## 3-B. 래더 계산 핵심 원칙

상세 사양은 `docs/LADDER_IMPLEMENTATION_SPEC.md`에 있다. **Phase 9 착수 시 반드시 먼저 읽는다.**
아래는 어느 Phase에서도 어기면 안 되는 핵심만 추린 것이다.

1. **공식은 하나뿐이다.** 스나이퍼용·라이플용 공식을 따로 만들지 않는다.
   계산된 `rating_update`를 weapon에 따라 **기록만 분리**한다
   (`sniper_rating_delta` / `rifle_rating_delta`). weapon은 계산에 영향을 주지 않는다.
2. **통합 래더 = `base_rating + sniper_rating_delta + rifle_rating_delta`.**
   무기 분리가 통합 래더 값을 바꾸면 안 된다. 공식 입력 `R`은 항상 통합 래더다.
3. **kill/death/assist/MVP/damage/headshot/dropout/연승연패는 공식에 넣지 않는다.**
4. **경기 당시 division 스냅샷을 저장하고 그것으로 계산한다.**
   현재 division을 쓰면 승격·강등 후 과거 경기가 오염된다.
   (`player_division_at_match` / `opponent_division_at_match`)
5. **과거(3rd.supply) 경기의 `rating_update`를 재계산하지 않는다.** 원본값 그대로 보존한다.
   신규 경기만 SACLOUD 공식으로 계산하고 `formula_version`을 남긴다.
6. **래더 상수를 하드코딩하지 않는다.** `RatingConfig`로 분리한다.
7. **배치고사 경기의 `rating_update`는 0이다.**
8. 미확인 항목(0.6 감쇠의 적용 위치 · 배치 종료 후 초기 래더 · 시즌 전환 규칙 · rounding)을
   임의로 확정하지 않는다.

---

## 4. V1 복원 예외사항 — 광고

3rd.supply의 **페이지 구조 · UI · 기능 · 사용자 흐름은 최대한 충실하게 재현**하지만, **광고는 복원 대상에서 완전히 제외한다.**
원본에 광고가 있다는 이유로 광고 관련 요소를 만들지 않는다. 이것은 3장 원칙(임의 기능 추가/삭제 금지)의 **명시적 예외**이며, 유일하게 허용된 의도적 제외 항목이다.

- **Google AdSense 및 기타 광고 코드 추가 금지** — 광고 스크립트, 태그, SDK, 계정 설정, 환경변수를 넣지 않는다.
- **광고 배너 및 인피드 광고 컴포넌트 생성 금지** — 목록 사이에 끼워 넣는 인피드 광고 슬롯도 만들지 않는다.
- **광고 placeholder 또는 빈 광고 영역 생성 금지** — `AdSlot`, `<div class="ad">`, 주석 처리된 광고 자리 등 어떤 형태의 자리표시자도 만들지 않는다.
- **광고 때문에 존재하는 여백도 제거** — 원본에서 광고를 담기 위해 비워둔 컨테이너·마진·고정 높이는 재현하지 않는다.
- **광고를 제외한 실제 콘텐츠가 자연스럽게 이어지도록 구성** — 광고가 빠진 자리는 빈 공간으로 남기지 말고, 앞뒤 콘텐츠가 정상적인 간격으로 연결되게 한다.
- **향후 광고는 별도 기능으로 추가하며 V1에서는 완전히 제외** — 광고 도입이 필요해지면 V1 완료 후 독립 작업으로 설계한다.

원본과 Chrome으로 비교 검수할 때(3장 8번), **광고 영역의 차이는 "차이 아님"으로 판정**한다. 그 외 콘텐츠는 원본에 맞춘다.

## 5. 작업 절차

1. 작업 시작 전 `CLAUDE.md` → 해당 Phase의 `docs/IMPLEMENTATION_PLAN_1.md` 절 → `docs/3rd-supply-structure.md`의 관련 절을 확인한다.
2. 구현 범위를 Phase 안에서 벗어나지 않게 잡는다. Phase를 건너뛰지 않는다.
3. 구현 → 타입/lint/테스트 → Chrome 비교 검수 → 문서 갱신 순서로 마무리한다.
4. 발견한 개선 아이디어는 구현하지 말고 `V2 후보`로 적는다.

### Phase 순서 (계획 문서 4-1의 권장 실행 순서)

```
Phase 0  계약 + Mock 생성기
Phase 1  공통 레이아웃 + 홈
Phase 3  리그 & 랭킹        ← Phase 2보다 먼저
Phase 2  플레이어 & 클랜 프로필
Phase 4  기록실 & 매치 상세
Phase 6  인증 & 관리 화면
Phase 5  게시판
─────────── M1: Mock 기반 화면·흐름 복원 완료 ───────────
Phase 7  DB + 실제 API
Phase 8  전적 수집 파이프라인
Phase 9  레이팅 / 배치고사 / 시즌 / 랭킹 배치
Phase 10 SSR / SEO / 성능 / 운영 → V1 릴리스
```

**V1 완료 = Phase 10 종료.** 그 이전에는 기능 삭제·변경·신규 콘텐츠 추가를 시작하지 않는다.

---

## 6. 도메인 용어 (임의로 바꾸지 말 것)

원본이 쓰는 표기를 그대로 사용한다. 번역하거나 "개선된" 명칭으로 바꾸지 않는다.

- **래더** — Elo 계열 점수. 티어 이름(브론즈/골드 등)은 원본에 **없다**. 만들지 않는다.
- **부리그(division)** — `division_count`가 1이면 단일리그, 2 이상이면 N부리그 탭.
- **배치고사(placement)** — 진행 중이면 랭킹 미표시, 래더 자리에 `배치고사` 표기.
- **알수없음** — 상대팀 딜량·헤드샷 등 결측값 표기. `null` 저장 + UI에서 `알수없음`.
- **탈주(dropout)**, **MVP**, **선레드/선블루**, **정보갱신/전적갱신**, **지난시즌**
- 무기: `0 = 라이플`, `1 = 스나이퍼` (라인업에 `[S]` 표기)
- 커서 페이지네이션: `base64url("next__<id>" | "prev__<id>")`, 랭킹 20건 / 게시판 15건 단위, 페이지 번호 없이 `더 불러오기`

---

## 7. 개발 환경 (2026-08-20 기준)

| 항목 | 상태 |
|---|---|
| 프로젝트 루트 | `C:\Users\LG\Desktop\서플라이` |
| OS / 셸 | Windows 10 Pro · PowerShell / Git Bash |
| Git | 2.55.0.windows.4 |
| Git 저장소 | 초기화 완료 (기본 브랜치 `main`) |
| Git 사용자 정보 | 이 저장소에만 로컬 설정: `SACLOUD Dev` / `sacloud@local.invalid` (전역 설정은 건드리지 않음) |
| Node.js / npm | v24.19.0 / 11.17.0 |
| pnpm | 11.22.0 (npm 전역 설치) — **패키지 매니저는 pnpm으로 통일한다** |
| TypeScript | **5.9 고정** (7.x는 `next build`가 `next.config.ts`를 읽지 못함 — `docs/DECISIONS.md` D-007) |
| Next.js / React | 15.5 / 19 |
| TanStack Query | Phase 1에서 도입 (서버 상태) |
| 진행 단계 | Phase 1 완료 |

### 저장소 구조

```
apps/web/            Next.js 15 (App Router) — 화면 + app/api/** 실제 API
apps/worker/         넥슨 수집 잡 + CLI (Phase 8). Redis/BullMQ 없이 ImportJob 체크포인트
packages/contract/   Zod 스키마 + 엔드포인트 레지스트리   ← 계약의 단일 진실 원천
packages/mock/       결정적 픽스처 생성기 + MSW 핸들러
packages/ui/         공용 컴포넌트 + 디자인 토큰(원본 실측값)
packages/db/         Prisma 스키마 · 시드 · legacy 도구 (Phase 7)
packages/nexon/      넥슨 Open API 클라이언트 · 응답 스키마 · 정규화 (Phase 8, 순수 — DB를 모른다)
docs/                기준 문서 · 결정 기록
```

새 패키지는 **해당 Phase에서 실제로 필요할 때 만든다.** 미리 만들지 않는다.

### 자주 쓰는 명령

```bash
pnpm install          # 의존성 설치
pnpm dev              # 개발 서버 (Mock 모드)
pnpm typecheck        # 전 패키지 tsc --noEmit
pnpm lint             # ESLint
pnpm test             # Vitest
pnpm verify           # typecheck + lint + test  ← 작업 완료 처리 전 필수
pnpm build            # Next.js 프로덕션 빌드 (clean 후 실행)
pnpm clean            # apps/web/.next 삭제 — CSS가 안 먹거나 화면이 깨지면 먼저 이것부터
pnpm dev:clean        # 기존 dev 서버 전부 종료 + .next 삭제 후 3000번에 하나만 기동

# --- Phase 7 이후 (DB) ---
pnpm db:start         # 로컬 개발용 PostgreSQL 기동 (127.0.0.1:5433). 개발 전용 (D-022)
pnpm db:stop          # 정지
pnpm db:reset         # 데이터 디렉터리를 지우고 다시 만든다 ← 로컬 개발 DB에만 쓴다
pnpm db:migrate       # Prisma 마이그레이션
pnpm db:seed          # Mock과 같은 결정적 픽스처를 DB에 적재
pnpm db:check         # 시드 결과 숫자 대조 (건수 · 한글 UTF8 왕복 · 래더 정합성 · 스냅샷 누락)
pnpm compare          # Mock ↔ 실제 API 응답을 **값까지** 대조 ← 두 모드가 같은지 판정하는 기준
pnpm clan:aliases     # data/clan/clan-aliases.json → packages/contract/src/clanAliasTable.ts
                      # 클랜 별칭 표를 **상수로 굽는다** (D-244). JSON 을 고쳤으면 이걸 돌린다

# --- Phase 8 이후 (넥슨 수집) ---
pnpm nexon:status     # 적재 현황 + API 키 설정 여부 (키 값은 찍지 않는다)
pnpm nexon:identities --nicknames "닉1,닉2" [--dry-run]   # 닉네임 → ouid
pnpm nexon:collect --all-identities [--limit N] [--dry-run]
pnpm nexon:project [--league <slug>]    # 스테이징 → 운영 Match (조건 미충족이면 보류)
pnpm nexon:poll --targets N             # 적응형 폴링 (활동량 기반, Phase 8.1)
pnpm nexon:report                       # 티어 분포 + 호출량 계측
pnpm nexon:refresh                      # 신선도 정책(기본 30일) 재수집
pnpm nexon:check                        # 숫자 대조 17항목
pnpm --filter @sacloud/worker nexon lineup-complete [--confirm]
                      # 3rd.supply 라인업으로 참가자 10명 명단 완성 (D-148, idempotent)
pnpm --filter @sacloud/worker nexon battlelog-import --file <수집.json> [--confirm]
                      # 병영수첩 BattleLog 원문 적재 — 좌표까지 그대로 (D-174, 멱등)
pnpm --filter @sacloud/worker nexon position-build [--labels <라벨.json>] [--min-games N] [--confirm]
                      # 좌표 → 격자 분포 → 포지션 판정. 라벨이 없으면 분포만 만든다 (D-174)
pnpm --filter @sacloud/worker nexon ipl-sanply-check
                      # 열산에 남은 **IPL끼리의 경기** 건수 (D-210). 0 이 아니면 exit 1
                      # 막는 규칙은 supply-import 안에 있다 — 이건 새는지 보는 대조다
pnpm --filter @sacloud/worker nexon ipl-sanply-purge [--confirm]
                      # 이미 들어온 IPL끼리의 경기를 지우고 IPL 클랜을 열산에서 뺀다 (D-210)
                      # **지우기 전에 백업 JSON 을 뜬다.** 원문(수집 JSONL)은 건드리지 않는다
pnpm --filter @sacloud/worker nexon independent-league [--confirm]
                      # 무소속리그(`nolink`) 만들기 (D-165, idempotent)
                      #   --register <클랜slug> --tier <1~5>   그 티어에 등록/이동
                      #   --sync                               Clan.tier ↔ LeagueClan.division 정합
```

> `--dry-run`은 **요청을 한 건도 보내지 않는다.** API 키 없이 파이프라인을 점검할 때 쓴다.
> **대규모 실수집은 사용자 승인 후에만 한다.**

**개발 시작 순서**: `pnpm db:start` → (최초 1회) `pnpm db:migrate` → `pnpm db:seed` → `pnpm dev:clean`

> **`pnpm db:start`는 켜 두는 명령이다.** 그 프로세스가 살아 있는 동안만 DB가 뜬다.
> 창을 닫거나 Ctrl+C를 누르면 PostgreSQL도 함께 내려가고 **API가 전부 500**이 된다.
> API가 갑자기 500이면 `netstat -ano | findstr :5433` 으로 DB부터 확인한다.
>
> **대량 적재 중 DB 가 죽으면 D-216 을 읽어라.** 원인은 공간이 아니라 **WAL 파일 생성**이었다.
> `min_wal_size` 를 2GB 로 올려 재활용 풀을 키우는 것으로 고쳤다 (2026-08-31).
> 또 죽으면 `pg_wal` 조각 수부터 센다 — 128개 근처에서 안정적이면 재활용이 도는 것이다.
> dev 서버와는 **별도 창**에서 띄운다.

`apps/web/.env.local` 의 `NEXT_PUBLIC_API_MODE` 로 모드를 바꾼다 (`live` / `mock`).
비밀키는 `.env.local` 에만 두고 **저장소에 넣지 않는다.**

### 인프라 설치 원칙

**"나중에 필요하니까" 미리 설치하지 않는다.** Redis, BullMQ, Prisma, PostgreSQL, 인증 라이브러리, 에디터 등은 해당 Phase에서 실제로 쓸 때 추가한다.

---

## 8. 현재 상태

- 완료: 개발환경 점검 / Git 초기화 / `docs` 기준 문서 정독 / `CLAUDE.md` 작성
- 완료: **Phase 0** — 모노레포 초기화, API 계약(Zod + 엔드포인트 레지스트리), 결정적 Mock 픽스처, MSW 핸들러
- 완료: **Phase 1** — 전역 셸(GNB/푸터), 홈(로고 · 통합검색 · 실시간 인기게시글), 약관 2종, 404,
  디자인 토큰을 **원본 실측값으로 교체**. Chrome 실브라우저로 원본과 비교 검수 완료.
- **진행 방식 변경 (2026-08-20)**: 이후 Phase는 **사용자 승인을 기다리지 않고 자율 진행**한다.
  Phase마다 `계획 확인 → 필요시 원본 조사 → 구현 → localhost 실행 → Chrome 렌더/기능 확인 →
  원본 비교 → QA 독립 검수 → FAIL 수정 → 재검증 → lint/typecheck/test/build → 회귀 →
  Git 체크포인트 커밋 → 다음 Phase`를 반복한다.
  사용자에게 묻는 경우는 `docs/DECISIONS.md` D-015에 정리한 목록으로 한정한다.
- 완료: **Phase 3 · 2 · 4 · 5 · 6** — 리그/랭킹, 플레이어·클랜 프로필, 기록실·매치 상세,
  게시판, 인증·마이페이지·관리 화면. 여기까지가 **M1(Mock 기반 화면·흐름 복원)**.
- **완료: Phase 7** (2026-08-21 최종 검수 완료) — `packages/db`(Prisma) 신설, 로컬 PostgreSQL,
  시드, 실제 API 전량(라우트 57개), 실제 인증(JWT + httpOnly 쿠키 + 자동 갱신).
  **Mock ↔ 실제 API 응답 값 대조 25/25 일치.** 이제 글·댓글·추천·설정이 **실제로 저장된다.**
  화면 검수에서 결함 3건을 발견해 고쳤다 (D-030 · D-031). 검수 계정은 D-033.
- **진행 중: Phase 8** (넥슨 Open API 전적 수집). 사용자 승인으로 착수했고,
  **API 키 없이 가능한 구간은 전부 완료**했다 — `packages/nexon`(클라이언트·정규화),
  `apps/worker`(수집 CLI), 스테이징 스키마, 투영 규칙, 검증 명령.
  넥슨이 주지 않는 값(무기·플레이시간·종료시각·선공진영·MVP·탈주)은 **전부 `null`** 이다 (D-034).
  **실응답 소량 검증도 끝났다**(닉네임 1명·호출 15회, 2026-08-21).
  실제 응답은 스펙과 두 가지가 달랐다 — 클랜명 필드가 `guild_name`(D-043)이고,
  **한 경기 응답에 양 팀이 함께 오지 않는다(D-044).** 후자는 Phase 9의 선결 과제다.
  **대규모 수집은 D-044가 풀리기 전에는 하지 않는다.**
- **Phase 8.1** — D-044 후속 검증(같은 match_id 재호출은 항상 같은 응답 → multi-OUID 합치기 불가)과
  **적응형 폴링**(활동량 기반 hot/warm/cold/dormant, 호출량 계측)을 추가했다. D-044는 **미해결**이다.
  사양은 `docs/NEXON_INGEST_SPEC.md`, 인수인계는 `docs/HANDOFF_CURRENT.md`.
- 자체 결정·임시값·원본과의 의도된 차이는 `docs/DECISIONS.md`에 있다. 그 값을 바꾸려면 문서도 함께 고친다.
  Phase 1 관련은 D-009 ~ D-013, Phase 7 관련은 D-022 ~ D-033, Phase 8 관련은 D-034 ~ D-051.

### Phase 7에서 남긴 숙제

- **서든어택 계정 연동이 소유권을 증명하지 않는다.** 닉네임으로 연결만 한다.
  운영에 노출하기 전에 반드시 해결한다 (`/api/me/link` 주석 참조).
- 캡차·이메일 발송·이미지 업로드(오브젝트 스토리지)는 아직 없다.
- 리치텍스트 에디터는 아직 없다 (글쓰기가 단순 textarea).
- 래더 점수는 여전히 픽스처 난수다. 실제 계산은 Phase 9.
- 넥슨 계정 신원(`NexonIdentity`)은 기본이 `unresolved`다. **연결은 사람이 판단한다** (D-036).
- 시드 데이터는 전부 가짜다. `Match.origin = "mock"` / `formulaVersion = "mock-fixture"`로
  표시해 두었다 (D-023).

---

## 9. UI 전면 교체 이후 (2026-08-30 · D-204)

**3rd.supply 재현은 끝났다.** 화면 작업의 기준은 `docs/DECISIONS.md` **D-204** 다.

```
--color-page  #060505   --color-card #120c0c   --color-card-2 #1a1010
--color-line  #2a1616   --color-line-soft #1a1010
--color-text  #d6c9c9   --color-text-strong #f6eded
--color-meta  #9a8080   --color-faint #6b5555
--color-accent #d92b2b  ← 색은 이것 하나뿐. 넓은 면에 칠하지 않는다
--font-display Black Han Sans (큰 제목만) · --font-body Noto Sans KR · --font-num JetBrains Mono
--layout-max 1120px · --section-gap 40px · --radius 2px · 루트 15px
```

- 헬퍼 클래스: `.btn-line`(테두리 버튼) · `.num`(숫자) · `.display`(큰 제목) · `.section-stack`
- 표는 얼룩무늬 없이 `--color-line-soft` 1px 로 행을 나눈다. 그림자(`shadow-*`)는 쓰지 않는다
- **`a { color: inherit }` 함정** — `<a>` 에 직접 준 색 유틸리티는 눌린다. 안쪽 `<span>` 으로 옮긴다
- 리그 이름은 **DPL · IPL · 열산**. slug(`supply`/`nolink`/`sanply`)와 라우트는 그대로다
- `인기게시판` 은 화면에서 **`Hot게시판`**. slug `hot` 은 그대로다
- **리그별로 칸을 감추는 분기를 만들지 않는다.** 모든 리그가 같은 화면을 쓴다

### 테스트는 순차로 돌린다

`pnpm test` 를 그냥 돌리면 DB 를 쓰는 테스트가 **5초 타임아웃**으로 실패한다.
D-187 이 로컬 루프백에 `connection_limit=5` 를 걸어 둔 탓이고 **코드 문제가 아니다.**

```bash
pnpm vitest run --no-file-parallelism
```

dev 서버가 떠 있으면 그쪽도 접속을 물고 있으니, 판정할 때는 **dev 서버를 내리고** 돌린다.


---

## 10. 사용자 상시 지시 (2026-08-31 문서화)

아래는 **한 번 말했지만 계속 유효한** 지시다. 전 세션 전사(`docs/session-ledger/9280cc83.md`)에는
원문이 남아 있는데 저장소에는 없어서 **세션마다 잊히고 있었다.** 원문 그대로 인용해 박아 둔다.
개별 작업 지시가 아니라 **작업 원칙**이므로 여기(3장 다음)에 둔다.

### 10-1. "지적해줘" 라고 하면 **비판 에이전트를 붙인다**

> "비판적 사고 서브에이전트를 한명 고용해서 지금까지의 과정과 내 계획이 사이트 흥행에
>  큰 도움이 되지 않을 가능성과 허점들을 지적해줘 **내가 지적해줘 라고 할때마다 지적해줘**
>  일단 지적해줘" — 2026-08-29 12:18:04

- 사용자가 **"지적해줘"** 라고 말하면 그것은 **트리거**다. 그 자리에서 답하지 말고
  **비판적 검토 전용 서브에이전트**를 띄운다.
- 그 에이전트의 일은 **동의가 아니라 반박**이다 — 허점 · 흥행에 도움이 안 될 가능성 ·
  근거 없는 낙관을 찾는다.
- 결과는 본체가 읽고 **추려서** 사용자에게 답한다 (D-190 「정한 것 ③」).

### 10-2. `@@` 로 시작하면 **1분 안에** 답한다 (2026-09-01 갱신)

> **⚠ 갱신 (2026-09-01)** — 아래 「5초」 규칙을 **`@@` 규칙으로 바꿨다.**
>
> ```
> 사용자가 메시지를 @@ 로 시작하면, 늦어도 1분 안에 답한다
> ```
>
> - **하던 일을 멈추지 않는다.** 도구 호출이 끝나기를 기다리지 말고 **먼저 짧게 답한다.**
> - 이미 아는 것만으로 답한다. 상태를 확인하러 가느라 늦지 않는다
> - 모르면 모른다고 쓴다 — `마지막으로 본 게 X 였고 아직 결과를 못 받았습니다`.
>   **끝났다고 지어내지 않는다**
> - 답한 직후 원래 작업을 그대로 이어간다. "중단할까요?" 는 묻지 않는다
>
> 이유는 아래와 같다 — 사용자는 화면만 보고 「생각 중」과 「죽음」을 구별할 수 없다.
> 한 번 스킬(`/하는중?`)로 만들려다 접었다. **트리거는 `@@` 하나면 된다.**

<details><summary>원래 규칙 (2026-08-30 · 지금은 위 `@@` 규칙이 대신한다)</summary>

#### "멈춘거 아니지?" 에는 **5초 안에** 답한다

> "앞으로 내가 멈춘거 아니지? 라고물어보면 **하던일 하면서 네 라고 대답을 5초안에 해**
>  5초안에 안하면 멈춘걸로 간주할게" — 2026-08-30 04:47:39

- **하던 일을 멈추지 않는다.** 도구 호출을 끝내고 답하는 게 아니라, **먼저 짧게 답한다.**
- 형식은 짧게 — "네, 하고 있어요." + 지금 무엇을 하는 중인지 한 줄.
- **5초를 넘기면 사용자는 세션이 죽은 것으로 간주한다.** 실제로 세션이 죽은 적이 있어서
  나온 지시다 (D-213).

</details>

### 10-3. **절대 쉬지 않는다** — 손이 비면 다음 일을 찾는다

> "너는 앞으로 쉬지 않고 할 일이 없으면 찾아서라도 작업해 절대 쉬지마 우리 달려야해"
> — 2026-08-29 13:15:30

- 작업 하나가 끝났다고 멈춰 서서 다음 지시를 기다리지 않는다.
- 할 일이 없어 보이면 **찾는다** — `docs/TASK_LEDGER.md` · 각 문서의 `[미확인]` 목록 ·
  `V2 후보` · 검증되지 않은 채 남은 숫자.
- 다만 **범위 밖으로 나가지는 않는다.** 3장 원칙(임의 기능 추가/삭제 금지)은 그대로다.
  찾은 일이 Phase 범위 밖이면 **하지 말고 기록**한다.

### 10-4. 방식을 바꾸면 **이전 방식 버전도 남긴다**

> "지금부터 너가 스스로 판단하는거야 (…) 어 그런데 이 방법이 훨씬 좋다 하면 너 판단으로 바꿔도 돼
>  **하지만 전의 방식 버전도 만들어놓고 너의 판단으로 바꿔놔**" — 2026-08-30 04:00:48

- 판단으로 방식을 바꾸는 것은 **허용된다.** 대신 **옛 버전을 지우지 않는다.**
- 코드에서는 옛 방식을 **살려 둔 채** 새 것을 기본값으로 한다
  (예: `packages/ui/src/record/TraitHexagon.tsx` 의 `variant` — 옛 `wide` 를 남기고
  `dense` 를 기본으로 했다).
- 문서에서는 **옛 서술을 지우지 말고** `⚠ 정정` / `⚠ 철회` 박스로 감싸
  **"그때는 이랬고 지금은 이렇다"** 를 남긴다 (예: D-190 · `docs/MIGRATION_GAPS.md` 0장).

### 10-5. 승인을 구하지 않는다 (D-190 ① 철회)

> "너한테 나의 모든 권한을 항상 허용한다 앞으로 나한테 허용할지 거부할지 묻지 않는다.
>  나는 너의 모든 허용or거부 요청을 받아줄 수 없고. 이것은 업무에 치명적인 비효율을 가져온더.
>  너는 나에게 묻지 않고 자율적으로 a부터z까지 실행 후에 최종 결과물만 가지고 온다."

- `docs/DECISIONS.md` **D-190** 의 「정한 것 ①」(착수 전 보고하고 **답을 기다린다**)은
  **철회됐다.** `.claude/settings.local.json` 의 `"defaultMode": "bypassPermissions"` 가
  이쪽에 맞춰져 있다.
- 진행 상황은 **알린다.** 그러나 **허락을 기다리지는 않는다.**
- 사용자에게 묻는 것은 `docs/DECISIONS.md` **D-015** 목록으로 한정한다.

> 원문 출처는 `docs/session-ledger/9280cc83.md` · `a0e55e51.md` 다.
> 인용문은 **오타 포함 원문 그대로** 옮겼다.
