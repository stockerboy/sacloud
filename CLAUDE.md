# CLAUDE.md — sacloud

> **먼저 `docs/STATE.md` 를 읽어라.** 지금 무엇이 되고 무엇이 안 되는지는 거기 있다.
> 이 파일은 **바뀌지 않는 규칙**만 담는다. 상태는 담지 않는다 — 그래서 534줄이 됐었다.
>
> 2026-09-02 에 534줄 → 이 길이로 줄였다. 원본은 `docs/archive/CLAUDE_2026-09-02_534lines.md`.

---

## 1. 사용자 상시 지시 — 이게 제일 위다

한 번 말했지만 **계속 유효한** 지시다. 원문 그대로 박아 둔다.
(원문 출처: `docs/archive/session-ledger/`)

### 1-1. "지적해줘" 라고 하면 **비판 에이전트를 붙인다**

> "비판적 사고 서브에이전트를 한명 고용해서 (…) 허점들을 지적해줘
>  **내가 지적해줘 라고 할때마다 지적해줘**" — 2026-08-29

그 자리에서 직접 답하지 말고 **따로 세워서** 검토시킨다. 그 에이전트의 일은
동의가 아니라 **반박**이다. 결과는 내가 추려서 사용자에게 전한다.

### 1-2. `@@` 로 시작하면 **1분 안에** 답한다

```
사용자가 메시지를 @@ 로 시작하면, 늦어도 1분 안에 답한다
```

- **하던 일을 멈추지 않는다.** 도구가 끝나기를 기다리지 말고 **먼저 짧게 답한다**
- 이미 아는 것만으로 답한다. 확인하러 가느라 늦으면 규칙을 어긴 것이다
- 모르면 «마지막으로 본 게 X 였다» 라고 쓴다. **끝났다고 지어내지 않는다**
- 답한 직후 원래 작업을 이어간다. "중단할까요?" 는 묻지 않는다

이유: 사용자는 화면만 보고 **「생각 중」과 「세션이 죽음」을 구별할 수 없다.**
실제로 죽은 적이 여러 번 있다. (옛 규칙은 "멈춘거 아니지?" 에 5초였다 — `@@` 가 대신한다)

### 1-3. **절대 쉬지 않는다** — 손이 비면 다음 일을 찾는다

> "너는 앞으로 쉬지 않고 할 일이 없으면 찾아서라도 작업해 절대 쉬지마 우리 달려야해"
> — 2026-08-29

멈춰 서서 다음 지시를 기다리지 않는다. 할 일이 없어 보이면 **찾는다** —
`docs/ORDERS.md` 대기 칸 · `[미확인]` 으로 남은 것 · 검증 안 된 숫자.
다만 **범위 밖으로 나가지 않는다.** 화면 6개(홈·선수·클랜·경기목록·경기상세·랭킹) 밖의 일은 하지 말고 `ORDERS.md` 에 적는다.

### 1-4. 방식을 바꾸면 **이전 방식 버전도 남긴다**

> "이 방법이 훨씬 좋다 하면 너 판단으로 바꿔도 돼
>  **하지만 전의 방식 버전도 만들어놓고** 너의 판단으로 바꿔놔" — 2026-08-30

- 판단으로 바꾸는 것은 **허용된다.** 대신 **옛 버전을 지우지 않는다**
- 코드: 옛 방식을 살려 둔 채 새 것을 기본값으로 (예: `TraitHexagon` 의 `variant`)
- 문서: 옛 서술을 지우지 말고 `⚠ 정정` 박스로 감싸 "그때는 이랬고 지금은 이렇다" 를 남긴다
- 파일: 지우지 말고 `docs/archive/` 로 옮긴다

### 1-5. 승인을 구하지 않는다

> "너한테 나의 모든 권한을 항상 허용한다 앞으로 나한테 허용할지 거부할지 묻지 않는다.
>  (…) 너는 나에게 묻지 않고 자율적으로 a부터z까지 실행 후에 최종 결과물만 가지고 온다."

진행 상황은 **알린다.** 그러나 **허락을 기다리지 않는다.** 되물어서 멈추는 것이
사용자에게는 가장 큰 비효율이다. 판단이 갈리면 **가정을 적고 진행**한다.

---

## 2. 절대 원칙

1. **없는 것을 지어내지 않는다.** 확인 안 된 것은 `[미확인]` 이라고 적는다.
   "됐습니다" 대신 **숫자**를 낸다. 이 규칙을 어겨서 일주일을 잃었다
2. **지우지 않는다. 숨긴다.** (1-4 와 같은 뜻이다)
3. **광고를 만들지 않는다.** 스크립트 · 배너 · 인피드 슬롯 · 빈 자리표시자
   (`AdSlot`, `<div class="ad">`) 전부. 광고 자리로 비워 둔 여백도 만들지 않는다
4. **원본(`3rd.supply`)의 코드 · 이미지 · 클랜마크 · 약관 문구를 복사하지 않는다.**
   관찰한 동작과 정보 구조만 새로 작성한다
5. **`main` 이 곧 운영 사이트다.** Vercel 이 GitHub `origin/main` 을 빌드한다.
   `pnpm typecheck` 와 테스트가 **초록일 때만** 민다
6. **비밀번호 · API 키를 채팅에 쓰지 않는다.** `.env.local` 에만 두고 이름으로만 부른다

---

## 3. 일하는 법 — A/B (2026-09-02)

```
A (기획)   사장님과 회의 → docs/ORDERS.md 맨 위에 지시서를 적는다. 코드는 안 고친다
B (실행)   지시서를 읽고 만든다 → 검증한다 → 커밋한다 → docs/STATE.md 를 갱신한다
```

- **지시서에 「어떻게 확인하나」가 비어 있으면 시작하지 않는다.** 그 칸이 없어서 일주일을 잃었다
- 새 아이디어는 그 자리에서 만들지 않는다. `ORDERS.md` 대기 칸으로 보낸다
- 동시에 **세션 2개 · 에이전트 3개**까지. 8GB 노트북이다. 넘기면 멈춘다

---

## 4. 화면 (D-204 → D-264 「3톤」)

**`3rd.supply` 재현은 끝났다.** 색을 새로 지어내지 말고 아래 토큰을 쓴다.

```
--color-page #303038   --color-card #2c304c   --color-card-2 #3a4067
--color-line #454b73   --color-line-soft #3a4067
--color-text #d9dbe4   --color-text-strong #f2f4f8
--color-meta #a8aec2   --color-faint #8f95af
--color-accent #5c80e0  ← 색은 이것 하나뿐. 넓은 면에 칠하지 않는다
--layout-max 1120px · --section-gap 40px · --radius 2px
```

> ⚠ **2026-09-02 정정.** 이 절에 D-204 「적진」 값(`#060505` 바탕 + `#d92b2b` 진홍)이
> 적혀 있었는데 **화면은 이미 그 색이 아니었다.** D-264(`b0c57f9` 「색을 3톤으로」)에서
> 바뀌었고 문서만 안 따라왔다. 로컬 화면을 열어서 파란 강조를 보고 찾았다.
>
> **값의 진실은 `packages/ui/src/styles.css` 다.** 이 표와 어긋나면 그쪽이 맞다.
> `styles.css` 안의 주석에도 아직 «진홍 #d92b2b» 라는 말이 남아 있다 — 값이 아니라 말이다.

- 헬퍼: `.btn-line` · `.num` · `.display` · `.section-stack`
- 표는 얼룩무늬 없이 `--color-line-soft` 1px 로 행을 나눈다. `shadow-*` 는 쓰지 않는다
- **`a { color: inherit }` 함정** — `<a>` 에 직접 준 색 유틸리티는 눌린다. 안쪽 `<span>` 으로 옮긴다
- 리그 이름은 **SPL · IPL · 10mountain**. slug(`supply`/`nolink`/`sanply`)와 라우트는 그대로다.
  `10mountain` 옆 산 표시는 이름이 아니라 화면 장식이다 (`LeagueLabel`)
- `인기게시판` 은 화면에서 **`Hot게시판`**. slug `hot` 은 그대로다
- **리그별로 칸을 감추는 분기를 만들지 않는다.** 모든 리그가 같은 화면을 쓴다
- 검수는 실제 브라우저로: 글자가 묻히지 않는가 / 가로 스크롤이 없는가 /
  진홍이 넓은 면에 칠해지지 않았는가 / **값이 사라지지 않았는가**

---

## 5. 도메인 용어 (임의로 바꾸지 말 것)

- **래더** — Elo 계열 점수. **티어 이름(브론즈/골드)은 없다. 만들지 않는다**
- **부리그(division)** — `division_count` 가 1이면 단일, 2 이상이면 N부리그 탭.
  **티어를 화면에 쓰는 리그는 IPL 뿐이다**
- **알수없음** — 결측값. `null` 저장 + 화면에 `알수없음`
- **탈주(dropout)** · **MVP** · **선레드/선블루** · **정보갱신/전적갱신** · **지난시즌**
- 무기: `0 = 라이플`, `1 = 스나이퍼` (라인업에 `[S]`)
- 커서: `base64url("next__<id>" | "prev__<id>")` · 랭킹 20건 / 게시판 15건 · `더 불러오기`
- ⚠ **배치고사는 폐지됐다** (2026-09-02). 옛 문서에 나오면 그건 지난 기록이다

---

## 6. 래더 — 어기면 안 되는 것

상세는 `docs/LADDER_IMPLEMENTATION_SPEC.md` · `docs/RATING_FINAL_SPEC.md`.

1. **공식은 하나뿐이다.** 스나이퍼용·라이플용을 따로 만들지 않는다.
   계산된 값을 무기별로 **기록만 분리**한다 (`sniper_rating_delta` / `rifle_rating_delta`)
2. **통합 래더 = `base_rating + sniper_delta + rifle_delta`.** 공식 입력 `R` 은 항상 통합 래더다
3. kill/death/assist/MVP/damage/headshot/dropout/연승연패는 **공식에 넣지 않는다**
4. **경기 당시 division 스냅샷으로 계산한다.** 현재 division 을 쓰면 승강 후 과거가 오염된다
5. **래더 상수를 하드코딩하지 않는다.** `RatingConfig` 로 분리한다
6. 미확인 항목(감쇠 위치 · 시즌 전환 · rounding)을 임의로 확정하지 않는다

---

## 7. 개발 환경

| | |
|---|---|
| 루트 | `C:\Users\LG\Desktop\서플라이` |
| OS / 셸 | Windows 10 Pro · PowerShell / Git Bash |
| 패키지 매니저 | **pnpm 으로 통일** |
| TypeScript | **5.9 고정** (7.x 는 `next build` 가 `next.config.ts` 를 못 읽는다 · D-007) |
| Next / React | 15.5 (App Router) / 19 |
| git 사용자 | 이 저장소에만 `SACLOUD Dev` (전역은 안 건드림) |

```
apps/web/          Next.js 화면 + app/api/** 실제 API
apps/worker/       수집 잡 + CLI
packages/contract/ Zod 스키마 + 엔드포인트 레지스트리  ← 계약의 단일 진실 원천
packages/mock/     결정적 픽스처 + MSW 핸들러
packages/ui/       공용 컴포넌트 + 디자인 토큰
packages/db/       Prisma 스키마 · 시드
packages/nexon/    넥슨 API 클라이언트 (순수 — DB 를 모른다)
docs/              지금 쓰는 문서 13개 · docs/archive/ 는 지난 기록
```

```bash
pnpm install
pnpm dev                              # 개발 서버
pnpm typecheck                        # 전 패키지 tsc --noEmit
pnpm test                             # 전체 테스트
pnpm vitest run --no-file-parallelism  # DB 테스트가 타임아웃날 때 (D-187)
```

- DB 테스트가 5초 타임아웃으로 깨지면 **코드 문제가 아니다.** 위 순차 실행으로 돌리고,
  판정할 때는 **dev 서버를 내린다** (접속을 물고 있다)
- `apps/web/.env.local` 의 `NEXT_PUBLIC_API_MODE` 로 `live` / `mock` 을 바꾼다
- **"나중에 필요하니까" 미리 설치하지 않는다.** 실제로 쓸 때 추가한다

---

## 8. 이 파일에 없는 것

| 찾는 것 | 어디에 |
|---|---|
| 지금 상태 · 오늘 정해진 것 | `docs/STATE.md` |
| 무엇을 만들지 (지시서) | `docs/ORDERS.md` |
| 옛 결정 | `docs/DECISIONS.md` (808KB · 전부 읽지 마라) |
| 지난 기록 · 옛 인계 · 숨긴 기능 사양 | `docs/archive/` (`README.md` 부터) |
| 이 파일의 옛 534줄 판 | `docs/archive/CLAUDE_2026-09-02_534lines.md` |
